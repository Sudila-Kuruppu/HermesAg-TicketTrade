<?php
/**
 * TicketTrade — Admin Route Map
 *
 * Phase 1 returns an empty array. Phase 8 populates the map with
 * admin routes (/admin/users, /admin/listings, /admin/reports, etc.).
 *
 * Format matches config/routes.php. Empty map renders the Phase 1
 * stub landing page with data-surface=admin (light theme default).
 */

declare(strict_types=1);

return [];
