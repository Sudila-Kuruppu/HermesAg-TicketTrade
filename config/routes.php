<?php
/**
 * TicketTrade — Student Route Map
 *
 * Phase 1 returns an empty array. Phase 2 populates the map with
 * student routes (/, /board, /listings/*, /tickets, /profile/*, etc.).
 *
 * Format: ['METHOD PATH' => 'HandlerClass::method']
 *   e.g. ['GET /' => 'HomeController::index']
 *
 * An empty map tells Support\Router::dispatch to render the Phase 1
 * stub landing page (a placeholder proving the front controller is wired).
 */

declare(strict_types=1);

return [];
