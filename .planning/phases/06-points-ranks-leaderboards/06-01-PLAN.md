---
phase: 6
plan: 01
type: feature
wave: 1
depends_on: []
files_modified:
  - migrations/018_points_log_indexes.sql
  - migrations/019_users_last_active.sql
  - src/Points/Model/points_log_model.php
  - src/Points/Service/points_service.php
  - src/User/Model/user_model.php
  - src/Support/View/partials/rank_badge.php
  - src/Support/View/partials/tier_progress.php
  - src/Support/View/partials/on_break_pill.php
  - src/Support/View/partials/velocity_flag_pill.php
  - public/assets/css/tickettrade.components.css
  - public/assets/js/tickettrade.js
  - config/contexts.php
autonomous: true
requirements:
  - PTS-01
  - PTS-02
  - PTS-03
  - PTS-04
  - PTS-08
  - PTS-10
  - PER-05

must_haves:
  truths:
    - "Migration 018 adds idx_points_user_event and idx_points_pair on points_log without altering existing rows."
    - "Migration 019 adds users.last_active_at DATETIME NULL plus a BEFORE INSERT trigger on points_log that refreshes last_active_at on every points_log INSERT."
    - "points_service::awardVerificationBonus() and awardReviewPoints() short-circuit when users.points_frozen=TRUE with {ok:true, data.skipped='points_frozen'} and write no points_log row."
    - "points_service exposes sumForUserInWindow(), countPairInDay(), recentForUser(), awardListingApproval(), awardReportValidated(), awardStreakBonus(), voidPoints(), clearPointsFreeze() callable from a Phase 8 admin Action."
    - "rank_badge partial renders a tier-letter pill with the canonical 6-tier shape and a Bootstrap tooltip formatted '{name} ({code}) — {min} to {max} points' (top tier '{name} ({code}) — {max}+ points')."
    - "tier_progress partial renders an 8px rounded-full bar with current-tier fill; tooltip reads 'X of Y toward {next tier name}' or 'Top tier reached' at S."
    - "on_break_pill partial renders only when users.last_active_at < NOW() - INTERVAL 14 DAY; copy per EXPERIENCE.md L153."
    - "velocity_flag_pill partial renders only when users.points_frozen=TRUE; copy per D-02 ('Earning paused — admin review')."
    - "phpunit phase-6-unit suite passes (tier + points + cap + freeze coverage)."
  artifacts:
    - migrations/018_points_log_indexes.sql
    - migrations/019_users_last_active.sql
    - src/Points/Model/points_log_model.php (sumForUserInWindow, countPairInDay, recentForUser)
    - src/Points/Service/points_service.php (awardListingApproval, awardReportValidated, awardStreakBonus, voidPoints, clearPointsFreeze)
    - src/User/Model/user_model.php (updateLastActive, findForLeaderboard)
    - src/Support/View/partials/rank_badge.php (canonical tooltip)
    - src/Support/View/partials/tier_progress.php (NEW)
    - src/Support/View/partials/on_break_pill.php (NEW)
    - src/Support/View/partials/velocity_flag_pill.php (NEW)
    - public/assets/css/tickettrade.components.css (.legend-glow keyframes honored + .tier-progress / .on-break-pill / .velocity-flag-pill rules)
    - public/assets/js/tickettrade.js (data-component='tier-progress' tooltip wiring)
    - config/contexts.php (Leaderboard context registered; Phase 6 plan 06-03 reads)
    - tests/Unit/Phase06/Points/TierFromPointsTest.php
    - tests/Unit/Phase06/Points/AwardListingApprovalTest.php
    - tests/Unit/Phase06/Points/AwardReportValidatedTest.php
    - tests/Unit/Phase06/Points/AwardStreakBonusTest.php
    - tests/Unit/Phase06/Points/VoidAndClearFreezeTest.php
    - tests/Integration/Phase06/MigrationIndexesTest.php
    - tests/Integration/Phase06/MigrationLastActiveTest.php
  key_links:
    - "points_service awardTransaction() still participates in caller transactions (Phase 4 contract preserved) — no signature change."
    - "Migration 019 trigger is the ONLY writer of users.last_active_at from the gamification side; PHP code never updates the column in points_service."
    - "rank_badge tooltip format derives from config/ranks.php (single source of truth) — partial does NOT duplicate the threshold table."
---

<objective>
Land the points engine substrate that Plan 06-02 layers velocity/pair-cap onto, plus the rank-badge and tier-progress partials that Plan 06-03 mounts on Profile and Leaderboards.

Purpose: this plan establishes the data surface and the read-only UI primitives. The new writers (awardListingApproval, awardReportValidated, awardStreakBonus) plus voidPoints / clearPointsFreeze are stubbed but the cap enforcement lands in Plan 06-02. The five new partials (rank_badge canonical tooltip, tier_progress, on_break_pill, velocity_flag_pill) are the locked Phase 6 design-system component inventory per 06-UI-SPEC.md.

Output: 4 SQL migrations (018, 019; 020 + 021 land in 06-03), extended points_log_model, extended points_service writers, 4 new partials, CSS for the four surfaces, JS tooltip wiring, and the unit + integration tests covering the new code paths.
</objective>

<execution_context>
@/home/user/hermesag/004/.opencode/gsd-core/workflows/execute-plan.md
@/home/user/hermesag/004/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/user/hermesag/004/tickettrade/AGENTS.md
@/home/user/hermesag/004/AGENTS.md
@/home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md
@/home/user/hermesag/004/tickettrade/.planning/phases/06-points-ranks-leaderboards/06-CONTEXT.md
@/home/user/hermesag/004/tickettrade/.planning/phases/06-points-ranks-leaderboards/06-UI-SPEC.md
@/home/user/hermesag/004/tickettrade/src/Points/Service/points_service.php
@/home/user/hermesag/004/tickettrade/src/Points/Model/points_log_model.php
@/home/user/hermesag/004/tickettrade/src/Auth/Service/auth_service.php
@/home/user/hermesag/004/tickettrade/config/ranks.php
@/home/user/hermesag/004/tickettrade/config/contexts.php
@/home/user/hermesag/004/tickettrade/migrations/005_password_resets.sql
@/home/user/hermesag/004/tickettrade/migrations/017_reviews.sql
@/home/user/hermesag/004/tickettrade/src/Support/View/partials/rank_badge.php
@/home/user/hermesag/004/tickettrade/tests/Integration/Phase04/Fixtures/Fixtures.php
@/home/user/hermesag/004/tickettrade/tests/Unit/Phase05/Points/AwardReviewPointsTest.php
</context>

<downstream_consumer>
Plan 06-02 reads the new points_log_model helpers (sumForUserInWindow, countPairInDay) to enforce velocity and pair-cap inside points_service::awardTransaction() / awardReviewPoints(). Plan 06-03 reads the partials (rank_badge, tier_progress, on_break_pill, velocity_flag_pill) to render Profile + Leaderboards, and consumes points_service::voidPoints / clearPointsFreeze only via the daily cron (no direct caller).
</downstream_consumer>

<tasks>

<task type="migration">
  <name>Task 1: migrations 018 (points_log indexes) + 019 (users.last_active_at + trigger)</name>
  <files>migrations/018_points_log_indexes.sql, migrations/019_users_last_active.sql</files>
  <read_first>/home/user/hermesag/004/tickettrade/migrations/005_password_resets.sql, /home/user/hermesag/004/tickettrade/migrations/013_tickets.sql, /home/user/hermesag/004/tickettrade/migrations/017_reviews.sql, /home/user/hermesag/004/tickettrade/migrations/014_users_redemption_count.sql</read_first>
  <acceptance_criteria>
    - 018 adds INDEX idx_points_user_event (user_id, event_at DESC) and INDEX idx_points_pair (user_id, reference_id, event_at DESC) inside `CREATE INDEX IF NOT EXISTS`-equivalent guards using `information_schema` pre-check (mirrors 014_users_redemption_count.sql pattern). Does NOT add or rename existing columns.
    - 019 adds `users.last_active_at DATETIME NULL` via `ADD COLUMN IF NOT EXISTS` guard, then `DROP TRIGGER IF EXISTS trg_points_log_refresh_last_active` followed by CREATE TRIGGER trg_points_log_refresh_last_active BEFORE INSERT ON points_log FOR EACH ROW UPDATE users SET last_active_at = NOW() WHERE user_id = NEW.user_id.
    - 018 + 019 both idempotent — re-running `php migrate.php` is a no-op.
  </acceptance_criteria>
  <action>
Write two SQL files. Migration 018 mirrors the Phase 4 014 guard pattern (check information_schema.STATISTICS before CREATE INDEX; ALTER TABLE ADD COLUMN guards). Migration 019 adds `last_active_at` via the same pattern then drops + recreates the trigger so re-running `php migrate.php` doesn't error on the existing trigger. Do NOT add any new columns to points_log; the trigger reads NEW.user_id from the table as-is. The trigger is the only writer of last_active_at from the gamification side (D-03).
  </action>
  <verify>
    <automated>php migrate.php</automated>
    <fails_when>non-zero exit, or stderr containing "SQLSTATE" / "syntax error"</fails_when>
  </verify>
  <verify>
    <automated>php migrate.php 2>&1 | grep -E "Already up-to-date|0 files to apply"</automated>
    <fails_when>exit code != 0, or the grep finds no match (means migrate.php tried to re-apply)</fails_when>
  </verify>
</task>

<task type="code">
  <name>Task 2: points_log_model helpers + points_service new writers + freeze / void methods</name>
  <files>src/Points/Model/points_log_model.php, src/Points/Service/points_service.php, src/User/Model/user_model.php</files>
  <read_first>/home/user/hermesag/004/tickettrade/src/Points/Service/points_service.php, /home/user/hermesag/004/tickettrade/src/Points/Model/points_log_model.php, /home/user/hermesag/004/tickettrade/src/User/Model/user_model.php, /home/user/hermesag/004/tickettrade/config/ranks.php</read_first>
  <acceptance_criteria>
    - points_log_model gains sumForUserInWindow(PDO $pdo, int $userId, string $interval, bool $excludePairCap): int (interval = '1 DAY' or '1 HOUR'; SQL `COALESCE(SUM(delta),0)` filtered by event_at >= NOW() - INTERVAL <interval> AND (pair_cap_hit IS NULL OR pair_cap_hit = 0); returns 0 when no rows).
    - points_log_model gains countPairInDay(PDO $pdo, int $userA, int $userB, int $ticketId): int (SQL counts distinct reference_id where user_id IN (a,b) AND reference_id = ticketId AND DATE(event_at) = CURDATE() AND pair_cap_hit = 0 AND reference_type IN ('final_session','transaction')).
    - points_log_model gains recentForUser(PDO $pdo, int $userId, int $limit): array (SELECT delta, reference_type, event_at, metadata ORDER BY event_at DESC, id DESC LIMIT $limit).
    - points_service::awardListingApproval(int $userId, int $listingId): array — +5 with frozen-gate short-circuit, no halving (per D-15 the multiplier is transaction-only), metadata = json_encode({listing_id, cap_hit:false}); calls auth_service::tierFromPoints and emits a tier-up toast marker via $GLOBALS['_tt_toast_queue'] for the View layer to pick up.
    - points_service::awardReportValidated(int $userId, int $reportId): array — +20, no halving, frozen-gate.
    - points_service::awardStreakBonus(int $userId, int $streakDays): array — +15 (streakDays==7) or +50 (streakDays==30), no halving, frozen-gate; reference_type 'streak_7day' or 'streak_30day'.
    - points_service::voidPoints(int $userId, int $delta, string $reason): array — locks the user row, computes new_balance = max(0, points - delta), INSERTs a negative-delta points_log row (event_uuid fresh, metadata.{reason,voided:true}), UPDATEs users.points = new_balance + tier recomputed; returns {ok:true, data:{voided: delta, balance_after: new_balance}} or {ok:false, error:{code:'E_VOID_INSUFFICIENT_BALANCE'}} when delta > points and points == 0.
    - points_service::clearPointsFreeze(int $userId): array — UPDATE users SET points_frozen = FALSE, frozen_at = NULL, last_unfrozen_at = NOW() WHERE user_id = ?; writes audit row 'points.unfrozen'.
    - points_service::awardReviewPoints() and awardVerificationBonus() short-circuit cleanly on frozen (already true in code — do NOT change behavior); Plan 06-02 layers velocity/pair-cap on top.
    - user_model::updateLastActive(PDO $pdo, int $userId): bool — UPDATE users SET last_active_at = NOW() WHERE user_id = ? (auth_service::recordLogin calls this in Plan 06-02; the trigger handles the points_log path).
    - All writers honor the existing `ownsTransaction` pattern (Plan 06-02 layers cap checks inside this same transaction).
  </acceptance_criteria>
  <action>
Add the three points_log_model helpers (sumForUserInWindow, countPairInDay, recentForUser) as static methods that accept the request PDO and return scalar / array. Add the five points_service writers using the same `ownsTransaction` + `SELECT ... FOR UPDATE` + `points_log_model::insert` + `users UPDATE` shape that awardTransaction uses. Each writer sets `metadata = json_encode([...], JSON_UNESCAPED_UNICODE)`. Tier-up toasts are queued via $GLOBALS['_tt_toast_queue'] = [['type'=>'success','message'=>"Tier up! You're now {name} ({code})."]] when the post-award tier differs from the pre-award tier; the toast partial (already ships from Phase 1) renders these on the next page load. voidPoints participates in the caller transaction or opens one when called standalone. Add user_model::updateLastActive as the canonical direct writer; the points_log trigger owns the gamification-side refresh.
  </action>
  <verify>
    <automated>vendor/bin/phpcs --standard=PSR12 src/Points/Service/points_service.php src/Points/Model/points_log_model.php src/User/Model/user_model.php</automated>
    <fails_when>non-zero exit, or output contains "ERROR" / "WARNING" lines</fails_when>
  </verify>
  <verify>
    <automated>vendor/bin/phpunit --testsuite=phase-6-unit --filter='TierFromPoints|AwardListingApproval|AwardReportValidated|AwardStreakBonus|VoidAndClearFreeze'</automated>
    <fails_when>non-zero exit, or "0 passed" in summary</fails_when>
  </verify>
</task>

<task type="code">
  <name>Task 3: rank_badge canonical tooltip + 3 new partials (tier_progress, on_break_pill, velocity_flag_pill) + CSS + JS wiring</name>
  <files>src/Support/View/partials/rank_badge.php, src/Support/View/partials/tier_progress.php, src/Support/View/partials/on_break_pill.php, src/Support/View/partials/velocity_flag_pill.php, public/assets/css/tickettrade.components.css, public/assets/js/tickettrade.js, config/contexts.php</files>
  <read_first>/home/user/hermesag/004/tickettrade/src/Support/View/partials/rank_badge.php, /home/user/hermesag/004/tickettrade/config/ranks.php, /home/user/hermesag/004/tickettrade/.planning/phases/06-points-ranks-leaderboards/06-UI-SPEC.md, /home/user/hermesag/004/tickettrade/public/assets/css/tickettrade.components.css, /home/user/hermesag/004/tickettrade/config/contexts.php</read_first>
  <acceptance_criteria>
    - rank_badge.php renders the existing inline SVG (unchanged) but its `data-bs-toggle="tooltip"` title becomes "{name} ({code}) — {min} to {max} points" for tiers E..A and "{name} ({code}) — {max}+ points" for tier S. The threshold numbers come from config/ranks.php (no duplication).
    - tier_progress.php accepts $userId, $points, $tier (string). Computes current tier min + next tier min from config/ranks.php; renders an 8px rounded-full bar with track `var(--color-surface-container)` and fill `var(--color-rank-{tier})`. Tooltip: "X of Y toward {next tier name}" where X = points - currentMin and Y = nextMin - currentMin; at tier S renders "Top tier reached" and bar at 100%.
    - on_break_pill.php accepts $lastActiveAt (DateTimeInterface|string|null). Renders only when `NOW() - lastActiveAt >= 14 days`. Wraps the parent rank badge in `.on-break` (CSS applies `filter: grayscale(0.8); opacity: 0.6;`); tooltip "Inactive 14+ days — next action restores full badge" per EXPERIENCE.md L153.
    - velocity_flag_pill.php accepts $isFrozen (bool). Renders only when $isFrozen === true. Copy "Earning paused — admin review" per D-02; CSS classes `velocity-flag-pill` (defined below).
    - public/assets/css/tickettrade.components.css adds: `.legend-glow` honors prefers-reduced-motion: reduce (override `animation: none; box-shadow: 0 0 12px rgba(198,40,40,0.35);`); `.tier-progress { height:8px; border-radius:var(--shape-full); background:var(--color-surface-container); overflow:hidden; } .tier-progress__fill { height:100%; background:var(--color-rank-{tier}); }`; `.on-break { filter:grayscale(0.8); opacity:0.6; } .on-break-pill { font-size:12px; padding:2px 8px; background:var(--color-surface-container-low); color:var(--color-on-surface-variant); border-radius:var(--shape-full); }`; `.velocity-flag-pill { font-size:12px; padding:4px 10px; background:var(--color-velocity-flag-bg); color:var(--color-velocity-flag); border-radius:var(--shape-full); }`.
    - public/assets/js/tickettrade.js adds ~20 LOC for the data-component='tier-progress' tooltip — Bootstrap 5's stock tooltip (data-bs-toggle) is already used by rank_badge; the new wiring only ensures the tooltip is initialized on page load for any element with data-component='tier-progress'.
    - config/contexts.php adds 'Leaderboard' to the bounded contexts list (Plan 06-03 ships the Service; this plan registers the context).
  </acceptance_criteria>
  <action>
Modify rank_badge.php to source the tooltip text from config/ranks.php (read once, cache to local). For non-S tiers: "{name} ({code}) — {min} to {max} points". For S: "{name} ({code}) — {max}+ points". Add the three new partials as small files that read from $GLOBALS['_tt_view_vars'] and emit HTML using the locked CSS classes / tokens — no inline styles. CSS additions in components.css go near the existing .legend-glow rule; new rules use existing --color-* tokens only (no new hex values). JS appends a `document.querySelectorAll('[data-component=\"tier-progress\"]').forEach(el => new bootstrap.Tooltip(el))` initialization guarded by `typeof bootstrap !== 'undefined'`. Update config/contexts.php to add 'Leaderboard' after 'Review'.
  </action>
  <verify>
    <automated>vendor/bin/phpcs --standard=PSR12 src/Support/View/partials/rank_badge.php src/Support/View/partials/tier_progress.php src/Support/View/partials/on_break_pill.php src/Support/View/partials/velocity_flag_pill.php config/contexts.php</automated>
    <fails_when>non-zero exit, or any "ERROR"/"WARNING" line in output</fails_when>
  </verify>
  <verify>
    <automated>node -e "require('/home/user/hermesag/004/tickettrade/public/assets/js/tickettrade.js')" 2>&1 | head -5 || php -l /home/user/hermesag/004/tickettrade/public/assets/js/tickettrade.js 2>&1 | head -5</automated>
    <fails_when>non-zero exit code, or any "SyntaxError" / "syntax error" in output</fails_when>
  </verify>
</task>

<task type="test">
  <name>Task 4: Phase-6 unit + integration tests for tier resolution, new writers, migrations</name>
  <files>tests/Unit/Phase06/Points/TierFromPointsTest.php, tests/Unit/Phase06/Points/AwardListingApprovalTest.php, tests/Unit/Phase06/Points/AwardReportValidatedTest.php, tests/Unit/Phase06/Points/AwardStreakBonusTest.php, tests/Unit/Phase06/Points/VoidAndClearFreezeTest.php, tests/Integration/Phase06/MigrationIndexesTest.php, tests/Integration/Phase06/MigrationLastActiveTest.php, tests/Integration/Phase06/Fixtures/Fixtures.php, phpunit.xml</files>
  <read_first>/home/user/hermesag/004/tickettrade/tests/Unit/Phase05/Points/AwardReviewPointsTest.php, /home/user/hermesag/004/tickettrade/tests/Integration/Phase05/MigrationTest.php, /home/user/hermesag/004/tickettrade/tests/Integration/Phase04/Fixtures/Fixtures.php, /home/user/hermesag/004/tickettrade/phpunit.xml, /home/user/hermesag/004/tickettrade/config/ranks.php</read_first>
  <acceptance_criteria>
    - tests/Unit/Phase06/Fixtures/Phase06Fixtures.php (or extend Phase04\Fixtures) adds resetTables entries for leaderboard_* + login_streaks (no-op for this plan; tables not yet created but truncate-with-IGNORE pattern from Phase 04 Fixtures line 105 handles missing tables). Adds a helper for the new points_service writers.
    - TierFromPointsTest covers: 0 -> E, 49 -> E, 50 -> D, 149 -> D, 150 -> C, 399 -> C, 400 -> B, 799 -> B, 800 -> A, 1499 -> A, 1500 -> S, 50000 -> S. All use the canonical tierFromPoints() helper.
    - AwardListingApprovalTest covers: +5 happy path, frozen short-circuit, no halving (redeemed_count=0 still gets +5).
    - AwardReportValidatedTest covers: +20 happy path, frozen short-circuit.
    - AwardStreakBonusTest covers: streakDays=7 -> +15 with reference_type='streak_7day'; streakDays=30 -> +50 with reference_type='streak_30day'; streakDays in {3,14,60} -> returns {ok:false, error:{code:'E_VALIDATION'}} (no row); frozen short-circuit.
    - VoidAndClearFreezeTest covers: voidPoints(50) on user with points=100 -> new_balance=50, negative-delta row written, tier recomputed; voidPoints(100) on user with points=50 -> {ok:true, data:{voided:50, balance_after:0}} (floored); voidPoints(200) on user with points=0 -> {ok:false, error:{code:'E_VOID_INSUFFICIENT_BALANCE'}} (no row). clearPointsFreeze() flips points_frozen to FALSE, writes audit row 'points.unfrozen'.
    - MigrationIndexesTest verifies idx_points_user_event + idx_points_pair exist via SHOW INDEX (mirrors MigrationTest::test_reviews_unique_index_per_role pattern).
    - MigrationLastActiveTest verifies users.last_active_at column exists + trg_points_log_refresh_last_active trigger exists via information_schema.TRIGGERS; inserts a points_log row and asserts users.last_active_at was refreshed.
    - phpunit.xml gains `<testsuite name="phase-6-unit">` (directory tests/Unit/Phase06) and `<testsuite name="phase-6-integration">` (directory tests/Integration/Phase06); the existing `phase-6` combined suite is added for the executor.
  </acceptance_criteria>
  <action>
Create the Phase 06 fixtures by extending the Phase 04 Fixtures class (copy the file to tests/Integration/Phase06/Fixtures/Fixtures.php) and extend resetTables() to include 'login_streaks' + the four leaderboard_* summary tables (the truncate-IGNORE pattern already wraps the loop). Add a Fixtures::seedPointsLog() helper that wraps points_log_model::insert with the right metadata. Write the seven test files mirroring the Phase 5 AwardReviewPointsTest style. Each unit test seeds two users via Fixtures::seedUser() then calls the Service; each integration test runs SQL assertions. Update phpunit.xml to register the new testsuites.
  </action>
  <verify>
    <automated>vendor/bin/phpunit --testsuite=phase-6</automated>
    <fails_when>non-zero exit, or "0 passed" / "FAILURES" in summary</fails_when>
  </verify>
  <verify>
    <automated>vendor/bin/phpunit --testsuite=phase-6 --filter='TierFromPoints|AwardListingApproval|AwardReportValidated|AwardStreakBonus|VoidAndClearFreeze|MigrationIndexes|MigrationLastActive'</automated>
    <fails_when>non-zero exit, or "0 passed" / any "FAILURES" / any "ERROR" in summary</fails_when>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web request → points_service | All writers are Service-layer; the Action layer never touches `points_log` directly. |
| points_service → users / points_log | Sole writer of both tables (AD-10). Cross-context calls go through the Service. |
| Migration 019 trigger → users.last_active_at | DB-side trigger refreshes the denormalized column; no PHP code mutates it on the points_log path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Spoofing | rank_badge tooltip reads `config/ranks.php` | low | accept | Source of truth is the config table; no client-supplied data. |
| T-06-02 | Tampering | Migration 019 trigger on points_log | low | accept | Trigger is migration-owned; only DBA / migrate.php can drop + recreate. |
| T-06-03 | Repudiation | voidPoints + clearPointsFreeze | high | mitigate | Every freeze / void writes `Support\Audit::log()` row with actor_user_id (Phase 8 passes admin id; Phase 6 stubs `actor=null` for tests). Phase 8 wraps the hash chain. |
| T-06-04 | Information Disclosure | tier_progress tooltip exposes exact point counts | low | accept | Tier progress is profile-visible data (PTS-09 same scope); no PII. |
| T-06-05 | Denial of Service | new writers run inside caller transaction | low | accept | `ownsTransaction` keeps writes atomic; no new connection load. |
| T-06-06 | Elevation of Privilege | voidPoints() callable from any Action | medium | mitigate | Method is Service-public per AD-1; only Phase 8 admin Actions call it. Phase 6 ships the method but no Action exposes it via HTTP. |
| T-06-SC | Tampering | npm/pip/cargo installs | n/a | n/a | Phase 6 ships no new dependencies — sole Composer dep `ramsey/uuid` already present. No package legitimacy gate triggered. |
</threat_model>

<verification>
1. `php migrate.php` succeeds; re-running is a no-op.
2. `vendor/bin/phpunit --testsuite=phase-6` shows the new unit + integration tests passing.
3. `vendor/bin/phpcs --standard=PSR12 src/Points src/Support/View/partials/rank_badge.php src/Support/View/partials/tier_progress.php src/Support/View/partials/on_break_pill.php src/Support/View/partials/velocity_flag_pill.php src/User/Model/user_model.php config/contexts.php` exits 0.
4. `config/contexts.php` lists `Leaderboard` (Plan 06-03 ships the Service + Model + Action + View).
5. `vendor/bin/phpunit --testsuite=phase-5` still passes (no Phase 4/5 contract regression — points_service::awardTransaction() signature unchanged).
</verification>

<success_criteria>
- Migrations 018 + 019 apply cleanly + idempotently.
- The five new points_service writers + two freeze methods compile under PSR-12 and pass unit tests.
- The four new / extended partials render with the locked CSS classes and tooltip text per 06-UI-SPEC.md.
- `vendor/bin/phpunit --testsuite=phase-6` is fully green; no Phase 4/5 regression.
- No new dependencies, no schema changes outside `users.last_active_at` + `points_log` indexes.
</success_criteria>

<output>
Create `.planning/phases/06-points-ranks-leaderboards/06-01-SUMMARY.md` with the must_haves checklist ticked off, files_modified echo, and any deviations from this plan.
</output>
