---
phase: 6
plan: 02
type: feature
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/Points/Service/points_service.php
  - src/Points/Model/points_log_model.php
  - src/Auth/Service/auth_service.php
  - src/Support/Auth.php
  - src/User/Model/user_model.php
  - config/routes.php
autonomous: true
requirements:
  - PTS-05
  - PTS-06
  - PTS-07
  - PTS-10

must_haves:
  truths:
    - "points_service::awardTransaction() enforces TWO independent checks: (a) the PTS-05 per-day transactional cap (150 pts/day from transactions, sale + purchase + review) — when day_total + effective > 150, inserts a zero-delta points_log row with metadata.velocity_cap_hit=TRUE and returns {ok:true, data:{skipped:'velocity_cap', ...}} WITHOUT setting users.points_frozen; (b) the FR-PTS-010 freeze-trigger (>300 pts/day OR >150 pts/hr from transactions) — when day_total > 300 OR hour_total > 150, sets users.points_frozen=TRUE (first hit only; subsequent hits no-op the flag) and returns the same velocity_cap envelope. A user can hit the daily cap (zero-delta rows, no freeze), earn nothing the rest of that day, and resume normal earning the next day without the flag ever flipping."
    - "points_service::awardTransaction() inserts a pair_cap_hit points_log row with delta=0 (metadata.pair_cap_hit=TRUE, pair_count_today) when the count of distinct reference_id rows for the (buyer,seller,ticket) tuple today >= 2."
    - "Both cap-hit paths return {ok:true, data:{skipped:'velocity_cap'|'pair_cap', event_uuid, ...}} and write a Support\Audit::log() row with action='points.velocity_cap' or 'points.pair_cap'."
    - "points_service::awardReviewPoints() applies the same PTS-05 per-day transactional cap (150 pts/day from transactions includes review per REQUIREMENTS.md PTS-05) and the same FR-PTS-010 freeze-trigger (>300/day OR >150/hr). It does NOT apply the pair-cap (review is not a counted transaction). Returns the same velocity_cap envelope on either hit."
    - "auth_service::recordLogin(int $userId) writes users.last_active_at = NOW() via user_model::updateLastActive() and is called from auth_service::login() after startSession() succeeds."
    - "Phase 8 admin wiring ships as `Points/Action/PointsAdminAction.php` with handleVoidPoints() + handleClearPointsFreeze() methods that call points_service::voidPoints / clearPointsFreeze; the routes are NOT registered in config/routes.php in Phase 6 (deferred to Phase 8 per D-02)."
    - "The existing Phase 4 awardTransaction() signature is preserved (buyerId, sellerId, ticketId, deltaBuyer, deltaSeller, referenceType)."
    - "The cap-hit rows are surfaced in Recent activity on Profile with the dim 'counted as 0' meta text per D-08 + the UI-SPEC copywriting contract."
  artifacts:
    - src/Points/Service/points_service.php (velocity + pair-cap enforcement layered into awardTransaction + awardReviewPoints; audit row on every cap-hit + freeze)
    - src/Points/Model/points_log_model.php (zero-delta insert with metadata JSON helpers — reuses existing insert())
    - src/Auth/Service/auth_service.php (recordLogin method + login() call after startSession)
    - src/Support/Auth.php (expose the login hook if needed for the recordLogin call from the existing login flow)
    - src/User/Model/user_model.php (updateLastActive already added in 06-01; reused here)
    - src/Points/Action/PointsAdminAction.php (NEW — Phase 8 will register routes; Phase 6 ships the class so the methods are reachable from Phase 8's admin surface)
    - tests/Unit/Phase06/Points/VelocityCapTest.php
    - tests/Unit/Phase06/Points/PairCapTest.php
    - tests/Unit/Phase06/Points/VelocityFreezeTest.php
    - tests/Unit/Phase06/Auth/RecordLoginTest.php
    - tests/Integration/Phase06/CapAuditRowsTest.php
  key_links:
    - "Velocity cap enforcement runs inside the same transaction as the points_log INSERT (FOR UPDATE on user row serializes concurrent writers) — cap state and insert are atomic."
    - "pair_cap_hit metadata is the filter the velocity window SUM uses (excludes zero-delta pair-cap rows from the user's daily/hourly cap). Round-trip is closed: insert with pair_cap_hit=TRUE, subsequent velocity SUM ignores those rows."
    - "recordLogin writes last_active_at ONLY from the auth login path; the points_log INSERT trigger is the gamification-side writer (D-03 trigger path is unaffected by this plan)."
    - "Phase 8 admin Action routes are NOT added to config/routes.php in Phase 6 (per D-02 the UI is deferred; the Action class is shipped but unreachable from HTTP until Phase 8)."
---

<objective>
Layer the PTS-05 per-day transactional cap (150 pts/day from transactions: sale + purchase + review) and the FR-PTS-010 freeze-trigger (>300 pts/day OR >150 pts/hr) as TWO INDEPENDENT checks at insert time per REQUIREMENTS.md PTS-05 / CONTEXT.md FR-PTS-010 / D-08, plus the pair cap (max 2 counted transactions/day per buyer-seller pair), into `points_service` at insert time, wire `auth_service::recordLogin()` so successful logins refresh `users.last_active_at` (D-03), and ship the Phase 8 admin Action shim (`PointsAdminAction::handleVoidPoints / handleClearFreeze`) callable but not route-registered in Phase 6.

Purpose: every cap-hit writes an audit row (D-04) so Phase 8's admin UI can render the velocity-flag pill click-through; the `recordLogin` method ensures the 14-day On-Break gate is honest on the login side (the points_log trigger handles the gamification side per D-03).

Output: extended points_service with cap enforcement + audit wiring on every writer, auth_service::recordLogin called from the existing login() path, a Phase 8 Action shim, and the unit + integration tests covering the cap branches.
</objective>

<execution_context>
@/home/user/hermesag/004/.opencode/gsd-core/workflows/execute-plan.md
@/home/user/hermesag/004/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/user/hermesag/004/tickettrade/AGENTS.md
@/home/user/hermesag/004/AGENTS.md
@/home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md
@/home/user/hermesag/004/tickettrade/.planning/phases/06-points-ranks-leaderboards/06-CONTEXT.md
@/home/user/hermesag/004/tickettrade/.planning/phases/06-points-ranks-leaderboards/06-UI-SPEC.md
@/home/user/hermesag/004/tickettrade/src/Points/Service/points_service.php
@/home/user/hermesag/004/tickettrade/src/Auth/Service/auth_service.php
@/home/user/hermesag/004/tickettrade/src/Support/Auth.php
@/home/user/hermesag/004/tickettrade/src/Support/Audit.php
@/home/user/hermesag/004/tickettrade/config/routes.php
@/home/user/hermesag/004/tickettrade/tests/Unit/Phase05/Points/AwardReviewPointsTest.php
@/home/user/hermesag/004/tickettrade/.planning/phases/06-points-ranks-leaderboards/06-01-PLAN.md
</context>

<downstream_consumer>
Plan 06-03 reads the velocity cap short-circuit behavior in points_service (the daily cron calls points_service indirectly via user updates + leaderboard refresh; the cron does NOT trigger cap checks). The Phase 8 admin Action class `App\Points\Action\PointsAdminAction` is reachable from Phase 8's route registration; Phase 6 leaves `config/routes.php` untouched.
</downstream_consumer>

<tasks>

<task type="code">
  <name>Task 1: velocity cap + pair cap + audit wiring inside points_service</name>
  <files>src/Points/Service/points_service.php, src/Points/Model/points_log_model.php</files>
  <read_first>/home/user/hermesag/004/tickettrade/src/Points/Service/points_service.php, /home/user/hermesag/004/tickettrade/src/Points/Model/points_log_model.php, /home/user/hermesag/004/tickettrade/src/Support/Audit.php, /home/user/hermesag/004/tickettrade/.planning/phases/06-points-ranks-leaderboards/06-01-PLAN.md</read_first>
  <acceptance_criteria>
    - `awardTransaction()` locks both user rows (FOR UPDATE) then computes effectiveBuyer / effectiveSeller via the FR-PTS-007 halving (unchanged). BEFORE inserting the normal points_log rows, it calls points_log_model::sumForUserInWindow($pdo, $buyerId, '1 DAY', true) + sumForUserInWindow($pdo, $sellerId, '1 DAY', true) and the '1 HOUR' variant for both users. Two INDEPENDENT checks run for each user: (1) **PTS-05 per-day transactional cap** — if `day_total + effective > 150` for either user, INSERT a zero-delta points_log row with `metadata.velocity_cap_hit = TRUE, day_total_before, effective_delta`; DO NOT set `users.points_frozen`; call `Support\Audit::log(null, 'points.velocity_cap', 'user', $userId, metadata)`; return `{ok:true, data:{skipped:'velocity_cap', event_uuid, day_total_before, effective_delta}}`. (2) **FR-PTS-010 freeze-trigger** — evaluated against the same pre-cap day_total/hour_total (so the freeze is the ceiling of the cap): if `day_total > 300` OR `hour_total > 150` for either user AND `users.points_frozen = FALSE`, `UPDATE users SET points_frozen = TRUE, frozen_at = NOW() WHERE user_id = ?`; call `Support\Audit::log(null, 'points.frozen', 'user', $userId, {trigger:'day_overflow'|'hour_overflow', day_total, hour_total})`. The freeze flip is a separate step from the cap hit — a user can hit the cap (zero-delta rows accumulating) without the freeze ever flipping.
    - When velocity cap passes, `awardTransaction()` checks points_log_model::countPairInDay($pdo, $buyerId, $sellerId, $ticketId) (both directions). When count >= 2, inserts a zero-delta row with metadata.pair_cap_hit=TRUE (and audit-logs), returns {ok:true, data:{skipped:'pair_cap', event_uuid, pair_count_today}}.
    - When neither cap hits, the original INSERT + users UPDATE path runs (delta > 0, balance_after reflects the awarded amount).
    - `awardReviewPoints()` applies the same PTS-05 per-day transactional cap (150 pts/day from transactions includes review per REQUIREMENTS.md PTS-05) AND the same FR-PTS-010 freeze-trigger (>300/day OR >150/hr). It does NOT apply the pair-cap (review is not a counted transaction). Both checks follow the same two-branch shape as `awardTransaction()`: cap returns `{skipped:'velocity_cap'}` with no freeze flip; freeze-trigger sets `users.points_frozen=TRUE` on first hit and returns the same envelope. Returns the same velocity_cap envelope on either hit.
    - Every cap-hit and every freeze-set writes exactly one Support\Audit::log() row: action='points.velocity_cap' or 'points.pair_cap' (actor_user_id=null from the Service; the caller may pass an actor via a future $actorUserId parameter — out of scope here). action='points.frozen' when the flag is first set on this writer call.
    - The cap-hit metadata JSON includes the pre-cap totals so the audit row tells admins what threshold was breached.
  </acceptance_criteria>
  <action>
Inside points_service::awardTransaction(), after the FOR UPDATE lock step and the halving computation but before the INSERT, call the new points_log_model::sumForUserInWindow() for both buyer and seller with '1 DAY' and '1 HOUR' intervals and the excludePairCap=true flag. Run TWO INDEPENDENT checks per user (buyer and seller each checked): (1) **PTS-05 per-day cap** — if `day_total + effective > 150` for either user, INSERT a zero-delta points_log row with `metadata = {velocity_cap_hit:true, effective_delta, day_total_before, cap:'pts05_daily', party:'buyer'|'seller'}` and call `Audit::log(null, 'points.velocity_cap', 'user', $userId, metadata)`; return `{ok:true, data:{skipped:'velocity_cap', ...}}` WITHOUT touching `users.points_frozen`. (2) **FR-PTS-010 freeze-trigger** — if `day_total > 300` OR `hour_total > 150` for either user AND `users.points_frozen = FALSE` (checked from the same locked row), `UPDATE users SET points_frozen = TRUE, frozen_at = NOW() WHERE user_id = ?` and call `Audit::log(null, 'points.frozen', 'user', $userId, {trigger, day_total, hour_total})`. The freeze flip and the cap hit are separate concerns — the cap can fire many times in a day without the freeze ever firing, and the freeze can fire (on its own day_total/hour_total check) without an immediate cap hit. When neither check trips for either user, run the existing pair-cap check (countPairInDay), then the normal INSERT + UPDATE. Mirror the cap+freeze two-check logic in awardReviewPoints() (no pair-cap path). Do NOT change the awardTransaction() public signature.
  </action>
  <verify>
    <automated>vendor/bin/phpcs --standard=PSR12 src/Points/Service/points_service.php src/Points/Model/points_log_model.php</automated>
    <fails_when>non-zero exit, or any "ERROR"/"WARNING" line in output</fails_when>
  </verify>
  <verify>
    <automated>vendor/bin/phpunit --testsuite=phase-6-unit --filter='VelocityCap|PairCap|VelocityFreeze'</automated>
    <fails_when>non-zero exit, or "0 passed" / any "FAILURES" / any "ERROR" in summary</fails_when>
  </verify>
</task>

<task type="code">
  <name>Task 2: auth_service::recordLogin wired into the post-authenticate login() flow</name>
  <files>src/Auth/Service/auth_service.php</files>
  <read_first>/home/user/hermesag/004/tickettrade/src/Auth/Service/auth_service.php, /home/user/hermesag/004/tickettrade/src/User/Model/user_model.php, /home/user/hermesag/004/tickettrade/src/Support/Auth.php</read_first>
  <acceptance_criteria>
    - `auth_service::recordLogin(int $userId): void` is the canonical post-authenticate hook. Calls user_model::updateLastActive(Db::pdo(), $userId) (the method shipped in 06-01); swallows exceptions (never aborts the login).
    - `auth_service::login()` calls self::recordLogin((int) $user['user_id']) AFTER startSession() succeeds (so a failed session insert doesn't refresh last_active_at).
    - The `consumePasswordReset()` and `register()` flows also call recordLogin() after startSession() succeeds (these are post-authenticate auto-login paths per D-02 / D-07).
    - No public signature change on login() / consumePasswordReset() / register().
  </acceptance_criteria>
  <action>
Add `public static function recordLogin(int $userId): void` to auth_service that calls user_model::updateLastActive(Db::pdo(), $userId) inside a try/catch (matches the existing endSession() idempotent pattern). Insert the call into login() right after the startSession() invocation. Insert the call into consumePasswordReset() right after startSession() and into register() if register() ever auto-logs in (it does not currently; skip if no auto-login). Do NOT introduce new dependencies.
  </action>
  <verify>
    <automated>vendor/bin/phpcs --standard=PSR12 src/Auth/Service/auth_service.php</automated>
    <fails_when>non-zero exit, or any "ERROR"/"WARNING" line in output</fails_when>
  </verify>
  <verify>
    <automated>vendor/bin/phpunit --testsuite=phase-6-unit --filter='RecordLogin'</automated>
    <fails_when>non-zero exit, or "0 passed" / any "FAILURES" / any "ERROR" in summary</fails_when>
  </verify>
</task>

<task type="code">
  <name>Task 3: Phase 8 admin Action shim — PointsAdminAction with handleVoidPoints + handleClearFreeze</name>
  <files>src/Points/Action/PointsAdminAction.php</files>
  <read_first>/home/user/hermesag/004/tickettrade/src/Points/Service/points_service.php, /home/user/hermesag/004/tickettrade/src/Support/Auth.php, /home/user/hermesag/004/tickettrade/config/routes.php, /home/user/hermesag/004/tickettrade/.planning/phases/06-points-ranks-leaderboards/06-CONTEXT.md</read_first>
  <acceptance_criteria>
    - `App\Points\Action\PointsAdminAction::handleVoidPoints()` validates $_POST['user_id'] (int) + $_POST['delta'] (int) + $_POST['reason'] (string, non-empty). Calls Support\Auth::requireReAuth(300); on success calls points_service::voidPoints($userId, $delta, $reason); emits 200 + JSON envelope on success, 403 on re-auth fail, 400 on validation fail, 500 on Service error.
    - `App\Points\Action\PointsAdminAction::handleClearFreeze()` validates $_POST['user_id']. Calls Support\Auth::requireReAuth(300); on success calls points_service::clearPointsFreeze($userId); emits 200 + JSON envelope.
    - The class file ships in Phase 6 but config/routes.php is UNCHANGED (no POST /admin/points/void or /admin/points/clear route — per D-02 the UI lands in Phase 8).
  </acceptance_criteria>
  <action>
Create src/Points/Action/PointsAdminAction.php as a thin Action that mirrors the Admin\Action\CronAction shape (validate input → call Service → JSON envelope). Both methods emit JSON; both depend on Support\Auth::requireReAuth() to enforce AD-19. The handleClearFreeze() method additionally calls Audit::log($actorUserId, 'points.unfrozen', 'user', $userId) before delegating (or after — both are equivalent since clearPointsFreeze itself writes an audit row; avoid double-writing). Comment the file with "Phase 6 ships the method; Phase 8 registers the routes" so the Phase 8 executor knows what to wire.
  </action>
  <verify>
    <automated>vendor/bin/phpcs --standard=PSR12 src/Points/Action/PointsAdminAction.php</automated>
    <fails_when>non-zero exit, or any "ERROR"/"WARNING" line in output</fails_when>
  </verify>
  <verify>
    <automated>grep -c "PointsAdminAction" /home/user/hermesag/004/tickettrade/config/routes.php</automated>
    <fails_when>non-zero exit, or output equals "0" (means routes.php does NOT contain PointsAdminAction — correct, route deferred to Phase 8)</fails_when>
  </verify>
</task>

<task type="test">
  <name>Task 4: cap-enforcement unit + audit-row integration tests</name>
  <files>tests/Unit/Phase06/Points/VelocityCapTest.php, tests/Unit/Phase06/Points/PairCapTest.php, tests/Unit/Phase06/Points/VelocityFreezeTest.php, tests/Unit/Phase06/Auth/RecordLoginTest.php, tests/Integration/Phase06/CapAuditRowsTest.php</files>
  <read_first>/home/user/hermesag/004/tickettrade/tests/Unit/Phase05/Points/AwardReviewPointsTest.php, /home/user/hermesag/004/tickettrade/tests/Integration/Phase04/Fixtures/Fixtures.php, /home/user/hermesag/004/tickettrade/src/Support/Audit.php, /home/user/hermesag/004/tickettrade/.planning/phases/06-points-ranks-leaderboards/06-CONTEXT.md</read_first>
  <acceptance_criteria>
    - VelocityCapTest covers: (a) PTS-05 cap — day_total already 140, +30 award (effective=15 under halving) → day_total + effective = 155 > 150 → velocity_cap_hit row with delta=0, points_log entry with metadata.velocity_cap_hit=true (cap:'pts05_daily'), balance_after unchanged, return skipped='velocity_cap', users.points_frozen stays FALSE; (b) hour_total already 145, +30 award → hour_total + effective > 150 → cap_hit (no freeze); (c) FR-PTS-010 freeze-trigger — day_total already 290 with a single 30-effective award (day_total + effective = 320 > 300) → cap hit AND freeze flip on first hit (UPDATE users SET points_frozen=TRUE), audit row 'points.frozen' written; (d) second hit on same frozen user → cap_hit row written, freeze flip is a no-op, audit row NOT written for 'points.frozen'; (e) day_total=100 + award=20 (under 150) → normal award, no cap row, no freeze row.
    - PairCapTest covers: (a) same pair already has 2 counted transactions today → pair_cap_hit row, metadata.pair_cap_hit=true, delta=0, return skipped='pair_cap'; (b) first transaction of a new day → normal award; (c) different buyer-seller pair → not capped.
    - VelocityFreezeTest covers: first velocity-cap hit on a non-frozen user → UPDATE users SET points_frozen=TRUE; second velocity-cap hit on the same user → still inserts the audit row but does NOT re-UPDATE; clearPointsFreeze() flips the flag back to FALSE and writes audit 'points.unfrozen'.
    - RecordLoginTest covers: login() → recordLogin() → SELECT last_active_at is within the last 5 seconds (DB-side). register() / consumePasswordReset() auto-login paths also refresh last_active_at.
    - CapAuditRowsTest covers: after a velocity-cap hit on buyer, SELECT * FROM audit_log WHERE action='points.velocity_cap' returns 1 row with metadata.event_uuid matching the points_log row; after pair-cap, audit row exists for 'points.pair_cap'.
  </acceptance_criteria>
  <action>
Write five test files. Each follows the Phase 5 AwardReviewPointsTest pattern: extend Phase04\Fixtures (or the Phase06\Fixtures copy from 06-01), seed users + listings + tickets via Fixtures helpers, call the Service, assert on $this->pdo queries against points_log + audit_log + users. Use Fixtures::seedPointsLog() (added in 06-01) to populate the pre-cap rows so the cap window SUM has data. For the RecordLoginTest, dispatch a real login flow (seed user with bcrypt hash, call auth_service::login()) and assert users.last_active_at is recent.
  </action>
  <verify>
    <automated>vendor/bin/phpunit --testsuite=phase-6 --filter='VelocityCap|PairCap|VelocityFreeze|RecordLogin|CapAuditRows'</automated>
    <fails_when>non-zero exit, or "0 passed" / any "FAILURES" / any "ERROR" in summary</fails_when>
  </verify>
  <verify>
    <automated>vendor/bin/phpunit --testsuite=phase-5</automated>
    <fails_when>non-zero exit, or "0 passed" / any "FAILURES" (Phase 4+5 contract regression check)</fails_when>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| points_service → users + points_log + audit_log | Sole writer (AD-10, AD-12). The cap enforcement runs inside the same transaction as the award; the audit row is appended under the same DB session. |
| auth_service::recordLogin → users.last_active_at | Login-side writer of the denormalized column; trigger owns the points_log path (D-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Tampering | Velocity cap FOR UPDATE serialization | medium | mitigate | The cap SUM reads run AFTER the FOR UPDATE lock; concurrent writers serialize via the row lock, preventing two writers from both passing the cap and each crediting a row. |
| T-06-08 | Tampering | Pair-cap COUNT(DISTINCT reference_id) | medium | mitigate | Counted-transaction rows are `reference_type IN ('final_session','transaction')` only; review/listing/streak rows do not count toward the pair (D-08 spec). |
| T-06-09 | Repudiation | Cap-hit audit rows | high | mitigate | Every cap-hit writes `Support\Audit::log()` (action=points.velocity_cap or points.pair_cap). Phase 8 wraps the hash chain. |
| T-06-10 | Elevation of Privilege | PointsAdminAction class ships but routes don't | high | mitigate | `config/routes.php` is unchanged; the class is unreachable from HTTP until Phase 8 registers the routes. `requireReAuth(300)` is in the method body so when Phase 8 wires the route, re-auth is enforced. |
| T-06-11 | Information Disclosure | Pair-cap row exposes other party | low | accept | Pair-cap metadata is stored on the user's own points_log row; the audit row's metadata is admin-only (Phase 8 UI). No PII. |
| T-06-12 | Spoofing | recordLogin called by anyone | low | accept | recordLogin is called only from auth_service::login() / consumePasswordReset() / register() — all post-authenticate paths. The method itself is idempotent (UPDATE last_active_at = NOW() with no permission check; the row is the user's own). |
| T-06-SC | Tampering | npm/pip/cargo installs | n/a | n/a | Phase 6 ships no new dependencies. No package legitimacy gate triggered. |
</threat_model>

<verification>
1. `vendor/bin/phpcs --standard=PSR12 src/Points src/Auth/Service/auth_service.php src/Points/Action/PointsAdminAction.php` exits 0.
2. `vendor/bin/phpunit --testsuite=phase-6` shows the new cap + login tests passing.
3. `vendor/bin/phpunit --testsuite=phase-5` still passes — no Phase 4/5 contract regression.
4. `config/routes.php` is unchanged (grep `PointsAdminAction` returns 0).
5. `points_service::awardTransaction()` public signature is byte-identical to the Phase 4 contract.
</verification>

<success_criteria>
- PTS-05 per-day transactional cap (>150 pts/day from transactions) inserts a zero-delta audit-trailed row WITHOUT setting users.points_frozen. FR-PTS-010 freeze-trigger (>300/day OR >150/hr) sets users.points_frozen=TRUE on the first hit and writes an audit row. The two are independent checks; a user can hit the cap without the freeze flipping.
- Pair cap 2-counted/day inserts a zero-delta audit-trailed row; returns the skipped envelope.
- audit_log has one row per cap-hit with the matching action name.
- auth_service::login() refreshes users.last_active_at on success.
- Phase 8 admin Action class ships but is not route-reachable.
</success_criteria>

<output>
Create `.planning/phases/06-points-ranks-leaderboards/06-02-SUMMARY.md` with the must_haves checklist ticked off, files_modified echo, and any deviations from this plan.
</output>
