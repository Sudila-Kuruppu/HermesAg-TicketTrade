---
phase: 04-purchases-tickets-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - config/routes.php
  - src/Ticket/Action/MyTicketsAction.php
  - src/Ticket/Action/SalesAction.php
  - src/Ticket/Action/PurchasesAction.php
  - src/Ticket/View/my_tickets.php
  - src/Ticket/View/sales.php
  - src/Ticket/View/purchases.php
  - src/Support/View/partials/list_view_toggle.php (already exists from Phase 1; verify usage)
  - src/Support/View/partials/empty_state.php (already exists from Phase 3; verify usage)
  - src/Support/View/partials/bottom_nav.php (already exists; verify active tab is correct)
  - src/Ticket/Service/ticket_service.php (modified -- add getTicketsForBuyer, getTicketsForSeller, getGroupedSales, getPurchaseHistory helpers)
  - src/Ticket/Model/ticket_model.php (modified -- add the queries referenced by ticket_service)
  - src/Listing/View/listing_modal.php (modified -- Buy now button posts to /listings/{id}/buy, hidden when self-owned or sold out)
  - tests/Integration/Phase04/Ticket/MyTicketsViewTest.php
  - tests/Integration/Phase04/Ticket/SalesViewTest.php
  - tests/Integration/Phase04/Ticket/PurchaseHistoryTest.php
  - tests/Integration/Phase04/Ticket/BuyNowFlowTest.php
  - tests/Integration/Phase04/Ticket/RedemptionFlowTest.php
  - tests/Integration/Phase04/Ticket/SessionConfirmFlowTest.php
  - tests/Integration/Phase04/Ticket/DisputeFlowTest.php
  - tests/Integration/Phase04/Support/RouteGuardTicketTest.php
autonomous: true
requirements:
  - BUY-01
  - BUY-02
  - TKT-01
  - TKT-02
  - TKT-03
  - TKT-04
  - TKT-05
  - TKT-06
  - TKT-07
  - TKT-08
  - TKT-09
  - TKT-10
  - TKT-11
  - TKT-12
  - REL-01
  - REL-02
  - REL-04
  - REL-05
  - REL-06
user_setup: []

estimate:
  tokens: 98000
  raw_tokens: 49000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "GET /my-tickets as a logged-in buyer returns 200 with 5 tabs (All / Active / Redeemed / Expired / Disputed) where the active tab is `aria-current='page'` and clicking a tab reloads with `?tab=active|redeemed|expired|disputed` (default tab is `active`); each ticket card is rendered using the `listing_card` partial with the ticket's listing title + status badge + price + seller info row + ticket-code-block partial + dispute button when eligible (status IN active/redeemed, dispute_status=none)."
    - "POST /listings/{id}/buy as a logged-in buyer with a `purchase` rate-limit allowance calls `ticket_service::createTicket()`, sets `View::flash('success', 'Ticket created. Code: TK-...')` (the actual code is interpolated), and 302s to /my-tickets. The redirect triggers the inline `setTimeout(() => document.querySelector('[data-ticket-id=&quot;{$new_id}&quot;]')?.focus(), 50)` script (per D-02)."
    - "POST /listings/{id}/buy when the buyer's `user_id == listing.seller_id` renders the listing modal View with `error.message='You cannot buy your own listing.'` (E_TICKET_SELF_PURCHASE per the Phase 4 Service guard); no ticket is inserted; the listing modal's &quot;Buy Now&quot; button is also hidden server-side per EXPERIENCE.md L196-197."
    - "POST /listings/{id}/buy when the listing is sold out (`quantity_sold == quantity`) renders the listing modal with `error.message='This listing is sold out.'` (E_LISTING_SOLD_OUT); the listing modal's &quot;Buy Now&quot; button is replaced with an &quot;Out of stock&quot; badge."
    - "POST /tickets/redeem as a logged-in seller with a valid `TK-...` code and a `redemption` rate-limit allowance calls `ticket_service::redeemTicket()`, sets `View::flash('success', 'Ticket redeemed. Handover complete.')`, and 302s to /sales. The points_log has 2 rows (buyer +10, seller +30 with FR-PTS-007 halving applied for the first 5 redemptions)."
    - "POST /tickets/redeem with an invalid code (wrong format or not in DB) re-renders the Sales View with `error.message='Ticket not found.'` (E_TICKET_NOT_FOUND); no ticket is updated, no points are awarded."
    - "POST /tickets/redeem where the ticket belongs to a different seller returns `E_TICKET_FORBIDDEN` (mapped to 404, not 403, per AD-14)."
    - "POST /tickets/{id}/confirm-session as the ticket's seller with `session_number < total_sessions` increments `session_number` by 1; if the new value equals `total_sessions`, the ticket is auto-marked `redeemed` and points are awarded (final session); intermediate sessions only append an audit_log row, no points."
    - "POST /tickets/{id}/dispute as buyer or seller with `status IN (active, redeemed) AND dispute_status=none` sets `dispute_status='pending'` and (if the ticket was `active`) `status='disputed'`; a `reports` row is inserted with `target_type='ticket'`; the View renders the dispute modal's success toast `Dispute submitted. Admin will review within 48 hours.`"
    - "POST /tickets/{id}/dispute with reason NOT in the 4-value ENUM returns the originating page with `error.message='Reason is invalid.'` (E_DISPUTE_INVALID_REASON); no `reports` row is inserted."
    - "GET /sales as a logged-in seller returns 200 with the seller-grouped listing layout per D-05: each listing card group shows the listing title + per-listing-group progress chip (only when `total_sessions > 1`) + ticket rows with status badge + `#N/M` session progress (via the `session_progress` partial) + &quot;Confirm next session&quot; button next to the in-progress ticket (the one with the highest `session_number` whose ticket is `active`); the redemption input box (paste the buyer's `TK-...` code) sits at the top of the page and is always visible."
    - "GET /sales with no sales renders the empty state named copy `No sales yet. Your first sale happens when someone buys one of your listings.` with a `View your listings` link to `/my-listings`."
    - "GET /purchases as a logged-in buyer returns 200 with a chronological table (or stacked rows on mobile) showing ticket code, status, listing title, price, seller name, and date; the `Leave review` affordance is NOT in Phase 4 — the column header reads `Date` and the row shows `created_at` formatted in Asia/Colombo. Phase 5 wires the review affordance."
    - "GET /my-tickets with no tickets renders the empty state named copy `No tickets yet. Buy your first item.` with a `Browse Board` link to `/board`."
  artifacts:
    - config/routes.php
    - src/Ticket/Action/MyTicketsAction.php
    - src/Ticket/Action/SalesAction.php
    - src/Ticket/Action/PurchasesAction.php
    - src/Ticket/View/my_tickets.php
    - src/Ticket/View/sales.php
    - src/Ticket/View/purchases.php
    - src/Ticket/Service/ticket_service.php
    - src/Ticket/Model/ticket_model.php
    - src/Listing/View/listing_modal.php
    - tests/Integration/Phase04/Ticket/MyTicketsViewTest.php
    - tests/Integration/Phase04/Ticket/SalesViewTest.php
    - tests/Integration/Phase04/Ticket/PurchaseHistoryTest.php
    - tests/Integration/Phase04/Ticket/BuyNowFlowTest.php
    - tests/Integration/Phase04/Ticket/RedemptionFlowTest.php
    - tests/Integration/Phase04/Ticket/SessionConfirmFlowTest.php
    - tests/Integration/Phase04/Ticket/DisputeFlowTest.php
    - tests/Integration/Phase04/Support/RouteGuardTicketTest.php
  key_links:
    - "`MyTicketsAction::handle()` reads `?tab` (default `active`), calls `Ticket\\Service\\ticket_service::getTicketsForBuyer($userId, $tab)`, and renders `src/Ticket/View/my_tickets.php` with the tickets + tab counts + active tab name. The Action never writes to the DB directly."
    - "`SalesAction::handle()` calls `Ticket\\Service\\ticket_service::getGroupedSales($userId)`, which returns an array of `[listing_row, tickets[]]` groups sorted by `created_at DESC`. The Action renders `src/Ticket/View/sales.php` with the grouped data; each group renders the per-listing progress chip (when `total_sessions > 1`) and the in-progress ticket's &quot;Confirm next session&quot; button."
    - "`PurchasesAction::handle()` calls `Ticket\\Service\\ticket_service::getPurchaseHistory($userId)`, which returns tickets sorted by `created_at DESC` joined with the listing title + seller name. The Action renders `src/Ticket/View/purchases.php` as a table on desktop / stacked rows on mobile (Bootstrap 5 responsive classes)."
    - "The Buy now button in `src/Listing/View/listing_modal.php` is an `<a href=&quot;/listings/{$id}/buy&quot; class=&quot;btn btn-primary&quot;>Buy now</a>` for logged-in users; the link is HIDDEN when `seller_id == current_user_id` (per EXPERIENCE.md L196-197) OR when `quantity_sold >= quantity` (out-of-stock badge replaces it). The link POSTs via a small inline `<form method=&quot;POST&quot; action=&quot;/listings/{$id}/buy&quot;><input type=&quot;hidden&quot; name=&quot;csrf_token&quot; value=&quot;{token}&quot;><button type=&quot;submit&quot;>Buy now</button></form>` so the `BuyAction::handlePost()` CSRF check fires. The Phase 3 listing modal uses an `<a>` tag — Phase 4 replaces it with a `<form>`."
    - "The redemption input on the Sales page is a `<form method=&quot;POST&quot; action=&quot;/tickets/redeem&quot;>` with `<input name=&quot;ticket_code&quot;>` + `<input type=&quot;hidden&quot; name=&quot;csrf_token&quot;>` + `<button type=&quot;submit&quot;>Redeem</button>`. The Action reads `$_POST['ticket_code']`, strips whitespace + dashes server-side, and looks up the dashed form directly per D-01."
    - "The dispute button on each ticket card opens the dispute modal (server-rendered into the page so the form action is `/tickets/{$id}/dispute`). The modal carries `data-scrim-guard=&quot;2&quot;` to suppress backdrop click for 2 seconds per D-03 + EXPERIENCE.md L157."
    - "The My Tickets ticket cards use the corkboard `listing_card_cork.php` partial for visual continuity with the board (Phase 3 D-09 convention). Each card carries `--rot: {crc32($listingId) % 5 - 2}deg` and `aria-hidden` on the rotation + pin elements (Phase 1 corkboard convention)."
---

# Artifacts this phase produces

## Modified files

- `config/routes.php` — no new routes; existing `GET /my-tickets`, `GET /sales`, `GET /purchases` route entries' Action class names stay the same (`App\\Ticket\\Action\\MyTicketsAction`, `App\\Ticket\\Action\\SalesAction`, `App\\Ticket\\Action\\PurchasesAction`); their bodies are replaced.
- `src/Ticket/Action/MyTicketsAction.php` — replaced with the real body: read `?tab`, call `Ticket\\Service\\ticket_service::getTicketsForBuyer()`, render `src/Ticket/View/my_tickets.php`.
- `src/Ticket/Action/SalesAction.php` — replaced with the real body: call `Ticket\\Service\\ticket_service::getGroupedSales()`, render `src/Ticket/View/sales.php`.
- `src/Ticket/Action/PurchasesAction.php` — replaced with the real body: call `Ticket\\Service\\ticket_service::getPurchaseHistory()`, render `src/Ticket/View/purchases.php`.
- `src/Ticket/View/my_tickets.php` — replaced with the real View: 5 tabs nav + ticket cards (listing_card_cork partial) + ticket-code-block partial + status-badge partial + dispute button + empty state.
- `src/Ticket/View/sales.php` — replaced with the real View: per-listing-group cards + redemption input form + confirm-session buttons + empty state.
- `src/Ticket/View/purchases.php` — replaced with the real View: chronological table (desktop) / stacked rows (mobile) with date column.
- `src/Ticket/Service/ticket_service.php` — adds `getTicketsForBuyer($buyerId, $tab): array`, `getGroupedSales($sellerId): array`, `getPurchaseHistory($buyerId): array`. All are read-only and use the existing `ticket_model::findByBuyer` / `findActiveBySeller` / `findBySellerGrouped` queries.
- `src/Ticket/Model/ticket_model.php` — adds `findByBuyerAndStatus`, `findActiveServicesBySeller` if needed for the grouping.
- `src/Listing/View/listing_modal.php` — Buy now button changed from `<a href=&quot;/listings/{id}#buy&quot;>` to `<form method=&quot;POST&quot; action=&quot;/listings/{id}/buy&quot;>` with a CSRF token; hidden when `seller_id == current_user_id` OR `quantity_sold >= quantity`.

## New files

- `tests/Integration/Phase04/Ticket/MyTicketsViewTest.php`
- `tests/Integration/Phase04/Ticket/SalesViewTest.php`
- `tests/Integration/Phase04/Ticket/PurchaseHistoryTest.php`
- `tests/Integration/Phase04/Ticket/BuyNowFlowTest.php`
- `tests/Integration/Phase04/Ticket/RedemptionFlowTest.php`
- `tests/Integration/Phase04/Ticket/SessionConfirmFlowTest.php`
- `tests/Integration/Phase04/Ticket/DisputeFlowTest.php`
- `tests/Integration/Phase04/Support/RouteGuardTicketTest.php`


<objective>
Wire the Phase 4 substrate to the real buyer/seller UI: replace the three ticket-context placeholder Actions (MyTicketsAction, SalesAction, PurchasesAction) with real implementations that render the Phase 4 ticket cards (using the `ticket_code_block`, `session_progress`, `status_badge` partials from Plan 04-01), add the per-listing-group placement on Sales (per D-05), wire the listing modal's &quot;Buy now&quot; button to POST `/listings/{id}/buy`, and surface the redemption input + dispute modals. Phase 4 plan 03 extends the cron Action with the dispute-auto-dismiss + ticket-expiry sweeps; this plan delivers the visible core loop (buy → ticket → redeem → confirm-session → dispute) that the cron sweep then ages out.

Purpose: this plan is the Wave-2 tracer that proves the substrate works end-to-end. Without the View layer, the substrate (Plan 04-01) is unreachable from the UI. The plan ships the WAD demo path: register → verify → browse board → click a textbook listing → &quot;Buy now&quot; → confirm → toast "Ticket created. Code: TK-..." → /my-tickets → masked code revealed → copy + WhatsApp share → switch to seller account → paste code into Sales redemption input → ticket redeemed, points awarded.

Output: 3 Actions replaced; 3 Views replaced; 1 View modified (listing_modal Buy now form); 3 ticket_service helpers added; 2 ticket_model queries added; 8 new test files.

Per CONTEXT D-02 the toast-on-redirect + inline auto-focus is reversible; per D-05 the per-listing-group placement is reversible.

## Plan scope — what is NOT in this plan

- The cron Action extension (24h listing auto-approve + 3-day dispute auto-dismiss + 7-day ticket expiry) — Plan 04-03.
- Admin resolution Actions for disputes (Force Expire / Force Redeem / Dismiss) — Phase 7.
- The `Leave review` affordance on redeemed tickets (Phase 5 layers it on top of `/purchases`).
- Evidence image upload on the dispute modal — D-04, deferred.
- Real-time ticket status updates (polling is sufficient for the WAD scope).
</objective>

<execution_context>
@/home/user/hermesag/004/.opencode/gsd-core/workflows/execute-plan.md
@/home/user/hermesag/004/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/user/hermesag/004/tickettrade/AGENTS.md
@/home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md
@/home/user/hermesag/004/tickettrade/DESIGN.md
@/home/user/hermesag/004/tickettrade/EXPERIENCE.md
@/home/user/hermesag/004/tickettrade/WAD-CONTEXT.md
@/home/user/hermesag/004/tickettrade/.planning/PROJECT.md
@/home/user/hermesag/004/tickettrade/.planning/ROADMAP.md
@/home/user/hermesag/004/tickettrade/.planning/REQUIREMENTS.md
@/home/user/hermesag/004/tickettrade/.planning/STATE.md
@/home/user/hermesag/004/tickettrade/.planning/phases/04-purchases-tickets-lifecycle/04-CONTEXT.md
@/home/user/hermesag/004/tickettrade/.planning/phases/04-purchases-tickets-lifecycle/04-01-PLAN.md
@/home/user/hermesag/004/tickettrade/src/Support/Db.php
@/home/user/hermesag/004/tickettrade/src/Support/Auth.php
@/home/user/hermesag/004/tickettrade/src/Support/Csrf.php
@/home/user/hermesag/004/tickettrade/src/Support/RateLimit.php
@/home/user/hermesag/004/tickettrade/src/Support/Error.php
@/home/user/hermesag/004/tickettrade/src/Support/View.php
@/home/user/hermesag/004/tickettrade/src/Ticket/Model/ticket_model.php
@/home/user/hermesag/004/tickettrade/src/Ticket/Service/ticket_service.php
@/home/user/hermesag/004/tickettrade/src/Ticket/Action/BuyAction.php
@/home/user/hermesag/004/tickettrade/src/Ticket/Action/RedeemAction.php
@/home/user/hermesag/004/tickettrade/src/Ticket/Action/ConfirmSessionAction.php
@/home/user/hermesag/004/tickettrade/src/Ticket/Action/DisputeAction.php
@/home/user/hermesag/004/tickettrade/src/Support/View/partials/ticket_code_block.php
@/home/user/hermesag/004/tickettrade/src/Support/View/partials/session_progress.php
@/home/user/hermesag/004/tickettrade/src/Support/View/partials/status_badge.php
@/home/user/hermesag/004/tickettrade/src/Support/View/partials/listing_card_cork.php
@/home/user/hermesag/004/tickettrade/src/Support/View/partials/empty_state.php
@/home/user/hermesag/004/tickettrade/src/Support/View/partials/bottom_nav.php
@/home/user/hermesag/004/tickettrade/src/Support/View/partials/list_view_toggle.php
@/home/user/hermesag/004/tickettrade/src/Listing/View/listing_modal.php
@/home/user/hermesag/004/tickettrade/src/Listing/Action/BrowseAction.php
@/home/user/hermesag/004/tickettrade/src/Auth/Service/auth_service.php
@/home/user/hermesag/004/tickettrade/src/Points/Service/points_service.php
@/home/user/hermesag/004/tickettrade/config/routes.php
@/home/user/hermesag/004/tickettrade/public/mockups/my-tickets.html (Phase 1 reference for ticket-card markup)
@/home/user/hermesag/004/tickettrade/public/assets/css/tickettrade.components.css (existing CSS for ticket-code-block, session-progress, status-badge, corkboard card)
@/home/user/hermesag/004/tickettrade/public/assets/js/tickettrade.js (existing `data-flash-toast`, `data-component=&quot;toast&quot;`, `data-component=&quot;ticket-code-block&quot;` patterns)
</context>

<tasks>

<task type=&quot;tracer&quot;>
  <name>Task 1: Tracer — buy a listing end-to-end: buy → ticket appears in My Tickets with code → seller redeems → points awarded</name>
  <files>
    src/Ticket/Action/MyTicketsAction.php
    src/Ticket/Action/SalesAction.php
    src/Ticket/Action/PurchasesAction.php
    src/Ticket/View/my_tickets.php
    src/Ticket/View/sales.php
    src/Ticket/View/purchases.php
    src/Ticket/Service/ticket_service.php
    src/Ticket/Model/ticket_model.php
    src/Listing/View/listing_modal.php
    config/routes.php
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/AGENTS.md (WAD assignment context; dark-mode student surfaces default; < 2s pages)
    - /home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md (AD-1 layered monolith; AD-9 atomic UPDATE; AD-16 failure envelope; AD-19 admin re-auth 300s sliding window for cron endpoints — not needed in Plan 02)
    - /home/user/hermesag/004/tickettrade/.planning/phases/04-purchases-tickets-lifecycle/04-CONTEXT.md (D-02 toast-on-redirect + inline auto-focus script; D-03 dual-state dispute branch with text-only modal; D-05 per-listing-group placement; the agent's Discretion: self-purchase prevention hides the Buy Now button + Service-side guard for defense in depth; empty-state named copy: &quot;No tickets yet. Buy your first item.&quot; with Browse Board link, &quot;No sales yet. Your first sale happens when someone buys one of your listings.&quot; with View your listings link; toast on dispute filed: "Dispute submitted. Admin will review within 48 hours."; toast on ticket created: "Ticket created. Code: TK-..."; toast on session confirmed: &quot;Session N of M confirmed.&quot; / final session &quot;Ticket redeemed. Handover complete.&quot;; My Tickets tab structure 5 tabs (All / Active / Redeemed / Expired / Disputed) per the mockup with tab counts in the header)
    - /home/user/hermesag/004/tickettrade/.planning/phases/04-purchases-tickets-lifecycle/04-01-PLAN.md (the substrate Plan 01 ships the `Ticket\\Service\\ticket_service` four public methods + `points_service::awardTransaction()` + the 4 Actions + the 3 View partials)
    - /home/user/hermesag/004/tickettrade/src/Ticket/Service/ticket_service.php (already shipped; this plan adds 3 read-only helpers: `getTicketsForBuyer`, `getGroupedSales`, `getPurchaseHistory`)
    - /home/user/hermesag/004/tickettrade/src/Ticket/Action/BuyAction.php (already shipped from Plan 04-01; the `BuyAction::handlePost()` is the entry point for the buyer's POST)
    - /home/user/hermesag/004/tickettrade/src/Ticket/Action/RedeemAction.php (already shipped; the `RedeemAction::handlePost()` is the entry point for the seller's POST)
    - /home/user/hermesag/004/tickettrade/src/Ticket/Action/ConfirmSessionAction.php (already shipped; the Action handles intermediate and final sessions)
    - /home/user/hermesag/004/tickettrade/src/Ticket/Action/DisputeAction.php (already shipped; the Action creates the dispute and the `reports` row)
    - /home/user/hermesag/004/tickettrade/src/Support/View/partials/ticket_code_block.php (already shipped; the partial embeds `data-code-value` + `data-seller-whatsapp` + the masked default)
    - /home/user/hermesag/004/tickettrade/src/Support/View/partials/session_progress.php (already shipped; the partial renders `#N/M` + bar fill)
    - /home/user/hermesag/004/tickettrade/src/Support/View/partials/status_badge.php (already shipped; the partial renders `.status-badge.status-{status}` with the human label)
    - /home/user/hermesag/004/tickettrade/src/Support/View/partials/listing_card_cork.php (Phase 3 corkboard card partial; reused on My Tickets for visual continuity)
    - /home/user/hermesag/004/tickettrade/src/Support/View/partials/empty_state.php (Phase 3 partial; reused on My Tickets / Sales / Purchases empty states)
    - /home/user/hermesag/004/tickettrade/src/Support/View/partials/bottom_nav.php (Phase 1 partial; active tab is `aria-current='page'` for the matching route)
    - /home/user/hermesag/004/tickettrade/src/Listing/View/listing_modal.php (the Phase 3 modal; Phase 4 replaces the Buy now `<a>` with a `<form>` that POSTs to /listings/{id}/buy)
    - /home/user/hermesag/004/tickettrade/src/Listing/Action/BrowseAction.php (the Board action reads `currentUser()` and renders the modal with the Buy now CTA; Phase 4 does not change BrowseAction but the modal markup must honor the self-owned + sold-out states)
    - /home/user/hermesag/004/tickettrade/public/mockups/my-tickets.html (Phase 1 reference for ticket-card markup: 5 tabs, ticket cards with status badge + ticket-code-block + WhatsApp CTA; Phase 4 production renders match this markup exactly)
    - /home/user/hermesag/004/tickettrade/public/assets/css/tickettrade.components.css (the `.ticket-code-block`, `.session-progress`, `.status-badge.status-{active|redeemed|expired|disputed}`, `.listing-card` styles already shipped from Phase 1)
    - /home/user/hermesag/004/tickettrade/public/assets/js/tickettrade.js (the `data-component=&quot;ticket-code-block&quot;` handler shipped from Plan 04-01; `data-flash-toast` for the redirect toast; `TicketTrade.toast.show()` for the client-side toast)
  </read_first>
  <behavior>
    - `MyTicketsAction::handle()` reads `$_GET['tab']` (default `active`); calls `Ticket\\Service\\ticket_service::getTicketsForBuyer($userId, $tab)`; renders `src/Ticket/View/my_tickets.php` with the tickets + tab counts + active tab name. The View renders 5 tabs (All / Active / Redeemed / Expired / Disputed) with `aria-current='page'` on the active tab; each ticket card uses the corkboard listing_card_cork partial with `--rot: crc32($listingId) % 5 - 2` deg + `aria-hidden` on the rotation + pin elements.
    - Each ticket card shows: the listing title + status-badge partial + price (formatted as `Rs {price_cents/100}`) + seller info row (avatar + nickname + rank badge via `auth_service::tierFromPoints`) + ticket-code-block partial (masked default `TK-****-****-****-****-****` + reveal + copy + WhatsApp share link) + per-session progress `#N/M` for service tickets + a `Dispute` button when `dispute_status='none' AND status IN ('active','redeemed')` that opens the dispute modal.
    - The My Tickets tab counts (`All 2`, `Active 2`) render as inline `bg-secondary` badges next to each tab label.
    - `SalesAction::handle()` calls `Ticket\\Service\\ticket_service::getGroupedSales($userId)`; renders `src/Ticket/View/sales.php` with the grouped data. The page header has the redemption input form (`POST /tickets/redeem`) with a CSRF token + the `Submit` button; below the input, each listing renders as a card group: listing title + per-listing-group progress chip (`2/5 sessions confirmed`, only when `total_sessions > 1`) + ticket rows (status badge + ticket-code-block with the buyer's nickname + `#N/M` progress + `Confirm next session` button next to the in-progress ticket).
    - `PurchasesAction::handle()` calls `Ticket\\Service\\ticket_service::getPurchaseHistory($userId)`; renders `src/Ticket/View/purchases.php` as a Bootstrap 5 table on desktop / stacked rows on mobile with columns: Code (masked `TK-****-****-****-****-****` + reveal button), Status (status-badge), Listing (title), Price (`Rs {cents/100}`), Seller (nickname), Date (`Y-m-d H:i` Asia/Colombo). The `Leave review` column is NOT rendered (Phase 5).
    - Empty states per the agent's Discretion: My Tickets = `No tickets yet. Buy your first item.` + `Browse Board` link to `/board`; Sales = `No sales yet. Your first sale happens when someone buys one of your listings.` + `View your listings` link to `/my-listings`; Purchase History = `No purchases yet. Your first purchase appears here.` + `Browse Board` link to `/board`.
    - The Buy now button in `src/Listing/View/listing_modal.php` is a `<form method=&quot;POST&quot; action=&quot;/listings/{$id}/buy&quot;><input type=&quot;hidden&quot; name=&quot;csrf_token&quot; value=&quot;{token}&quot;><button type=&quot;submit&quot; class=&quot;btn btn-primary&quot;>Buy now</button></form>` for logged-in users; HIDDEN when `seller_id == current_user_id` (replaced with the named copy `This is your listing.` per EXPERIENCE.md L196-197) OR when `quantity_sold >= quantity` (replaced with the named copy `Out of stock.`); for guests, the button is `<a href=&quot;/login?next=/board&quot;>Sign in to buy</a>` per D-09 of Phase 3 (unchanged).
    - The dispute modal in the My Tickets View is rendered as a `<form method=&quot;POST&quot; action=&quot;/tickets/{$id}/dispute&quot; data-scrim-guard=&quot;2&quot;>` with the reason `<select>` (4 options per D-03) + 200-char text `<textarea>` + a live character counter + Cancel/Submit buttons. The modal is hidden by default and shown via `bootstrap.Modal.show()` on the `Dispute` button click.
  </behavior>
  <action>
    - Replace `src/Ticket/Action/MyTicketsAction.php` with the real body: read `$_GET['tab']` (default `active`); validate against `['all','active','redeemed','expired','disputed']`; call `Ticket\\Service\\ticket_service::getTicketsForBuyer($userId, $tab)`; render `src/Ticket/View/my_tickets.php` with `['tickets' => $tickets, 'tab' => $tab, 'tab_counts' => $tabCounts, 'user' => $user]`.
    - Replace `src/Ticket/Action/SalesAction.php` with the real body: call `Ticket\\Service\\ticket_service::getGroupedSales($userId)`; render `src/Ticket/View/sales.php` with `['groups' => $groups, 'user' => $user]` where `$groups` is an array of `['listing' => $row, 'tickets' => [...]]`.
    - Replace `src/Ticket/Action/PurchasesAction.php` with the real body: call `Ticket\\Service\\ticket_service::getPurchaseHistory($userId)`; render `src/Ticket/View/purchases.php` with `['tickets' => $tickets, 'user' => $user]`.
    - Replace `src/Ticket/View/my_tickets.php` with the real View: 5 tabs nav (`?tab=active` etc.) + per-ticket card using `listing_card_cork.php` partial + ticket-code-block + status-badge + dispute button (when eligible). The empty state renders via `empty_state.php` partial.
    - Replace `src/Ticket/View/sales.php` with the real View: redemption input form at top + per-listing-group card loop + per-ticket row with confirm-session button. The empty state renders via `empty_state.php` partial.
    - Replace `src/Ticket/View/purchases.php` with the real View: Bootstrap 5 table (desktop) / stacked rows (mobile). The empty state renders via `empty_state.php` partial.
    - Modify `src/Ticket/Service/ticket_service.php` to add `getTicketsForBuyer($buyerId, $tab): array`, `getGroupedSales($sellerId): array`, `getPurchaseHistory($buyerId): array`. All are read-only and use the existing `ticket_model::findByBuyer` / `findActiveBySeller` / `findBySellerGrouped` queries; they return arrays directly (no envelope wrapping for read-only paths).
    - Modify `src/Ticket/Model/ticket_model.php` to add `findByBuyerAndStatus($buyerId, $status, $limit): array` (for the tab filter) and `findActiveServicesBySeller($sellerId): array` (for the per-listing-group placement when `total_sessions > 1`). The existing `findActiveBySeller` + `findBySellerGrouped` queries stay.
    - Modify `src/Listing/View/listing_modal.php`: replace the Buy now `<a href=&quot;/listings/{id}#buy&quot;>` with a `<form method=&quot;POST&quot; action=&quot;/listings/{id}/buy&quot;>`; render the form ONLY when `current_user` is logged in AND `seller_id != current_user_id` AND `quantity_sold < quantity`. For self-owned listings, render the named copy `This is your listing.` For sold-out listings, render the named copy `Out of stock.` For guests, render the existing `<a href=&quot;/login?next=/board&quot;>Sign in to buy</a>` per Phase 3 D-09.
    - Add a small inline `<script>` at the bottom of `my_tickets.php` that implements the auto-focus per D-02: `setTimeout(() => document.querySelector('[data-ticket-id=&quot;{$new_ticket_id}&quot;]')?.focus(), 50);`. The `$new_ticket_id` is read from `$_GET['new']` (the BuyAction redirects with `?new={$ticketId}` so the View knows which card to focus; or the Action can set a flash-toast with the ticket id and the View can pick it up from there — the simplest path is `?new={$ticketId}` in the redirect URL).
  </action>
  <verify>
    <automated>cd /home/user/hermesag/004/tickettrade && APP_ENV=test php migrate.php && echo "MIGRATE_OK" && APP_ENV=test vendor/bin/phpunit --testsuite=phase-4 --filter='MyTicketsView|SalesView|PurchaseHistory|BuyNowFlow|RedemptionFlow|SessionConfirmFlow|DisputeFlow|RouteGuardTicket' 2>&1 | tail -15</automated>
    <fails_when>non-zero exit, or MIGRATE_OK missing, or "0 passed" / &quot;FAILURES&quot; / &quot;ERRORS&quot; in the phpunit output</fails_when>
  </verify>
</task>

<task type=&quot;standard&quot;>
  <name>Task 2: Listing modal Buy now form + ticket-code-block UI behaviors (mask/reveal/copy/WhatsApp share)</name>
  <files>
    src/Listing/View/listing_modal.php
    src/Ticket/View/my_tickets.php
    src/Ticket/View/sales.php
    src/Support/View/partials/ticket_code_block.php
    public/assets/js/tickettrade.js
    tests/Integration/Phase04/Ticket/MyTicketsViewTest.php
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/src/Listing/View/listing_modal.php (existing modal markup; the Phase 4 Buy now form replaces the `<a>` tag)
    - /home/user/hermesag/004/tickettrade/src/Support/View/partials/ticket_code_block.php (already shipped from Plan 04-01; this task wires it into the My Tickets + Sales + Purchases Views)
    - /home/user/hermesag/004/tickettrade/public/assets/js/tickettrade.js (the Phase 1 toast + bottom-nav + skeleton components; Plan 04-01 added the ticket-code-block handler; this task verifies the JS handler is wired and the reveal/copy/WhatsApp behaviors work end-to-end)
    - /home/user/hermesag/004/tickettrade/EXPERIENCE.md (L146 disabled state with tooltip `Seller has not shared WhatsApp`; L157 dispute modal scrim-guard 2s; L196-197 self-owned + sold-out affordances)
    - /home/user/hermesag/004/tickettrade/DESIGN.md (`ticket-code-block` component: monospace amber on near-black, letter-spacing 0.04em; `status-badge` 8 variants; `dispute-modal` 4 reasons + 200-char text + optional image deferred)
  </read_first>
  <behavior>
    - On the listing modal: clicking the Buy now form submits POST `/listings/{id}/buy` with the CSRF token; the BuyAction handles it (rate-limit + Service + redirect on success / render-with-error on failure).
    - On the My Tickets + Sales + Purchases Views: each ticket-code-block renders with the masked default `TK-****-****-****-****-****`. Clicking the reveal button toggles `aria-pressed` and replaces the masked text with the full code from `data-code-value`. Clicking the copy button copies the full code to clipboard via `navigator.clipboard.writeText()` and emits a `TicketTrade.toast.show({type:'success', message:'Copied', duration:1500})` confirmation.
    - The WhatsApp share link is built server-side: `https://wa.me/?text=My%20ticket%20code%3A%20{urlencode($code)}` when the seller's `users.whatsapp` is NULL; `https://wa.me/{$sellerWhatsapp}?text=My%20ticket%20code%3A%20{urlencode($code)}` otherwise. The disabled state (when `seller_whatsapp` is NULL) shows a tooltip `Share via WhatsApp — seller has not shared their number.` via the `title` attribute.
    - The dispute modal carries `data-scrim-guard=&quot;2&quot;`; backdrop click is suppressed for 2 seconds after open per the Phase 1 `data-scrim-guard` attribute pattern.
  </behavior>
  <action>
    - In `src/Listing/View/listing_modal.php`, locate the Buy now `<a>` tag and replace with a `<form>` per the must_haves section. Add the conditions: only render the form when `current_user` is logged in AND `seller_id != current_user_id` AND `quantity_sold < quantity`. Add the self-owned and sold-out named copy per EXPERIENCE.md L196-197. The guest `<a>` to `/login?next=/board` stays per Phase 3 D-09.
    - In `src/Ticket/View/my_tickets.php`, ensure every ticket card renders the `ticket_code_block` partial with the ticket's code + the seller's WhatsApp (or NULL).
    - In `src/Ticket/View/sales.php`, ensure every ticket row renders the `ticket_code_block` partial with the buyer's code + the buyer's WhatsApp (so the seller can contact the buyer to coordinate handover).
    - In `src/Ticket/View/purchases.php`, ensure every ticket row renders the `ticket_code_block` partial with the ticket's code + the seller's WhatsApp.
    - Verify `public/assets/js/tickettrade.js` registers the `data-component=&quot;ticket-code-block&quot;` handler on `DOMContentLoaded`. The handler queries all `[data-component=&quot;ticket-code-block&quot;]` elements, reads `data-code-value` + `data-seller-whatsapp`, binds click on the reveal button (toggles `aria-pressed` + replaces the masked code with the full code from `data-code-value`), binds click on the copy button (`navigator.clipboard.writeText(data-code-value)` + emits a toast). The WhatsApp share link's `href` is server-built; the JS does NOT touch it.
    - Verify the dispute modal's `data-scrim-guard=&quot;2&quot;` attribute is on the modal element (Phase 1 JS reads this attribute and suppresses backdrop click for the configured seconds).
  </action>
  <verify>
    <automated>cd /home/user/hermesag/004/tickettrade && APP_ENV=test vendor/bin/phpunit --testsuite=phase-4 --filter='MyTicketsView|TicketCode' 2>&1 | tail -15</automated>
    <fails_when>non-zero exit, or "0 passed" / &quot;FAILURES&quot; / &quot;ERRORS&quot; in the phpunit output</fails_when>
  </verify>
</task>

<task type=&quot;standard&quot;>
  <name>Task 3: Tests — Integration for Views + Actions + End-to-end ticket lifecycle (buy → redeem → confirm-session → dispute)</name>
  <files>
    tests/Integration/Phase04/Ticket/MyTicketsViewTest.php
    tests/Integration/Phase04/Ticket/SalesViewTest.php
    tests/Integration/Phase04/Ticket/PurchaseHistoryTest.php
    tests/Integration/Phase04/Ticket/BuyNowFlowTest.php
    tests/Integration/Phase04/Ticket/RedemptionFlowTest.php
    tests/Integration/Phase04/Ticket/SessionConfirmFlowTest.php
    tests/Integration/Phase04/Ticket/DisputeFlowTest.php
    tests/Integration/Phase04/Support/RouteGuardTicketTest.php
    phpunit.xml
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/phpunit.xml (existing testsuites: phase-1, phase-2, phase-3, phase-4)
    - /home/user/hermesag/004/tickettrade/tests/Integration/Phase03/Listing/CreateListingFlowTest.php (canonical integration test pattern for Action + Service + DB)
    - /home/user/hermesag/004/tickettrade/tests/Integration/Phase03/Support/RouteGuardListingTest.php (canonical route-guard test pattern: assert guest 302, / assert logged-in 200)
    - /home/user/hermesag/004/tickettrade/.planning/phases/04-purchases-tickets-lifecycle/04-01-PLAN.md (the substrate Plan 01 ships 10 test files; this plan's tests extend them with the View-level + flow-level assertions)
  </read_first>
  <behavior>
    - `MyTicketsViewTest`: assert `MyTicketsAction::handle()` renders the 5 tabs nav + the ticket cards + the empty state when the user has no tickets.
    - `SalesViewTest`: assert `SalesAction::handle()` renders the redemption input + the per-listing-group cards + the per-ticket rows with confirm-session button + the empty state when the user has no sales.
    - `PurchaseHistoryTest`: assert `PurchasesAction::handle()` renders the chronological table + the empty state when the user has no purchases.
    - `BuyNowFlowTest`: end-to-end — buyer logs in, navigates to a textbook listing, clicks Buy now, sees the toast `Ticket created. Code: TK-...`, redirected to /my-tickets with the new ticket card auto-focused; the listing's `quantity_sold` is incremented; the audit_log + ticket rows exist.
    - `RedemptionFlowTest`: end-to-end — buyer buys a ticket, switches to seller account, pastes the `TK-...` code into the Sales redemption input, sees the toast `Ticket redeemed. Handover complete.`; the ticket is `redeemed`, the points_log has 2 rows, `users.points` + `users.tier` are updated for both parties, `redeemed_count` is incremented.
    - `SessionConfirmFlowTest`: end-to-end for a service ticket — buyer buys a 5-session tutoring ticket, seller confirms session 1 (no points), session 2 (no points), ..., session 5 (points awarded, ticket auto-redeemed); the audit_log has 5 rows with `action='ticket.session_confirmed'` and `is_final=false,1,2,3,true`.
    - `DisputeFlowTest`: end-to-end — buyer buys a ticket, opens the dispute modal, selects reason `seller_unresponsive`, enters text, submits; the ticket's `dispute_status='pending'` AND `status='disputed'` (dual-state per D-03); a `reports` row is inserted; the audit_log has a `ticket.dispute_filed` row; seller cannot redeem the disputed ticket (returns `E_TICKET_INVALID_STATE`).
    - `RouteGuardTicketTest`: assert `GET /my-tickets` without session returns 302 to `/login?next=/my-tickets`; `GET /sales` without session returns 302 to `/login?next=/sales`; `GET /purchases` without session returns 302 to `/login?next=/purchases`.
  </behavior>
  <action>
    - Create the 8 new test files per the behavior section above. Each test extends `PhaseX\\Integration\\DatabaseTestCase` (copy from Phase 3's pattern) that boots a fresh test DB in `setUp()` and truncates the affected tables.
    - The View tests use a simple output buffer capture (`ob_start()` + `View::render()` + `ob_get_clean()`) and assert substrings of the rendered HTML.
    - The Flow tests use a more end-to-end approach: bootstrap the request via `Support\\Router::dispatch('student', '/my-tickets')` + simulate the form POST by setting `$_POST` + `$_SERVER['REQUEST_METHOD']='POST'` before dispatch.
    - Add the 8 new test files to the existing `<testsuite name=&quot;phase-4&quot;>` in `phpunit.xml`.
  </action>
  <verify>
    <automated>cd /home/user/hermesag/004/tickettrade && APP_ENV=test vendor/bin/phpunit --testsuite=phase-4 2>&1 | tail -15</automated>
    <fails_when>non-zero exit, or "0 passed" / &quot;FAILURES&quot; / &quot;ERRORS&quot; in the phpunit output</fails_when>
  </verify>
</task>
</tasks>

<output>
Create `.planning/phases/04-purchases-tickets-lifecycle/04-02-SUMMARY.md` when done.
</output>



<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /listings/{id}/buy | Untrusted buyer intent (Phase 4 Plan 01 covers this). |
| Browser → POST /tickets/redeem | Untrusted ticket code + redemption intent (Phase 4 Plan 01). |
| Browser → POST /tickets/{id}/confirm-session | Untrusted session-confirm intent (Phase 4 Plan 01). |
| Browser → POST /tickets/{id}/dispute | Untrusted dispute reason + text (Phase 4 Plan 01). |
| Browser → GET /my-tickets | Authenticated read; Service returns sanitized ticket rows joined with listing title + seller info. |
| Browser → GET /sales | Authenticated read; Service returns sanitized ticket rows grouped by listing. |
| Browser → GET /purchases | Authenticated read; Service returns sanitized ticket rows sorted by created_at DESC. |
| Action → View | Action passes sanitized arrays; View renders via `htmlspecialchars()` (AD-16). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-18 | Information Disclosure | View rendering | medium | mitigate | Every dynamic value in the View is rendered via `htmlspecialchars()` (AD-16). The ticket-code-block, session-progress, status-badge partials escape all values. The dispute modal's text field is server-truncated to 200 chars. |
| T-04-19 | Tampering | View state mutation | low | mitigate | Views are read-only templates; no `$_POST` writes from the View. The Action handles all state mutations. |
| T-04-20 | Elevation of Privilege | Cross-seller ticket read | medium | mitigate | `MyTicketsAction::handle()` queries `WHERE buyer_id = $userId`; cross-buyer reads return zero rows. `SalesAction::handle()` queries `WHERE seller_id = $userId`. The Service never returns tickets the user is not a party to. |
| T-04-21 | Elevation of Privilege | Cross-seller ticket redemption | high | mitigate | (already covered in Plan 01, T-04-04) |
| T-04-22 | Tampering | WhatsApp share link | low | mitigate | The link's `href` is server-built; the JS does NOT touch it. The `data-code-value` attribute is `htmlspecialchars()`-escaped in the partial. |
| T-04-23 | Spoofing | Clipboard write | low | mitigate | `navigator.clipboard.writeText()` is the standard browser API; the action is gated by `https` in prod (the dev server is `http://localhost:8000` which works for `localhost`). The toast confirms the copy with a 1.5s success indicator. |
| T-04-24 | Information Disclosure | `/my-tickets` 403 vs 404 | low | mitigate | Unauthenticated access returns 302 to `/login?next=/my-tickets` (per AD-14); no 403 path. |
| T-04-25 | Tampering | Composer installs | high | mitigate | No new Composer packages. |

## Trust-Boundary Coverage

- T-04-18: View rendering escapes all dynamic values; the partials are the canonical escape wrappers.
- T-04-20, T-04-21: Action-level Service queries scope by buyer_id / seller_id.
- T-04-22: Server-built WhatsApp URL; client-side JS is read-only.

## Package Legitimacy Audit

- No new Composer packages.
- T-04-25: not applicable.
</threat_model>

<verification>
- [ ] `vendor/bin/phpunit --testsuite=phase-4` is green (≥18 test files, ≥80 cases after this plan).
- [ ] `vendor/bin/phpunit` (full suite) is green — no Phase 1/2/3 test broke.
- [ ] `vendor/bin/phpcs --standard=PSR12 src/Ticket` exits 0.
- [ ] Clicking Buy now on a textbook listing as a logged-in buyer: redirects to /my-tickets with the new ticket card auto-focused; toast `Ticket created. Code: TK-...` is announced via `aria-live`.
- [ ] The listing modal hides the Buy now button when `seller_id == current_user_id` AND when `quantity_sold >= quantity`.
- [ ] The My Tickets 5 tabs render with `aria-current='page'` on the active tab.
- [ ] The Sales page renders the redemption input at the top + the per-listing-group cards with confirm-session buttons.
- [ ] The Purchase History page renders a chronological table on desktop / stacked rows on mobile.
- [ ] Submitting a dispute on an active ticket sets `dispute_status='pending'` AND `status='disputed'`; a `reports` row is inserted with `target_type='ticket'`.
</verification>

<success_criteria>
- The Phase 4 UI is operational: a buyer can buy a listing, see the ticket in /my-tickets with the code block, and dispute if needed.
- A seller can redeem a code in /sales, confirm sessions for service tickets, and see all their sales grouped by listing.
- All 18 test files pass; the full PHPUnit suite is green.
- All artifacts listed under &quot;Artifacts this phase produces&quot; exist on disk.
- The listing modal hides the Buy now button per the self-owned + sold-out affordances.
- The dispute modal's scrim-guard prevents accidental backdrop dismissal per the Phase 1 pattern.
</success_criteria>

