---
phase: 04-purchases-tickets-lifecycle
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - config/routes.php
  - src/Listing/Action/ListingAutoApproveAction.php (renamed → src/Admin/Action/CronAction.php; class name updated)
  - src/Ticket/Service/ticket_service.php (modified: add runTicketExpirySweep, runDisputeAutoDismissSweep, restoreListingStockForExpiredTicket)
  - src/Ticket/Model/ticket_model.php (modified: add findExpiringTickets, findStaleDisputes, decrementListingStockForExpiredTicket)
  - src/Listing/Service/listing_service.php (modified: add runAutoApproveSweep delegate; deprecate the old method name or keep it as a wrapper)
  - src/Admin/Action/CronAction.php (new)
  - tests/Integration/Phase04/Cron/CronSweepTest.php
  - tests/Integration/Phase04/Cron/IdempotencyTest.php
  - tests/Integration/Phase04/Cron/DisputeAutoDismissTest.php
  - tests/Integration/Phase04/Cron/TicketExpiryTest.php
  - tests/Integration/Phase04/Cron/PerformanceTest.php
autonomous: true
requirements:
  - TKT-06
  - TKT-07
  - TKT-08
  - TKT-09
  - TKT-10
  - REL-01
  - REL-02
  - REL-04
  - REL-05
  - REL-06
user_setup: []

estimate:
  tokens: 78000
  raw_tokens: 39000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "POST /admin/cron/ticket-expiry as a logged-in admin WITH a fresh re-auth (within 300s) runs the three sweeps in order per D-07: (1) 24h listing auto-approve (Phase 3 kept), (2) 3-day dispute auto-dismiss, (3) 7-day ticket expiry; returns JSON `{ok:true, sweeps:{listing_auto_approve:{processed:N1}, dispute_auto_dismiss:{processed:N2, affected_tickets:[...]}, ticket_expiry:{processed:N3}}, errors:[]}`. Each sweep appends a `cron_log` row with the job_name + processed count + errors_json + actor_user_id."
    - "The 24h listing auto-approve sweep runs `UPDATE listings SET status='active', approved_at=NOW(), approved_by=NULL, updated_at=NOW() WHERE status='pending' AND created_at <= NOW() - INTERVAL 24 HOUR`; `rowCount()` is the `processed` count; re-running inside the 24h window is idempotent (N=0)."
    - "The 3-day dispute auto-dismiss sweep runs `UPDATE tickets SET dispute_status='rejected', updated_at=NOW() WHERE dispute_status='pending' AND disputed_at <= NOW() - INTERVAL 3 DAY`; for each affected ticket, the Service restores the ticket's `status` to its pre-dispute value (`active` if filed on `active`, `redeemed` if filed on `redeemed`) via `Support\\Audit::log('ticket.dispute_auto_dismissed')`; the `created_at` is NEVER touched (per D-07). The sweep runs BEFORE the ticket-expiry sweep so dismissed-then-expired tickets land in `expired` in the same tick (PRD §4.2 composition note)."
    - "The 7-day ticket expiry sweep runs `UPDATE tickets SET status='expired', updated_at=NOW() WHERE status='active' AND dispute_status != 'pending' AND expires_at <= NOW()`; for each affected ticket, the Service decrements `listings.quantity_sold` by 1 (products) or `total_sessions - (session_number - 1)` (services); restores `listings.status='active'` when `quantity_sold < quantity AND status='sold'`; appends `Support\\Audit::log('ticket.expired')`. The sweep SKIPS tickets with `dispute_status='pending'` (admin must resolve first per PRD §4.2)."
    - "POST /admin/cron/ticket-expiry without admin session returns 404 (admin guard, unchanged from Phase 3). Without re-auth returns 403 with JSON `{ok:false, error:'re-auth required'}`. Re-running the endpoint with no eligible rows is idempotent (all sweeps return N=0, no errors)."
    - "Re-running the cron with the same DB state 5 times in succession produces the same end state: the same ticket statuses, the same `quantity_sold` values, the same `cron_log` row count delta of 5 (one per run) — but the business state is unchanged after run 2 (per NFR-REL-002). Performance: the ticket expiry sweep completes in < 30s for 10k tickets per NFR-PER-004 (single guarded UPDATE statement)."
    - "`src/Listing/Action/ListingAutoApproveAction.php` is RENAMED to `src/Admin/Action/CronAction.php` (the class name changes too); the existing `POST /admin/cron/ticket-expiry` route entry's class name updates to `App\\Admin\\Action\\CronAction`. The Phase 3 24h auto-approve logic moves into the new `CronAction::run24hListingAutoApproveSweep()` method. The old `listing_service::runAutoApproveSweep()` method is KEPT (as a wrapper that delegates to the new cron Action's sweep) so Phase 3 tests still pass."
  artifacts:
    - config/routes.php
    - src/Listing/Action/ListingAutoApproveAction.php
    - src/Admin/Action/CronAction.php
    - src/Ticket/Service/ticket_service.php
    - src/Ticket/Model/ticket_model.php
    - src/Listing/Service/listing_service.php
    - tests/Integration/Phase04/Cron/CronSweepTest.php
    - tests/Integration/Phase04/Cron/IdempotencyTest.php
    - tests/Integration/Phase04/Cron/DisputeAutoDismissTest.php
    - tests/Integration/Phase04/Cron/TicketExpiryTest.php
    - tests/Integration/Phase04/Cron/PerformanceTest.php
  key_links:
    - "`App\\Admin\\Action\\CronAction::handle()` is the renamed dispatcher: it validates `Support\\Auth::requireReAuth(300)`, then calls the three sweep methods in order: `run24hListingAutoApproveSweep()`, `runDisputeAutoDismissSweep()`, `runTicketExpirySweep()`; collects the per-sweep results into the JSON envelope `{ok:true, sweeps:{...}, errors:[]}`; emits a 200 + JSON response (or 500 + error envelope on failure)."
    - "Each sweep method delegates to its respective Service (`Listing\\Service\\listing_service::runAutoApproveSweep()` for the 24h sweep, `Ticket\\Service\\ticket_service::runDisputeAutoDismissSweep()` for the dispute sweep, `Ticket\\Service\\ticket_service::runTicketExpirySweep()` for the ticket expiry sweep). Each Service method returns the AD-16 envelope with `data.processed` + `data.affected_tickets`; the Action concatenates them into the per-sweep JSON."
    - "The 3-day dispute auto-dismiss sweep restores the pre-dispute `status` via a `CASE WHEN` in the UPDATE (the Service reads the old `status` BEFORE the UPDATE, captures it in a per-ticket log, then applies the UPDATE with the matching restored value). For tickets filed on `active`, the new `status='active'`; for tickets filed on `redeemed`, the new `status='redeemed'`. The `disputed_at` is NEVER touched; the `created_at` is NEVER touched."
    - "The 7-day ticket expiry sweep decrements `listings.quantity_sold` per the AD-7 inventory invariant: products decrement by 1, services decrement by `total_sessions - (session_number - 1)` (the undelivered sessions). The Service runs a single `UPDATE listings SET quantity_sold = quantity_sold - ?, updated_at=NOW() WHERE id = ?` per affected ticket; for services, the decrement value is computed per ticket from the ticket's `session_number` and `total_sessions`. After the decrement, if `quantity_sold < quantity AND status='sold'`, the Service runs `UPDATE listings SET status='active', updated_at=NOW() WHERE id = ? AND quantity_sold < quantity AND status='sold'` to restore the listing's active status."
    - "The cron is idempotent: re-running the endpoint with the same DB state returns `processed=0` for all sweeps (per NFR-REL-002). The ticket expiry sweep's single guarded UPDATE means a re-run on already-expired tickets is a no-op (the WHERE clause filters them out). The dispute auto-dismiss sweep's single guarded UPDATE means a re-run on already-rejected disputes is a no-op."
    - "Performance: the ticket expiry sweep runs as a single `UPDATE tickets SET ... WHERE ...` + a per-ticket loop for the `listings.quantity_sold` decrement + `audit_log` insert. For 10k tickets, the per-ticket loop is bounded by the number of expiring tickets (not 10k); the WHERE clause filters by `expires_at <= NOW()` so only the expired tickets are touched. Per NFR-PER-004 the cron must complete in < 30s for 10k tickets — the single UPDATE on 10k rows is the dominant cost."


---

# Artifacts this phase produces

## New files

- `src/Admin/Action/CronAction.php` — the renamed cron Action with three sweep methods + the `handle()` dispatcher.
- `tests/Integration/Phase04/Cron/CronSweepTest.php`
- `tests/Integration/Phase04/Cron/IdempotencyTest.php`
- `tests/Integration/Phase04/Cron/DisputeAutoDismissTest.php`
- `tests/Integration/Phase04/Cron/TicketExpiryTest.php`
- `tests/Integration/Phase04/Cron/PerformanceTest.php`

## Modified files

- `config/routes.php` — change the `POST /admin/cron/ticket-expiry` route entry's class name from `App\\Listing\\Action\\ListingAutoApproveAction` to `App\\Admin\\Action\\CronAction`.
- `src/Listing/Action/ListingAutoApproveAction.php` — becomes a deprecation shim: the `handle()` method forwards to `App\\Admin\\Action\\CronAction::handle()` and emits a one-time `error_log` warning so any leftover callers are visible during the Phase 4 demo. Phase 3 tests that exercise `POST /admin/cron/ticket-expiry` go through the renamed route entry and now hit `CronAction::handle()`.
- `src/Admin/Action/CronAction.php` — new file (see New files).
- `src/Ticket/Service/ticket_service.php` — adds `runTicketExpirySweep($actorUserId): array`, `runDisputeAutoDismissSweep($actorUserId): array`, `decrementListingStockForExpiredTicket(int $ticketId): void`, `restorePreDisputeStatus(int $ticketId): void`.
- `src/Ticket/Model/ticket_model.php` — adds `findExpiringTickets(PDO $pdo): array` (active tickets with `expires_at <= NOW()` and `dispute_status != 'pending'`), `findStaleDisputes(PDO $pdo): array` (tickets with `dispute_status='pending'` and `disputed_at <= NOW() - INTERVAL 3 DAY`).
- `src/Listing/Service/listing_service.php` — keeps `runAutoApproveSweep($actorUserId): array` as a thin wrapper that delegates to the new `CronAction`'s 24h sweep logic; the implementation is duplicated inline (one is in `listing_service`, one is in `CronAction`) — both produce the same SQL.


<objective>
Extend the existing cron Action (`POST /admin/cron/ticket-expiry`) to include the Phase 4 dispute auto-dismiss + ticket expiry sweeps per AD-11 (cron ownership) + D-07 (dispatch order). The 24h listing auto-approve sweep is Phase 3's existing behavior and stays unchanged. The new sweeps: (1) 3-day dispute auto-dismiss — any ticket with `dispute_status='pending'` for ≥ 3 days is auto-rejected, with the ticket's `status` restored to its pre-dispute value (`active` or `redeemed`); (2) 7-day ticket expiry — any ticket with `expires_at <= NOW()` and `dispute_status != 'pending'` is marked `expired`, and the listing's `quantity_sold` is decremented per the AD-7 inventory invariant (1 for products, `total_sessions - (session_number - 1)` for services). The plan also renames `src/Listing/Action/ListingAutoApproveAction.php` to `src/Admin/Action/CronAction.php` (admin context is the correct home for cron endpoints per AD-2 bounded contexts) and updates the route entry accordingly. Phase 9 wires the scheduled cron (cron tab / systemd timer); Phase 4 ships the hand-triggered endpoint.

Purpose: this plan completes the ticket lifecycle end-to-end. Without these sweeps, tickets created by Plan 02 would stay in `active` forever (no 7-day aging) and disputed tickets would never auto-dismiss (the 3-day timer would never fire). The WAD demo path requires a working cron to show the dispute-auto-dismiss and ticket-expiry behaviors in the admin dashboard.

Output: 1 renamed Action + 1 new Action file + 2 new Service methods + 2 new Model queries + 1 deprecation shim + 5 new test files.

Per CONTEXT D-07 the dispatch order is locked (dispute sweep BEFORE expiry sweep) so dismissed-then-expired tickets land in `expired` in the same tick (PRD §4.2 composition note). Per the agent's Discretion the `created_at` is NEVER touched by the dispute auto-dismiss sweep.

## Plan scope — what is NOT in this plan

- Scheduled cron (cron tab / systemd timer / lightweight daemon) — Phase 9.
- Admin re-auth UI (re-auth modal + admin_reauth table) — Phase 8.
- Admin resolution Actions for disputes (Force Expire / Force Redeem / Dismiss) — Phase 7.
- The full hash-chained audit log (AD-12) — Phase 8.
- Velocity / same-pair points caps (FR-PTS-005 / FR-PTS-006) — Phase 6.
- Performance optimization for > 10k tickets — the single-guarded-UPDATE pattern already satisfies NFR-PER-004.
</objective>

<execution_context>
@/home/user/hermesag/004/.opencode/gsd-core/workflows/execute-plan.md
@/home/user/hermesag/004/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/user/hermesag/004/tickettrade/AGENTS.md
@/home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md
@/home/user/hermesag/004/tickettrade/DESIGN.md
@/home/user/hermesag/004/tickettrade/EXPERIENCE.md
@/home/user/hermesag/004/tickettrade/WAD-CONTEXT.md
@/home/user/hermesag/004/tickettrade/.planning/PROJECT.md
@/home/user/hermesag/004/tickettrade/.planning/ROADMAP.md
@/home/user/hermesag/004/tickettrade/.planning/REQUIREMENTS.md
@/home/user/hermesag/004/tickettrade/.planning/STATE.md
@/home/user/hermesag/004/tickettrade/.planning/phases/04-purchases-tickets-lifecycle/04-CONTEXT.md
@/home/user/hermesag/004/tickettrade/.planning/phases/04-purchases-tickets-lifecycle/04-0[1-2]-PLAN.md
@/home/user/hermesag/004/tickettrade/src/Support/Db.php
@/home/user/hermesag/004/tickettrade/src/Support/Auth.php (the `requireReAuth(300)` gate for admin cron endpoints per AD-19)
@/home/user/hermesag/004/tickettrade/src/Support/Csrf.php
@/home/user/hermesag/004/tickettrade/src/Support/Error.php
@/home/user/hermesag/004/tickettrade/src/Support/Audit.php
@/home/user/hermesag/004/tickettrade/src/Listing/Service/listing_service.php (the existing `runAutoApproveSweep()` is the Phase 3 24h sweep — kept as a wrapper)
@/home/user/hermesag/004/tickettrade/src/Listing/Action/ListingAutoApproveAction.php (to be renamed → CronAction)
@/home/user/hermesag/004/tickettrade/src/Ticket/Service/ticket_service.php (already shipped from Plan 01; this plan adds the sweep methods)
@/home/user/hermesag/004/tickettrade/src/Ticket/Model/ticket_model.php (already shipped from Plan 01; this plan adds the sweep queries)
@/home/user/hermesag/004/tickettrade/config/routes.php
@/home/user/hermesag/004/tickettrade/config/error_codes.php
@/home/user/hermesag/004/tickettrade/migrations/012_cron_log.sql (the `cron_log` table the sweeps write to)
@home/user/hermesag/004/tickettrade/.planning/phases/03-marketplace-listings-discovery/03-01-PLAN.md (Phase 3 cron plan — the canonical pattern for the sweep implementation)
@home/user/hermesag/004/tickettrade/.planning/phases/03-marketplace-listings-discovery/03-01-SUMMARY.md
@/home/user/hermesag/004/tickettrade/.planning/phases/03-marketplace-listings-discovery/03-VERIFICATION.md
</context>

<tasks>

<task type=&quot;tracer&quot;>
  <name>Task 1: Substrate tracer — rename to CronAction + add dispute auto-dismiss + ticket expiry sweeps, end-to-end</name>
  <files>
    config/routes.php
    src/Listing/Action/ListingAutoApproveAction.php
    src/Admin/Action/CronAction.php
    src/Ticket/Service/ticket_service.php
    src/Ticket/Model/ticket_model.php
    src/Listing/Service/listing_service.php
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/AGENTS.md (WAD assignment context; cron < 30s for 10k tickets per NFR-PER-004)
    - /home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md (AD-2 bounded contexts: `Cron` is listed; AD-7 inventory invariant; AD-11 cron ownership — single `jobs/ticket_expiry.php` owns 24h auto-approve + 7-day expiry + 3-day dispute auto-dismiss; AD-12 audit_log hash chain stub; AD-19 admin re-auth 300s sliding window)
    - /home/user/hermesag/004/tickettrade/.planning/phases/04-purchases-tickets-lifecycle/04-CONTEXT.md (D-07 dispatch order: 24h listing auto-approve → 3-day dispute auto-dismiss → 7-day ticket expiry; the auto-dismiss sweep runs BEFORE the expiry sweep so dismissed-then-expired tickets land in `expired` in the same tick; the agent's Discretion: 7-day expiry write-once — `expires_at` is NEVER updated by the auto-dismiss branch; cron idempotency per NFR-REL-002; performance < 30s for 10k tickets per NFR-PER-004 — single guarded UPDATE is the dominant cost)
    - /home/user/hermesag/004/tickettrade/.planning/phases/04-purchases-tickets-lifecycle/04-0[1-2]-PLAN.md (the Phase 4 substrate + tracer plans ship `ticket_service::createTicket`, `redeemTicket`, `confirmSession`, `fileDispute` + the 4 Actions + the 3 View partials; this plan adds the cron sweeps that age out the tickets the previous plans create)
    - /home/user/hermesag/004/tickettrade/src/Support/Db.php (singleton PDO; `Support\\Db::pdo()`)
    - /home/user/hermesag/004/tickettrade/src/Support/Auth.php (`requireReAuth(300)` per AD-19; `currentUser()` returns the joined user row)
    - /home/user/hermesag/004/tickettrade/src/Support/Audit.php (`Audit::log()` is forward-compatible with Phase 8 hash chain)
    - /home/user/hermesag/004/tickettrade/src/Listing/Service/listing_service.php (the existing `runAutoApproveSweep()` method is the 24h Phase 3 sweep — kept as a wrapper for backward compatibility with Phase 3 tests)
    - /home/user/hermesag/004/tickettrade/src/Listing/Action/ListingAutoApproveAction.php (the existing Action class — to be renamed to `App\\Admin\\Action\\CronAction`; the old file becomes a deprecation shim)
    - /home/user/hermesag/004/tickettrade/src/Ticket/Service/ticket_service.php (already shipped from Plan 01; this plan adds the sweep methods + the stock-decrement helper)
    - /home/user/hermesag/004/tickettrade/src/Ticket/Model/ticket_model.php (already shipped from Plan 01; this plan adds the sweep queries)
    - /home/user/hermesag/004/tickettrade/config/routes.php (existing `POST /admin/cron/ticket-expiry` route entry's class name updates to `App\\Admin\\Action\\CronAction`)
    - /home/user/hermesag/004/tickettrade/config/error_codes.php (no new codes for the sweeps — they return the AD-16 envelope with the existing `E_INTERNAL` / `E_VALIDATION` codes)
    - /home/user/hermesag/004/tickettrade/migrations/012_cron_log.sql (the `cron_log` table the sweeps append rows to)
    - /home/user/hermesag/004/tickettrade/.planning/phases/03-marketplace-listings-discovery/03-01-PLAN.md (the Phase 3 cron plan — the canonical pattern for the sweep implementation: `Support\\Db::pdo()->prepare()` + `execute()` + `$stmt->rowCount()` + `writeCronLog()` helper)
  </read_first>
  <behavior>
    - `POST /admin/cron/ticket-expiry` as a logged-in admin WITH a fresh re-auth (within 300s) runs three sweeps in order: (1) 24h listing auto-approve (Phase 3 kept), (2) 3-day dispute auto-dismiss, (3) 7-day ticket expiry. The response is JSON `{ok:true, sweeps:{listing_auto_approve:{processed:N1}, dispute_auto_dismiss:{processed:N2, affected_tickets:['TK-...', ...]}, ticket_expiry:{processed:N3}}, errors:[]}`. Each sweep appends one `cron_log` row with `job_name` + `processed_count` + `errors_json` + `actor_user_id`.
    - The 24h listing auto-approve sweep runs `UPDATE listings SET status='active', approved_at=NOW(), approved_by=NULL, updated_at=NOW() WHERE status='pending' AND created_at <= NOW() - INTERVAL 24 HOUR`; `rowCount()` is the `processed` count; re-running inside the 24h window is idempotent (N=0).
    - The 3-day dispute auto-dismiss sweep runs `UPDATE tickets SET dispute_status='rejected', updated_at=NOW() WHERE dispute_status='pending' AND disputed_at <= NOW() - INTERVAL 3 DAY`; for each affected ticket, the Service restores the ticket's `status` to its pre-dispute value via a per-ticket `SELECT ... FOR UPDATE` + `UPDATE tickets SET status = ?, updated_at=NOW() WHERE id = ?`; the `disputed_at` is NEVER touched; `created_at` is NEVER touched. The sweep SKIPS tickets whose `dispute_status='pending'` is older than 7 days — wait, no, the threshold is 3 days per FR-TKT-008 + D-07. After the UPDATE, each affected ticket gets an `Audit::log('ticket.dispute_auto_dismissed')` row.
    - The 7-day ticket expiry sweep runs `UPDATE tickets SET status='expired', updated_at=NOW() WHERE status='active' AND dispute_status != 'pending' AND expires_at <= NOW()`; for each affected ticket, the Service calls `decrementListingStockForExpiredTicket($ticketId)`: products decrement `listings.quantity_sold` by 1; services decrement by `total_sessions - (session_number - 1)`. After the decrement, if `quantity_sold < quantity AND status='sold'`, the Service runs `UPDATE listings SET status='active', updated_at=NOW() WHERE id = ? AND quantity_sold < quantity AND status='sold'` to restore the listing's active status. Each expired ticket gets an `Audit::log('ticket.expired')` row. The sweep SKIPS tickets with `dispute_status='pending'` (admin must resolve first per PRD §4.2).
    - POST /admin/cron/ticket-expiry without admin session returns 404 (admin guard, unchanged from Phase 3). Without re-auth returns 403 with JSON `{ok:false, error:'re-auth required'}`. Re-running the endpoint with no eligible rows is idempotent (all sweeps return N=0, no errors).
    - Re-running the cron with the same DB state 5 times in succession produces the same end state: the same ticket statuses, the same `quantity_sold` values, the same `cron_log` row count delta of 5 (one per run) — but the business state is unchanged after run 2 (per NFR-REL-002).
    - Performance: the ticket expiry sweep completes in < 30s for 10k tickets per NFR-PER-004 (the single guarded UPDATE on 10k rows is the dominant cost; the per-ticket `listings.quantity_sold` decrement is bounded by the number of expiring tickets, not 10k).
  </behavior>
  <action>
    - Create `src/Admin/Action/CronAction.php` with the renamed cron Action. The class is in the `App\\Admin\\Action` namespace (matching AD-2's `Admin` bounded context for cron endpoints). The `handle()` method: validates `Support\\Auth::requireReAuth(300)`; runs the three sweep methods in order via the corresponding Services; collects the per-sweep results into the JSON envelope `{ok:true, sweeps:{...}, errors:[]}`; emits 200 + JSON response (or 500 + error envelope on failure).
    - Modify `src/Listing/Action/ListingAutoApproveAction.php` to be a deprecation shim: the `handle()` method forwards to `App\\Admin\\Action\\CronAction::handle()` and emits a one-time `error_log` warning `'[cron] deprecated route hit: POST /admin/cron/ticket-expiry — please update config/routes.php to use App\\Admin\\Action\\CronAction'`. Phase 3 tests that exercise the old class name still pass (the shim forwards); new tests use the new class name.
    - Modify `config/routes.php` to update the `POST /admin/cron/ticket-expiry` route entry's class name from `App\\Listing\\Action\\ListingAutoApproveAction` to `App\\Admin\\Action\\CronAction`. The route entry's other opts stay the same (`auth=>true, admin=>true, csrf=>true, rate_limit=>'admin_cron'`).
    - Modify `src/Ticket/Service/ticket_service.php` to add the sweep methods:
      - `runDisputeAutoDismissSweep($actorUserId): array` runs `UPDATE tickets SET dispute_status='rejected', updated_at=NOW() WHERE dispute_status='pending' AND disputed_at <= NOW() - INTERVAL 3 DAY`; for each affected ticket, restores the pre-dispute `status` via a per-ticket `SELECT ... FOR UPDATE` + `UPDATE tickets SET status=?, updated_at=NOW() WHERE id=?`; appends `Audit::log('ticket.dispute_auto_dismissed', 'ticket', $ticketId, ['old_dispute_status'=>'pending', 'new_dispute_status'=>'rejected'])` per affected ticket. Returns `['ok'=>true, 'data'=>['processed'=>$processed, 'affected_tickets'=>[...]]]`. On failure, returns `E_INTERNAL` envelope.
      - `runTicketExpirySweep($actorUserId): array` runs `UPDATE tickets SET status='expired', updated_at=NOW() WHERE status='active' AND dispute_status != 'pending' AND expires_at <= NOW()`; for each affected ticket, calls `decrementListingStockForExpiredTicket($ticketId)`; appends `Audit::log('ticket.expired', 'ticket', $ticketId, ['expires_at'=>$expiresAt])` per affected ticket. Returns `['ok'=>true, 'data'=>['processed'=>$processed, 'affected_tickets'=>[...]]]`. On failure, returns `E_INTERNAL` envelope.
      - `decrementListingStockForExpiredTicket(int $ticketId): void` is a private helper: loads the ticket; computes the decrement (1 for product, `total_sessions - (session_number - 1)` for service); runs `UPDATE listings SET quantity_sold = quantity_sold - ?, updated_at=NOW() WHERE id = ?`; if the decrement causes `quantity_sold < quantity AND listings.status='sold'`, runs `UPDATE listings SET status='active', updated_at=NOW() WHERE id=? AND quantity_sold < quantity AND status='sold'`. Uses a single `SELECT ... FOR UPDATE` per ticket to prevent race conditions.
      - `restorePreDisputeStatus(int $ticketId, string $oldStatus): void` is a private helper: runs `UPDATE tickets SET status=?, updated_at=NOW() WHERE id=?`.
    - Modify `src/Ticket/Model/ticket_model.php` to add the sweep queries:
      - `findExpiringTickets(PDO $pdo): array` returns active tickets with `expires_at <= NOW()` AND `dispute_status != 'pending'`, joined with `listings.id` and `users.buyer_id, seller_id` for the per-ticket stock decrement.
      - `findStaleDisputes(PDO $pdo): array` returns tickets with `dispute_status='pending'` AND `disputed_at <= NOW() - INTERVAL 3 DAY`, joined with the pre-dispute `status` (the `status` column BEFORE the auto-dismiss sweep runs).
    - Modify `src/Listing/Service/listing_service.php` to keep `runAutoApproveSweep($actorUserId): array` as a thin wrapper that delegates to the new `App\\Admin\\Action\\CronAction::run24hListingAutoApproveSweep()` (the wrapper preserves backward compatibility with Phase 3 tests).
  </action>
  <verify>
    <automated>cd /home/user/hermesag/004/tickettrade && APP_ENV=test php migrate.php && echo "MIGRATE_OK" && APP_ENV=test vendor/bin/phpunit --testsuite=phase-4 --filter='CronSweep|Idempotency|DisputeAutoDismiss|TicketExpiry|Performance' 2>&1 | tail -15</automated>
    <fails_when>non-zero exit, or MIGRATE_OK missing, or "0 passed" / "FAILURES" / "ERRORS" in the phpunit output</fails_when>
  </verify>
</task>

<task type=&quot;standard&quot;>
  <name>Task 2: Test fixtures + integration tests for the three sweeps (dispute dismiss, ticket expiry, 24h auto-approve)</name>
  <files>
    tests/Integration/Phase04/Cron/CronSweepTest.php
    tests/Integration/Phase04/Cron/IdempotencyTest.php
    tests/Integration/Phase04/Cron/DisputeAutoDismissTest.php
    tests/Integration/Phase04/Cron/TicketExpiryTest.php
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/tests/Integration/Phase03/MigrationTest.php (canonical test DB setup)
    - /home/user/hermesag/004/tickettrade/.planning/phases/03-marketplace-listings-discovery/03-01-PLAN.md (the Phase 3 cron test pattern)
    - /home/user/hermesag/004/tickettrade/src/Ticket/Service/ticket_service.php (already shipped from Plan 01 + Plan 03 Task 1; the test files call the sweep methods)
  </read_first>
  <behavior>
    - `CronSweepTest`: end-to-end — seed 3 listings (one pending > 24h old, one active with `quantity_sold < quantity`, one sold-out with `quantity_sold == quantity`); seed 3 tickets (one `dispute_status='pending'` with `disputed_at` > 3 days old, one `expires_at` > 7 days old with `status='active'`, one `expires_at` < NOW() with `status='active'`); POST /admin/cron/ticket-expiry as admin WITH fresh re-auth; assert the response JSON shows `processed > 0` for each of the three sweeps; assert the DB end state matches the expected (expired ticket is `status='expired'`, listing's `quantity_sold` is decremented, disputed ticket is `dispute_status='rejected'` with pre-dispute `status` restored, pending listing is `status='active'`).
    - `IdempotencyTest`: POST /admin/cron/ticket-expiry 5 times in succession; assert the DB end state is the same after runs 2, 3, 4, 5; assert `cron_log` has 5 rows (one per run); assert the response JSON shows `processed=0` for runs 2-5.
    - `DisputeAutoDismissTest`: seed a ticket with `dispute_status='pending'` and `disputed_at = NOW() - INTERVAL 4 DAY`; POST /admin/cron/ticket-expiry; assert `dispute_status='rejected'`, `status='active'` (the pre-dispute value); assert `created_at` is unchanged (D-07 invariant); assert `disputed_at` is unchanged; assert an `audit_log` row with `action='ticket.dispute_auto_dismissed'` exists.
    - `TicketExpiryTest`: seed a ticket with `expires_at = NOW() - INTERVAL 1 HOUR` AND `status='active'` AND `dispute_status='none'`; seed a listing with `quantity_sold=2, quantity=5`; POST /admin/cron/ticket-expiry; assert ticket's `status='expired'`; assert listing's `quantity_sold=1` (decremented by 1); assert listing's `status` is still `active` (not `sold` because quantity_sold < quantity). Then seed another ticket that would expire on a `sold` listing with `quantity_sold=quantity=1`; POST /admin/cron/ticket-expiry; assert the listing's `status='active'` (restored from `sold`).
  </behavior>
  <action>
    - Create the 4 new test files per the behavior section above. Each test extends `PhaseX\\Integration\\DatabaseTestCase` (copy from Phase 3's pattern) that boots a fresh test DB in `setUp()` and truncates the affected tables.
    - The test fixtures use `Support\\Db::pdo()` to seed listings + tickets + users at known timestamps via direct SQL inserts (the Phase 3 pattern).
    - The cron call goes through `App\\Admin\\Action\\CronAction::handle()` directly (no router dispatch needed for unit testing).
  </action>
  <verify>
    <automated>cd /home/user/hermesag/004/tickettrade && APP_ENV=test vendor/bin/phpunit --testsuite=phase-4 --filter='CronSweep|Idempotency|DisputeAutoDismiss|TicketExpiry' 2>&1 | tail -15</automated>
    <fails_when>non-zero exit, or "0 passed" / "FAILURES" / "ERRORS" in the phpunit output</fails_when>
  </verify>
</task>

<task type=&quot;standard&quot;>
  <name>Task 3: Performance test — cron completes in < 30s for 10k tickets per NFR-PER-004</name>
  <files>
    tests/Integration/Phase04/Cron/PerformanceTest.php
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/AGENTS.md (NFR-PER-004: cron < 30s for 10k tickets)
    - /home/user/hermesag/004/tickettrade/src/Ticket/Service/ticket_service.php (the `runTicketExpirySweep()` method's per-ticket loop is the bounded cost)
  </read_first>
  <behavior>
    - Seed 10k tickets with `expires_at = NOW() - INTERVAL 1 HOUR` AND `status='active'` AND `dispute_status='none'`; seed 10k listings with `quantity_sold=1, quantity=2` (so the decrement brings them back to `quantity_sold=0, quantity=2`).
    - POST /admin/cron/ticket-expiry; assert the wall-clock time from request start to response end is < 30 seconds; assert all 10k tickets have `status='expired'` after the sweep; assert all 10k listings have `quantity_sold=0`.
  </behavior>
  <action>
    - Create `tests/Integration/Phase04/Cron/PerformanceTest.php` with the seeding + sweep + assertion per the behavior section.
    - The test uses a loop of 10k INSERT statements to seed the tickets + listings; the sweep itself runs as a single `UPDATE tickets ...` (the dominant cost); the per-ticket loop for the `listings.quantity_sold` decrement is the bounded cost.
    - The test asserts the wall-clock time via `microtime(true)` before + after the cron call.
  </action>
  <verify>
    <automated>cd /home/user/hermesag/004/tickettrade && APP_ENV=test vendor/bin/phpunit --testsuite=phase-4 --filter='Performance' 2>&1 | tail -10</automated>
    <fails_when>non-zero exit, or wall-clock time > 30s, or "0 passed" / "FAILURES" / "ERRORS" in the phpunit output</fails_when>
  </verify>
</task>
</tasks>

<output>
Create `.planning/phases/04-purchases-tickets-lifecycle/04-03-SUMMARY.md` when done.
</output>


<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /admin/cron/ticket-expiry | Admin-only; re-auth gated; rate-limited. |
| Service → Support\\Audit | Audit log append; never throws (D-04 forward-compat). |
| Service → Support\\Db | Trusted code path; all writes via prepared statements. |
| Cron sweep → `listings.quantity_sold` | Per-ticket decrement; single guarded UPDATE per ticket. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-26 | Elevation of Privilege | Admin re-auth bypass | high | mitigate | `Support\\Auth::requireReAuth(300)` per AD-19; the cron Action is the canonical example. Re-auth failure returns 403 JSON `{ok:false, error:'re-auth required'}`. |
| T-04-27 | Tampering | Cron over-write | high | mitigate | Each sweep runs a single guarded `UPDATE` with a `WHERE` clause that filters eligible rows; `rowCount()===0` is a no-op. Re-running is idempotent per NFR-REL-002. |
| T-04-28 | Denial of Service | Cron rate limit | medium | mitigate | `rate_limit='admin_cron'` (5/min/IP) per Phase 3 D-30; excess returns 429. |
| T-04-29 | Denial of Service | 10k-ticket sweep performance | medium | mitigate | Single guarded UPDATE on 10k rows is the dominant cost; per-ticket loop is bounded by the number of expiring tickets, not 10k. Performance test asserts < 30s per NFR-PER-004. |
| T-04-30 | Information Disclosure | Affected ticket list in response | low | mitigate | The response JSON includes `affected_tickets: ['TK-...', ...]`; the codes are not sensitive (they are visible to the buyer in their /my-tickets page). The admin needs the list for the cron run report. |
| T-04-31 | Tampering | Composer installs | high | mitigate | No new Composer packages. |

## Trust-Boundary Coverage

- T-04-26, T-04-28: Admin-only + re-auth + rate-limit gates (unchanged from Phase 3).
- T-04-27: Single guarded UPDATE pattern (AD-9) per sweep; idempotent re-runs.
- T-04-29: Performance test asserts NFR-PER-004.

## Package Legitimacy Audit

- No new Composer packages.
- T-04-31: not applicable.
</threat_model>

<verification>
- [ ] `vendor/bin/phpunit --testsuite=phase-4` is green (≥23 test files, ≥120 cases after this plan).
- [ ] `vendor/bin/phpunit` (full suite) is green — no Phase 1/2/3 test broke.
- [ ] `vendor/bin/phpcs --standard=PSR12 src/Admin src/Ticket src/Support/Audit.php` exits 0.
- [ ] POST /admin/cron/ticket-expiry as admin WITH fresh re-auth: response JSON shows `processed > 0` for each sweep on eligible rows; response JSON shows `processed = 0` on re-runs.
- [ ] Re-running the cron 5 times in succession: DB end state is identical after run 2 (idempotency per NFR-REL-002).
- [ ] Ticket expiry sweep decrements `listings.quantity_sold` per the AD-7 inventory invariant: 1 for products, `total_sessions - (session_number - 1)` for services.
- [ ] Dispute auto-dismiss sweep restores the pre-dispute `status` (active → active, redeemed → redeemed); `created_at` is NEVER touched; `disputed_at` is NEVER touched.
- [ ] Performance: cron completes in < 30s for 10k tickets per NFR-PER-004.
</verification>

<success_criteria>
- The Phase 4 cron is operational: a hand-triggered POST /admin/cron/ticket-expiry runs the three sweeps in order (24h listing auto-approve → 3-day dispute auto-dismiss → 7-day ticket expiry) and the DB end state matches the expected.
- Re-running the cron is idempotent per NFR-REL-002.
- Performance: < 30s for 10k tickets per NFR-PER-004.
- All 23 test files pass; the full PHPUnit suite is green.
- All artifacts listed under "Artifacts this phase produces" exist on disk.
- The 3-day dispute auto-dismiss sweep runs BEFORE the 7-day ticket expiry sweep so dismissed-then-expired tickets land in `expired` in the same tick (PRD §4.2 composition note).
</success_criteria>
