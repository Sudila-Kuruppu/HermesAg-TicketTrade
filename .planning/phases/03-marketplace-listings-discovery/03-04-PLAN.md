---
phase: 03-marketplace-listings-discovery
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/Auth/Action/HomeAction.php
  - src/Auth/View/home.php
  - src/Auth/View/partials/hero.php
  - src/Auth/View/partials/vision_mission.php
  - src/Auth/View/partials/how_it_works.php
  - src/Auth/View/partials/team_section.php
  - src/Auth/View/partials/landing_footer.php
  - config/team.php
  - tests/Integration/Phase03/Landing/HomeLandingTest.php
  - tests/Integration/Phase03/Landing/TeamSectionTest.php
  - tests/Integration/Phase03/Listing/ListingAutoApproveSweepTest.php
autonomous: true
requirements:
  - LND-01
  - LND-02
  - LND-03
  - LND-04
  - LND-05
  - LND-06
user_setup: []

estimate:
  tokens: 54000
  raw_tokens: 27000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "GET / as a guest: 200 response, HTML body contains the hero `<h1>Every Trade Ends With Proof</h1>`, the tagline `<p class='lead'>NSBM's campus-only marketplace...</p>`, two CTAs `Get Started` (linking to `/register`) and `Explore Marketplace` (linking to `/board`); the body sections in order: Hero → Vision & Mission (2 cards) → How It Works (5 step cards: Register & verify, List or browse, Buy with a digital ticket, Redeem in person, Rate & review) → Team (6 cards from `config/team.php`) → Footer (NSBM branding + 'Simulation only' disclaimer + link to GitHub/Drive)."
    - "GET / as a logged-in user: the `Get Started` CTA flips to `My listings` linking to `/my-listings`; the `Explore Marketplace` CTA stays the same."
    - "The Team section renders 6 cards from `config/team.php`, each with initials avatar (2 letters, on `surface-container-high` token), full name, role, and a one-line bio. Editing the team config re-renders the section on the next page load."
    - "The Hero section is full-width, dark-on-light (or light-on-dark per the theme tokens), with the `display-lg` Inter font (Phase 1 typography tokens)."
    - "The Vision & Mission section uses 2 Bootstrap cards side-by-side on desktop (`col-md-6`), stacked on mobile (`col-12`)."
    - "The How It Works section uses 5 Bootstrap cards in a row on desktop (`col-md` grid), with step number badges."
    - "The Footer includes: 'TicketTrade - NSBM Marketplace', 'A WAD coursework project (Batch 26.1, 2026-09-02)', 'Simulation only - no real money flows', a link to the GitHub repo, a link to the Drive folder (when configured)."
    - "POST /admin/cron/ticket-expiry from Plan 03-02 remains the auto-approve endpoint; Plan 03-04 ADDS a more thorough integration test that creates 25 listings with `created_at` 25 hours in the past, runs the endpoint, and asserts all 25 are flipped to `status='active', approved_at` set, `approved_by=NULL`."
  artifacts:
    - src/Auth/Action/HomeAction.php (replaced with real landing implementation)
    - src/Auth/View/home.php (replaced with the new landing layout)
    - src/Auth/View/partials/hero.php (new)
    - src/Auth/View/partials/vision_mission.php (new)
    - src/Auth/View/partials/how_it_works.php (new)
    - src/Auth/View/partials/team_section.php (new)
    - src/Auth/View/partials/landing_footer.php (new)
    - config/team.php (modified: ensure 6 entries; the file may already be filled — verify)
    - tests/Integration/Phase03/Landing/HomeLandingTest.php (new)
    - tests/Integration/Phase03/Landing/TeamSectionTest.php (new)
    - tests/Integration/Phase03/Listing/ListingAutoApproveSweepTest.php (new)
  key_links:
    - "`config/team.php` (already shipped by Phase 1+2; verify 6 entries exist) returns `[['name'=>'Tharushi P.', 'role'=>'Backend Lead', 'initials'=>'TP', 'bio'=>'Owner of Auth/Service and bcrypt sniff'], ...]`. The `team_section.php` partial iterates this array and renders 6 Bootstrap cards."
    - "`HomeAction::handle` reads `$GLOBALS['current_user']` to determine if the user is logged in; logged-in users see `My listings` instead of `Get Started`."
    - 'The landing page reuses the standard `Support\View\layout.php` with `data-surface=''public''` to render the public theme tokens (light mode by default per UX-06; the student surface is dark by default but the public landing is light to match the WAD rubric''s screenshot-ready expectation).'
    - "The 5 How-It-Works steps are hard-coded in `how_it_works.php` per D-25: 1. Register & verify, 2. List or browse, 3. Buy with a digital ticket, 4. Redeem in person, 5. Rate & review. Each step has a number badge, a title, and a 1-2 sentence description."
    - "The auto-approve sweep test creates 25 listings by directly inserting rows with `created_at = NOW() - INTERVAL 25 HOUR` (bypassing the Service to set the past timestamp); runs the endpoint via the test HTTP harness; asserts 25 rows are flipped to `active`."
---

# Artifacts this phase produces
- 1 modified Action (`HomeAction`).
- 1 replaced View (`home.php`).
- 5 new View partials.
- 3 new integration test files.

<objective>
Ship the public landing page at `/` (replacing the Phase 2 stub) and harden the hand-triggered auto-approve Action with a thorough integration test. The landing page is the WAD rubric entry-point and the demo video's opening shot: visitors land here, see the value prop, and either register or browse.

Purpose: the WAD rubric "professional, responsive, easy-to-use UI" is judged primarily on the landing page (and the board, which Plan 03-03 ships). The Team section also feeds the WAD rubric's "Project Report ... group member names + roles" requirement. The auto-approve sweep test is the regression net for the admin hand-trigger.

Output: 1 modified Action, 1 replaced View, 5 new partials, 3 new test files, 1 confirmed config file.
</objective>

<execution_context>
@/home/user/hermesag/004/.opencode/gsd-core/workflows/execute-plan.md
@/home/user/hermesag/004/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/user/hermesag/004/tickettrade/AGENTS.md
@/home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md
@/home/user/hermesag/004/tickettrade/EXPERIENCE.md
@/home/user/hermesag/004/tickettrade/DESIGN.md
@/home/user/hermesag/004/tickettrade/WAD-CONTEXT.md
@/home/user/hermesag/004/tickettrade/.planning/PROJECT.md
@/home/user/hermesag/004/tickettrade/.planning/REQUIREMENTS.md
@/home/user/hermesag/004/tickettrade/.planning/phases/03-marketplace-listings-discovery/03-CONTEXT.md
@/home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-0[1-3]-SUMMARY.md
@/home/user/hermesag/004/tickettrade/src/Support/View/layout.php
@/home/user/hermesag/004/tickettrade/src/Auth/View/home.php
@/home/user/hermesag/004/tickettrade/config/team.php
@/home/user/hermesag/004/tickettrade/src/Support/Auth.php
@/home/user/hermesag/004/tickettrade/src/Listing/Service/listing_service.php
@/home/user/hermesag/004/tickettrade/src/Support/View/partials/{head,bottom_nav,toast_container,flash_toast,skip_link}.php
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — landing page renders all 5 sections (hero, vision/mission, how-it-works, team, footer) for guest and logged-in users</name>
  <files>
    src/Auth/Action/HomeAction.php
    src/Auth/View/home.php
    src/Auth/View/partials/hero.php
    src/Auth/View/partials/vision_mission.php
    src/Auth/View/partials/how_it_works.php
    src/Auth/View/partials/team_section.php
    src/Auth/View/partials/landing_footer.php
    config/team.php
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/AGENTS.md
    - /home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md (AD-1, AD-3, AD-16)
    - /home/user/hermesag/004/tickettrade/.planning/phases/03-marketplace-listings-discovery/03-CONTEXT.md (D-25 landing sections; D-26 team config; D-27 Explore Marketplace routing; LND-01..LND-06)
    - /home/user/hermesag/004/tickettrade/.planning/PROJECT.md (the Team roster table — the source of truth for who is on the team; matches `config/team.php`)
    - /home/user/hermesag/004/tickettrade/WAD-CONTEXT.md (rubric lines 20% UI Design, 15% Report; the Team section feeds both)
    - /home/user/hermesag/004/tickettrade/EXPERIENCE.md (the WAD-friendly empty/error/cold-load pattern; the landing has no cold-load because it's static)
    - /home/user/hermesag/004/tickettrade/DESIGN.md (typography: `display-lg` for the hero, `body` for body, color tokens)
    - /home/user/hermesag/004/tickettrade/src/Support/View/layout.php (the standard layout; the landing sets `data-surface='public'` for the public theme)
    - /home/user/hermesag/004/tickettrade/src/Auth/View/home.php (existing stub to REPLACE)
    - /home/user/hermesag/004/tickettrade/config/team.php (verify 6 entries; if missing, fill in placeholders)
    - /home/user/hermesag/004/tickettrade/src/Support/Auth.php (`currentUser(): ?array` — to determine if the user is logged in)
  </read_first>
  <behavior>
    - `GET /` as a guest: 200 response, HTML body contains `<h1 class='display-lg'>Every Trade Ends With Proof</h1>`, `<p class='lead'>NSBM's campus-only marketplace where every purchase produces a confirmable digital ticket.</p>`, two buttons: `<a class='btn btn-primary' href='/register'>Get Started</a> <a class='btn btn-outline-primary' href='/board'>Explore Marketplace</a>`. Below the hero: `<section class='container py-5 vision-mission'>` with 2 cards (Vision, Mission). Below that: `<section class='how-it-works'>` with 5 step cards. Below that: `<section class='team'>` with 6 cards (initials avatar, name, role, bio). Below that: `<footer class='landing-footer'>` with NSBM branding + 'Simulation only' disclaimer.
    - `GET /` as a logged-in user: the `Get Started` button flips to `My listings` linking to `/my-listings`; the `Explore Marketplace` button stays the same.
    - The Team section renders 6 cards. Each card has `<div class='avatar bg-surface-container-high text-on-surface-variant rounded-circle'>{initials}</div>`, `<h3 class='h6 mb-0'>{name}</h3>`, `<p class='small text-on-surface-variant mb-1'>{role}</p>`, `<p class='small'>{bio}</p>`. The 6 entries match the WAD roster (Backend Lead, Backend Member, Frontend Lead, Frontend Member, Database Engineer, QA/Docs Lead).
    - The Footer has `<p class='small text-on-surface-variant'>TicketTrade - NSBM Marketplace. A WAD coursework project (Batch 26.1, 2026-09-02). Simulation only - no real money flows.</p>` and a `<a href='https://github.com/...'>GitHub</a>` and `<a href='https://drive.google.com/...'>Drive</a>`.
    - The How-It-Works section has 5 step cards. Each card has `<span class='badge bg-primary'>{N}</span> <h3 class='h6'>{step_title}</h3> <p class='small text-on-surface-variant'>{step_description}</p>`. The 5 steps per D-25: 1. Register & verify, 2. List or browse, 3. Buy with a digital ticket, 4. Redeem in person, 5. Rate & review.
    - The Vision card has `<h2>Our Vision</h2><p>Build trust in campus trade...</p>`. The Mission card has `<h2>Our Mission</h2><p>Give every NSBM student a safe place...</p>`. (Wording is the team's choice; placeholders are fine for Phase 3.)
  </behavior>
  <action>
    1. **Action — `src/Auth/Action/HomeAction.php` (REPLACE stub)**: namespace `App\Auth\Action`; class `HomeAction`. `handle(): void` (GET only): reads `$GLOBALS['current_user']`; reads `config/team.php` via `require __DIR__ . '/../../config/team.php'`; renders `src/Auth/View/home.php` with `$vars = ['current_user' => $currentUser, 'team' => $team, 'is_logged_in' => $currentUser !== null]`. The Action is intentionally thin — no DB call (the team is a static config; the listings count is not on the landing).
    2. **View — `src/Auth/View/home.php` (REPLACE stub)**: PHP template inside the layout. Top: `<?= $this->partial('hero', ['is_logged_in' => $is_logged_in]) ?>`. Then: `<?= $this->partial('vision_mission') ?>`. Then: `<?= $this->partial('how_it_works') ?>`. Then: `<?= $this->partial('team_section', ['team' => $team]) ?>`. Then: `<?= $this->partial('landing_footer') ?>`. The body is wrapped in `<main id='main' data-surface='public'>`.
    3. **Partial — `src/Auth/View/partials/hero.php` (new)**: `<section class='hero bg-primary text-on-primary py-5'> <div class='container text-center'> <h1 class='display-lg'>Every Trade Ends With Proof</h1> <p class='lead mt-3'>NSBM's campus-only marketplace where every purchase produces a confirmable digital ticket.</p> <div class='d-flex gap-3 justify-content-center mt-4'> <a class='btn btn-primary btn-lg' href='<?= $is_logged_in ? '/my-listings' : '/register' ?>'><?= $is_logged_in ? 'My listings' : 'Get Started' ?></a> <a class='btn btn-outline-primary btn-lg' href='/board'>Explore Marketplace</a> </div> </div> </section>`.
    4. **Partial — `src/Auth/View/partials/vision_mission.php` (new)**: `<section class='vision-mission container py-5'> <h2 class='visually-hidden'>Vision and Mission</h2> <div class='row g-4'> <div class='col-12 col-md-6'> <div class='card h-100'> <div class='card-body'> <h2 class='h4'>Our Vision</h2> <p>Build trust in campus trade so NSBM students can buy, sell, and exchange goods and services with confidence.</p> </div> </div> </div> <div class='col-12 col-md-6'> <div class='card h-100'> <div class='card-body'> <h2 class='h4'>Our Mission</h2> <p>Give every NSBM student a safe, identity-verified platform with a confirmable digital ticket, gamified reputation, and seller ratings.</p> </div> </div> </div> </div> </section>`.
    5. **Partial — `src/Auth/View/partials/how_it_works.php` (new)**: `<section class='how-it-works bg-surface-container py-5'> <div class='container'> <h2 class='h3 text-center mb-4'>How It Works</h2> <div class='row g-4'> <?php $steps = [ ['n' => 1, 'title' => 'Register & verify', 'desc' => 'Sign up with your @students.nsbm.ac.lk email and student ID. Verified students get a +50 trust bonus.'], ['n' => 2, 'title' => 'List or browse', 'desc' => 'Sellers post products or services with photos and price. Buyers browse the corkboard by category or search.'], ['n' => 3, 'title' => 'Buy with a digital ticket', 'desc' => 'A unique ticket code is generated at purchase. Share it with the seller via WhatsApp or in person.'], ['n' => 4, 'title' => 'Redeem in person', 'desc' => 'The seller enters the code to confirm handover. Both parties earn trust points.'], ['n' => 5, 'title' => 'Rate & review', 'desc' => 'After redemption, both parties leave a 1-5 star rating. Reviews build the seller\'s reputation.'], ]; foreach ($steps as $step): ?> <div class='col-12 col-md-6 col-lg'> <div class='card h-100 text-center'> <div class='card-body'> <span class='badge bg-primary mb-2' style='width:32px;height:32px;line-height:24px;font-size:1rem'><?= $step['n'] ?></span> <h3 class='h6'><?= htmlspecialchars($step['title']) ?></h3> <p class='small text-on-surface-variant'><?= htmlspecialchars($step['desc']) ?></p> </div> </div> </div> <?php endforeach; ?> </div> </div> </section>`.
    6. **Partial — `src/Auth/View/partials/team_section.php` (new)**: `<section class='team container py-5'> <h2 class='h3 text-center mb-4'>Meet the Team</h2> <p class='text-center text-on-surface-variant mb-4'>A six-person WAD Batch 26.1 team from NSBM Green University.</p> <div class='row g-4'> <?php foreach ($team as $member): ?> <div class='col-12 col-sm-6 col-md-4 col-lg-2'> <div class='card h-100 text-center'> <div class='card-body'> <div class='avatar rounded-circle bg-surface-container-high text-on-surface-variant d-flex align-items-center justify-content-center mx-auto mb-3' style='width:64px;height:64px;font-weight:600'><?= htmlspecialchars($member['initials']) ?></div> <h3 class='h6 mb-1'><?= htmlspecialchars($member['name']) ?></h3> <p class='small text-on-surface-variant mb-1'><?= htmlspecialchars($member['role']) ?></p> <p class='small'><?= htmlspecialchars($member['bio']) ?></p> </div> </div> </div> <?php endforeach; ?> </div> </section>`.
    7. **Partial — `src/Auth/View/partials/landing_footer.php` (new)**: `<footer class='landing-footer bg-surface-container py-4'> <div class='container text-center'> <p class='small text-on-surface-variant mb-2'>TicketTrade - NSBM Marketplace</p> <p class='small text-on-surface-variant mb-2'>A WAD coursework project (Batch 26.1, 2026-09-02). Simulation only - no real money flows.</p> <p class='small'> <a href='https://github.com/' class='text-on-surface-variant me-3'>GitHub</a> <a href='https://drive.google.com/' class='text-on-surface-variant'>Drive</a> </p> </div> </footer>`.
    8. **Config — `config/team.php` (verify)**: ensure the file returns 6 entries matching the WAD roster. The file may already be filled (Phase 1 shipped it for the WAD report). If only partial entries exist, fill the missing ones with placeholder names + roles. (The team is finalizable at submission time per WAD_Batch26.md sec 4; the Phase 3 placeholder names are fine for development.)
  </action>
  <verify>
    <automated>cd /home/user/hermesag/004/tickettrade && APP_ENV=test vendor/bin/phpunit --testsuite=phase-3 --filter='HomeLanding|TeamSection' 2>&1 | tail -10</automated>
    <fails_when>non-zero exit, or "0 passed" / "FAILURES" / "ERRORS" in the output</fails_when>
  </verify>
  <done>
    - `HomeAction` is replaced with the real landing implementation.
    - `home.php` is replaced; 5 new partials exist.
    - `config/team.php` has 6 entries.
    - All 2 new test files pass; the `phase-3` testsuite is still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Auto-approve sweep integration test (25-hour-old listings)</name>
  <files>
    tests/Integration/Phase03/Listing/ListingAutoApproveSweepTest.php
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/src/Listing/Service/listing_service.php (the `runAutoApproveSweep` method from Plan 03-02)
    - /home/user/hermesag/004/tickettrade/src/Listing/Action/ListingAutoApproveAction.php (the Action from Plan 03-02)
    - /home/user/hermesag/004/tickettrade/src/Support/Auth.php (re-auth helper)
  </read_first>
  <behavior>
    - `testSweepApproves25HourOldPendingListings`: 25 listings seeded with `status='pending', created_at=NOW() - INTERVAL 25 HOUR`; POST /admin/cron/ticket-expiry returns `{ok:true, processed:25, errors:[]}`; all 25 rows in the DB now have `status='active', approved_at` set, `approved_by=NULL`.
    - `testSweepIgnoresRecentListings`: 10 listings seeded with `created_at=NOW() - INTERVAL 23 HOUR`; the endpoint returns `{ok:true, processed:0, errors:[]}`; all 10 rows remain `status='pending'`.
    - `testSweepIsIdempotent`: run the endpoint twice in a row; both return `processed:25` (the second run finds 0 eligible rows because all are now `active`).
    - `testSweepLogsToCronLog`: 1 listing seeded with `status='pending', created_at=NOW() - INTERVAL 25 HOUR`; after the endpoint, the `cron_log` table has 1 row with `job_name='listing.auto_approve', processed_count=1, actor_user_id=<test admin id>`.
    - `testSweepWithoutReAuthReturns403`: admin without re-auth → 403 JSON `{ok:false, error:'re-auth required'}`; the listing remains `status='pending'`.
  </behavior>
  <action>
    - Create `tests/Integration/Phase03/Listing/ListingAutoApproveSweepTest.php` with 5 test methods covering the behaviors above. Use the Phase 2 test pattern: transaction-rolled-back Integration, seed user + admin in `setUp`, seed listings directly via SQL (bypassing the Service to control `created_at`).
    - Run `vendor/bin/phpunit --testsuite=phase-3` and iterate until green.
  </action>
  <verify>
    <automated>cd /home/user/hermesag/004/tickettrade && APP_ENV=test vendor/bin/phpunit --testsuite=phase-3 --filter='ListingAutoApproveSweep' 2>&1 | tail -10</automated>
    <fails_when>non-zero exit, or "0 passed" / "FAILURES" / "ERRORS" in the output</fails_when>
  </verify>
  <done>
    - 5 new test methods pass; the sweep handles 25h-old listings, ignores recent ones, is idempotent, logs to `cron_log`, and respects the re-auth gate.
  </done>
</task>

<task type="auto">
  <name>Task 3: PSR-12 lint + final phase-3 testsuite + Phase 3 completion check</name>
  <files>
    src/Auth/Action/HomeAction.php
    src/Auth/View/home.php
    src/Auth/View/partials/{hero,vision_mission,how_it_works,team_section,landing_footer}.php
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/phpcs.xml
    - /home/user/hermesag/004/tickettrade/.planning/phases/03-marketplace-listings-discovery/03-CONTEXT.md (the goal-backward criteria)
    - /home/user/hermesag/004/tickettrade/.planning/REQUIREMENTS.md (LND-01..LND-06 success criteria)
    - /home/user/hermesag/004/tickettrade/WAD-CONTEXT.md (the WAD rubric lines this phase produces evidence for)
  </read_first>
  <behavior>
    - `vendor/bin/phpcs --standard=PSR12 src/Auth/Action/HomeAction.php src/Auth/View/home.php src/Auth/View/partials` exits 0.
    - `vendor/bin/phpunit --testsuite=phase-3` reports ≥20 tests passing.
    - `vendor/bin/phpunit` (full suite) is green.
    - Visual: open `/` in a browser; the 5 sections render in order (hero, vision/mission, how-it-works, team, footer); the CTAs work (`/register` and `/board`); the Team section shows 6 cards.
  </behavior>
  <action>
    1. Run `vendor/bin/phpcs` and fix any reported issues.
    2. Run `vendor/bin/phpunit --testsuite=phase-3` and the full `vendor/bin/phpunit`.
    3. Spin up the dev server; visit `/` in a browser; confirm the landing renders correctly.
    4. Update `.planning/ROADMAP.md` to mark Phase 3 as complete in the plan list (use Edit, scoped).
    5. Commit: `git add .planning/phases/03-marketplace-listings-discovery/03-0*-PLAN.md .planning/ROADMAP.md && git commit -m "docs(03): create phase 3 plans"` (the actual source code lands in subsequent execute-phase runs; this commit is for the plans only).
  </action>
  <verify>
    <automated>cd /home/user/hermesag/004/tickettrade && APP_ENV=test vendor/bin/phpunit 2>&1 | tail -8 && echo "---PSR12---" && vendor/bin/phpcs --standard=PSR12 src/Auth/Action/HomeAction.php src/Auth/View/home.php src/Auth/View/partials 2>&1 | tail -8</automated>
    <fails_when>non-zero exit, or "0 passed" / "FAILURES" / "ERRORS" in phpunit output, or any phpcs "ERROR" / "FOUND" lines</fails_when>
  </verify>
  <done>
    - PSR-12 is clean on Phase 3 source.
    - All tests pass (full suite).
    - The landing page renders correctly.
    - ROADMAP.md is updated; commit is created.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET / | Public; no auth required. |
| Browser → POST /admin/cron/ticket-expiry | Admin-only; re-auth gated; rate-limited. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-29 | Tampering | Team config | low | mitigate | `config/team.php` is gitignored (per Phase 1 conventions) and edited only by the team lead at submission time; no runtime write. |
| T-03-30 | XSS | Team member fields | high | mitigate | `htmlspecialchars` is applied to `name`, `role`, `bio`, `initials` in the partial. |
| T-03-31 | XSS | Hero/mission text | medium | mitigate | Hard-coded in partials; no user input. |
| T-03-32 | Spoofing | `is_logged_in` flag | low | mitigate | The flag is computed from `$GLOBALS['current_user']`; the auth guard is the single source of truth. |
| T-03-33 | Denial of Service | `/admin/cron/ticket-expiry` | medium | mitigate | Rate-limited at 5/min/IP via `admin_cron`. |
| T-03-34 | Tampering | Sweep test data | low | mitigate | The test seeds with past `created_at`; the production code never accepts user-provided `created_at` (the Service sets it server-side). |
| T-03-SC | Tampering | Composer installs | high | mitigate | No new Composer packages. |

## Trust-Boundary Coverage

- T-03-30, T-03-31: htmlspecialchars on all user-derived text.
- T-03-33: rate-limit + admin-only + re-auth.

## Package Legitimacy Audit

- No new Composer packages.
</threat_model>

<verification>
- [ ] `vendor/bin/phpunit` (full suite) is green.
- [ ] `vendor/bin/phpcs --standard=PSR12` is clean on Phase 3 source.
- [ ] `GET /` renders all 5 sections (hero, vision/mission, how-it-works, team, footer).
- [ ] The Team section shows 6 cards with initials, name, role, and bio.
- [ ] `POST /admin/cron/ticket-expiry` approves 25h-old pending listings and logs to `cron_log`.
- [ ] The landing page passes the WAD rubric visual review (responsive, AA contrast, professional polish).
- [ ] ROADMAP.md is updated; commit is created.
</verification>

<success_criteria>
- The public landing page at `/` is the WAD rubric entry-point: hero with CTAs, vision/mission, how-it-works (5 steps), team (6 cards), and footer (NSBM branding + simulation disclaimer).
- The hand-triggered admin auto-approve Action handles 25h-old pending listings, logs to `cron_log`, and is gated by re-auth + rate-limit + admin role.
- All Phase 3 tests pass; PSR-12 is clean.
- The WAD demo flow "register → list → admin approve → log in as buyer → browse corkboard → click listing → see modal" works end-to-end.
- The WAD rubric lines for UI Design, Admin Panel, and Student Panel are all evidenced by this phase.
</success_criteria>

<output>
Create `.planning/phases/03-marketplace-listings-discovery/03-04-SUMMARY.md` when done.
</output>
