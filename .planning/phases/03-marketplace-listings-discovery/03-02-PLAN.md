---
phase: 03-marketplace-listings-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - config/routes.php
  - config/error_codes.php
  - config/contexts.php
  - src/Listing/Action/BrowseAction.php
  - src/Listing/Action/MyListingsAction.php
  - src/Listing/Action/CreateListingAction.php
  - src/Listing/Action/EditListingAction.php
  - src/Listing/Action/DeleteListingAction.php
  - src/Listing/Action/RelistListingAction.php
  - src/Listing/Action/SubmitDraftAction.php
  - src/Listing/View/board.php
  - src/Listing/View/my_listings.php
  - src/Listing/View/create.php
  - src/Listing/View/edit.php
  - src/Support/View/partials/listing_status_pill.php
  - src/Support/View/partials/seller_dashboard_tabs.php
  - src/Support/View/partials/empty_state.php
  - tests/Integration/Phase03/Listing/CreateListingFlowTest.php
  - tests/Integration/Phase03/Listing/EditListingFlowTest.php
  - tests/Integration/Phase03/Listing/DeleteListingFlowTest.php
  - tests/Integration/Phase03/Listing/RelistFlowTest.php
  - tests/Integration/Phase03/Listing/SubmitDraftFlowTest.php
  - tests/Integration/Phase03/Listing/MyListingsTabsTest.php
  - tests/Integration/Phase03/Support/RouteGuardListingTest.php
autonomous: true
requirements:
  - LST-01
  - LST-02
  - LST-07
  - LST-09
  - LST-13
  - LST-14
  - LST-15
  - LST-16
  - SEC-04
user_setup: []

estimate:
  tokens: 92000
  raw_tokens: 46000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "GET /listings/create as a logged-in seller returns 200 with the form (title 80-char max input, description 2000-char max textarea, price LKR input group, category select populated with 7 active categories, type radio product/service, condition select for products, service detail fields for services, image input `multiple` accepting up to 8 files, draft + submit buttons); CSRF token hidden; the form posts to itself."
    - "POST /listings/create with valid input returns 302 to /my-listings with a flash-toast `Listing created - pending admin approval`; the listing row exists with `status='pending'`; an audit-style `listing_revisions` row is NOT written (it's only for edits to `active` listings per D-09)."
    - "POST /listings/create with invalid input (empty title, price=0, quantity=0) returns 200 with the form re-rendered, the field-level error messages displayed next to each invalid input (`aria-describedby` linking to the message), and the values the user submitted are preserved (per UX-DR-XX). The DB row is NOT inserted."
    - "POST /listings/create without a session returns 302 to /login?next=/listings/create."
    - "POST /listings/create on the 21st call within 60 minutes returns 200 with the form re-rendered and an inline error `E_RATE_LIMIT` `Too many listings. Try again later`."
    - "GET /listings/{id}/edit as the listing's owner returns 200 with the form pre-populated; the rejection-reason banner is shown when `status='rejected'` (per D-04) and the status flips to `draft` on form load; the `review_flag` is set when the original status was `active` (per D-09)."
    - "GET /listings/{id}/edit as a non-owner returns 404 (per D-14: same response as unknown — never leak existence)."
    - "POST /listings/{id}/delete on an `active` listing soft-deletes: `status='removed'`, the row stays in DB; on `pending` or `draft`, hard-deletes (or also soft-deletes for audit consistency; choice: hard-delete only `draft` and `pending`, soft-delete `active`/`rejected`)."
    - "POST /listings/{id}/relist on a `sold` listing creates a new row with `status='draft'`, copies all fields except `quantity_sold` (reset to 0); the seller is redirected to /listings/{id_new}/edit with a flash toast `Relisted as draft - edit and submit when ready`."
    - "POST /listings/{id}/submit on a `draft` listing flips status to `pending`; the seller is redirected to /my-listings with a flash toast `Submitted for review`."
    - "GET /my-listings as a logged-in seller returns 200 with 4 tabs (Active / Pending / Sold / Draft), each showing the count next to the label (not as a badge, per D-01); the active tab is `aria-current='page'`; clicking a tab reloads with `?tab=active|pending|sold|draft`; the default tab is `active`."
    - "Each tab's per-listing row is the 3-column layout per CONTEXT specifics: `[64px thumbnail] [title + price + status pill + meta] [actions right-aligned]`. The actions are per-state per D-02: Active→Edit+Delete; Pending→Edit+Delete; Sold→Relist; Draft→Edit+Submit; Rejected→Edit+Delete (Edit flips to draft)."
    - "An empty Active tab renders the named copy from EXPERIENCE.md: `No active listings yet - your first listing is one click away` with a primary CTA `Create your first listing`. An empty Draft tab renders `Submit a draft to make it live`."
    - "POST /admin/cron/ticket-expiry as a logged-in admin WITH a fresh re-auth (within 300s) runs `UPDATE listings SET status='active', approved_at=NOW(), approved_by=NULL WHERE status='pending' AND created_at <= NOW() - INTERVAL 24 HOUR`; returns JSON `{ok:true, processed:N, errors:[]}` where N is the affected row count; a `.cron_log` plain-text line is appended with `listing.auto_approve: processed=N, actor=user_id`."
    - "POST /admin/cron/ticket-expiry without admin session returns 404 (admin guard). Without re-auth returns 403 with JSON `{ok:false, error:'re-auth required'}`. Re-running the endpoint while no rows match is idempotent (N=0, no errors)."
  artifacts:
    - config/routes.php (modified: add 6 new route entries)
    - config/error_codes.php (modified: add `E_LISTING_REVIEW_FLAG` if not present)
    - config/contexts.php (no change; contexts already listed)
    - src/Listing/Action/BrowseAction.php (replaced with real body)
    - src/Listing/Action/MyListingsAction.php (replaced with real body + tabs)
    - src/Listing/Action/CreateListingAction.php (new)
    - src/Listing/Action/EditListingAction.php (new)
    - src/Listing/Action/DeleteListingAction.php (new)
    - src/Listing/Action/RelistListingAction.php (new)
    - src/Listing/Action/SubmitDraftAction.php (new)
    - src/Listing/View/board.php (modified: real browse implementation; modal is Plan 03-03)
    - src/Listing/View/my_listings.php (replaced with real seller dashboard)
    - src/Listing/View/create.php (new)
    - src/Listing/View/edit.php (new)
    - src/Support/View/partials/listing_status_pill.php (new)
    - src/Support/View/partials/seller_dashboard_tabs.php (new)
    - src/Support/View/partials/empty_state.php (new)
    - tests/Integration/Phase03/Listing/CreateListingFlowTest.php (new)
    - tests/Integration/Phase03/Listing/EditListingFlowTest.php (new)
    - tests/Integration/Phase03/Listing/DeleteListingFlowTest.php (new)
    - tests/Integration/Phase03/Listing/RelistFlowTest.php (new)
    - tests/Integration/Phase03/Listing/SubmitDraftFlowTest.php (new)
    - tests/Integration/Phase03/Listing/MyListingsTabsTest.php (new)
    - tests/Integration/Phase03/Support/RouteGuardListingTest.php (new)
  key_links:
    - 'All Actions delegate to `Listing\Service\listing_service` (sole writer per AD-1). The Action''s only responsibilities: CSRF check (already enforced by `Support\Csrf` at bootstrap), rate-limit check (`listing_create` 20/hr/user on create), call Service, render View. The Action never writes to the DB directly.'
    - "`config/routes.php` ADDS 6 entries: `GET /listings/create`, `POST /listings/create`, `GET /listings/{id}/edit`, `POST /listings/{id}/edit`, `POST /listings/{id}/delete`, `POST /listings/{id}/relist`, `POST /listings/{id}/submit`, `POST /admin/cron/ticket-expiry`. The existing `GET /board` and `GET /my-listings` route entries stay; their Action class names do not change. `POST /admin/cron/ticket-expiry` requires `auth=>true, admin=>true, csrf=>true, rate_limit=>'admin_cron'`."
    - "`ListingService::saveDraft` and `submitDraft` enforce the state-machine transitions from D-02: `draft→pending` (SubmitDraftAction), `active→active_with_review_flag` (EditListingAction on an `active` listing, per D-09 — the row's `review_flag=1, review_flag_at=NOW()` and a `listing_revisions` snapshot is appended), `sold→draft` (RelistListingAction)."
    - "The form views (`create.php`, `edit.php`) use Bootstrap 5 form controls (input, form-label, invalid-feedback, btn, btn-primary, btn-outline-secondary, btn-outline-danger, input-group with LKR prefix, form-select for category, type radio, condition select); the Bootstrap JS auto-validates via `is-invalid` class; server-side `E_VALIDATION` field errors are rendered as `<div class='invalid-feedback'>{message}</div>` with the input getting `class='form-control is-invalid'`."
    - "`ListingService::uploadImages` is called from `CreateListingAction` and `EditListingAction` after the row is created/updated; images are optional on edit (existing images are kept if no new files are uploaded). The Service's 8-file cap applies to the TOTAL count (existing + new), not just new."
    - '`POST /admin/cron/ticket-expiry` invokes `ListingService::runAutoApproveSweep()` which runs the UPDATE in a single statement; the result is logged to a plain `.cron_log` file under `var/cron/` (or a `cron_log` table created in this plan); a JSON response is emitted. The endpoint requires a fresh re-auth token (AD-19) checked via `Support\Auth::requireReAuth(300)`.'
    - "Empty states from EXPERIENCE.md are reproduced verbatim: `No active listings yet - your first listing is one click away` (with primary CTA), `No pending listings - all your listings have been reviewed`, `No sold listings yet`, `Submit a draft to make it live` (for the Draft tab when there are 0 drafts and 0 active listings). The empty-state partial (`empty_state.php`) takes `title`, `body`, and optional `cta_label`/`cta_href`."
---

# Artifacts this phase produces
- 6 new Action classes (`CreateListingAction`, `EditListingAction`, `DeleteListingAction`, `RelistListingAction`, `SubmitDraftAction`, `ListingAutoApproveAction`).
- 2 modified Action classes (`BrowseAction`, `MyListingsAction`) — stub bodies replaced with real implementations.
- 2 new Views (`create.php`, `edit.php`); 2 modified Views (`board.php`, `my_listings.php`).
- 3 new View partials (`listing_status_pill.php`, `seller_dashboard_tabs.php`, `empty_state.php`).
- 6 new integration test files.
- `config/routes.php` modified with 6 new route entries.
- `var/cron/cron_log` file or `cron_log` table (the Plan picks one — recommendation: a `cron_log` table `id, job_name, run_at, processed_count, errors_json, actor_user_id` for the manual-trigger; the `jobs/` directory + `php jobs/ticket_expiry.php` lands in Phase 9 with `flock()`).

<objective>
Wire the listing CRUD Actions and the state-machine transitions for the seller flow. By the end of this plan, a logged-in seller can create, save as draft, submit, edit, delete, and relist a listing; the seller dashboard renders 4 tabs (Active/Pending/Sold/Draft) with per-state action affordances and named empty-state copy; the hand-triggered admin auto-approve Action approves listings older than 24 hours.

Purpose: plans 03-03 (board + modal) and 03-04 (landing + cron) both depend on this — the modal opens listings that the create/edit Actions wrote, and the cron Action lives in this plan. The WAD rubric line "Admin can approve product listings" is partially met here (the Action + re-auth gate ship; the admin UI is Phase 8).

Output: 6 new Action classes, 2 new View templates, 3 new View partials, 7 modified existing files, 6 new integration tests, 1 new route group, 1 new admin endpoint.
</objective>

<execution_context>
@/home/user/hermesag/004/.opencode/gsd-core/workflows/execute-plan.md
@/home/user/hermesag/004/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/user/hermesag/004/tickettrade/AGENTS.md
@/home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md
@/home/user/hermesag/004/tickettrade/EXPERIENCE.md
@/home/user/hermesag/004/tickettrade/DESIGN.md
@/home/user/hermesag/004/tickettrade/WAD-CONTEXT.md
@/home/user/hermesag/004/tickettrade/.planning/PROJECT.md
@/home/user/hermesag/004/tickettrade/.planning/ROADMAP.md
@/home/user/hermesag/004/tickettrade/.planning/REQUIREMENTS.md
@/home/user/hermesag/004/tickettrade/.planning/STATE.md
@/home/user/hermesag/004/tickettrade/.planning/phases/03-marketplace-listings-discovery/03-CONTEXT.md
@/home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-CONTEXT.md
@/home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-0[1-3]-PLAN.md
@/home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-0[1-3]-SUMMARY.md
@/home/user/hermesag/004/tickettrade/src/Support/Auth.php
@/home/user/hermesag/004/tickettrade/src/Support/Csrf.php
@/home/user/hermesag/004/tickettrade/src/Support/RateLimit.php
@home/user/hermesag/004/tickettrade/src/Support/Error.php
@/home/user/hermesag/004/tickettrade/src/Support/View.php
@/home/user/hermesag/004/tickettrade/src/Auth/Service/auth_service.php
@/home/user/hermesag/004/tickettrade/src/Listing/Service/listing_service.php
@/home/user/hermesag/004/tickettrade/src/Category/Service/category_service.php
@/home/user/hermesag/004/tickettrade/config/routes.php
@/home/user/hermesag/004/tickettrade/config/error_codes.php
@/home/user/hermesag/004/tickettrade/config/rate_limits.php
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — create listing draft, submit for review, see it on seller dashboard, admin can run auto-approve, all end-to-end</name>
  <files>
    config/routes.php
    config/error_codes.php
    src/Listing/Action/CreateListingAction.php
    src/Listing/Action/SubmitDraftAction.php
    src/Listing/Action/MyListingsAction.php
    src/Listing/Action/ListingAutoApproveAction.php
    src/Listing/Service/listing_service.php
    src/Listing/View/create.php
    src/Listing/View/my_listings.php
    src/Support/View/partials/seller_dashboard_tabs.php
    src/Support/View/partials/listing_status_pill.php
    src/Support/View/partials/empty_state.php
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/AGENTS.md
    - /home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md (AD-1, AD-4, AD-13, AD-16, AD-19)
    - /home/user/hermesag/004/tickettrade/.planning/phases/03-marketplace-listings-discovery/03-CONTEXT.md (D-01..D-04 state machine UX, D-09 review_flag, D-13 storage, D-15 listed actions, D-28..D-30 cron Action, D-31 categories seed)
    - /home/user/hermesag/004/tickettrade/src/Support/Auth.php (`currentUser(): ?array`, `requireAuth(): array`, `requireAdmin(): array`, `requireReAuth(int $seconds): array` — read the file to know the exact re-auth method name)
    - /home/user/hermesag/004/tickettrade/src/Support/Csrf.php (enforced at bootstrap for POST/PUT/PATCH/DELETE; no need to re-check in Action)
    - /home/user/hermesag/004/tickettrade/src/Support/RateLimit.php (`hit($name, $key): array{allowed, count, retry_after}`)
    - /home/user/hermesag/004/tickettrade/src/Support/Error.php (envelope helpers: `validation(array $fields)`, `notFound()`, `forbidden()`)
    - /home/user/hermesag/004/tickettrade/src/Support/View.php (`render($template, $vars)`, `layout($title, $content, $vars)`, `flash($key, $message)`)
    - /home/user/hermesag/004/tickettrade/src/Listing/Service/listing_service.php (from Plan 03-01: `createDraft`, `submitDraft`, `getSellerListings`, `runAutoApproveSweep`)
    - /home/user/hermesag/004/tickettrade/src/Category/Service/category_service.php (`listActive()` for the category select)
    - /home/user/hermesag/004/tickettrade/config/routes.php (existing student routes; the route shape per D-11)
    - /home/user/hermesag/004/tickettrade/config/rate_limits.php (`listing_create` 20/hr/user, `admin_cron` 5/min/IP from Plan 03-01)
    - /home/user/hermesag/004/tickettrade/config/error_codes.php (codes from Plan 03-01: `E_LISTING_NOT_FOUND`, `E_LISTING_FORBIDDEN`, `E_RATE_LIMIT`, `E_CSRF`, `E_VALIDATION`)
    - /home/user/hermesag/004/tickettrade/src/Listing/Action/BrowseAction.php (existing stub to REPLACE in Task 2; not used in this Task 1 tracer)
    - /home/user/hermesag/004/tickettrade/src/Listing/Action/MyListingsAction.php (existing stub to REPLACE; the tracer uses the replaced version)
    - /home/user/hermesag/004/tickettrade/src/Auth/Action/HomeAction.php (existing real Action — copy its thin-controller pattern: validate input, call Service, render View, return nothing or redirect)
  </read_first>
  <behavior>
    - `GET /listings/create` as a logged-in seller: 200 response, HTML body contains `<form method="POST" action="/listings/create" enctype="multipart/form-data">`, the category `<select name="category_id">` has 7 `<option>` children with values 1..7, the type radio has 2 options (`product` and `service`), the form contains a hidden `<input name="csrf_token" value="...">`, the submit button label is `Submit for review` and a secondary button is `Save as draft`.
    - `POST /listings/create` with `title='Calculus 101', description='Barely used textbook', price_cents=150000, category_id=1, type='product', quantity=1`: 302 redirect to `/my-listings?tab=pending`, response cookie carries `flash=Listing created - pending admin approval`; the DB has 1 row in `listings` with `status='pending', quantity_sold=0, seller_id=<test user>`; the DB has 0 rows in `listing_revisions` (not an edit).
    - `POST /listings/create` with `title=''`: 200 response, HTML body has `<input name="title" class="form-control is-invalid" aria-describedby="title-err">` and `<div id="title-err" class="invalid-feedback">Title is required</div>`; no row inserted.
    - `POST /listings/create` 21 times in 60 minutes: 21st call returns 200 with the form re-rendered and an inline error `Too many listings. Try again later`; the `E_RATE_LIMIT` envelope is logged.
    - `POST /listings/{id}/submit` on a `draft` listing: 302 to `/my-listings?tab=pending`; the row's `status='pending'`; a flash toast `Submitted for review`.
    - `GET /my-listings` as the seller: 200 response, HTML body has `<nav role="tablist">` with 4 tabs; the Active tab shows the count `0` next to the label (NOT a Bootstrap badge); the active tab carries `aria-current="page"`; the body shows the empty-state partial for the active tab: `<h2>No active listings yet</h2><p>Your first listing is one click away</p><a class="btn btn-primary" href="/listings/create">Create your first listing</a>`.
    - `GET /my-listings?tab=pending` after creating a listing: shows the row with `<span class="badge bg-warning text-dark">Pending</span>` status pill and `Edit` + `Delete` action buttons.
    - `POST /admin/cron/ticket-expiry` as an admin WITH a fresh re-auth (within 300s) and a listing `created_at` 25 hours ago: 200 JSON `{ok:true, processed:1, errors:[]}`; the listing's `status='active'`, `approved_at=NOW()`, `approved_by=NULL`; a line is appended to `var/cron/cron_log` (or the `cron_log` table) with the run details.
    - `POST /admin/cron/ticket-expiry` without re-auth: 403 JSON `{ok:false, error:'re-auth required'}`.
    - `POST /admin/cron/ticket-expiry` without admin session: 404 (admin guard returns same as unknown route per D-10).
    - `POST /admin/cron/ticket-expiry` with no eligible rows (all listings < 24h old): 200 JSON `{ok:true, processed:0, errors:[]}`; no log line.
  </behavior>
  <action>
    1. **Routes — `config/routes.php` (modify)**: ADD 6 entries after the existing `GET /my-listings` line:
       - `'GET /listings/create' => ['App\Listing\Action\CreateListingAction', 'handle', ['auth' => true, 'admin' => false, 'csrf' => false, 'rate_limit' => null]]`
       - `'POST /listings/create' => ['App\Listing\Action\CreateListingAction', 'handlePost', ['auth' => true, 'admin' => false, 'csrf' => true, 'rate_limit' => 'listing_create']]`
       - `'GET /listings/{id}/edit' => ['App\Listing\Action\EditListingAction', 'handle', ['auth' => true, 'admin' => false, 'csrf' => false, 'rate_limit' => null]]`
       - `'POST /listings/{id}/edit' => ['App\Listing\Action\EditListingAction', 'handlePost', ['auth' => true, 'admin' => false, 'csrf' => true, 'rate_limit' => null]]`
       - `'POST /listings/{id}/delete' => ['App\Listing\Action\DeleteListingAction', 'handle', ['auth' => true, 'admin' => false, 'csrf' => true, 'rate_limit' => null]]`
       - `'POST /listings/{id}/relist' => ['App\Listing\Action\RelistListingAction', 'handle', ['auth' => true, 'admin' => false, 'csrf' => true, 'rate_limit' => null]]`
       - `'POST /listings/{id}/submit' => ['App\Listing\Action\SubmitDraftAction', 'handle', ['auth' => true, 'admin' => false, 'csrf' => true, 'rate_limit' => null]]`
       - `'POST /admin/cron/ticket-expiry' => ['App\Listing\Action\ListingAutoApproveAction', 'handle', ['auth' => true, 'admin' => true, 'csrf' => true, 'rate_limit' => 'admin_cron']]`
    2. **Error codes — `config/error_codes.php` (modify)**: ADD `E_LISTING_REVIEW_FLAG => 'Edits to active listings are pending admin review'` (per D-09). Other codes from Plan 03-01 are already present.
    3. **Action — `src/Listing/Action/CreateListingAction.php` (new)**: namespace `App\Listing\Action`; class `CreateListingAction`. Public methods: `handle(): void` (GET), `handlePost(): void` (POST). `handle()`: `Support\Auth::requireAuth()`; fetch active categories via `CategoryService::listActive()`; render `src/Listing/View/create.php` with `$vars = ['categories' => $data, 'csrf_token' => $_SESSION['csrf_token'], 'errors' => [], 'values' => []]`. `handlePost()`: `Support\Auth::requireAuth()`; call `ListingService::createDraft($userId, $_POST)`; on `ok===true`, optional `uploadImages($listingId, $userId, $_FILES['images'])`; call `Support\View::flash('toast', 'Listing created - pending admin approval')`; redirect to `/my-listings?tab=pending`; on `ok===false`, render the form re-populated with `$_POST` values + `error.fields` map.
    4. **Action — `src/Listing/Action/SubmitDraftAction.php` (new)**: namespace `App\Listing\Action`; class `SubmitDraftAction`. Public method `handle(): void`. Reads `:id` from the route params; calls `ListingService::submitDraft($listingId, $userId)`; on success, `Support\View::flash('toast', 'Submitted for review')`; redirect to `/my-listings?tab=pending`; on `E_LISTING_FORBIDDEN` or `E_LISTING_NOT_FOUND`, return 404 (same as unknown).
    5. **Action — `src/Listing/Action/MyListingsAction.php` (REPLACE the Phase 2 stub)**: namespace `App\Listing\Action`; class `MyListingsAction`. `handle(): void`: `Support\Auth::requireAuth()`; reads `?tab` from `$_GET`; default `tab='active'`; calls `ListingService::getSellerListings($userId, $tab, 1)`; renders `src/Listing/View/my_listings.php` with `$vars = ['rows' => $rows, 'tab' => $tab, 'counts' => $allFourCounts, 'csrf_token' => $_SESSION['csrf_token']]`. Tab counts are 4 numbers: `['active' => N, 'pending' => N, 'sold' => N, 'draft' => N]` (single query, `SELECT status, COUNT(*) FROM listings WHERE seller_id=? GROUP BY status`).
    6. **Action — `src/Listing/Action/ListingAutoApproveAction.php` (new)**: namespace `App\Listing\Action`; class `ListingAutoApproveAction`. `handle(): void`: `Support\Auth::requireAdmin()`; `$reAuth = Support\Auth::requireReAuth(300)`; on missing re-auth: `http_response_code(403); header('Content-Type: application/json'); echo json_encode(['ok' => false, 'error' => 're-auth required']); exit;`. On success: `$result = ListingService::runAutoApproveSweep($userId)`; `$result` = `['ok' => true, 'data' => ['processed' => N, 'errors' => []], 'error' => null]`. Emit JSON: `http_response_code(200); header('Content-Type: application/json'); echo json_encode($result['data']); exit;`. Log a line to `var/cron/cron_log` (or `cron_log` table) with `listing.auto_approve: processed=N, actor=user_id, run_at=<ISO8601>`.
    7. **Service — `src/Listing/Service/listing_service.php` (EXTEND)**: ADD public method `runAutoApproveSweep(int $actorUserId): array` — runs the SQL `UPDATE listings SET status='active', approved_at=NOW(), approved_by=NULL WHERE status='pending' AND created_at <= NOW() - INTERVAL 24 HOUR`; returns `['ok' => true, 'data' => ['processed' => $pdo->rowCount(), 'errors' => []], 'error' => null]`. ADD public method `softDelete(int $listingId, int $sellerId): array` — UPDATE status to `removed`; returns affected row count. ADD public method `hardDelete(int $listingId, int $sellerId): array` — DELETE row (only for `draft` or `pending`); returns affected row count. The `submitDraft` method is already from Plan 03-01; verify it exists and call it.
    8. **View — `src/Listing/View/create.php` (new)**: PHP template that renders inside `src/Support/View/layout.php`. Header: `<h1>Create a listing</h1>`. Form: `<form method='POST' action='/listings/create' enctype='multipart/form-data' class='row g-3'>`. Fields: title (`<input type='text' name='title' maxlength='80' class='form-control <?= !empty($errors['title']) ? 'is-invalid' : '' ?>' value='<?= htmlspecialchars($values['title'] ?? '') ?>' aria-describedby='title-err' required>` + invalid-feedback div), description (`<textarea name='description' maxlength='2000' class='form-control' required>`), price (LKR input group: `<div class='input-group'><span class='input-group-text'>LKR</span><input type='number' name='price_rupees' min='1' max='100000' class='form-control' value='<?= htmlspecialchars($values['price_rupees'] ?? '') ?>' required></div>` + hidden `<input type='hidden' name='price_cents' value='<?= $values['price_cents'] ?? '' ?>'>` populated by client-side JS OR by server on re-render), category select (Bootstrap `form-select`), type radio (`<input type='radio' name='type' value='product' checked> Product <input type='radio' name='type' value='service'> Service`), conditional fields (PHP `if ($values['type'] === 'product')` shows condition select; `if ($values['type'] === 'service')` shows duration/delivery/availability fields), quantity (`<input type='number' name='quantity' min='1' max='999' class='form-control' required>`), images (`<input type='file' name='images[]' multiple accept='image/*' class='form-control'>` with a `<small class='text-muted'>Up to 8 images, max 5MB each, formats: JPG, PNG, WebP</small>`), CSRF hidden input, two buttons: `<button type='submit' name='action' value='save_draft' class='btn btn-outline-secondary'>Save as draft</button> <button type='submit' name='action' value='submit' class='btn btn-primary'>Submit for review</button>`. Client-side JS: a 30-LOC inline `<script>` block at the bottom that shows/hides condition vs service-detail fields based on the type radio selection; calls `URL.createObjectURL` for client-side previews (D-08 in CONTEXT).
    9. **View — `src/Listing/View/my_listings.php` (REPLACE stub)**: PHP template that renders inside the layout. Top: `<h1>My listings</h1>`. Tabs: `<?= $this->partial('seller_dashboard_tabs', ['tab' => $tab, 'counts' => $counts, 'csrf_token' => $csrf_token]) ?>`. Body: `<?php if (empty($rows)): ?> <?= $this->partial('empty_state', [...]) ?> <?php else: ?> <ul class='list-group'> <?php foreach ($rows as $row): ?> <li class='list-group-item d-flex align-items-center gap-3'> <img src='/img/<?= $row['id'] ?>/thumb' class='rounded' width='64' height='64' alt=''> <div class='flex-grow-1'> <h2 class='h5 mb-1'><?= htmlspecialchars($row['title']) ?></h2> <div class='d-flex gap-3 text-muted small'> <span>LKR <?= number_format($row['price_cents'] / 100, 0) ?></span> <?= $this->partial('listing_status_pill', ['status' => $row['status'], 'review_flag' => $row['review_flag']]) ?> <span><?= $row['quantity_sold'] ?> / <?= $row['quantity'] ?> sold</span> </div> </div> <div class='d-flex gap-2'> [per-state action buttons per D-02] </div> </li> <?php endforeach; ?> </ul> <?php endif; ?>`. Each row's actions are per D-02: Active → Edit (`/listings/{id}/edit`) + Delete (POST `/listings/{id}/delete`); Pending → Edit + Delete; Sold → Relist (POST `/listings/{id}/relist`); Draft → Edit + Submit (POST `/listings/{id}/submit`); Rejected → Edit + Delete.
    10. **View partial — `src/Support/View/partials/seller_dashboard_tabs.php` (new)**: Bootstrap nav-tabs. 4 tabs in fixed order: Active, Pending, Sold, Draft. Each is `<a class='nav-link <?= $tab === 'active' ? 'active' : '' ?>' href='/my-listings?tab=active' aria-current='<?= $tab === 'active' ? 'page' : 'false' ?>'>` + label + count + `</a>`. The count is `<span class='text-muted ms-1'>(<?= $counts['active'] ?>)</span>` (NOT a Bootstrap badge per D-01).
    11. **View partial — `src/Support/View/partials/listing_status_pill.php` (new)**: maps status to Bootstrap pill classes per CONTEXT specifics: `draft` → `surface-container-high text-on-surface-variant` (dashed border), `pending` → `bg-warning text-dark`, `active` → `bg-success`, `rejected` → `bg-error-fill text-on-error`, `removed` → `bg-error-fill text-on-error`, `sold` → `surface-container-high text-on-surface-variant`. The `review_flag` adds a small inline pill: `<span class='badge bg-warning text-dark ms-1' aria-label='Edits pending admin review'>Under review</span>` when `review_flag=TRUE`.
    12. **View partial — `src/Support/View/partials/empty_state.php` (new)**: takes `title`, `body`, `cta_label`, `cta_href`. Renders: `<div class='text-center py-5'><h2 class='h4 text-on-surface-variant'><?= htmlspecialchars($title) ?></h2><p class='text-on-surface-variant'><?= htmlspecialchars($body) ?></p><?php if ($cta_label): ?><a class='btn btn-primary' href='<?= htmlspecialchars($cta_href) ?>'><?= htmlspecialchars($cta_label) ?></a><?php endif; ?></div>`. Uses EXPERIENCE.md's named copy verbatim.
    13. **Cron log — choose the simpler of (a) `var/cron/cron_log` plain-text file or (b) `cron_log` table**: this plan ADDS a `cron_log` table via a new migration `012_cron_log.sql` (id BIGINT UNSIGNED PK, job_name VARCHAR(60), run_at DATETIME, processed_count INT, errors_json JSON, actor_user_id BIGINT UNSIGNED FK users(id) ON DELETE SET NULL). The `ListingAutoApproveAction::handle` writes a row after every sweep. (Phase 4 migrates these to the hash-chained `audit_log` per CONTEXT deferral.) Add migration `012_cron_log.sql` to the same project.
    14. **Update `migrations/.applied`**: the next `php migrate.php` run picks up `012_cron_log.sql` automatically.
  </action>
  <verify>
    <automated>cd /home/user/hermesag/004/tickettrade && APP_ENV=test php migrate.php && echo "MIGRATE_OK" && APP_ENV=test vendor/bin/phpunit --testsuite=phase-3 --filter='CreateListing|SubmitDraft|MyListings|ListingAutoApprove' 2>&1 | tail -15</automated>
    <fails_when>non-zero exit, or MIGRATE_OK missing, or "0 passed" / "FAILURES" in the phpunit output</fails_when>
  </verify>
  <done>
    - 6 new Action classes exist; `BrowseAction` and `MyListingsAction` are replaced with real bodies.
    - 2 new Views (`create.php`, `my_listings.php`); 3 new View partials.
    - 8 new route entries in `config/routes.php`.
    - 1 new error code (`E_LISTING_REVIEW_FLAG`).
    - 1 new migration `012_cron_log.sql`; the test DB has the `cron_log` table.
    - `ListingService::createDraft`, `submitDraft`, `getSellerListings`, `runAutoApproveSweep`, `softDelete`, `hardDelete` all work end-to-end.
    - All new tests in the verify command pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Edit + delete + relist flows, plus review_flag on edit-to-active</name>
  <files>
    src/Listing/Action/EditListingAction.php
    src/Listing/Action/DeleteListingAction.php
    src/Listing/Action/RelistListingAction.php
    src/Listing/Service/listing_service.php
    src/Listing/View/edit.php
    tests/Integration/Phase03/Listing/EditListingFlowTest.php
    tests/Integration/Phase03/Listing/DeleteListingFlowTest.php
    tests/Integration/Phase03/Listing/RelistFlowTest.php
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/src/Listing/Service/listing_service.php (from Plan 03-01 + Task 1: `saveDraft`, `relist`, `setReviewFlag`, `uploadImages`; the `loadForEdit` helper that checks seller_id and status)
    - /home/user/hermesag/004/tickettrade/src/Listing/Model/listing_model.php (`update` is the inverse of `insert`; `setReviewFlag($id, $on, $at)` from Plan 03-01)
    - /home/user/hermesag/004/tickettrade/src/Listing/View/create.php (from Task 1; copy its form structure for `edit.php`, pre-populate values)
    - /home/user/hermesag/004/tickettrade/.planning/phases/03-marketplace-listings-discovery/03-CONTEXT.md (D-02 per-state actions; D-04 Rejected Edit flips to draft; D-09 review_flag on edit-to-active; D-14 one-click Relist; D-15 seller dashboard tabs; D-16 drag-to-reorder is Phase 3 out of scope)
  </read_first>
  <behavior>
    - `GET /listings/{id}/edit` as the listing's owner with `status='rejected'`: 200 response, HTML body has a `<div class='alert alert-danger' role='alert' aria-live='polite'>` banner with the rejection reason text, the form is pre-populated with the original fields, and the submit button label is `Resubmit for review`.
    - `POST /listings/{id}/edit` on an `active` listing: the row's fields update, `review_flag=1`, `review_flag_at=NOW()`; a new row is appended to `listing_revisions` with the PRE-edit `snapshot_json`; a flash toast `Listing saved. Edits to active listings are reviewed by an admin before they go live` is set.
    - `POST /listings/{id}/edit` on a `draft` or `pending` listing: the row's fields update, `review_flag` stays 0, NO `listing_revisions` row is appended.
    - `POST /listings/{id}/delete` on an `active` listing: the row's `status='removed'`; the row stays in DB (soft-delete).
    - `POST /listings/{id}/delete` on a `draft` listing: the row is hard-deleted.
    - `POST /listings/{id}/delete` as a non-owner: 404 (not 403, per D-14).
    - `POST /listings/{id}/relist` on a `sold` listing: a NEW row is inserted with `status='draft'`, all fields copied except `quantity_sold=0`; the seller is redirected to `/listings/{new_id}/edit`.
    - `POST /listings/{id}/relist` on a non-`sold` listing: 200 with the form re-rendered and `E_VALIDATION` `Only sold listings can be relisted`.
  </behavior>
  <action>
    1. **Action — `src/Listing/Action/EditListingAction.php` (new)**: namespace `App\Listing\Action`; class `EditListingAction`. `handle()` (GET): requireAuth, load listing via `ListingService::loadForEdit($listingId, $userId)`; on not-found or not-owned, return 404; if `status='rejected'`, flip to `draft` (call `ListingService::saveDraft` with no field changes, or a dedicated `flipRejectedToDraft`); if `status='active'`, set the `review_flag` warning in the view vars; render `edit.php` with pre-populated values + existing images. `handlePost()` (POST): requireAuth; call `ListingService::saveDraft($listingId, $userId, $_POST)`; if the original status was `active`, the Service sets `review_flag=1` and appends a `listing_revisions` snapshot; on success, redirect to `/my-listings?tab=active` with a flash toast; on validation error, re-render `edit.php` with the form values + `error.fields`.
    2. **Action — `src/Listing/Action/DeleteListingAction.php` (new)**: namespace `App\Listing\Action`; class `DeleteListingAction`. `handle()` (POST): requireAuth; call `ListingService::softDelete($listingId, $userId)` if `status IN ('active', 'rejected')`, else `ListingService::hardDelete`; on success, redirect to `/my-listings?tab=<originalTab>` with flash `Listing removed`; on not-found or not-owned, return 404.
    3. **Action — `src/Listing/Action/RelistListingAction.php` (new)**: namespace `App\Listing\Action`; class `RelistListingAction`. `handle()` (POST): requireAuth; call `ListingService::relist($listingId, $userId)`; on success, the Service returns `['ok'=>true, 'data'=>['new_listing_id'=>N], 'error'=>null]`; redirect to `/listings/{new_listing_id}/edit` with flash `Relisted as draft - edit and submit when ready`; on `E_VALIDATION` (not sold), re-render the referer page with the error.
    4. **Service — `src/Listing/Service/listing_service.php` (EXTEND)**: ADD `saveDraft(int $listingId, int $sellerId, array $data): array` — if the listing's current status is `active`, set `review_flag=1, review_flag_at=NOW()` and append a `listing_revisions` snapshot BEFORE applying the update; else (draft/pending/rejected), just update. Returns the standard envelope. ADD `relist(int $soldListingId, int $sellerId): array` — validates `status='sold'`, copies all fields (title, description, price_cents, type, condition/service details, category_id, quantity) into a new row with `status='draft', quantity_sold=0, review_flag=0`; returns the new listing_id. ADD `softDelete`, `hardDelete` as in Task 1. ADD private helper `appendRevisionSnapshot(int $listingId, int $by, array $beforeData): void` that calls `listing_revision_model::insert` with `json_encode($beforeData)`.
    5. **View — `src/Listing/View/edit.php` (new)**: same form structure as `create.php` (copy the markup), pre-populate `value='<?= htmlspecialchars($listing['title']) ?>'` etc., show the rejection banner when `!empty($listing['rejection_reason'])`, show the review_flag warning when `!empty($listing['review_flag'])`, list the existing images (thumbnails) with a small `Remove` link that POSTs to a new `/listings/{id}/images/{imageId}/delete` endpoint (Phase 3 scope: just render the images, deletion is a follow-up in Phase 6).
    6. **Tests**: write 3 integration tests covering the behaviors above. Use the Phase 2 test pattern: transaction-rolled-back, seed user + listing in `setUp`.
  </action>
  <verify>
    <automated>cd /home/user/hermesag/004/tickettrade && APP_ENV=test vendor/bin/phpunit --testsuite=phase-3 --filter='EditListing|DeleteListing|Relist' 2>&1 | tail -15</automated>
    <fails_when>non-zero exit, or "0 passed" / "FAILURES" / "ERRORS" in the output</fails_when>
  </verify>
  <done>
    - 3 new Action classes (`EditListingAction`, `DeleteListingAction`, `RelistListingAction`).
    - 1 new View (`edit.php`).
    - `ListingService::saveDraft`, `relist`, `softDelete`, `hardDelete`, `appendRevisionSnapshot` all work.
    - Edit-to-active sets `review_flag=1` and appends a `listing_revisions` row.
    - Edit-to-rejected flips to `draft` and shows the rejection banner.
    - Relist copies fields and resets `quantity_sold=0` in a new draft row.
    - All 3 new test files pass; the full `phase-3` testsuite is still green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tests + PSR-12 lint + phase-3 testsuite wired</name>
  <files>
    tests/Integration/Phase03/Listing/MyListingsTabsTest.php
    tests/Integration/Phase03/Support/RouteGuardListingTest.php
    tests/Integration/Phase03/Listing/CreateListingFlowTest.php
    phpunit.xml
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/tests/Integration/Phase02/Support/AuthGuardTest.php (existing route-guard test pattern)
    - /home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-0[1-3]-PLAN.md (test patterns)
    - /home/user/hermesag/004/tickettrade/src/Support/Auth.php (`requireAuth`, `requireAdmin` — to test the 404 on non-admin)
  </read_first>
  <behavior>
    - `tests/Integration/Phase03/Listing/MyListingsTabsTest::testTabsRenderWithCounts`: 4 listings seeded (1 active, 2 pending, 1 sold, 0 drafts); GET /my-listings renders 4 tab links with counts `(1) (2) (1) (0)`; the Active tab carries `aria-current='page'`; clicking the Pending tab (simulated via `?tab=pending`) shows the 2 pending rows.
    - `tests/Integration/Phase03/Listing/MyListingsTabsTest::testEmptyStateForActiveTab`: 0 listings; the Active tab shows `No active listings yet - your first listing is one click away` and a primary CTA `Create your first listing` linking to `/listings/create`.
    - `tests/Integration/Phase03/Support/RouteGuardListingTest::testCreateRequiresAuth`: GET /listings/create without session returns 302 to `/login?next=/listings/create`.
    - `tests/Integration/Phase03/Support/RouteGuardListingTest::testCronRequiresAdmin`: POST /admin/cron/ticket-expiry as a non-admin returns 404.
    - `tests/Integration/Phase03/Support/RouteGuardListingTest::testCronRequiresReAuth`: POST /admin/cron/ticket-expiry as an admin without re-auth returns 403 JSON.
    - `tests/Integration/Phase03/Listing/CreateListingFlowTest::testDraftSaveThenSubmit`: POST /listings/create with `action=save_draft` returns 302 to /my-listings?tab=draft with flash toast `Draft saved`; the row's `status='draft'`. POST /listings/{id}/submit flips to `status='pending'`.
  </behavior>
  <action>
    - Create 4 test files at the paths listed; follow the Phase 2 test pattern.
    - Run `vendor/bin/phpunit --testsuite=phase-3` and iterate until green.
    - Run `vendor/bin/phpcs --standard=PSR12 src/Listing src/Support/View/partials/listing_status_pill.php src/Support/View/partials/seller_dashboard_tabs.php src/Support/View/partials/empty_state.php` and fix any reported issues.
    - Run the full `vendor/bin/phpunit` to confirm no Phase 2 test broke.
  </action>
  <verify>
    <automated>cd /home/user/hermesag/004/tickettrade && APP_ENV=test vendor/bin/phpunit --testsuite=phase-3 2>&1 | tail -8 && echo "---PSR12---" && vendor/bin/phpcs --standard=PSR12 src/Listing src/Support/View/partials/listing_status_pill.php src/Support/View/partials/seller_dashboard_tabs.php src/Support/View/partials/empty_state.php 2>&1 | tail -8</automated>
    <fails_when>non-zero exit, or "0 passed" / "FAILURES" / "ERRORS" in phpunit output, or any phpcs "ERROR" / "FOUND" lines in phpcs output</fails_when>
  </verify>
  <done>
    - 4 test files pass; the full `phase-3` testsuite is green.
    - `vendor/bin/phpcs --standard=PSR12` reports no errors on the Phase 3 source files.
    - The full PHPUnit suite is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /listings/create | Untrusted seller input. Service-validated. |
| Browser → POST /listings/{id}/edit | Untrusted edit data + ownership check. |
| Browser → POST /listings/{id}/delete | Untrusted delete; soft-delete vs hard-delete is data-driven. |
| Browser → POST /listings/{id}/relist | Untrusted relist; only `sold` listings can be relisted by the seller. |
| Browser → POST /admin/cron/ticket-expiry | Admin-only; re-auth gated; rate-limited. |
| Seller → listing_revisions | Pre-edit snapshot is appended server-side; the seller cannot prevent it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-11 | Elevation of Privilege | `EditListingAction` | high | mitigate | `ListingService::saveDraft` and `loadForEdit` both enforce `seller_id = CURRENT_USER`; cross-seller edits return `E_LISTING_FORBIDDEN` (mapped to 404, not 403, per D-14). |
| T-03-12 | Tampering | `listings` | high | mitigate | All edits go through `ListingService`; no Action writes the DB directly. `price_cents` is sign-checked at the Service. |
| T-03-13 | Repudiation | `listing_revisions` | medium | mitigate | Every edit to an `active` listing appends a `listing_revisions` row BEFORE the update, with the pre-edit `snapshot_json` and `created_by = CURRENT_USER`. The Phase 4 hash-chained `audit_log` (AD-12) will chain from this table. |
| T-03-14 | Information Disclosure | `/listings/{id}/edit` 403 | low | mitigate | Non-owner access returns 404 (same as unknown route), never 403, so listing existence is not leaked. |
| T-03-15 | Denial of Service | `POST /listings/create` | medium | mitigate | Rate-limited at 20/hr/user via `listing_create`. |
| T-03-16 | Denial of Service | `POST /admin/cron/ticket-expiry` | medium | mitigate | Admin-only + re-auth gated + rate-limited at 5/min/IP via `admin_cron`. |
| T-03-17 | Tampering | `EditListingAction` on `active` | high | mitigate | `review_flag=1` is set server-side inside the Service transaction; the seller cannot bypass it. The admin queue (Phase 8) surfaces flagged listings alongside pending ones. |
| T-03-18 | Information Disclosure | rejection reason | low | mitigate | The rejection reason is shown ONLY to the listing's owner on the edit form (D-04). It is not in any public surface. |
| T-03-19 | Spoofing | `RelistListingAction` | medium | mitigate | Only `sold` listings can be relisted; the Service returns `E_VALIDATION` for any other status. The new draft's `quantity_sold` is hard-coded to 0 in the SQL. |
| T-03-20 | Tampering | `cron_log` table | low | mitigate | The cron Action writes a structured row with `actor_user_id`; Phase 4 migrates to hash-chained `audit_log`. |
| T-03-SC | Tampering | Composer installs | high | mitigate | No new Composer packages in this plan. |

## Trust-Boundary Coverage

- T-03-11, T-03-12: Action → Service → Model; the Service is the sole writer.
- T-03-13: pre-edit snapshot via the Service.
- T-03-17: review_flag is set inside the Service's transaction.

## Package Legitimacy Audit

- No new Composer packages. T-03-SC: not applicable.
</threat_model>

<verification>
- [ ] All 6 Actions + 2 Views + 3 Partials + 1 Migration + 1 cron endpoint exist.
- [ ] `vendor/bin/phpunit --testsuite=phase-3` is green (≥10 cases).
- [ ] `vendor/bin/phpunit` (full suite) is green.
- [ ] `vendor/bin/phpcs --standard=PSR12` reports no errors on Phase 3 source.
- [ ] Creating a listing and submitting it shows on the seller's Pending tab.
- [ ] Editing an `active` listing sets `review_flag=1` and appends a `listing_revisions` row.
- [ ] `POST /admin/cron/ticket-expiry` as admin + re-auth approves pending listings older than 24h.
</verification>

<success_criteria>
- A logged-in seller can create a listing (draft or pending), edit it (with the review_flag on active), delete it (soft for active, hard for draft/pending), and relist a sold listing as a new draft.
- The seller dashboard renders 4 tabs with per-state counts and per-state action affordances, plus named empty-state copy.
- The hand-triggered admin auto-approve Action approves pending listings older than 24h, logs to `cron_log`, and is gated by re-auth + rate-limit + admin role.
- All Phase 3 tests pass and PSR-12 is clean.
</success_criteria>

<output>
Create `.planning/phases/03-marketplace-listings-discovery/03-02-SUMMARY.md` when done.
</output>
