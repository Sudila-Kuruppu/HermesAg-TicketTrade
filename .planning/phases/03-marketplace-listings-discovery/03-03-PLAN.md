---
phase: 03-marketplace-listings-discovery
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/Listing/Action/BrowseAction.php
  - src/Listing/View/board.php
  - src/Listing/View/listing_modal.php
  - src/Support/View/partials/category_tabs.php
  - src/Support/View/partials/pagination.php
  - src/Support/View/partials/search_box.php
  - src/Support/View/partials/listing_modal_carousel.php
  - src/Support/View/partials/list_view_toggle.php
  - public/assets/js/listing_modal.js
  - tests/Integration/Phase03/Listing/BrowseBoardTest.php
  - tests/Integration/Phase03/Listing/SearchTest.php
  - tests/Integration/Phase03/Listing/PaginationTest.php
  - tests/Integration/Phase03/Listing/ModalRenderTest.php
  - tests/Integration/Phase03/Listing/GuestBrowseTest.php
autonomous: true
requirements:
  - LST-03
  - LST-04
  - LST-05
  - LST-06
  - LND-07
  - LND-08
  - PER-02
user_setup: []

estimate:
  tokens: 88000
  raw_tokens: 44000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "GET /board as a guest: 200 response, HTML body shows the corkboard with up to 50 listing cards, each card has `±2°` rotation seeded by `crc32(listing_id) % 5 - 2`, a red or blue pushpin graphic (`listing_id % 2`), and `aria-hidden='true'` on the rotation/pin decoration; the card's CTA reads `Sign in to buy` (not `Buy Now`) and links to `/login?next=/board`."
    - "GET /board as a logged-in buyer: same corkboard but the card CTA is `Buy Now` and links to `/listings/{id}` (a placeholder; Phase 4 wires the actual modal+confirmation). The card carries the seller's nickname and rank badge."
    - "GET /board?q=calculus: 200 response, only listings matching `MATCH(title,description) AGAINST('calculus*' IN BOOLEAN MODE)` are rendered; the search box is pre-populated with `value='calculus'`; the `?q=` is preserved in the pagination URLs."
    - "GET /board?cat=2 (Electronics): 200 response, only listings in category_id=2 are rendered; the Electronics tab carries `aria-current='page'`."
    - "GET /board?cat=2&q=phone&page=2: combines category + search + pagination filters; 200 response with the 51-100th matching listings."
    - "GET /board with 60 active listings: the response shows exactly 50 cards; the pagination control renders `Prev 1 2 Next` at the top and bottom (mobile: bottom only); clicking page 2 URL has `?page=2`."
    - "The list-view toggle in the board header (corkboard vs plain-grid) is a single button with `aria-pressed` state; clicking it flips to plain-grid and persists per session via `sessionStorage.setItem('listView', 'grid')`; the corkboard view restores on a new tab/session."
    - "Clicking a corkboard card opens a Bootstrap modal (`modal-fullscreen-sm-down`, `modal-dialog-centered` on desktop) with the listing's image carousel, title, description, price, seller info (nickname + rank badge + verified checkmark), Next/Previous buttons, ESC handler, backdrop click handler; the URL updates to `/board#listing-{id}`."
    - "The image carousel uses Bootstrap 5 carousel with `interval: false` (no auto-advance); indicators are dots at the bottom; prev/next arrows render only when more than 1 image exists."
    - "Next/Previous buttons in the modal header cycle through listings in the same category the modal was opened from, ordered by `created_at DESC`; end-of-list wraps to the start. The buttons are `<` and `>` icon buttons in the modal header."
    - "Keyboard: left/right arrow keys navigate prev/next within the modal; ESC closes the modal and returns focus to the originating card on the corkboard; Tab cycles within the modal (focus trap)."
    - "Mobile swipe: `touchstart`/`touchend` with a 50px threshold navigates prev/next. The visual slide is replaced with a cross-fade when `prefers-reduced-motion: reduce` is set."
    - "Closing the modal (ESC, backdrop, X button) returns focus to the card that opened it and removes the `#listing-{id}` from the URL."
    - "The board auto-degrades below 768px (`<md`): the corkboard paper-card rotation is preserved, but the grid becomes 2 columns (down from 3-4); the `prefers-reduced-motion: reduce` user agent removes the hover-lift transform."
  artifacts:
    - src/Listing/Action/BrowseAction.php (replaced with real board implementation)
    - src/Listing/View/board.php (replaced with the corkboard + list-view layout)
    - src/Listing/View/listing_modal.php (new)
    - src/Support/View/partials/category_tabs.php (new)
    - src/Support/View/partials/pagination.php (new)
    - src/Support/View/partials/search_box.php (new)
    - src/Support/View/partials/listing_modal_carousel.php (new)
    - src/Support/View/partials/list_view_toggle.php (new)
    - public/assets/js/listing_modal.js (new)
    - tests/Integration/Phase03/Listing/BrowseBoardTest.php (new)
    - tests/Integration/Phase03/Listing/SearchTest.php (new)
    - tests/Integration/Phase03/Listing/PaginationTest.php (new)
    - tests/Integration/Phase03/Listing/ModalRenderTest.php (new)
    - tests/Integration/Phase03/Listing/GuestBrowseTest.php (new)
  key_links:
    - "`ListingService::getSearchResults($q, $categoryId, $page)` (from Plan 03-01) returns `['rows'=>[...], 'total'=>N, 'page'=>$page, 'pages'=>ceil($total/50)]`; the BoardView renders the rows using `listing_card_cork.php` partial; the `q` and `category_id` are preserved in pagination URLs."
    - "`ListingService::getNextInCategory($listingId, $categoryId, $direction)` (NEW) returns the next or previous listing id in the same category (ordered `created_at DESC`); wraps to the first/last id. The modal's Next/Previous buttons call this via an AJAX endpoint OR pre-render the next/prev IDs at page load (the latter avoids extra requests; choice: pre-render 1 prev + 1 next in `BrowseAction` and pass them to the View as `$prev_id` / `$next_id`)."
    - "`ListingService::getWithImages($id)` (from Plan 03-01) is called when the modal is opened server-side; for an AJAX-served modal, the BrowseAction renders the modal HTML inline once (not per-card) and the JS swaps the content; for simplicity, Phase 3 renders the modal HTML for the first visible listing inline, and the JS uses `fetch()` for prev/next swaps to avoid a full page reload."
    - "`category_tabs` partial renders the 8 tabs (All + 7 specific) in `?cat=` order; the active tab carries `aria-current='page'` and the underline pattern from Phase 1; the strip is horizontally scrollable on mobile (`<md`) with the active tab snapped into view via `scrollIntoView` on initial load."
    - "`pagination` partial renders the numbered pages with `?page=N` URLs that preserve `q` and `cat`; the control is rendered at the top AND bottom on desktop, bottom only on mobile (Bootstrap `d-none d-md-block` on the top one)."
    - "`search_box` partial is a single `<input type='search' name='q' placeholder='Search by title or description'>` inside a `<form method='GET' action='/board'>`; submitting the form preserves `cat` and `page` via hidden inputs."
    - "`listing_modal.js` (~120 LOC) handles: (a) the open/close lifecycle; (b) the keyboard arrows; (c) the touchstart/touchend swipe; (d) the focus trap (Tab cycles within `.modal-dialog`); (e) the URL fragment update (`#listing-{id}` on open, removed on close); (f) the prefers-reduced-motion check for the cross-fade. The script is a self-registering component (`data-component='listingModal'`) and is loaded via the existing `public/assets/js/tickettrade.js` bundle — no new `<script src>` tag in the View."
    - "`list_view_toggle` partial is a single button with `data-component='listViewToggle'` (already shipped in Phase 1). Phase 3's BrowseAction renders it in the board header; the `aria-pressed` state is read from `sessionStorage.getItem('listView')` (Phase 1 component)."
    - "The `<768px` mobile breakpoint: Bootstrap's `col-12 col-sm-6 col-md-4 col-lg-3` grid for the corkboard (1/2/3/4 columns). Below 768px, the cork texture is still visible but the cards are tighter; the hover-lift transform is suppressed (existing Phase 1 `prefersReducedMotion` component also handles the touch case)."
---

# Artifacts this phase produces
- 1 modified Action (`BrowseAction`).
- 1 replaced View (`board.php`); 1 new View (`listing_modal.php`).
- 5 new View partials.
- 1 new JS file (`listing_modal.js`).
- 5 new integration test files.

<objective>
Ship the corkboard board view at `/board` and the full-screen listing modal. By the end of this plan, a guest can browse the corkboard, filter by category, search by keyword, paginate 50/page, and open a listing in the modal with full keyboard and mobile-swipe navigation. A logged-in buyer sees the same surface with the seller info and rank badge on the modal, and the corkboard's "Sign in to buy" CTA flips to "Buy Now" (which is a placeholder link in Phase 3; Phase 4 wires the actual purchase flow).

Purpose: this plan is the buyer/guest surface for Phase 3. The WAD rubric lines "Browse products", "Search by category", and the corkboard visual signature all live here. The board view is also the WAD demo entry-point: "register → list → admin approve → log in as buyer → browse corkboard → click listing → see modal" is the entire 90-second WAD video path.

Output: 1 modified Action, 1 replaced View, 1 new View, 5 new partials, 1 new JS file, 5 new test files.
</objective>

<execution_context>
@/home/user/hermesag/004/.opencode/gsd-core/workflows/execute-plan.md
@/home/user/hermesag/004/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/user/hermesag/004/tickettrade/AGENTS.md
@/home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md
@/home/user/hermesag/004/tickettrade/EXPERIENCE.md
@/home/user/hermesag/004/tickettrade/DESIGN.md
@/home/user/hermesag/004/tickettrade/.planning/PROJECT.md
@/home/user/hermesag/004/tickettrade/.planning/REQUIREMENTS.md
@/home/user/hermesag/004/tickettrade/.planning/phases/03-marketplace-listings-discovery/03-CONTEXT.md
@/home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-0[1-3]-SUMMARY.md
@/home/user/hermesag/004/tickettrade/src/Support/Auth.php
@/home/user/hermesag/004/tickettrade/src/Support/View.php
@/home/user/hermesag/004/tickettrade/src/Support/View/layout.php
@/home/user/hermesag/004/tickettrade/src/Support/View/partials/{head,bottom_nav,toast_container,flash_toast,skip_link,listing_card_cork,listing_card}.php
@/home/user/hermesag/004/tickettrade/src/Listing/Service/listing_service.php
@/home/user/hermesag/004/tickettrade/src/Category/Service/category_service.php
@/home/user/hermesag/004/tickettrade/src/Listing/View/board.php
@/home/user/hermesag/004/tickettrade/public/assets/js/tickettrade.js
@/home/user/hermesag/004/tickettrade/public/mockups/board-mobile.html
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — corkboard board with 50 listings, search + category + pagination filters, modal opens with carousel + keyboard nav</name>
  <files>
    src/Listing/Action/BrowseAction.php
    src/Listing/View/board.php
    src/Listing/View/listing_modal.php
    src/Support/View/partials/category_tabs.php
    src/Support/View/partials/pagination.php
    src/Support/View/partials/search_box.php
    src/Support/View/partials/listing_modal_carousel.php
    src/Support/View/partials/list_view_toggle.php
    public/assets/js/listing_modal.js
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/AGENTS.md
    - /home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md (AD-1, AD-16, AD-17)
    - /home/user/hermesag/004/tickettrade/.planning/phases/03-marketplace-listings-discovery/03-CONTEXT.md (D-15..D-19 board loading; D-20..D-24 modal; D-27 guest browse; LND-07 corkboard paper-card; LND-08 list-view toggle)
    - /home/user/hermesag/004/tickettrade/EXPERIENCE.md (Component Patterns for listing card cork, listing modal; Interaction Primitives for keyboard nav; State Patterns for empty/error/cold-load)
    - /home/user/hermesag/004/tickettrade/DESIGN.md (corkboard tokens, paper-card surface #FFF8E7, push pin red/blue, ±2° rotation, tier colors, status-pending-fill / status-active-fill / status-rejected-fill)
    - /home/user/hermesag/004/tickettrade/src/Listing/Service/listing_service.php (`getSearchResults($q, $categoryId, $page)`, `getWithImages($id)`, `getNextInCategory`)
    - /home/user/hermesag/004/tickettrade/src/Category/Service/category_service.php (`listActive()` for the tab strip)
    - /home/user/hermesag/004/tickettrade/src/Support/View/partials/listing_card_cork.php (from Plan 03-01: paper-card with rotation, pushpin, primary thumbnail)
    - /home/user/hermesag/004/tickettrade/src/Support/View/partials/listing_card.php (from Plan 03-01: list-view variant, no rotation)
    - /home/user/hermesag/004/tickettrade/src/Support/View/layout.php (the standard page layout)
    - /home/user/hermesag/004/tickettrade/public/mockups/board-mobile.html (the visual reference for the corkboard; the markup MUST match this)
    - /home/user/hermesag/004/tickettrade/public/assets/js/tickettrade.js (existing self-registering components: `toast`, `listViewToggle`, `skeleton`, `prefersReducedMotion`)
    - /home/user/hermesag/004/tickettrade/src/Support/Auth.php (`currentUser(): ?array` — to determine guest vs buyer CTA copy)
  </read_first>
  <behavior>
    - `GET /board` with 60 active listings in the DB and no `?q` or `?cat`: 200 response, HTML body has a `<section class='corkboard' data-component='corkboard'>` containing 50 `.listing-card-cork` divs; each div has a `style='transform: rotate(<N>deg)'` where `N` is `crc32(id) % 5 - 2` (range -2..+2), a `pin` div with class `pin-red` or `pin-blue` (id % 2), and `aria-hidden='true'` on the rotation wrapper.
    - `GET /board?q=calculus` with 5 listings containing "calculus" in title/description: 200 response with exactly 5 cards rendered; the search box has `value='calculus'`; the response URL is preserved as `?q=calculus` in the pagination links.
    - `GET /board?cat=2` with 10 listings in category_id=2: 200 response with exactly 10 cards; the Electronics tab carries `aria-current='page'` and the `active` Bootstrap class.
    - `GET /board?cat=2&q=phone&page=2` with 60 listings in cat=2 matching "phone": 200 response with cards 51-100; pagination shows `Prev 1 2 3 Next`; the prev/next links preserve `cat=2&q=phone`.
    - `GET /board` as a guest: each card's CTA button text is `Sign in to buy` linking to `/login?next=/board` (NOT `/listings/{id}`).
    - `GET /board` as a logged-in buyer: each card's CTA button text is `Buy Now` linking to `/listings/{id}#buy` (a placeholder; Phase 4 wires the actual modal+confirmation).
    - `GET /board` with the list-view toggle button in the header: the button has `aria-pressed='false'` by default; clicking it sets `aria-pressed='true'` and switches the container from `.corkboard` to `.list-grid` (plain Bootstrap grid); the state persists in `sessionStorage.getItem('listView')` and is restored on subsequent navigations.
    - `GET /board` with 0 listings: the body shows the named empty-state copy `No listings yet - check back soon` with a subline `New listings appear here within 24 hours of submission`.
    - `GET /board?cat=99` (non-existent category): 200 response, the All tab is active, 0 cards, the same empty-state.
    - `GET /board` then `GET /board#listing-3` (or click a card): the listing modal opens with the listing's title, description, price, image carousel, seller info (nickname + rank badge + verified checkmark), and the URL updates to `/board#listing-3`.
    - The modal's image carousel: dots at the bottom, prev/next arrows only when more than 1 image; `interval: false` (no auto-advance); `data-bs-ride='false'`.
    - Pressing `←` in the modal navigates to the previous listing in the same category; pressing `→` navigates to next; pressing `Esc` closes the modal and returns focus to the originating card; pressing `Tab` cycles within the modal (focus trap).
    - On a touch device: a left swipe (touchstart→touchend with dx>50px) navigates to next; a right swipe navigates to prev.
    - Closing the modal (ESC, X button, backdrop click) removes the `#listing-{id}` from the URL and returns focus to the originating card.
    - With `prefers-reduced-motion: reduce` set: the carousel slide animation is replaced with a cross-fade; the corkboard hover-lift transform is suppressed.
  </behavior>
  <action>
    1. **Action — `src/Listing/Action/BrowseAction.php` (REPLACE stub)**: namespace `App\Listing\Action`; class `BrowseAction`. `handle(): void` (GET only — no POST): reads `?q`, `?cat`, `?page` from `$_GET`; default `q=null, cat=null, page=1`; calls `CategoryService::listActive()` for the tab strip; calls `ListingService::getSearchResults($q, $cat, $page)` for the result set; calls `ListingService::getSellerListings(null, 'active', 1)` ONLY when `?q`/`?cat` are absent (to get the 4 counts for the seller dashboard; for the board, no counts needed); reads `$GLOBALS['current_user']` to determine guest vs buyer CTA. Renders `src/Listing/View/board.php` with `$vars = ['rows' => $rows, 'total' => $total, 'page' => $page, 'pages' => $pages, 'q' => $q, 'cat' => $cat, 'categories' => $cats, 'is_guest' => $currentUser === null, 'csrf_token' => $_SESSION['csrf_token']]`.
    2. **Service — `src/Listing/Service/listing_service.php` (EXTEND)**: ADD `getNextInCategory(int $listingId, ?int $categoryId, string $direction): ?int` — returns the next/prev listing id in the same category (ordered `created_at DESC`); wraps via SQL `SELECT id FROM listings WHERE category_id = ? AND status = 'active' AND id <> ? ORDER BY created_at DESC, id DESC LIMIT 1 OFFSET 0` for "next" (and ASC for "prev"); if the result is null, loops to the first/last.
    3. **View — `src/Listing/View/board.php` (REPLACE stub)**: PHP template inside the layout. Top: `<h1 class='visually-hidden'>Marketplace board</h1>`. Header bar: search box partial + list-view toggle partial. Tabs: category_tabs partial. Body: `<?php if (empty($rows)): ?> <?= $this->partial('empty_state', ['title' => 'No listings yet', 'body' => 'New listings appear here within 24 hours of submission', 'cta_label' => null]) ?> <?php else: ?> <div class='corkboard row g-3' data-component='corkboard'> <?php foreach ($rows as $row): ?> <div class='col-12 col-sm-6 col-md-4 col-lg-3 cork-cell' data-listing-id='<?= $row['id'] ?>' style='transform: rotate(<?= (crc32((string)$row['id']) % 5) - 2 ?>deg)' aria-hidden='true'> <div class='pin <?= $row['id'] % 2 === 0 ? 'pin-red' : 'pin-blue' ?>' aria-hidden='true'></div> <a href='#listing-<?= $row['id'] ?>' class='listing-card-cork-link' data-bs-toggle='modal' data-bs-target='#listingModal' data-listing-id='<?= $row['id'] ?>'> <?= $this->partial('listing_card_cork', ['listing' => $row, 'is_guest' => $is_guest]) ?> </a> </div> <?php endforeach; ?> </div> <?php endif; ?>`. Pagination at top + bottom (mobile bottom only).
    4. **View partial — `src/Support/View/partials/category_tabs.php` (new)**: 8 tabs (All + 7). The active tab is the one matching `?cat=` or All when `?cat` is null. Each tab is `<a class='nav-link <?= $active ? 'active' : '' ?>' href='/board?cat=<?= $catId ?? '' ?><?= $q ? '&q='.urlencode($q) : '' ?>' aria-current='<?= $active ? 'page' : 'false' ?>'><?= htmlspecialchars($catName) ?></a>`. Mobile (`<md`): the strip is `<div class='d-flex flex-nowrap overflow-auto'>` with the active tab snapped into view via inline JS `document.querySelector('.nav-link.active').scrollIntoView({inline:'center', behavior:'smooth'})`.
    5. **View partial — `src/Support/View/partials/pagination.php` (new)**: takes `$page, $pages, $q, $cat`. Renders Bootstrap `pagination` component: `<?php if ($pages > 1): ?> <nav aria-label='Page navigation'><ul class='pagination'> <li class='page-item <?= $page <= 1 ? 'disabled' : '' ?>'><a class='page-link' href='?page=<?= max(1, $page - 1) ?>&cat=<?= $cat ?? '' ?>&q=<?= urlencode($q ?? '') ?>'>Prev</a></li> <?php for ($i = 1; $i <= $pages; $i++): ?> <li class='page-item <?= $i === $page ? 'active' : '' ?>'><a class='page-link' href='?page=<?= $i ?>&cat=<?= $cat ?? '' ?>&q=<?= urlencode($q ?? '') ?>'><?= $i ?></a></li> <?php endfor; ?> <li class='page-item <?= $page >= $pages ? 'disabled' : '' ?>'><a class='page-link' href='?page=<?= min($pages, $page + 1) ?>&cat=<?= $cat ?? '' ?>&q=<?= urlencode($q ?? '') ?>'>Next</a></li> </ul></nav> <?php endif; ?>`. The top copy is wrapped in `<div class='d-none d-md-block'>`; the bottom copy always renders.
    6. **View partial — `src/Support/View/partials/search_box.php` (new)**: a `<form method='GET' action='/board' role='search' class='d-flex'> <input type='search' name='q' class='form-control' placeholder='Search by title or description' value='<?= htmlspecialchars($q ?? '') ?>' aria-describedby='search-help'> <button type='submit' class='btn btn-primary ms-2'>Search</button> </form> <small id='search-help' class='text-muted'>Type at least 4 characters</small>`. The form preserves `cat` and `page` via hidden inputs.
    7. **View partial — `src/Support/View/partials/list_view_toggle.php` (new)**: a single button with `data-component='listViewToggle'` and `aria-pressed='false'`. The Phase 1 JS component already wires the toggle; this partial just renders the button.
    8. **View — `src/Listing/View/listing_modal.php` (new)**: a single Bootstrap modal at the bottom of the page (NOT inside each card). Markup: `<div class='modal fade' id='listingModal' tabindex='-1' aria-labelledby='listingModalTitle' aria-hidden='true'> <div class='modal-dialog modal-fullscreen-sm-down modal-dialog-centered'> <div class='modal-content'> <div class='modal-header'> <button class='btn btn-link prev-listing' aria-label='Previous listing'>&larr;</button> <h2 class='modal-title h5' id='listingModalTitle'>[title]</h2> <button class='btn btn-link next-listing' aria-label='Next listing'>&rarr;</button> <button type='button' class='btn-close' data-bs-dismiss='modal' aria-label='Close'></button> </div> <div class='modal-body'> [carousel + details + seller info; the body is initially empty and the JS loads content via fetch OR the server renders the FIRST visible listing's content inline for the initial state] </div> </div> </div> </div>`. The `<div class='modal-body'>` is the placeholder; the JS swaps its content when prev/next is clicked. (Implementation choice: pre-render the first 3 listings' modals server-side and have the JS toggle `display:none/block` between them; this avoids a fetch roundtrip and keeps the demo fast.)
    9. **View partial — `src/Support/View/partials/listing_modal_carousel.php` (new)**: Bootstrap 5 carousel with `data-bs-ride='false'`, `data-bs-interval='false'`. Indicators (`<ol class='carousel-indicators'>`) and prev/next controls (`<button class='carousel-control-prev'>`) only render when `$images` has more than 1 element. Each slide: `<div class='carousel-item <?= $i === 0 ? 'active' : '' ?>'><img src='/img/<?= $listing_id ?>/full' class='d-block w-100' alt='<?= htmlspecialchars($listing['title']) ?>'></div>`.
    10. **JS — `public/assets/js/listing_modal.js` (new)**: self-registering component via `document.addEventListener('DOMContentLoaded', () => { document.querySelectorAll('[data-component="listingModal"]').forEach(setupListingModal); })`. Functions: `setupListingModal(root)`, `openListing(id)`, `closeListing()`, `navigateListing(direction)`, `loadContentForListing(id)`, `trapFocus(modalEl)`, `releaseFocus()`. The script: (a) binds click handlers to `.listing-card-cork-link` to open the modal; (b) binds `keydown` to the document for `←`/`→`/`Esc` when the modal is open; (c) binds `touchstart`/`touchend` for mobile swipe with a 50px threshold; (d) implements the focus trap by intercepting `Tab` and cycling within the modal; (e) updates `window.location.hash` to `#listing-{id}` on open, removes on close; (f) checks `window.matchMedia('(prefers-reduced-motion: reduce)').matches` and uses a cross-fade instead of slide animation; (g) returns focus to the originating card on close via `document.querySelector('[data-listing-id="' + id + '"]').focus()`. The script size: ~120 LOC.
    11. **Bootstrap CDN — listing the carousel/modal CSS classes**: Bootstrap 5.3 CDN (already shipped in Phase 1) provides `modal`, `modal-fullscreen-sm-down`, `modal-dialog-centered`, `carousel`, `carousel-control-prev/next`, `carousel-indicators`, `carousel-item`. No new CSS dependencies.
    12. **Cold-load skeleton**: the `data-skeleton` component (Phase 1) renders 12 placeholder cards while the board page loads. Wrap the corkboard `<div class='corkboard'>` in a `data-skeleton` for the first paint; the JS removes the skeleton when the actual cards are rendered.
  </action>
  <verify>
    <automated>cd /home/user/hermesag/004/tickettrade && APP_ENV=test vendor/bin/phpunit --testsuite=phase-3 --filter='BrowseBoard|Search|Pagination|ModalRender|GuestBrowse' 2>&1 | tail -15</automated>
    <fails_when>non-zero exit, or "0 passed" / "FAILURES" / "ERRORS" in the output</fails_when>
  </verify>
  <done>
    - `BrowseAction` is replaced with the real board implementation.
    - `board.php` is replaced; `listing_modal.php` is new.
    - 5 new View partials exist.
    - `listing_modal.js` exists and is loaded.
    - All 5 new test files pass; the `phase-3` testsuite is still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Search, pagination, and empty-state edge cases</name>
  <files>
    src/Listing/Action/BrowseAction.php
    src/Listing/View/board.php
    tests/Integration/Phase03/Listing/SearchTest.php
    tests/Integration/Phase03/Listing/PaginationTest.php
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/src/Listing/Model/listing_model.php (`search` and `getSearchCount` from Plan 03-01)
    - /home/user/hermesag/004/tickettrade/.planning/phases/03-marketplace-listings-discovery/03-CONTEXT.md (D-15 FULLTEXT search; D-16 numbered pagination; D-18 newest-first sort; D-19 guest browse)
  </read_first>
  <behavior>
    - `GET /board?q=cal`: matches "cal*" via BOOLEAN MODE prefix-match; the search input has `value='cal'`; the response renders only listings whose `MATCH(title,description) AGAINST('cal*' IN BOOLEAN MODE)` matches.
    - `GET /board?q=` (empty query): behaves as if no query — renders all active listings, paginated.
    - `GET /board?cat=` (empty cat): behaves as if no category — renders all active listings.
    - `GET /board?q=%3Cscript%3Ealert(1)%3C%2Fscript%3E`: the response escapes the query string in the search input via `htmlspecialchars`; no script execution.
    - `GET /board?cat=1&page=999` (page beyond range): 200 response, page 1 of 1, the empty-state renders, the pagination control is hidden (because `$pages === 1`).
    - `GET /board?page=0`: the BrowseAction coerces to 1.
    - `GET /board` with 0 active listings and 0 pending: the empty-state renders the named copy.
    - `GET /board?cat=2&q=phone` with 0 matches: the empty-state renders the named copy `No matches for "phone" in Electronics`.
  </behavior>
  <action>
    1. **Action — `src/Listing/Action/BrowseAction.php` (refine)**: harden input parsing: `$_GET['q']` is `mb_substr(trim($q), 0, 100)`; `$_GET['cat']` is `is_numeric($cat) ? (int)$cat : null`; `$_GET['page']` is `max(1, (int)($_GET['page'] ?? 1))`. If `$cat` is set but the category doesn't exist or `is_active=FALSE`, treat as `cat=null` (All).
    2. **Service — `src/Listing/Model/listing_model.php` (refine)**: the `search` method's FULLTEXT query is `MATCH(title, description) AGAINST(? IN BOOLEAN MODE)` where `?` is `$q.'*'` if `$q` is non-empty (the `*` enables prefix matching for partial keywords per D-15); the `getSearchCount` mirrors the same WHERE clause.
    3. **View — `src/Listing/View/board.php` (refine)**: the empty-state copy for "no matches" is `No matches for "<?= htmlspecialchars($q ?? '') ?>" in <?= htmlspecialchars($catName ?? 'all categories') ?>`. The empty-state for "no listings" is the named copy.
    4. **Tests**: 2 test files (SearchTest, PaginationTest) covering the behaviors above. Seed 10+ listings with varied titles/categories.
  </action>
  <verify>
    <automated>cd /home/user/hermesag/004/tickettrade && APP_ENV=test vendor/bin/phpunit --testsuite=phase-3 --filter='Search|Pagination' 2>&1 | tail -10</automated>
    <fails_when>non-zero exit, or "0 passed" / "FAILURES" / "ERRORS" in the output</fails_when>
  </verify>
  <done>
    - Search input is XSS-safe (htmlspecialchars).
    - Out-of-range page numbers coerce to 1.
    - Empty results show the named empty-state copy.
    - All 2 new test files pass; the full `phase-3` testsuite is green.
  </done>
</task>

<task type="auto">
  <name>Task 3: PSR-12 lint + final phase-3 testsuite verification + visual review</name>
  <files>
    public/assets/js/listing_modal.js
    src/Listing/Action/BrowseAction.php
    src/Listing/View/board.php
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/phpcs.xml (PSR-12 config)
    - /home/user/hermesag/004/tickettrade/public/assets/js/tickettrade.js (existing JS conventions; copy the IIFE pattern)
  </read_first>
  <behavior>
    - `vendor/bin/phpcs --standard=PSR12 src/Listing src/Support/View/partials/{category_tabs,pagination,search_box,listing_modal_carousel,list_view_toggle}.php src/Listing/View/board.php src/Listing/View/listing_modal.php` exits 0.
    - `vendor/bin/phpunit --testsuite=phase-3` reports all tests passing.
    - `vendor/bin/phpunit` (full suite) is green.
    - Visual: open `/board` in a browser; the corkboard paper-card with ±2° rotation is visible; the pushpin graphic alternates red/blue; the search box, category tabs, and pagination render correctly.
  </behavior>
  <action>
    1. Run `vendor/bin/phpcs` and fix any reported issues.
    2. Run the full test suite and confirm green.
    3. Use `php -S 127.0.0.1:18000 -t public public/router.php` (or the project's normal dev server) to spin up a local server; visit `/board` in a browser (or use `curl` + `grep` to verify the markup); confirm the corkboard renders the named visual signature.
  </action>
  <verify>
    <automated>cd /home/user/hermesag/004/tickettrade && APP_ENV=test vendor/bin/phpunit 2>&1 | tail -8 && echo "---PSR12---" && vendor/bin/phpcs --standard=PSR12 src/Listing src/Support/View/partials/category_tabs.php src/Support/View/partials/pagination.php src/Support/View/partials/search_box.php src/Support/View/partials/listing_modal_carousel.php src/Support/View/partials/list_view_toggle.php src/Listing/View/board.php src/Listing/View/listing_modal.php 2>&1 | tail -8</automated>
    <fails_when>non-zero exit, or "0 passed" / "FAILURES" / "ERRORS" in phpunit output, or any phpcs "ERROR" / "FOUND" lines</fails_when>
  </verify>
  <done>
    - PSR-12 is clean on Phase 3 source.
    - All tests pass.
    - The board renders correctly in a local browser.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /board?q=...&cat=...&page=... | Untrusted query strings. Service-validated; XSS-escaped in View. |
| Browser → GET /board#listing-{id} | Fragment-only; the modal JS reads it but the server doesn't. |
| Service → Support\ImageProxy | Internal; the modal's image src is `/img/{id}/full`, which the proxy auth-gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-21 | XSS | Search input | high | mitigate | The query string is `htmlspecialchars`'d in the search input `value=` and in the empty-state copy. The View uses `htmlspecialchars($q)` everywhere. |
| T-03-22 | XSS | Listing title/description | high | mitigate | The `listing_card_cork` partial already wraps `htmlspecialchars` around title/description. Plan 03-01 already enforces this. |
| T-03-23 | Tampering | `?page=999` | low | mitigate | Out-of-range page coerces to 1; the empty-state renders. |
| T-03-24 | Tampering | `?cat=999` (non-existent) | low | mitigate | The Service treats non-existent categories as All; the All tab is active. |
| T-03-25 | Information Disclosure | Listing id enumeration via `/board#listing-{id}` | low | accept | The id is part of the public URL; the corkboard shows all `active` listings. The id is not sensitive. |
| T-03-26 | Spoofing | `aria-current='page'` on inactive tab | low | mitigate | The View's `$active` boolean drives the attribute; the integration tests assert the attribute is on the correct tab. |
| T-03-27 | Denial of Service | FULLTEXT search on huge queries | low | mitigate | The query is capped at 100 chars (Action input parsing). The DB has a FULLTEXT index; the query is O(log n). |
| T-03-28 | Denial of Service | Image carousel `/img/full` hot-linking | medium | mitigate | The ImageProxy auth-gates `full` size (Plan 03-01 T-03-03); only the seller, ticket holder, or admin can fetch. |
| T-03-SC | Tampering | Composer installs | high | mitigate | No new Composer packages. |

## Trust-Boundary Coverage

- T-03-21, T-03-22: All user-derived strings are `htmlspecialchars`'d before render.
- T-03-23, T-03-24: Input coercion in `BrowseAction::handle`.
- T-03-28: The image proxy auth gate (Plan 03-01) protects full-size reads.

## Package Legitimacy Audit

- No new Composer packages.
</threat_model>

<verification>
- [ ] `vendor/bin/phpunit --testsuite=phase-3` is green (≥15 cases total across Plans 03-01, 03-02, 03-03).
- [ ] `vendor/bin/phpunit` (full suite) is green.
- [ ] `vendor/bin/phpcs --standard=PSR12` reports no errors on Phase 3 source.
- [ ] The corkboard renders the paper-card with ±2° rotation, pushpin graphic, and named empty-state copy.
- [ ] Search, category filter, and pagination work; the URL preserves filters across pagination.
- [ ] The modal opens, navigates prev/next, closes, and returns focus to the originating card.
- [ ] Mobile swipe works (50px threshold).
</verification>

<success_criteria>
- A guest can browse the corkboard, search, filter by category, paginate, and open the modal.
- A logged-in buyer sees the same surface with seller info on the modal.
- The WAD demo flow "register → list → admin approve → log in as buyer → browse corkboard → click listing → see modal" works end-to-end.
- All tests pass; PSR-12 is clean.
</success_criteria>

<output>
Create `.planning/phases/03-marketplace-listings-discovery/03-03-SUMMARY.md` when done.
</output>
