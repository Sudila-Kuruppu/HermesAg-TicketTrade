---
phase: 03-marketplace-listings-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/008_listings.sql
  - migrations/009_categories.sql
  - migrations/010_listing_revisions.sql
  - config/uploads.php
  - config/rate_limits.php
  - config/error_codes.php
  - config/contexts.php
  - config/routes.php
  - public/uploads/.htaccess
  - public/uploads/listings/.gitkeep
  - public/img/.htaccess
  - src/Support/ImageUpload.php
  - src/Support/ImageProxy.php
  - src/Support/View/partials/listing_card.php
  - src/Support/View/partials/listing_card_cork.php
  - src/Listing/Model/listing_model.php
  - src/Listing/Model/listing_image_model.php
  - src/Listing/Model/listing_revision_model.php
  - src/Listing/Service/listing_service.php
  - src/Category/Model/category_model.php
  - src/Category/Service/category_service.php
  - tests/Integration/Phase03/Listing/ListingServiceTest.php
  - tests/Integration/Phase03/Listing/ListingModelTest.php
  - tests/Integration/Phase03/Category/CategoryServiceTest.php
  - tests/Integration/Phase03/MigrationTest.php
  - tests/Unit/Phase03/Support/ImageUploadTest.php
  - tests/Unit/Phase03/Support/ImageProxyTest.php
  - phpunit.xml
autonomous: true
requirements:
  - LST-08
  - LST-10
  - LST-11
  - LST-12
  - PER-02
  - PER-03
  - SEC-03
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 47500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Running `php migrate.php` against the existing 7-table database creates 4 new tables (`categories`, `listings`, `listing_images`, `listing_revisions`) with all foreign keys, indexes (incl. `FULLTEXT(title, description)`), and ENUM columns; the 7-row `categories` seed inserts in the same transaction; running `php migrate.php` a second time is a no-op (zero DDL executed) and exits 0."
    - '`Listing\Service\listing_service::createDraft($sellerId, $data)` returns a populated listing row when given valid input (title 80-char, description 2000-char, price_cents, category_id, type ''product'', quantity 1..999); the row''s `status = ''draft''`, `created_at` and `updated_at` are set in Asia/Colombo timezone, and the `quantity_sold` defaults to 0.'
    - '`Support\ImageUpload::process($listingId, $files)` accepts a mixed-source array of `[name,tmp_name,size,error,type]` files; for each file it runs the 4-layer pipeline (finfo MIME -> getimagesize dims<=4000px, size<=5MB -> magic bytes -> GD re-encode to WebP) and on success writes 3 thumbnails (200, 600, 1200 px) to `<storage_root>/<sha256>.webp`; failed files raise `E_IMAGE_INVALID` with a per-file message; the function never writes a non-WebP file.'
    - 'An image with a manipulated extension (e.g., `.jpg` with PHP payload) is rejected at the magic-byte check; the listing record is still created and a `Support\Toast` warning flashes the per-image error.'
    - '`Support\ImageProxy::serve($listingId, $size)` reads the SHA256 file by listing_id, looks up the `listing_images` row for the requested size, and emits the WebP bytes with `Content-Type: image/webp` and `Cache-Control: public, max-age=86400`; for `size=full` (1200px) the proxy requires `$_SESSION[''user_id'']` AND the user is the seller, ticket holder, or admin — otherwise returns 404 (not 403, per AD-14).'
    - '`Support\ImageProxy::serve` is rate-limited at 60/min/IP for `thumb` (200) and `medium` (600) sizes via the existing `Support\RateLimit` named-limit pattern; `full` (1200) is rate-limited at 30/min/user.'
    - '`Listing\Model\listing_model::search($q, $categoryId, $offset, $limit)` runs a prepared `MATCH(title, description) AGAINST(? IN BOOLEAN MODE)` query with optional `category_id = ?` and `status = ''active''` filters, returns newest-first (`ORDER BY created_at DESC`) rows joined to `categories.name` and the primary image''s thumbnail path; default `limit=50` per NFR-PER-002.'
  artifacts:
    - migrations/008_listings.sql
    - migrations/009_categories.sql
    - migrations/010_listing_revisions.sql
    - config/uploads.php
    - config/rate_limits.php
    - config/error_codes.php
    - config/contexts.php
    - config/routes.php
    - public/uploads/.htaccess
    - public/uploads/listings/.gitkeep
    - public/img/.htaccess
    - src/Support/ImageUpload.php
    - src/Support/ImageProxy.php
    - src/Support/View/partials/listing_card.php
    - src/Support/View/partials/listing_card_cork.php
    - src/Listing/Model/listing_model.php
    - src/Listing/Model/listing_image_model.php
    - src/Listing/Model/listing_revision_model.php
    - src/Listing/Service/listing_service.php
    - src/Category/Model/category_model.php
    - src/Category/Service/category_service.php
    - tests/Integration/Phase03/Listing/ListingServiceTest.php
    - tests/Integration/Phase03/Listing/ListingModelTest.php
    - tests/Integration/Phase03/Category/CategoryServiceTest.php
    - tests/Integration/Phase03/MigrationTest.php
    - tests/Unit/Phase03/Support/ImageUploadTest.php
    - tests/Unit/Phase03/Support/ImageProxyTest.php
    - phpunit.xml
  key_links:
    - '`Support\ImageUpload::process` writes to `<config/uploads.php>::storage_root`; in dev this is `__DIR__ . ''/../public/uploads/listings''`, in prod `/var/www/uploads/listings`. The proxy reads from the same root.'
    - '`Support\ImageProxy` enforces the auth rules from CONTEXT.md D-14: `thumb`/`medium` (200/600) public + rate-limited 60/min/IP via the existing `Support\RateLimit` named-limit pattern; `full` (1200) requires session AND one of (seller, ticket holder, admin) — missing auth returns 404 not 403.'
    - '`Listing\Service\listing_service::createDraft` enforces all field-level validation per D-09 (title 1..80, description 1..2000, price_cents >0 and <=100_000_00, quantity 1..999, type ENUM ''product''|''service'', category_id must exist and is_active=TRUE) and returns `E_VALIDATION` with a `fields` map; rate-limited at 20/hr/user via the new `listing_create` rate-limit.'
    - "`config/contexts.php` already lists `Listing` and `Category` bounded contexts (per CONTEXT canonical refs); no change needed there. The Service-layer sole-writer rule (AD-1) is enforced: `listing_service` is the only writer to `listings`, `listing_images`, `listing_revisions`; `category_service` is the only writer to `categories`."
    - '`config/rate_limits.php` adds 2 named limits per CONTEXT D-09 + D-30: `listing_create` (20/hr/user, key=user_id) and `admin_cron` (5/min/IP, key=ip). `Support\RateLimit::hit(''listing_create'', (string)$userId)` returns `[''allowed''=>false]` after 20 calls in 60 minutes for that user.'
    - "The `listings` table's `status ENUM('draft','pending','active','rejected','sold','removed')` and `quantity_sold INT DEFAULT 0` invariants are enforced at the Service layer (CONTEXT D-09/D-11); `category_id` is `BIGINT UNSIGNED NOT NULL` with `FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE RESTRICT` (no orphan listings on category soft-delete since the FK only blocks hard deletes)."
---

# Artifacts this phase produces
- `migrations/008_listings.sql` (new): creates `listings`, `listing_images` tables with `FULLTEXT(title, description)` index, `status ENUM`, `price_cents BIGINT`, `quantity`/`quantity_sold` INT, `type ENUM('product','service')`, `condition ENUM` (product only), service details (services only), `review_flag` TINYINT, `approved_at`/`approved_by`/`rejection_reason` columns.
- `migrations/009_categories.sql` (new): creates `categories` table with `is_active` flag; inserts 7 seed rows (Textbooks, Electronics, Fashion, Services, Food, Events, Other) with stable `sort_order` 1..7.
- `migrations/010_listing_revisions.sql` (new): creates `listing_revisions(id, listing_id BIGINT, snapshot_json JSON, created_at, created_by BIGINT)` for the edit-history audit.
- `config/uploads.php` (new): returns `['storage_root' => <dev-or-prod-path>, 'thumb_px' => 200, 'medium_px' => 600, 'full_px' => 1200, 'webp_quality' => 80, 'max_files' => 8, 'max_file_bytes' => 5_242_880, 'max_dim_px' => 4000]`.
- `src/Support/ImageUpload.php` (new): `process($listingId, array $files): array` returning `[['path'=>..., 'sha256'=>..., 'size_px'=>..., 'thumb_url'=>...], 'errors'=>[]]`; 4-layer validation pipeline.
- `src/Support/ImageProxy.php` (new): `serve($listingId, $size): void`; auth-gated full-size reads; rate-limited thumbnail reads; `Content-Type: image/webp`.
- `src/Listing/Model/listing_model.php` (new): `insert`, `findById`, `findBySellerId($sellerId, $status, $limit)`, `search($q, $categoryId, $offset, $limit)`, `setStatus($id, $status, $extra)`, `incrementSold($id)`, `setReviewFlag($id, $bool)`, `appendRevision($id, $snapshot, $by)`.
- `src/Listing/Model/listing_image_model.php` (new): `insert($listingId, $sha256, $size, $isPrimary, $sortOrder)`, `findByListingId($listingId)`, `deleteByListingId($listingId)`, `updateSortOrder($idsInNewOrder)`.
- `src/Listing/Model/listing_revision_model.php` (new): `insert($listingId, $snapshotJson, $by)`, `findByListingId($listingId)`.
- `src/Listing/Service/listing_service.php` (new): `createDraft($sellerId, $data)`, `saveDraft($listingId, $sellerId, $data)`, `submitDraft($listingId, $sellerId)`, `relist($soldListingId, $sellerId)`, `setReviewFlag($listingId)`, `uploadImages($listingId, $sellerId, $files)`, `validateListingData($data)`, `getWithImages($id)`, `getSearchResults($q, $categoryId, $page)`. Sole writer.
- `src/Category/Model/category_model.php` (new): `findAllActive()`, `findById($id)`, `insert($name, $sortOrder)`, `setActive($id, $bool)`.
- `src/Category/Service/category_service.php` (new): `listActive()`, `getById($id)`. Read-only in Phase 3 (admin CRUD in Phase 8).
- `src/Support/View/partials/listing_card.php` (new): list-view card partial (Bootstrap card, no rotation).
- `src/Support/View/partials/listing_card_cork.php` (new): corkboard card partial (paper surface, ±2° rotation, pushpin, `aria-hidden`).
- `config/routes.php` (modify): ADD `GET /img/{listing_id}/{size}` route to `App\Support\Action\ImageProxyAction` (new Action or direct dispatch in router).
- `public/uploads/.htaccess` (new): `Deny from all` neutralizer.
- `public/img/.htaccess` (new): routes all `/img/*` through `public/router.php`.
- `tests/Integration/Phase03/Listing/ListingServiceTest.php`, `ListingModelTest.php`, `Category/CategoryServiceTest.php`, `MigrationTest.php` (new).
- `tests/Unit/Phase03/Support/ImageUploadTest.php`, `ImageProxyTest.php` (new).
- `phpunit.xml` (modify): add `phase-3` testsuite entries.

<objective>
Land the Phase 3 substrate: 4 migrations (listings, listing_images, categories, listing_revisions), the 4-layer image upload pipeline (`Support\ImageUpload`) that produces three WebP thumbnails per image, the auth-gated image proxy (`Support\ImageProxy`) with per-size rate limits, the listing/category Service+Model layer, and the per-image rate-limit registrations. By the end of this plan, a developer can `INSERT INTO listings` via the Service, upload images via the 4-layer pipeline, and serve them through the proxy at `/img/{listing_id}/{size}`. No UI yet — Phase 3 plans 02/03/04 wire the routes and views.

Purpose: this plan is the Wave-1 foundation that plans 02 (CRUD Actions), 03 (board view + modal), and 04 (landing + cron) all depend on. Without these models, the listings service, the search, the modal image carousel, and the auto-approve Action have nothing to read or write. Per CONTEXT canonical-refs section, every later Phase 3 plan imports `Listing\Service\listing_service` and `Category\Service\category_service`.

Output: 4 new tables; 2 new Support classes; 2 new bounded-context services + 5 new models; 2 new config files; 1 modified config file (rate_limits.php); 6 new test files; updated phpunit.xml.
</objective>

<execution_context>
@/home/user/hermesag/004/.opencode/gsd-core/workflows/execute-plan.md
@/home/user/hermesag/004/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/user/hermesag/004/tickettrade/AGENTS.md
@/home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md
@/home/user/hermesag/004/tickettrade/DESIGN.md
@/home/user/hermesag/004/tickettrade/EXPERIENCE.md
@/home/user/hermesag/004/tickettrade/WAD-CONTEXT.md
@/home/user/hermesag/004/tickettrade/.planning/PROJECT.md
@/home/user/hermesag/004/tickettrade/.planning/ROADMAP.md
@/home/user/hermesag/004/tickettrade/.planning/REQUIREMENTS.md
@/home/user/hermesag/004/tickettrade/.planning/STATE.md
@/home/user/hermesag/004/tickettrade/.planning/phases/03-marketplace-listings-discovery/03-CONTEXT.md
@/home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-CONTEXT.md
@/home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-0[1-3]-SUMMARY.md
@/home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-RESEARCH.md
@/home/user/hermesag/004/tickettrade/src/Support/Db.php
@/home/user/hermesag/004/tickettrade/src/Support/RateLimit.php
@/home/user/hermesag/004/tickettrade/src/Support/Auth.php
@/home/user/hermesag/004/tickettrade/src/Support/Error.php
@/home/user/hermesag/004/tickettrade/src/Support/View.php
@/home/user/hermesag/004/tickettrade/src/Auth/Service/auth_service.php
@/home/user/hermesag/004/tickettrade/src/User/Service/user_service.php
@/home/user/hermesag/004/tickettrade/src/Listing/Action/BrowseAction.php
@/home/user/hermesag/004/tickettrade/src/Listing/Action/MyListingsAction.php
@/home/user/hermesag/004/tickettrade/src/Listing/View/board.php
@/home/user/hermesag/004/tickettrade/config/rate_limits.php
@/home/user/hermesag/004/tickettrade/config/error_codes.php
@/home/user/hermesag/004/tickettrade/config/routes.php
@/home/user/hermesag/004/tickettrade/config/contexts.php
@/home/user/hermesag/004/tickettrade/migrate.php
@/home/user/hermesag/004/tickettrade/phpunit.xml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Substrate tracer — migrations + uploads config + ImageUpload + ImageProxy + Listing/Category Models+Services + rate-limit registration, end-to-end</name>
  <files>
    migrations/008_listings.sql
    migrations/009_categories.sql
    migrations/010_listing_revisions.sql
    config/uploads.php
    config/rate_limits.php
    config/error_codes.php
    config/contexts.php
    config/routes.php
    public/uploads/.htaccess
    public/uploads/listings/.gitkeep
    public/img/.htaccess
    src/Support/ImageUpload.php
    src/Support/ImageProxy.php
    src/Listing/Model/listing_model.php
    src/Listing/Model/listing_image_model.php
    src/Listing/Model/listing_revision_model.php
    src/Listing/Service/listing_service.php
    src/Category/Model/category_model.php
    src/Category/Service/category_service.php
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/AGENTS.md
    - /home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md (AD-1 layered monolith; AD-2 bounded contexts `Listing`, `Category`; AD-5 PDO with utf8mb4, EMULATE_PREPARES=false; AD-6 migrations runner; AD-13 rate-limit + session; AD-14 image pipeline 4-layer + ImageProxy auth rules; AD-15 phase gate for tickets; AD-16 failure envelope; AD-17 php -S + migrate.php + phpcs)
    - /home/user/hermesag/004/tickettrade/.planning/phases/03-marketplace-listings-discovery/03-CONTEXT.md (D-09..D-14 image pipeline specifics: 8-image cap, WebP-only output, SHA256 storage, 4-layer validation; D-11..D-14 4-layer order; D-31/D-32 7-category seed; D-09 20/hr/user listing_create rate limit; D-30 5/min/IP admin_cron rate limit)
    - /home/user/hermesag/004/tickettrade/src/Support/Db.php (singleton PDO; use `Support\Db::pdo()` from every Model)
    - /home/user/hermesag/004/tickettrade/src/Support/RateLimit.php (`hit($name, $key): array{allowed:bool, count:int, retry_after:int}`; uses `cache_rate` table)
    - /home/user/hermesag/004/tickettrade/src/Support/Auth.php (`currentUser(): array|null`, `requireAuth(): array`; sets `$GLOBALS['current_user']`)
    - /home/user/hermesag/004/tickettrade/src/Support/Error.php (`validation(array $fields): array`, `notFound(): array` — failure envelope helpers)
    - /home/user/hermesag/004/tickettrade/src/Auth/Service/auth_service.php (sole bcrypt writer AD-18; `sanitizeUser($row): array` to strip `password_hash`)
    - /home/user/hermesag/004/tickettrade/src/User/Service/user_service.php (sole writer pattern; read `findById` to copy style)
    - /home/user/hermesag/004/tickettrade/config/rate_limits.php (existing named limits `login`, `register`, `forgot_password`, `profile_edit`; ADD `listing_create` 20/hr/user and `admin_cron` 5/min/IP)
    - /home/user/hermesag/004/tickettrade/config/error_codes.php (existing codes; ADD `E_IMAGE_INVALID`, `E_IMAGE_TOO_LARGE`, `E_LISTING_NOT_FOUND`, `E_LISTING_FORBIDDEN`, `E_CATEGORY_NOT_FOUND`, `E_RATE_LIMIT`)
    - /home/user/hermesag/004/tickettrade/config/contexts.php (already lists `Listing` and `Category`; no changes)
    - /home/user/hermesag/004/tickettrade/config/routes.php (Phase 2 routes; ADD `GET /img/{listing_id}/{size}` for ImageProxy)
    - /home/user/hermesag/004/tickettrade/migrate.php (the `migrations/.applied` set runner; new files pick up automatically)
    - /home/user/hermesag/004/tickettrade/migrations/005_password_resets.sql (recent example of an `if not exists` migration — copy the D-22..D-28 header + transaction structure verbatim)
    - /home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-0[1-3]-PLAN.md (the established Service+Model pattern: Model = raw PDO via `Support\Db::pdo()`; Service = validation + multi-row writes + sole-writer rule; always return `['ok'=>bool, 'data'=>mixed, 'error'=>[...]]` envelope)
  </read_first>
  <behavior>
    - Calling `Support\Db::pdo()` from any Model/Service returns a singleton `PDO` with `ERRMODE_EXCEPTION`, `EMULATE_PREPARES=false`, `DEFAULT_FETCH_MODE=FETCH_ASSOC`, charset `utf8mb4`. The PDO instance is the same one Phase 2's Models use.
    - Running `php migrate.php` against the existing 7-table database creates 4 new tables: `categories` (8 cols incl. `is_active`, `sort_order` UNIQUE), `listings` (status ENUM, type ENUM, condition ENUM, price_cents BIGINT, quantity/quantity_sold INT, review_flag TINYINT, approved_at/approved_by, rejection_reason, FULLTEXT index on (title, description)), `listing_images` (FK to listings ON DELETE CASCADE, sha256 CHAR(64), size ENUM('thumb','medium','full'), is_primary TINYINT, sort_order INT), `listing_revisions` (FK to listings ON DELETE CASCADE, snapshot_json JSON, created_by BIGINT FK to users). The 7-row categories seed inserts in the same transaction.
    - Re-running `php migrate.php` is a no-op (zero DDL executed); the `migrations/.applied` file lists 10 filenames in lex order.
    - `Listing\Service\listing_service::createDraft($sellerId, ['title'=>'Calculus 101','description'=>'Barely used','price_cents'=>150000,'category_id'=>1,'type'=>'product','quantity'=>1])` returns `['ok'=>true, 'data'=>['listing_id'=>N, 'status'=>'draft', 'created_at'=>...], 'error'=>null]`. The row's `status='draft'`, `quantity_sold=0`, `created_at` and `updated_at` are populated in Asia/Colombo.
    - `Listing\Service\listing_service::createDraft` with `title=''` returns `['ok'=>false, 'data'=>null, 'error'=>['code'=>'E_VALIDATION', 'message'=>'Title is required', 'fields'=>['title'=>'Title is required']]]`. Same for `price_cents=0`, `quantity=0`, `category_id=999` (non-existent), `category_id` of a soft-deleted category (`is_active=FALSE`), `type='invalid'`.
    - `Support\ImageUpload::process($listingId, [$fakeJpgFile])` runs the 4-layer pipeline: (1) `finfo_file` confirms MIME `image/jpeg|png|webp`; (2) `getimagesize` returns dimensions `<=4000px` and `filesize()<=5MB`; (3) `fread` magic bytes match JPEG (`FF D8 FF`), PNG (`89 50 4E 47`), or WebP (`52 49 46 46 ... 57 45 42 50`); (4) `imagecreatefromstring` + `imagewebp($gd, $path, 80)` re-encodes to WebP. Three thumbnails (200/600/1200 px) are written to `<storage_root>/<sha256>.webp` (one file, used at all sizes via `imagescale()` or `imagecopyresampled` — implementation may use one WebP per size or one file + resize-on-serve; the simpler pattern is three files at upload time per D-13).
    - An image with extension `.jpg` but PHP payload `<?php system($_GET['c']);` is rejected at the magic-byte check (or GD `imagecreatefromstring` returns false), `E_IMAGE_INVALID` is raised, the function returns the error and zero files are written.
    - `Support\ImageProxy::serve($listingId, 'thumb')` reads the SHA256 file by listing_id, emits `Content-Type: image/webp` with the WebP bytes, and `Cache-Control: public, max-age=86400`; the call is rate-limited at 60/min/IP via `Support\RateLimit::hit('img_thumb', $ip)`.
    - `Support\ImageProxy::serve($listingId, 'full')` for an unauthenticated request returns 404 (not 403, per AD-14); for a logged-in user who is NOT the seller AND NOT a ticket holder AND NOT an admin, also returns 404. For the seller, ticket holder, or admin, returns the 1200px WebP bytes.
    - `Support\ImageProxy::serve` accepts `size` in `{thumb, medium, full}`; any other value returns 404.
    - `Listing\Model\listing_model::search('calculus', 1, 0, 50)` returns rows where `MATCH(title, description) AGAINST('calculus*' IN BOOLEAN MODE)` matches, filtered by `category_id=1` and `status='active'`, ordered `created_at DESC`, joined to `categories.name`. Default `limit=50` (PER-02).
  </behavior>
  <action>
    1. **Migrations** — Create `migrations/008_listings.sql`, `migrations/009_categories.sql`, `migrations/010_listing_revisions.sql`. Each file starts with a D-22..D-28-style header comment block. The transaction wrapper is `START TRANSACTION; ... COMMIT;` at file scope. Use `CREATE TABLE IF NOT EXISTS` for idempotency. All `FOREIGN KEY` constraints use `ON DELETE RESTRICT` for `listings.category_id` (per CONTEXT D-32 — soft-deletable via `is_active`, no hard delete) and `ON DELETE CASCADE` for `listing_images.listing_id` and `listing_revisions.listing_id`. Indexes: `FULLTEXT KEY ft_title_desc (title, description)` on `listings`; `KEY idx_listing_seller_status (seller_id, status)`; `KEY idx_listing_category_status (category_id, status, created_at)`; `KEY idx_category_active_sort (is_active, sort_order)`.
    2. **Migration 008 `listings` schema**: `id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY`, `seller_id BIGINT UNSIGNED NOT NULL FK users(id) ON DELETE RESTRICT`, `category_id BIGINT UNSIGNED NOT NULL FK categories(id) ON DELETE RESTRICT`, `title VARCHAR(80) NOT NULL`, `description TEXT NOT NULL` (2000-char enforced at Service), `price_cents BIGINT NOT NULL` (sign-checked at Service), `type ENUM('product','service') NOT NULL`, `condition ENUM('new','like_new','good','fair') NULL` (products only), `duration_minutes INT NULL` (services only, 1..600), `delivery_method ENUM('in_person','online','hybrid') NULL` (services only), `availability VARCHAR(500) NULL` (services only), `quantity INT NOT NULL DEFAULT 1`, `quantity_sold INT NOT NULL DEFAULT 0`, `status ENUM('draft','pending','active','rejected','sold','removed') NOT NULL DEFAULT 'draft'`, `review_flag TINYINT(1) NOT NULL DEFAULT 0`, `review_flag_at DATETIME NULL`, `rejection_reason TEXT NULL`, `approved_at DATETIME NULL`, `approved_by BIGINT UNSIGNED NULL FK users(id) ON DELETE SET NULL`, `created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP`, `updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`. Charset `utf8mb4`, engine `InnoDB`.
    3. **Migration 008 `listing_images` schema**: `id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY`, `listing_id BIGINT UNSIGNED NOT NULL FK listings(id) ON DELETE CASCADE`, `sha256 CHAR(64) NOT NULL` (hex of original bytes), `size ENUM('thumb','medium','full') NOT NULL`, `is_primary TINYINT(1) NOT NULL DEFAULT 0`, `sort_order INT NOT NULL DEFAULT 0`, `created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP`, `KEY idx_listing (listing_id, sort_order)`.
    4. **Migration 009 `categories`**: `id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY`, `name VARCHAR(60) NOT NULL UNIQUE`, `description VARCHAR(200) NULL`, `sort_order INT NOT NULL UNIQUE`, `is_active TINYINT(1) NOT NULL DEFAULT 1`, `created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP`. INSERT 7 seed rows in the same transaction: Textbooks (sort 1, desc "Course books, reference material, notes"), Electronics (2, "Phones, laptops, accessories, gadgets"), Fashion (3, "Clothing, shoes, accessories"), Services (4, "Tutoring, design, freelance help"), Food (5, "Homemade, snacks, baked goods"), Events (6, "Tickets, group buys, event services"), Other (7, "Anything else campus-trade").
    5. **Migration 010 `listing_revisions`**: `id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY`, `listing_id BIGINT UNSIGNED NOT NULL FK listings(id) ON DELETE CASCADE`, `snapshot_json JSON NOT NULL`, `created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP`, `created_by BIGINT UNSIGNED NOT NULL FK users(id) ON DELETE RESTRICT`, `KEY idx_listing_created (listing_id, created_at)`.
    6. **Config — `config/uploads.php` (new)**: returns `['storage_root' => __DIR__ . '/../public/uploads/listings', 'thumb_px' => 200, 'medium_px' => 600, 'full_px' => 1200, 'webp_quality' => 80, 'max_files' => 8, 'max_file_bytes' => 5_242_880, 'max_dim_px' => 4000]` in dev. Read the `APP_ENV` env var: if `prod`, return `['storage_root' => '/var/www/uploads/listings', ...]`. Same shape, different root.
    7. **Config — `config/rate_limits.php` (modify)**: ADD two named limit entries to the existing array — `'listing_create' => ['window_seconds' => 3600, 'max_calls' => 20]` and `'admin_cron' => ['window_seconds' => 60, 'max_calls' => 5]`. ALSO add `'img_thumb' => ['window_seconds' => 60, 'max_calls' => 60]` and `'img_full' => ['window_seconds' => 60, 'max_calls' => 30]` for ImageProxy. The existing keys (`login`, `register`, etc.) stay unchanged.
    8. **Config — `config/error_codes.php` (modify)**: ADD these codes to the existing map: `E_IMAGE_INVALID => 'Image is invalid or failed validation'`, `E_IMAGE_TOO_LARGE => 'Image exceeds the 5MB size limit'`, `E_LISTING_NOT_FOUND => 'Listing not found'`, `E_LISTING_FORBIDDEN => 'You do not have permission to modify this listing'`, `E_CATEGORY_NOT_FOUND => 'Category not found'`, `E_RATE_LIMIT => 'Too many requests. Try again later'`. Keep existing codes unchanged.
    9. **Config — `config/routes.php` (modify)**: ADD one route entry: `'GET /img/{listing_id}/{size}' => ['App\Support\Action\ImageProxyAction', 'handle', ['auth' => false, 'admin' => false, 'csrf' => false, 'rate_limit' => null]]`. The `ImageProxyAction` is the same class as the new `Support\ImageProxy`; the Action's `handle()` is a thin wrapper that calls `Support\ImageProxy::serve($listing_id, $size)`. The Auth guard runs but `auth:false` allows guest access for `thumb`/`medium`.
    10. **Filesystem — `public/uploads/.htaccess` (new)**: single line `Deny from all`. Neutralizes accidental direct reads (the proxy is the canonical read path per D-14).
    11. **Filesystem — `public/uploads/listings/.gitkeep` (new)**: empty file so git tracks the directory.
    12. **Filesystem — `public/img/.htaccess` (new)**: `RewriteEngine On` + `RewriteRule ^(.*)$ router.php?img_route=$1 [L,QSA]` — routes all `/img/*` through the dev-server router. (Or use the existing `public/router.php` pattern from Phase 1 — read the existing file before adding this.)
    13. **Support — `src/Support/ImageUpload.php` (new)**: namespace `App\Support`; class `ImageUpload`. Public static method `process(int $listingId, array $files): array` — iterates files (PHP `$_FILES['images']` shape: `name[]`, `tmp_name[]`, `size[]`, `error[]`, `type[]`); normalizes to a list of file descriptors; for each, runs the 4-layer pipeline; returns `['ok' => true, 'data' => ['uploaded' => [['sha256' => '...', 'size' => 200], ...], 'errors' => []], 'error' => null]`. Use `finfo_file($tmp, FILEINFO_MIME_TYPE)` for layer 1; `getimagesize($tmp)` for layer 2 (checks `0 < $size[0] <= 4000 && 0 < $size[1] <= 4000`); `fread(fopen($tmp,'rb'), 12)` magic-byte check for layer 3; `imagecreatefromstring(file_get_contents($tmp))` + `imagewebp` for layer 4. Compute SHA256 of original bytes (BEFORE re-encoding) for filename + integrity. Three thumbnails written at upload time: copy via `imagecopyresampled` to a 200/600/1200-px canvas, then `imagewebp($gd, $path, 80)`. All three files saved to `<storage_root>/<sha256>_<size>.webp` (e.g., `abc123..._thumb.webp`, `abc123..._medium.webp`, `abc123..._full.webp`). Max 8 files per listing (D-09) — files beyond the 8th are skipped with `E_IMAGE_INVALID` per-file. Throw no exceptions; return error envelope.
    14. **Support — `src/Support/ImageProxy.php` (new)**: namespace `App\Support`; class `ImageProxy`. Public static method `serve(int $listingId, string $size): void` — looks up `listing_images` row for the `(listing_id, size)` pair; reads the WebP file from `<storage_root>/<sha256>_<size>.webp`; emits headers `Content-Type: image/webp`, `Cache-Control: public, max-age=86400`, `Content-Length: <size>`; emits the bytes via `readfile()`. Auth check: if `size==='full'`, require `$_SESSION['user_id']` AND (user is seller for that listing OR user has an active ticket for that listing OR user is admin); missing → `http_response_code(404); exit;`. Rate limit: `Support\RateLimit::hit('img_thumb', $ip)` for thumb/medium, `'img_full', (string)$userId` for full. Missing rate-limit name in `config/rate_limits.php` → log + allow (defensive default; tests will pin this). Invalid `size` not in `{thumb, medium, full}` → 404. Missing listing or image row → 404.
    15. **Support — `src/Support/Action/ImageProxyAction.php` (new)**: thin Action wrapper. `handle(): void` reads `$_GET['listing_id']` and `$_GET['size']` from the route params (set by Router), validates as int + enum, calls `Support\ImageProxy::serve((int)$listing_id, $size)`. Returns nothing (binary write). The `auth:false` route flag means the Action runs `Support\Auth::boot()` but does NOT redirect on missing session — the proxy's own auth gate handles the per-size check.
    16. **Listing Model — `src/Listing/Model/listing_model.php` (new)**: namespace `App\Listing\Model`; class `ListingModel`. All methods return arrays (no objects). Public methods: `insert(array $data): int` (returns new `id`); `findById(int $id): ?array`; `findBySellerId(int $sellerId, ?string $status, int $limit, int $offset): array`; `search(?string $q, ?int $categoryId, int $offset, int $limit): array` (uses `MATCH(title, description) AGAINST(? IN BOOLEAN MODE)` with `?` = `$q.'*'` if `$q` non-empty; joins `categories.name AS category_name` and the primary image's `sha256` as `primary_sha256`); `setStatus(int $id, string $status, ?string $rejectionReason=null, ?int $approvedBy=null): int` (rows affected); `incrementSold(int $id, int $n): int`; `decrementSold(int $id, int $n): int`; `setReviewFlag(int $id, bool $on, ?DateTimeInterface $at=null): int`; `getSearchCount(?string $q, ?int $categoryId): int`. All queries use `Support\Db::pdo()->prepare(...)`; never concatenate.
    17. **Listing Image Model — `src/Listing/Model/listing_image_model.php` (new)**: namespace `App\Listing\Model`; class `ListingImageModel`. Methods: `insert(int $listingId, string $sha256, string $size, bool $isPrimary, int $sortOrder): int`; `findByListingId(int $listingId): array` (returns rows ordered by `sort_order, id`); `findOne(int $listingId, string $size): ?array`; `deleteByListingId(int $listingId): int`; `updateSortOrder(array $idsInNewOrder): int` (UPDATE by id with new sort_order); `countByListingId(int $listingId): int`. Note: image rows are inserted by `Support\ImageUpload::process` via the Service, not directly by the Model.
    18. **Listing Revision Model — `src/Listing/Model/listing_revision_model.php` (new)**: namespace `App\Listing\Model`; class `ListingRevisionModel`. Methods: `insert(int $listingId, string $snapshotJson, int $createdBy): int`; `findByListingId(int $listingId): array` (ordered `created_at DESC`).
    19. **Listing Service — `src/Listing/Service/listing_service.php` (new)**: namespace `App\Listing\Service`; class `ListingService`. SOLE WRITER to `listings`, `listing_images`, `listing_revisions` (AD-1). All public methods return `['ok'=>bool, 'data'=>mixed, 'error'=>['code'=>string, 'message'=>string, 'fields'=>array|null]]` (AD-16 failure envelope). Public methods: `createDraft(int $sellerId, array $data): array`; `saveDraft(int $listingId, int $sellerId, array $data): array`; `submitDraft(int $listingId, int $sellerId): array` (draft→pending); `relist(int $soldListingId, int $sellerId): array` (sold→new draft, copies fields except `quantity_sold` reset to 0); `setReviewFlag(int $listingId, int $sellerId): array` (auto-set on edit-to-active per D-09); `uploadImages(int $listingId, int $sellerId, array $files): array`; `validateListingData(array $data): array` (returns `['ok'=>true, 'data'=>$sanitized, 'error'=>null]` or validation error with `fields`); `getWithImages(int $id): ?array` (joins listing + images + category name); `getSearchResults(?string $q, ?int $categoryId, int $page=1): array` (returns `['rows'=>[...], 'total'=>N, 'page'=>$page, 'pages'=>ceil($total/50)]`); `getSellerListings(int $sellerId, ?string $status, int $page=1): array`. Internal helper `enforceRateLimit(int $userId): array` calls `Support\RateLimit::hit('listing_create', (string)$userId)`. Internal helper `loadForEdit(int $listingId, int $sellerId): array` returns listing only if seller matches AND status IN (draft, pending, active, rejected). All DB writes inside transactions via `Support\Db::pdo()->beginTransaction()` / `commit()` / `rollBack()`.
    20. **Category Model — `src/Category/Model/category_model.php` (new)**: namespace `App\Category\Model`; class `CategoryModel`. Methods: `findAllActive(): array` (ordered `sort_order ASC`); `findById(int $id): ?array`; `insert(string $name, int $sortOrder, ?string $description=null): int`; `setActive(int $id, bool $active): int`. The Model never filters `is_active` except in `findAllActive`; `findById` returns inactive rows too (Service decides).
    21. **Category Service — `src/Category/Service/category_service.php` (new)**: namespace `App\Category\Service`; class `CategoryService`. SOLE WRITER to `categories` (admin CRUD is Phase 8; in Phase 3 the Service is read-only). Public methods: `listActive(): array` (returns `['ok'=>true, 'data'=>[['id'=>1, 'name'=>'Textbooks', ...], ...], 'error'=>null]`); `getById(int $id): array` (returns 404 envelope if not found OR `is_active=FALSE`).
    22. **View partials — `src/Support/View/partials/listing_card.php` and `listing_card_cork.php` (new)**: thin PHP partials. `listing_card` is the plain-grid Bootstrap card (used in list-view toggle and My Listings). `listing_card_cork` is the corkboard variant: paper surface (`background: #FFF8E7`), ±2° rotation seeded by `crc32($listing['id']) % 5 - 2`, pushpin graphic alternating red/blue by `$listing['id'] % 2`, `aria-hidden` on rotation/pin decoration. Both render thumbnail via `<img src="/img/{$listing['id']}/thumb" loading="lazy">`. Both use Bootstrap classes already shipped in Phase 1. NO business logic; the controller passes a `$listing` array with the 8+ keys the partial needs (`id, title, price_cents, primary_sha256, seller_nickname, seller_tier, seller_is_verified, status, review_flag, quantity, quantity_sold, created_at`).
    23. **Run `php migrate.php`** to apply migrations 008/009/010 against the test database. Verify the 4 tables exist with the expected schema (column names, types, indexes). Re-run to confirm idempotency.
    24. **Run the existing PHPUnit suite** to confirm no Phase 2 test broke. Add the new tests in Task 3 below.
  </action>
  <verify>
    <automated>cd /home/user/hermesag/004/tickettrade && APP_ENV=test php migrate.php && echo "MIGRATE_OK" && APP_ENV=test php migrate.php && echo "MIGRATE_IDEMPOTENT_OK" && APP_ENV=test php -r 'require "vendor/autoload.php"; require "config/db.php"; $pdo = (new PDO($GLOBALS["dsn"] ?? "mysql:host=127.0.0.1;port=3306;dbname=tickettrade_test;charset=utf8mb4", $GLOBALS["db_user"] ?? "root", $GLOBALS["db_pass"] ?? "")); $tables = $pdo->query("SELECT TABLE_NAME FROM information_schema.TABLES WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME IN ("categories", "listings", "listing_images", "listing_revisions") ORDER BY TABLE_NAME")->fetchAll(PDO::FETCH_COLUMN); $expected = ["categories", "listings", "listing_images", "listing_revisions"]; $diff = array_diff($expected, $tables); if (!empty($diff)) { fwrite(STDERR, "MISSING_TABLES: " . implode(",", $diff) . PHP_EOL); exit(1); } $cats = (int)$pdo->query("SELECT COUNT(*) FROM categories")->fetchColumn(); if ($cats !== 7) { fwrite(STDERR, "CATEGORY_SEED_WRONG: expected 7, got $cats" . PHP_EOL); exit(1); } $ftIdx = $pdo->query("SHOW INDEX FROM listings WHERE Key_name = "ft_title_desc"")->fetchAll(); if (count($ftIdx) < 2) { fwrite(STDERR, "FULLTEXT_INDEX_MISSING" . PHP_EOL); exit(1); } echo "SCHEMA_OK" . PHP_EOL;'</automated>
    <fails_when>non-zero exit, or any of MIGRATE_OK, MIGRATE_IDEMPOTENT_OK, SCHEMA_OK is missing from stdout</fails_when>
  </verify>
  <done>
    - 4 migrations (008/009/010) run idempotently; the test database has `categories` (7 rows seeded), `listings`, `listing_images`, `listing_revisions` tables.
    - `config/uploads.php`, `config/rate_limits.php` (4 new named limits), `config/error_codes.php` (6 new codes), `config/routes.php` (1 new route) all updated.
    - `public/uploads/.htaccess`, `public/uploads/listings/.gitkeep`, `public/img/.htaccess` exist.
    - `src/Support/ImageUpload.php` and `src/Support/ImageProxy.php` exist; `ImageProxyAction` is a thin wrapper.
    - `src/Listing/Model/{listing,listing_image,listing_revision}_model.php` and `src/Listing/Service/listing_service.php` exist; `src/Category/Model/category_model.php` and `src/Category/Service/category_service.php` exist.
    - 2 view partials (`listing_card`, `listing_card_cork`) exist.
    - All Phase 2 tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Real validation, error states, and rate-limit enforcement</name>
  <files>
    src/Listing/Service/listing_service.php
    src/Support/ImageUpload.php
    src/Support/ImageProxy.php
    src/Support/Error.php
    config/error_codes.php
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/src/Support/Error.php (existing `validation(array $fields): array` and `notFound(): array` envelope helpers; copy this style)
    - /home/user/hermesag/004/tickettrade/src/Support/RateLimit.php (`hit($name, $key): array{allowed:bool, count:int, retry_after:int}`; the Service's `enforceRateLimit` returns this envelope mapped to E_RATE_LIMIT)
    - /home/user/hermesag/004/tickettrade/.planning/phases/03-marketplace-listings-discovery/03-CONTEXT.md (D-09 8-image cap, 4-layer pipeline; D-11 storage path SHA256; D-12 dev/prod storage swap; D-13 three-thumbnail sizes; D-14 ImageProxy auth rules; D-31 7 categories)
    - /home/user/hermesag/004/tickettrade/src/Listing/Service/listing_service.php (the class from Task 1 — `createDraft` signature, `validateListingData` helper, failure-envelope return shape)
  </read_first>
  <behavior>
    - Calling `ListingService::createDraft($sellerId, $data)` 21 times in a row within 60 minutes: calls 1-20 return `['ok'=>true, 'data'=>..., 'error'=>null]`; call 21 returns `['ok'=>false, 'data'=>null, 'error'=>['code'=>'E_RATE_LIMIT', 'message'=>'Too many listings. Try again later', 'fields'=>null]]`.
    - `ListingService::uploadImages($listingId, $sellerId, $files)` with 9 files: the first 8 process normally; the 9th is dropped with a per-file `E_IMAGE_INVALID` entry in `data.errors[]` and the listing still has 8 image rows.
    - `Support\ImageUpload::process` with a file whose `$_FILES['error'] === UPLOAD_ERR_INI_SIZE`: that file is rejected with `E_IMAGE_TOO_LARGE`, no rows inserted.
    - `Support\ImageUpload::process` with a file whose first 4 bytes are `\xFF\xD8\xFF\xE0` (real JPEG) but `getimagesize` reports `0x0`: rejected at layer 2 with `E_IMAGE_INVALID`.
    - `Support\ImageProxy::serve` called 61 times in 60 seconds from the same IP for `thumb`: calls 1-60 return the WebP bytes; call 61 returns 429 with `Retry-After: <seconds>` header and a plain-text body.
  </behavior>
  <action>
    - Extend `Listing\Service\listing_service::createDraft` and `saveDraft` to call `enforceRateLimit($sellerId)` BEFORE any DB write. On `['allowed'=>false]`, return `['ok'=>false, 'data'=>null, 'error'=>['code'=>'E_RATE_LIMIT', 'message'=>$error_codes['E_RATE_LIMIT'], 'fields'=>null]]`.
    - Extend `Listing\Service\listing_service::uploadImages` to enforce the 8-file cap from D-09: count existing `listing_images` rows for the listing; if `count($files) + $existing > 8`, reject the excess files with per-file `E_IMAGE_INVALID` entries; the rest proceed.
    - Extend `Support\ImageUpload::process` to handle all `$_FILES['error']` codes: `UPLOAD_ERR_OK` proceeds; `UPLOAD_ERR_INI_SIZE`/`UPLOAD_ERR_FORM_SIZE` → `E_IMAGE_TOO_LARGE`; `UPLOAD_ERR_NO_FILE` is silently skipped (no error, no row); `UPLOAD_ERR_*` other → `E_IMAGE_INVALID`.
    - Extend `Support\ImageUpload::process` layer 1 to reject non-image MIME types from `finfo`: `image/jpeg`, `image/png`, `image/webp` are the only accepted MIMEs; anything else (e.g., `text/plain`, `application/x-php`) → `E_IMAGE_INVALID`.
    - Extend `Support\ImageProxy::serve` to emit `http_response_code(429)` + `Retry-After: <retry_after>` + `Content-Type: text/plain; charset=utf-8` + body `Rate limit exceeded` when `Support\RateLimit::hit` returns `['allowed'=>false]`. (Per D-13, image rate limit is 60/min/IP; the proxy consults the rate limit BEFORE reading the file.)
    - Add `E_IMAGE_INVALID` and `E_IMAGE_TOO_LARGE` to `config/error_codes.php` (if not already present from Task 1).
  </action>
  <verify>
    <automated>cd /home/user/hermesag/004/tickettrade && APP_ENV=test vendor/bin/phpunit --testsuite=phase-3 --filter='RateLimit|EnforceValidation|ImageUpload' 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary, or "FAILURES" in the output</fails_when>
  </verify>
  <done>
    - Rate limit returns `E_RATE_LIMIT` envelope after 20 listings in 60 minutes.
    - 8-image cap enforced at both client (UI) and Service layers; the 9th file is dropped with a per-file error.
    - `$_FILES['error']` codes are mapped to specific error codes; non-image MIME is rejected at layer 1; oversized files are rejected at layer 2.
    - ImageProxy returns 429 with `Retry-After` when the rate limit is exceeded.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tests — Integration for Service+Model+Migration, Unit for ImageUpload+ImageProxy</name>
  <files>
    tests/Integration/Phase03/Listing/ListingServiceTest.php
    tests/Integration/Phase03/Listing/ListingModelTest.php
    tests/Integration/Phase03/Category/CategoryServiceTest.php
    tests/Integration/Phase03/MigrationTest.php
    tests/Unit/Phase03/Support/ImageUploadTest.php
    tests/Unit/Phase03/Support/ImageProxyTest.php
    phpunit.xml
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-0[1-3]-PLAN.md (test patterns: transaction-rolled-back Integration, `setUp` seeds users, `tearDown` rolls back; PHPUnit `<server>` sets `APP_ENV=test`; PSR-12 namespacing under `tests/Integration/Phase02/...` and `tests/Unit/Phase02/...`)
    - /home/user/hermesag/004/tickettrade/tests/Integration/Phase02/Support/AuthGuardTest.php (representative Integration test — copy the bootstrap pattern: require autoload, read `config/db.php`, create a user in `setUp`, begin transaction in each test, roll back in `tearDown`)
    - /home/user/hermesag/004/tickettrade/phpunit.xml (existing testsuites; ADD `<testsuite name="phase-3-integration">` and `<testsuite name="phase-3-unit">` entries pointing at the new test dirs)
    - /home/user/hermesag/004/tickettrade/src/Listing/Service/listing_service.php (the class to test)
    - /home/user/hermesag/004/tickettrade/src/Support/ImageUpload.php (the class to test; needs real image bytes for the happy-path test)
  </read_first>
  <behavior>
    - `tests/Integration/Phase03/Listing/ListingServiceTest::testCreateDraftHappyPath`: seeds a user and a category, calls `ListingService::createDraft($userId, $validData)`, asserts `ok===true`, the row exists in `listings` with `status='draft'`, `quantity_sold=0`, and the returned `data.listing_id` matches `lastInsertId()`.
    - `tests/Integration/Phase03/Listing/ListingServiceTest::testCreateDraftValidation`: 5+ invalid inputs (empty title, oversized title 81 chars, negative price_cents, quantity 0, category_id 999, type 'invalid') all return `ok===false` with `error.code='E_VALIDATION'` and the `fields` map contains the offending key.
    - `tests/Integration/Phase03/Listing/ListingServiceTest::testRateLimitEnforced`: 21 calls in a row; calls 1-20 succeed, call 21 returns `E_RATE_LIMIT`.
    - `tests/Integration/Phase03/Listing/ListingServiceTest::testUploadImagesEightFileCap`: 9 files passed; only 8 rows in `listing_images`; the 9th is in `data.errors` with code `E_IMAGE_INVALID`.
    - `tests/Integration/Phase03/Category/CategoryServiceTest::testListActiveReturnsSevenSeeded`: asserts 7 rows with the expected names in order.
    - `tests/Integration/Phase03/MigrationTest::testAllMigrationsIdempotent`: runs `php migrate.php` against a fresh schema, asserts 10 applied migrations; re-runs, asserts 0 additional rows in `migrations/.applied` and zero DDL statements.
    - `tests/Unit/Phase03/Support/ImageUploadTest::testAcceptsRealJpeg`: writes a real 100x100 JPEG to `sys_get_temp_dir()`, passes it as `['tmp_name'=>$path, 'name'=>'test.jpg', 'size'=>filesize($path), 'error'=>UPLOAD_ERR_OK, 'type'=>'image/jpeg']`, asserts the result has `ok===true`, `data.uploaded[0].sha256` is a 64-char hex, and three files exist at the storage root with names matching `<sha256>_(thumb|medium|full).webp`.
    - `tests/Unit/Phase03/Support/ImageUploadTest::testRejectsPhpPayloadAsJpg`: writes a file with `.jpg` extension and content `<?php system($_GET[c]); ?>`; asserts `ok===true, data.uploaded=[]` and `data.errors[0].code='E_IMAGE_INVALID'`.
    - `tests/Unit/Phase03/Support/ImageProxyTest::testFullSizeRequiresAuth`: calls `ImageProxy::serve($listingId, 'full')` with no session; asserts `http_response_code()===404`. (Use `xdebug_get_headers()` or `headers_list()` if available; otherwise assert via `expectExceptionMessage` or output buffering.)
  </behavior>
  <action>
    - Create the 6 test files at the paths listed. Follow the Phase 2 test pattern: each Integration test class has a `setUp()` that opens a transaction, seeds a user + category, and a `tearDown()` that calls `Support\Db::pdo()->rollBack()`. Each Unit test is self-contained (no DB).
    - Add `<testsuite name="phase-3-integration"><directory>tests/Integration/Phase03</directory></testsuite>` and `<testsuite name="phase-3-unit"><directory>tests/Unit/Phase03</directory></testsuite>` to `phpunit.xml` inside the `<testsuites>` block.
    - Run `vendor/bin/phpunit --testsuite=phase-3-integration` and `vendor/bin/phpunit --testsuite=phase-3-unit`; iterate until green.
    - Run the full `vendor/bin/phpunit` to confirm no Phase 2 test broke.
  </action>
  <verify>
    <automated>cd /home/user/hermesag/004/tickettrade && APP_ENV=test vendor/bin/phpunit --testsuite=phase-3 2>&1 | tail -10</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary, or any "FAILURES"/"ERRORS" in the output</fails_when>
  </verify>
  <done>
    - All 6 new test files exist and pass; the `phase-3-integration` and `phase-3-unit` testsuites are registered in `phpunit.xml`.
    - The full `vendor/bin/phpunit` run is green; Phase 2 tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /listings/create | Untrusted seller input (title, description, price, quantity, type, category) crosses here. Validated Service-side. |
| Browser → POST /listings/{id}/images | Untrusted image file bytes cross here. Validated via 4-layer pipeline. |
| Browser → GET /img/{id}/{size} | Untrusted listing_id + size cross here. Auth-gated for `full` size. Rate-limited per IP. |
| Service → Support\ImageUpload | Internal call; bytes flow from `$_FILES['tmp_name']` to disk via `imagewebp`. |
| Service → Support\Db | Trusted code path; all writes via prepared statements. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `listings` table | high | mitigate | All writes via `Listing\Service\listing_service` (AD-1 sole writer). `price_cents` sign-checked at Service. No raw SQL from Actions. |
| T-03-02 | Tampering | `listing_images` | high | mitigate | `Support\ImageUpload` re-encodes all uploads to WebP (D-11). Original bytes never reach the disk; SHA256 is computed BEFORE re-encoding for filename only. |
| T-03-03 | Information Disclosure | `Support\ImageProxy::serve` | high | mitigate | `full` size (1200px) requires session + (seller OR ticket holder OR admin); missing → 404 (not 403, per AD-14). `thumb`/`medium` are public but rate-limited at 60/min/IP. |
| T-03-04 | Denial of Service | `Support\ImageUpload` | medium | mitigate | Max 8 images/listing (D-09). Max 5MB per file. Max 4000px per dim. Rate limit on listing creation: 20/hr/user. |
| T-03-05 | Denial of Service | `Support\ImageProxy` | medium | mitigate | Rate limit: thumb 60/min/IP, medium 60/min/IP, full 30/min/user. 429 + Retry-After on excess. |
| T-03-06 | Repudiation | `listing_revisions` | medium | mitigate | Every edit to an `active` listing writes a snapshot row before the change (per D-09). `created_by` is the actor. Hash-chained audit log wires in Phase 4 (AD-12). |
| T-03-07 | Elevation of Privilege | `ListingService` | high | mitigate | `loadForEdit` enforces `seller_id = CURRENT_USER`; cross-seller edits return `E_LISTING_FORBIDDEN`. `setStatus` and `setReviewFlag` accept `$sellerId` and check ownership. |
| T-03-08 | Spoofing | Image MIME | high | mitigate | 4-layer pipeline: finfo MIME → getimagesize → magic bytes → GD re-encode. A file claiming `image/jpeg` with PHP payload is rejected at layer 3 (magic bytes) or layer 4 (GD decode). |
| T-03-09 | Information Disclosure | `/img/*` direct path | medium | mitigate | `public/uploads/.htaccess` denies direct access. `public/img/.htaccess` routes through `router.php`. Webroot is `public/`; `/var/www/uploads/listings/` in prod is outside webroot. |
| T-03-10 | Tampering | Category seed | low | mitigate | 7 categories inserted in the same transaction as the `categories` table creation. `sort_order` UNIQUE prevents duplicates. `is_active` flag for soft-delete (admin Phase 8). |
| T-03-SC | Tampering | Composer installs | high | mitigate | No new Composer packages in this plan (AD-7 / NFR-OPS-007: only `ramsey/uuid` is added). Package legitimacy gate: N/A. |

## Trust-Boundary Coverage

- T-03-01, T-03-07: Action → Service → Model data flow; the Action's only inputs are CSRF-checked, rate-limited, and Service-validated.
- T-03-02, T-03-08: `Support\ImageUpload` is the ONLY path to write a listing image; re-encoding to WebP neutralizes polyglot attacks.
- T-03-03, T-03-09: `Support\ImageProxy` is the ONLY path to read a listing image; auth gates prevent unauthorized full-size reads; rate limits prevent enumeration.
- T-03-04, T-03-05: Service-layer + Proxy-layer rate limits prevent resource exhaustion.

## Package Legitimacy Audit

- No new Composer packages. AD-7 + NFR-OPS-007 lock `ramsey/uuid` as the sole runtime dep. Plan installs zero new packages.
- T-03-SC: not applicable (no installs).
</threat_model>

<verification>
- [ ] `php migrate.php` against the test DB creates `categories` (7 rows), `listings`, `listing_images`, `listing_revisions`; re-run is a no-op.
- [ ] `vendor/bin/phpunit --testsuite=phase-3` is green (≥6 test files, ≥20 cases).
- [ ] `vendor/bin/phpunit` (full suite) is green — no Phase 2 test broke.
- [ ] `vendor/bin/phpcs --standard=PSR12 src/Listing src/Category src/Support/ImageUpload.php src/Support/ImageProxy.php` exits 0.
- [ ] Calling `ListingService::createDraft` 21 times in 60 min: 21st call returns `E_RATE_LIMIT`.
- [ ] Uploading 9 images: 8 rows in `listing_images`, 1 entry in `data.errors` with `code='E_IMAGE_INVALID'`.
- [ ] `Support\ImageProxy::serve($id, 'full')` without session returns 404.
</verification>

<success_criteria>
- Phase 3 substrate is operational: a developer can use `ListingService::createDraft`, `uploadImages`, and `ImageProxy::serve` to drive the listings feature.
- All 6 test files pass; the full PHPUnit suite is green.
- All artifacts listed under "Artifacts this phase produces" exist on disk.
- The 4 migrations are idempotent and match the schema in D-09..D-14 + D-31.
- The 4-layer image pipeline rejects a PHP-payload-as-JPEG attack.
- The proxy returns 404 for unauthorized full-size reads.
</success_criteria>

<output>
Create `.planning/phases/03-marketplace-listings-discovery/03-01-SUMMARY.md` when done.
</output>
