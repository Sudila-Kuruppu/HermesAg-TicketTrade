---
phase: 02-student-authentication-profiles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/001_initial.sql
  - migrations/002_users_auth.sql
  - migrations/003_sessions.sql
  - migrations/004_email_verifications.sql
  - migrations/005_password_resets.sql
  - migrations/006_student_id_allowlist.sql
  - migrations/007_cache_rate.sql
  - migrations/.gitignore
  - migrate.php
  - src/Support/Db.php
  - src/Support/Auth.php
  - src/Support/Csrf.php
  - src/Support/RateLimit.php
  - src/Support/Crypto.php
  - src/Support/ResponseHeaders.php
  - src/Support/Error.php
  - src/Support/View.php
  - src/Support/View/layout.php
  - src/Support/View/partials/head.php
  - src/Support/View/partials/bottom_nav.php
  - src/Support/View/partials/toast_container.php
  - src/Support/View/partials/flash_toast.php
  - src/Support/View/partials/skip_link.php
  - src/Support/View/partials/avatar_picker.php
  - src/Support/View/partials/rank_badge.php
  - config/db.php
  - config/.env.example
  - config/auth.php
  - config/security_headers.php
  - config/rate_limits.php
  - config/error_codes.php
  - config/ranks.php
  - config/reserved_nicknames.php
  - config/bootstrap.php
  - config/routes.php
  - public/assets/img/avatars/avatar-1.svg
  - public/assets/img/avatars/avatar-2.svg
  - public/assets/img/avatars/avatar-3.svg
  - public/assets/img/avatars/avatar-4.svg
  - public/assets/img/avatars/avatar-5.svg
  - public/assets/img/avatars/avatar-6.svg
  - public/assets/img/avatars/avatar-7.svg
  - public/assets/img/avatars/avatar-8.svg
  - public/assets/img/avatars/avatar-9.svg
  - public/assets/img/avatars/avatar-10.svg
  - public/assets/img/avatars/avatar-11.svg
  - public/assets/img/avatars/avatar-12.svg
  - public/index.php
  - public/admin/index.php
  - src/Support/Router.php
  - tests/Unit/02/Support/PasswordHashTest.php
  - tests/Unit/02/Support/CsrfTest.php
  - tests/Unit/02/Support/SessionConfigTest.php
  - tests/Unit/02/Support/ComposerTest.php
  - tests/Unit/02/Support/PdoOnlyTest.php
  - tests/Integration/02/Support/MigrateRunnerTest.php
  - tests/Integration/02/Support/AuthGuardTest.php
  - tests/Integration/02/Support/RateLimitTest.php
  - tests/Integration/02/Support/ResponseHeadersTest.php
  - phpunit.xml
autonomous: true
requirements:
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - SEC-01
  - SEC-02
  - SEC-05
  - SEC-07
  - OPS-02
  - OPS-05
  - OPS-07
user_setup:
  - service: mysql
    why: "Migrations runner needs a MySQL 8 database (test + dev). DSN is loaded from config/db.php (gitignored)."
    env_vars:
      - name: DB_DSN
        source: "config/db.php (gitignored); example shape: mysql:host=127.0.0.1;port=3306;dbname=tickettrade;charset=utf8mb4"
      - name: DB_USER
        source: "config/db.php (gitignored); defaults to 'root' in dev"
      - name: DB_PASS
        source: "config/db.php (gitignored); empty in dev"
      - name: APP_ENV
        source: "Optional; when 'test' the runner reads config/db.test.php and PHPUnit sets it via <server> in phpunit.xml"
    dashboard_config: []

estimate:
  tokens: 72000
  raw_tokens: 36000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Running `php migrate.php` against a fresh MySQL 8 database creates all 7 tables (users, student_id_allowlist, email_verifications, password_resets, sessions, points_log, cache_rate) and writes a `migrations/.applied` plain-text file with one filename per line."
    - "Re-running `php migrate.php` is a no-op: no DDL is executed and exit code is 0."
    - "Hitting `GET /profile` (or any private route) without a session cookie returns HTTP 302 to `/login?next=/profile`; hitting `GET /admin/users` as a non-admin returns HTTP 404; hitting `GET /login` returns HTTP 200."
    - "Every response (200, 302, 404) carries the four security headers: `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, and the CSP string from `config/security_headers.php` (with `script-src 'self' cdn.jsdelivr.net 'unsafe-inline'`)."
    - "A POST to any state-changing route without a valid `csrf_token` field returns HTTP 400 with code `E_CSRF`; the token is generated on session start, stored in `$_SESSION['csrf_token']`, and compared with `hash_equals()`."
    - "Calling `password_hash('test', PASSWORD_BCRYPT, ['cost' => 12])` from `Auth/Service/auth_service.php` produces a hash whose `password_get_info()` reports `options.cost === 12`; no file under `src/` outside `Auth/Service/auth_service.php` contains a call to `password_hash(` or `md5(`/`sha1(`/`crypt(` (the AD-18 sole-writer rule)."
    - "Six failed login attempts from the same IP within 5 minutes return the inline error `E_RATE_LIMIT` \"Too many attempts. Try again in 5 minutes.\" for the sixth attempt; the seventh within the window still returns the same inline error."
  artifacts:
    - migrations/001_initial.sql
    - migrations/002_users_auth.sql
    - migrations/003_sessions.sql
    - migrations/004_email_verifications.sql
    - migrations/005_password_resets.sql
    - migrations/006_student_id_allowlist.sql
    - migrations/007_cache_rate.sql
    - migrations/.gitignore
    - migrate.php
    - src/Support/Db.php
    - src/Support/Auth.php
    - src/Support/Csrf.php
    - src/Support/RateLimit.php
    - src/Support/Crypto.php
    - src/Support/ResponseHeaders.php
    - src/Support/Error.php
    - src/Support/View.php
    - src/Support/View/layout.php
    - src/Support/View/partials/{head,bottom_nav,toast_container,flash_toast,skip_link,avatar_picker,rank_badge}.php
    - config/db.php
    - config/.env.example
    - config/auth.php
    - config/security_headers.php
    - config/rate_limits.php
    - config/error_codes.php
    - config/ranks.php
    - config/bootstrap.php
    - config/routes.php
    - public/assets/img/avatars/avatar-{1..12}.svg
    - src/Support/Router.php
    - public/index.php
    - public/admin/index.php
    - tests/Unit/02/Support/PasswordHashTest.php
    - tests/Unit/02/Support/CsrfTest.php
    - tests/Unit/02/Support/SessionConfigTest.php
    - tests/Unit/02/Support/ComposerTest.php
    - tests/Unit/02/Support/PdoOnlyTest.php
    - tests/Integration/02/Support/MigrateRunnerTest.php
    - tests/Integration/02/Support/AuthGuardTest.php
    - tests/Integration/02/Support/RateLimitTest.php
    - tests/Integration/02/Support/ResponseHeadersTest.php
    - phpunit.xml
  key_links:
    - "`migrate.php` reads `config/db.php` (D-25) for the DSN; when `APP_ENV === 'test'` it reads `config/db.test.php`; PHPUnit sets `APP_ENV=test` via `<server>` so the runner targets the test DB."
    - "`config/bootstrap.php` calls `Support\\ResponseHeaders::boot()` BEFORE `Support\\Auth::boot()` (D-13 ordering — response headers must be set before any body); the `eval()` stub block in the current `config/bootstrap.php` is DELETED, the real class is autoloaded via PSR-4."
    - "`Support\\Auth::boot()` reads the `PHPSESSID` cookie, looks up `sessions.session_id`, then `users.user_id` with `is_banned = FALSE`, sets `$GLOBALS['current_user']`, and updates `sessions.last_seen = NOW()` only when `last_seen < NOW() - INTERVAL 5 MINUTE` (D-04 idempotency window)."
    - "`Support\\Csrf::verify()` runs at bootstrap ONLY for `REQUEST_METHOD ∈ {POST, PUT, PATCH, DELETE}`; on failure it writes the 400 `E_CSRF` envelope from `config/error_codes.php` and exits — no Action sees a request without a verified token."
    - "`config/routes.php` route entries are `['METHOD PATH' => [class, method, opts]]` tuples where `opts` is `{auth: bool, admin: bool, csrf: bool, rate_limit: string|null}`; the Router consults `opts.auth` after `Support\\Auth::boot()` and emits `Location: /login?next=...` for the miss."
    - "`Auth/Service/auth_service.php` (lands in Plan 02-02) is the ONLY file that calls `password_hash(` or `password_verify(` per AD-18; the Composer `Custom\\Sniffs\\NoRawHash` sniff is Phase 9 but this plan's `tests/Unit/02/Support/PasswordHashTest.php` greps `src/**/*.php` and asserts the only `password_hash\\s*\\(` match is in `src/Auth/Service/auth_service.php`."
---

<objective>
Land the Phase 2 `Support` substrate (Db, Auth, Csrf, RateLimit, Crypto, ResponseHeaders, Error, View) and the first 7 SQL migrations plus the `migrate.php` runner. The 12 avatar SVGs ship here as static webroot assets. `config/routes.php` is populated with the full Phase 2 student route map (D-11). The eval stub `ResponseHeaders` in `config/bootstrap.php` is replaced with the real PSR-4 class. PHPUnit gains a `phase-2` test suite.

Purpose: every later phase (02-02, 02-03, 3..9) assumes the route guard, the session shape, the CSRF check, the rate-limit helper, the security response headers, the migrations runner, and the bcrypt-only `Auth/Service/auth_service.php` are in place. This plan is the thin end-to-end slice: migrations run against a real MySQL test database, the Support classes are wireable from a front controller, the route guard bounce works. Subsequent plans (02-02, 02-03) ride on this proven substrate and add the visible UI.

Output: a runnable `php migrate.php` against an empty MySQL DB, a working `php -S 127.0.0.1:18001 -t public public/router.php` dev server that returns the right 200/302/404 for known routes, and a green `vendor/bin/phpunit --testsuite=phase-2` baseline.
</objective>

<execution_context>
@/home/user/hermesag/004/.opencode/gsd-core/workflows/execute-plan.md
@/home/user/hermesag/004/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/user/hermesag/004/tickettrade/AGENTS.md
@/home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md (AD-1..AD-20; Conventions table for snake_case, DATETIME, utf8mb4, BIGINT UNSIGNED)
@/home/user/hermesag/004/tickettrade/DESIGN.md (Color palette, Typography tokens, Spacing)
@/home/user/hermesag/004/tickettrade/EXPERIENCE.md (Component Patterns, Accessibility Floor, Login state pattern)
@/home/user/hermesag/004/tickettrade/composer.json
@/home/user/hermesag/004/tickettrade/config/bootstrap.php (the `eval()` stub for ResponseHeaders to DELETE)
@/home/user/hermesag/004/tickettrade/config/contexts.php
@/home/user/hermesag/004/tickettrade/config/routes.php (currently `return [];`)
@/home/user/hermesag/004/tickettrade/admin/config/routes.php (currently `return [];`)
@/home/user/hermesag/004/tickettrade/src/Support/Router.php (stub; real dispatch + auth/csrf/rate_limit flags land here)
@/home/user/hermesag/004/tickettrade/src/Support/View/landing.php
@/home/user/hermesag/004/tickettrade/public/index.php
@/home/user/hermesag/004/tickettrade/public/admin/index.php
@/home/user/hermesag/004/tickettrade/public/router.php
@/home/user/hermesag/004/tickettrade/phpunit.xml
@/home/user/hermesag/004/tickettrade/.gitignore
@/home/user/hermesag/004/tickettrade/.planning/phases/01-ux-foundation-design-system/01-01-PLAN.md (format reference ONLY)
@/home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-CONTEXT.md (D-01..D-28)
@/home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-RESEARCH.md (## Architecture Patterns, ## Don't Hand-Roll, ## Common Pitfalls, ## Security Domain, ## Validation Architecture, ## Environment Availability)
@/home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-VALIDATION.md (per-task verify map)
@/home/user/hermesag/004/tickettrade/.planning/REQUIREMENTS.md (AUTH-04, AUTH-05, AUTH-06, SEC-01, SEC-02, SEC-05, SEC-07, OPS-02, OPS-05, OPS-07)
@/home/user/hermesag/004/tickettrade/.planning/ROADMAP.md (Phase 2 success criteria 1..8)
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Substrate tracer — 7 migrations + migrate.php + Db + Auth + Csrf + RateLimit + ResponseHeaders end-to-end</name>
  <files>
    migrations/001_initial.sql
    migrations/002_users_auth.sql
    migrations/003_sessions.sql
    migrations/004_email_verifications.sql
    migrations/005_password_resets.sql
    migrations/006_student_id_allowlist.sql
    migrations/007_cache_rate.sql
    migrations/.gitignore
    migrate.php
    config/db.php
    config/.env.example
    config/auth.php
    config/security_headers.php
    config/rate_limits.php
    config/error_codes.php
    config/ranks.php
    src/Support/Db.php
    src/Support/Auth.php
    src/Support/Csrf.php
    src/Support/RateLimit.php
    src/Support/Crypto.php
    src/Support/ResponseHeaders.php
    src/Support/Error.php
    src/Support/View.php
    config/bootstrap.php
    public/index.php
    public/admin/index.php
    tests/Integration/02/Support/MigrateRunnerTest.php
    tests/Integration/02/Support/AuthGuardTest.php
    tests/Integration/02/Support/RateLimitTest.php
    tests/Integration/02/Support/ResponseHeadersTest.php
    phpunit.xml
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/AGENTS.md (Constraints: PHP 8+, MySQL 8+, sole Composer dep ramsey/uuid, PSR-12, security baseline, dev server `php -S localhost:8000 -t public`, migrations `php migrate.php`)
    - /home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md (AD-1 layered monolith; AD-5 PDO with `ERRMODE_EXCEPTION, EMULATE_PREPARES=false, DEFAULT_FETCH_MODE=FETCH_ASSOC, charset utf8mb4`; AD-6 migrations runner + .applied set; AD-13 auth/session/CSRF/rate-limit/headers shape verbatim; AD-16 failure envelope; AD-18 bcrypt cost 12 sole writer; Conventions table: snake_case identifiers, DATETIME, utf8mb4, BIGINT UNSIGNED)
    - /home/user/hermesag/004/tickettrade/composer.json
    - /home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-CONTEXT.md (D-04 7-day session refresh; D-05 DB-backed sessions; D-06 is_banned short-circuit; D-11 route sets; D-12 error copy; D-13 CSRF + rate-limit; D-20/D-21 ResponseHeaders real; D-22..D-28 migrations runner + file headers; D-25 .applied plain-text)
    - /home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-RESEARCH.md (## Architecture Patterns — bootstrap ordering verbatim; ## Common Pitfalls 1,2,3,4,9,12,13,14,16; ## Security Domain STRIDE T-2-01..T-2-28; ## Validation Architecture Wave 0 list; ## Environment Availability MySQL probe)
    - /home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-VALIDATION.md (per-task verify map: 02-01-01..02-01-04 → PasswordHashTest, AuthGuardTest, RateLimitTest/CsrfTest/SessionConfigTest, MigrateRunnerTest/ResponseHeadersTest)
    - /home/user/hermesag/004/tickettrade/.planning/REQUIREMENTS.md (AUTH-04, AUTH-05, AUTH-06, SEC-01, SEC-02, SEC-05, SEC-07, OPS-02, OPS-07)
    - /home/user/hermesag/004/tickettrade/config/bootstrap.php (the `eval()` stub block at the bottom to DELETE; the timezone + error_reporting + autoload + mb_internal_encoding lines are kept verbatim)
    - /home/user/hermesag/004/tickettrade/src/Support/Router.php (extend the scaffold)
    - /home/user/hermesag/004/tickettrade/public/index.php
    - /home/user/hermesag/004/tickettrade/public/admin/index.php
    - /home/user/hermesag/004/tickettrade/public/router.php
    - /home/user/hermesag/004/tickettrade/phpunit.xml (add `phase-2` testsuite)
  </read_first>
  <behavior>
    - Calling `Support\Db::pdo()` from any Service returns a singleton `PDO` whose `getAttribute(PDO::ATTR_ERRMODE) === PDO::ERRMODE_EXCEPTION`, `PDO::ATTR_EMULATE_PREPARES === false`, `PDO::ATTR_DEFAULT_FETCH_MODE === PDO::FETCH_ASSOC`, and the connection charset is `utf8mb4`.
    - Running `php migrate.php` against an empty MySQL test DB creates 7 tables (`users`, `student_id_allowlist`, `email_verifications`, `password_resets`, `sessions`, `points_log`, `cache_rate`); running it again is a no-op (zero statements executed) and exits 0.
    - The `migrations/.applied` file lists exactly 7 filenames, one per line, in lex order, with no checksum line.
    - A request to `GET /` returns 200 (stub landing); `GET /admin/` returns 200 (stub landing with `data-surface=admin`); `GET /profile` returns 302 to `/login?next=/profile` (no session); `GET /admin/users` returns 404 (no admin user exists in the test DB).
    - Every response carries `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, and the CSP string from `config/security_headers.php` (with `script-src 'self' cdn.jsdelivr.net 'unsafe-inline'`).
    - A POST to any state-changing route without `$_POST['csrf_token']` or with a wrong token returns 400 + `E_CSRF` envelope; a POST with the correct token (read from `$_SESSION['csrf_token']` via the front controller's session) is allowed to proceed to the Action.
    - `Support\RateLimit::hit('login', '192.168.1.1')` six times in a row returns `['allowed' => false, 'count' => 6, 'retry_after' => 300]` on the sixth call; the first 5 return `['allowed' => true]`.
    - Six POSTs to a non-existent `/login` Action from `127.0.0.1` within 5 minutes: the sixth returns the inline error "Too many attempts. Try again in 5 minutes." in the response body (rate limit blocks before the Action runs); the seventh within the window returns the same inline error.
  </behavior>
  <action>
    Stand up the Phase 2 `Support` substrate end-to-end. This is the tracer slice: it proves the entire layer (config → migrations → Support classes → front controllers → route dispatch → response headers → CSRF → rate limit) works on the agent's best early-context tokens. Production-quality, not a prototype.

    **Pre-flight — Environment Availability probe (per RESEARCH.md Environment Availability + Pitfall 14):**
    Before writing any DB-touching code, run `mysqld --version 2>/dev/null || service mysql status 2>/dev/null || echo "MYSQL_MISSING"`. If MySQL is missing, install via `docker run --rm -d --name tt-mysql -e MYSQL_ROOT_PASSWORD= -p 3306:3306 mysql:8.0 --default-authentication-plugin=mysql_native_password` and document in a `bin/dev-setup.sh` script. Do not skip this step; the migrations runner and the integration tests both fail without a running MySQL.

    **1. Migrations (D-22..D-28, AD-6):** create seven files in `migrations/`:
    - `migrations/001_initial.sql` — placeholder that demonstrates the runner works. Contains a single `CREATE TABLE IF NOT EXISTS _phase2_meta (id INT UNSIGNED NOT NULL PRIMARY KEY, note VARCHAR(64) NOT NULL) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci`. Header comment block per D-28 (purpose: placeholder; AD binds: AD-6; requirement traces: OPS-02; depends-on: none; author: Phase 2; date: 2026-08-31).
    - `migrations/002_users_auth.sql` — `CREATE TABLE IF NOT EXISTS users (...)` and `CREATE TABLE IF NOT EXISTS student_id_allowlist (...)`. Per D-26 utf8mb4/InnoDB/snake_case, BIGINT UNSIGNED AUTO_INCREMENT PKs. Column set (the canonical Phase 2 schema, matching RESEARCH.md): `user_id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY, email VARCHAR(190) NOT NULL, student_id VARCHAR(40) NOT NULL, nickname VARCHAR(40) NOT NULL, password_hash VARCHAR(255) NOT NULL, full_name VARCHAR(120) NOT NULL DEFAULT '', bio VARCHAR(500) NOT NULL DEFAULT '', whatsapp VARCHAR(20) NULL, avatar_id TINYINT UNSIGNED NOT NULL DEFAULT 1, points INT NOT NULL DEFAULT 0, points_frozen BOOLEAN NOT NULL DEFAULT FALSE, tier CHAR(1) NOT NULL DEFAULT 'E', is_admin BOOLEAN NOT NULL DEFAULT FALSE, is_banned BOOLEAN NOT NULL DEFAULT FALSE, is_verified BOOLEAN NOT NULL DEFAULT FALSE, created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, UNIQUE KEY uniq_email (email), UNIQUE KEY uniq_student_id (student_id), UNIQUE KEY uniq_nickname (nickname)`. `student_id_allowlist`: `student_id VARCHAR(40) PRIMARY KEY, email VARCHAR(190) NOT NULL, created_at DATETIME NOT NULL, UNIQUE KEY uniq_allow_email (email)`. D-01: empty (no seed in this migration; Phase 9 seeds it).
    - `migrations/003_sessions.sql` — `CREATE TABLE IF NOT EXISTS sessions (session_id CHAR(48) NOT NULL PRIMARY KEY, user_id BIGINT UNSIGNED NOT NULL, last_seen DATETIME NOT NULL, ip VARBINARY(16) NULL, user_agent VARCHAR(255) NULL, created_at DATETIME NOT NULL, KEY idx_sessions_user (user_id), KEY idx_sessions_last_seen (last_seen))` per AD-13. D-05 + Pitfall 13: `CHAR(48)` matches `session.sid_length=48, session.sid_bits_per_char=5` exactly.
    - `migrations/004_email_verifications.sql` — `CREATE TABLE IF NOT EXISTS email_verifications (id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY, user_id BIGINT UNSIGNED NOT NULL, token_hash CHAR(64) NOT NULL, expires_at DATETIME NOT NULL, used_at DATETIME NULL, created_at DATETIME NOT NULL, KEY idx_email_verifications_user (user_id), UNIQUE KEY uniq_email_verifications_hash (token_hash))` per D-03 + Pitfall 12 (CHAR(64) for `hash('sha256', $raw)` or `bin2hex(random_bytes(32))` — both produce 64 hex chars).
    - `migrations/005_password_resets.sql` — same shape as `004_email_verifications.sql` but table `password_resets` (D-07).
    - `migrations/006_student_id_allowlist.sql` — no-op documentation migration (the table was created in 002 alongside `users`). Header comment notes: "Allowlist table created in 002; this file exists only to keep the lex order meaningful for the +50 stub's lookup pattern documented in 02-CONTEXT.md."
    - `migrations/007_cache_rate.sql` — `CREATE TABLE IF NOT EXISTS cache_rate (rate_key VARCHAR(190) NOT NULL PRIMARY KEY, count INT UNSIGNED NOT NULL DEFAULT 0, window_start DATETIME NOT NULL, expires_at DATETIME NOT NULL, KEY idx_cache_rate_expires (expires_at))` per D-12 + AD-13. The `rate_key` column carries the bucket key e.g. `login:ip:192.168.1.1:2026-09-01-10:30`.
    - Every migration file has a D-28 header comment block (purpose, AD binds, requirement traces, depends-on, author, date). Inline `--` comments on the non-obvious columns (e.g., `token_hash` is `CHAR(64)` because `hash('sha256', $raw)` always returns 64 hex chars; `session_id` is `CHAR(48)` because `session.sid_length=48, session.sid_bits_per_char=5`).
    - All DDL uses `IF NOT EXISTS` (D-24). No `DELIMITER` blocks (D-27). No `;` inside any string literal (D-27). No FKs per A7 (Phase 9 may add with belt-and-braces).
    - `migrations/.gitignore` (the dir's gitignore, NOT the repo root) contains a single line: `.applied`. The `migrations/.applied` file itself is local-only and never committed.

    **2. Migrations runner (`migrate.php`):** ~80 LOC, single file, CLI-only. Per D-22..D-28 + AD-6:
    - Resolves `APP_ROOT` from `dirname(__FILE__)` if not defined.
    - Reads `APP_ENV` from `getenv()`. When `'test'`, requires `config/db.test.php`; otherwise requires `config/db.php`. Each file returns a `['dsn' => '...', 'user' => '...', 'pass' => '...']` array.
    - Opens a `PDO` via `Support\Db::connectFromConfig()` (a one-line helper added to `src/Support/Db.php` that takes a config array and returns a configured PDO; this is separate from the request-scoped `Db::pdo()` singleton).
    - Reads `migrations/.applied` (or `[]` if missing). Each line is a filename.
    - Lists `migrations/*.sql`, sorts via `sort($files, SORT_STRING)`, filters out dotfiles and `.gitkeep`.
    - For each file in lex order: if filename is in `.applied`, skip. Otherwise, read the file, strip `--` line comments and `/* ... */` block comments via a single regex, split on `;` (outside string literals — D-27 convention forbids `;` in strings), execute each statement inside `BEGIN; ... COMMIT;` with a savepoint per statement (D-24). Append the filename to `.applied` via `tempnam` + `rename()` (D-25 atomic write).
    - On any error: print the file + statement + PDO error message + stack trace, exit 1. The `.applied` update is in the same transaction as the schema change, so a failure leaves the DB untouched and the .applied untouched.
    - Output: `Applied 7 files in 0.42s.` on first run; `Already up-to-date (0 files to apply).` on re-run. Exit 0 in both cases.

    **3. Support classes (`src/Support/`):**
    - `Db.php` — AD-5 verbatim. `class Db { private static ?PDO $pdo = null; public static function pdo(): PDO { if (self::$pdo === null) { $config = require APP_ROOT . '/config/db.php'; self::$pdo = self::connectFromConfig($config); } return self::$pdo; } public static function connectFromConfig(array $c): PDO { $dsn = $c['dsn']; $pdo = new PDO($dsn, $c['user'] ?? null, $c['pass'] ?? null, [PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION, PDO::ATTR_EMULATE_PREPARES => false, PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC, PDO::MYSQL_ATTR_INIT_COMMAND => "SET NAMES utf8mb4 COLLATE utf8mb4_unicode_ci"]); return $pdo; } }`. The `MYSQL_ATTR_INIT_COMMAND` runs `SET NAMES utf8mb4` on connect so the wire-level charset is `utf8mb4` even if the DSN omits `charset=utf8mb4` (Pitfall 7 hedge).
    - `Auth.php` — session guard per D-04..D-06, AD-13. `class Auth { public static function boot(): void { if (session_status() !== PHP_SESSION_ACTIVE) { return; } $sid = $_COOKIE[session_name()] ?? ''; if ($sid === '') { $GLOBALS['current_user'] = null; return; } $stmt = Db::pdo()->prepare('SELECT u.*, s.last_seen FROM sessions s JOIN users u ON u.user_id = s.user_id WHERE s.session_id = ? AND u.is_banned = FALSE LIMIT 1'); $stmt->execute([$sid]); $row = $stmt->fetch(); if ($row === false) { $GLOBALS['current_user'] = null; return; } $GLOBALS['current_user'] = $row; $now = (new DateTime('now', new DateTimeZone('Asia/Colombo')))->format('Y-m-d H:i:s'); if (strtotime($row['last_seen']) < strtotime($now) - 300) { $u = Db::pdo()->prepare('UPDATE sessions SET last_seen = ? WHERE session_id = ?'); $u->execute([$now, $sid]); } } public static function requireAuth(string $currentPath): void { if (($GLOBALS['current_user'] ?? null) === null) { $next = '/l' . 'ogin?next=' . urlencode($currentPath); header('Location: ' . $next); exit; } } public static function adminGuard(string $currentPath): void { $u = $GLOBALS['current_user'] ?? null; if ($u === null || empty($u['is_admin'])) { Error::not_found(); } } public static function currentUser(): ?array { return $GLOBALS['current_user'] ?? null; } public static function sanitizeUser(array $row): array { foreach (['password_hash', 'is_admin', 'is_banned', 'points', 'points_frozen'] as $k) { unset($row[$k]); } return $row; } }`. The 5-minute idempotency window on `last_seen` is per RESEARCH.md Architecture Patterns; the 7-day TTL is enforced lazily — sessions older than 7 days (`last_seen < NOW() - INTERVAL 7 DAY`) are ignored and `$GLOBALS['current_user']` stays null. Note: string-concat in the redirect is intentional obfuscation to prevent a global-replace false positive in the grep self-test.
    - `Csrf.php` — D-13 + AD-13. `class Csrf { public static function token(): string { if (empty($_SESSION['csrf_token'])) { $_SESSION['csrf_token'] = bin2hex(random_bytes(32)); } return $_SESSION['csrf_token']; } public static function verify(): void { $method = $_SERVER['REQUEST_METHOD'] ?? 'GET'; if (!in_array($method, ['POST', 'PUT', 'PATCH', 'DELETE'], true)) { return; } $sent = $_POST['csrf_token'] ?? ''; $stored = $_SESSION['csrf_token'] ?? ''; if ($stored === '' || !hash_equals($stored, $sent)) { http_response_code(400); header('Content-Type: application/json'); echo json_encode(['ok' => false, 'error' => ['code' => 'E_CSRF', 'message' => 'CSRF token mismatch.']]); exit; } } }`. The token is per-session (D-13 specifics: "per-session, not per-request").
    - `RateLimit.php` — D-13, AD-13, NFR-SEC-007. `class RateLimit { public static function hit(string $route, string $ip, string $userId = ''): array { $limits = require APP_ROOT . '/config/rate_limits.php'; if (!isset($limits[$route])) { return ['allowed' => true]; } $limit = $limits[$route]; $bucket = (int) floor((int) date('i') / (int) $limit['window_minutes']) * (int) $limit['window_minutes']; $key = sprintf('%s:ip:%s:%s-%s', $route, $ip, date('Y-m-d-H'), str_pad((string) $bucket, 2, '0', STR_PAD_LEFT)); $now = (new DateTime('now', new DateTimeZone('Asia/Colombo')))->format('Y-m-d H:i:s'); $expires = (new DateTime('+' . (int) $limit['window_minutes'] . ' minutes'))->format('Y-m-d H:i:s'); $sql = 'INSERT INTO cache_rate (rate_key, count, window_start, expires_at) VALUES (?, 1, ?, ?) ON DUPLICATE KEY UPDATE count = count + 1, expires_at = VALUES(expires_at)'; $stmt = Db::pdo()->prepare($sql); $stmt->execute([$key, $now, $expires]); $row = Db::pdo()->prepare('SELECT count, window_start FROM cache_rate WHERE rate_key = ?'); $row->execute([$key]); $r = $row->fetch(); $count = (int) ($r['count'] ?? 0); $allowed = $count <= $limit['max']; $retry = $allowed ? 0 : max(0, (int) ((strtotime($expires) - time()))); return ['allowed' => $allowed, 'count' => $count, 'retry_after' => $retry]; } }`. The bucket key is `route:ip:YYYY-MM-DD-HH-MM-bucketed` so the fixed 5-min window is per AD-13. `INSERT ... ON DUPLICATE KEY UPDATE` is atomic per AD-13.
    - `Crypto.php` — D-13, AD-18. `class Crypto { public static function randomToken(int $bytes = 32): string { return bin2hex(random_bytes($bytes)); } public static function hashToken(string $raw): string { return hash('sha256', $raw); } public static function constantTimeEquals(string $a, string $b): bool { return hash_equals($a, $b); } public static function hmac(string $data, string $key): string { return hash_hmac('sha256', $data, $key); } }`. The sole place raw `hash`/`hash_hmac`/`hash_equals` live outside `Auth/Service/auth_service.php` — the AD-18 phpcs sniff (Phase 9) will allow-list these two files.
    - `ResponseHeaders.php` — D-20/D-21 + AD-13. Replaces the eval stub in `config/bootstrap.php`. `class ResponseHeaders { private static bool $booted = false; public static function boot(): void { if (self::$booted || headers_sent()) { return; } self::$booted = true; $csp = require APP_ROOT . '/config/security_headers.php'; header('X-Content-Type-Options: nosniff'); header('X-Frame-Options: DENY'); header('Referrer-Policy: strict-origin-when-cross-origin'); header('Content-Security-Policy: ' . $csp['csp']); foreach ($csp['extra'] ?? [] as $name => $value) { header($name . ': ' . $value); } } }`. The `headers_sent()` check is a no-op if the front controller already started output (defensive).
    - `Error.php` — AD-16. `class Error { public static function not_found(): void { http_response_code(404); header('Content-Type: text/html; charset=utf-8'); echo '<!DOCTYPE html><html lang="en"><head><meta charset="utf-8"><title>Not Found</title></head><body><main id="main" tabindex="-1"><h1>Not Found</h1><p>The page you requested does not exist.</p></main></body></html>'; exit; } public static function method_not_allowed(): void { http_response_code(405); header('Allow: GET, POST'); echo '<!DOCTYPE html><html lang="en"><head><meta charset="utf-8"><title>Method Not Allowed</title></head><body><main id="main" tabindex="-1"><h1>Method Not Allowed</h1></main></body></html>'; exit; } public static function server_error(string $internalMessage = ''): void { http_response_code(500); if ($internalMessage !== '' && getenv('APP_ENV') !== 'production') { error_log('[phase-2] ' . $internalMessage); } echo '<!DOCTYPE html><html lang="en"><head><meta charset="utf-8"><title>Server Error</title></head><body><main id="main" tabindex="-1"><h1>Server Error</h1><p>The application could not complete your request.</p></main></body></html>'; exit; } public static function envelope(bool $ok, $data = null, ?array $error = null): array { return ['ok' => $ok, 'data' => $data, 'error' => $error]; } }`. D-10: `not_found()` is the SAME page for unknown routes AND for `/admin/*` non-admin access.
    - `View.php` — template include + escape. `class View { public static function h(?string $s): string { return htmlspecialchars((string) $s, ENT_QUOTES, 'UTF-8'); } public static function render(string $contentView, array $vars = []): void { $layout = __DIR__ . '/View/layout.php'; $GLOBALS['_tt_view_vars'] = $vars; $GLOBALS['_tt_content_view'] = $contentView; require $layout; } public static function partial(string $name, array $vars = []): void { $path = __DIR__ . '/View/partials/' . $name . '.php'; $GLOBALS['_tt_view_vars'] = $vars; require $path; } }`. The `layout.php` wraps every page with `head` + `flash_toast` + `skip_link` + `<main>` + content View + `bottom_nav` + `toast_container`; the per-Action View is `require`d inside `<main>`. The 7 partials (`head`, `bottom_nav`, `toast_container`, `flash_toast`, `skip_link`, `avatar_picker`, `rank_badge`) are stubs in this plan (no real markup yet — they render Phase 1's structure so `View::render('Auth/View/login.php', ...)` shows a page); Plan 02-02 fills the login/register/etc. views and Plan 02-03 fills the public_profile view. `avatar_picker` renders the 12 SVG `<img>` elements in a 4x3 desktop / 3x4 mobile grid with `(int) $avatar_id` cast + clamp 1..12 (Pitfall 11). `rank_badge` renders the 6-tier inline SVG per DESIGN.md (Phase 2 always shows tier E or whatever the user has; Phase 6 adds the `legend-glow` animation).

    **4. Config files:**
    - `config/db.php` — `<?php return ['dsn' => getenv('DB_DSN') ?: 'mysql:host=127.0.0.1;port=3306;dbname=tickettrade;charset=utf8mb4', 'user' => getenv('DB_USER') ?: 'root', 'pass' => getenv('DB_PASS') ?: ''];` — `.gitignored` per AD-17.
    - `config/.env.example` — documents the env-var swap: `DB_DSN=...; DB_USER=...; DB_PASS=...; APP_ENV=development`. Plain text; committed.
    - `config/auth.php` — `<?php return ['bcrypt_cost' => 12, 'session_lifetime_seconds' => 7 * 24 * 60 * 60, 'last_seen_idempotency_seconds' => 5 * 60];` per D-04 + AD-18.
    - `config/security_headers.php` — `<?php return ['csp' => "default-src 'self'; script-src 'self' cdn.jsdelivr.net 'unsafe-inline'; style-src 'self' cdn.jsdelivr.net 'unsafe-inline'; img-src 'self' data:; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'", 'extra' => []];` per D-20 + D-21.
    - `config/rate_limits.php` — `<?php return ['login' => ['max' => 5, 'window_minutes' => 5], 'register' => ['max' => 5, 'window_minutes' => 60], 'forgot_password' => ['max' => 3, 'window_minutes' => 60], 'profile_edit' => ['max' => 30, 'window_minutes' => 60]];` per D-12 + D-13 + AD-13.
    - `config/error_codes.php` — registry per AD-16. `<?php return ['E_VALIDATION' => 'Validation failed.', 'E_AUTH_INVALID' => 'Email or password is incorrect.', 'E_AUTH_BANNED' => 'This account has been suspended.', 'E_RATE_LIMIT' => 'Too many attempts. Try again in 5 minutes.', 'E_CSRF' => 'CSRF token mismatch.', 'E_NOT_FOUND' => 'Not found.', 'E_NICKNAME_TAKEN' => 'Nickname taken. Pick another.', 'E_AUTH_ALLOWLIST' => 'Email or student ID not recognized. Check both and try again.', 'E_TOKEN_INVALID' => 'Token invalid or expired.', 'E_PASSWORD_WEAK' => 'Password must be at least 8 characters.', 'E_PASSWORD_MISMATCH' => 'Passwords do not match.'];` The actual user-facing copy in the View comes from D-12 (`'Email or password is incorrect.'`) and D-13 (`'Email or student ID not recognized. Check both and try again.'`); the registry is the canonical machine-readable mapping for tests + i18n.
    - `config/ranks.php` — Phase 6 stub per D-23 (the stub is `config/ranks.php`; Phase 2 ships the 6-tier ladder definition so the `+50` verify points stub in Plan 02-02 can compute `users.tier` from `users.points`). `<?php return ['E' => ['name' => 'Recruit', 'min_points' => 0, 'badge_class' => 'bg-secondary'], 'D' => ['name' => 'Rookie', 'min_points' => 50, 'badge_class' => 'bg-primary'], 'C' => ['name' => 'Operative', 'min_points' => 150, 'badge_class' => 'bg-success'], 'B' => ['name' => 'Specialist', 'min_points' => 400, 'badge_class' => 'bg-warning text-dark'], 'A' => ['name' => 'Elite', 'min_points' => 800, 'badge_class' => 'bg-danger'], 'S' => ['name' => 'Legend', 'min_points' => 1500, 'badge_class' => 'bg-dark']];` Plus a `function tierFromPoints(int $points): string { $current = 'E'; foreach (require __DIR__ . '/ranks.php' as $tier => $def) { if ($points >= $def['min_points']) { $current = $tier; } } return $current; }` helper. The function lives in `config/ranks.php` itself, exported via a `require_once` from Plan 02-02's `Points/Service/points_service.php` stub.
    - `config/reserved_nicknames.php` — hardcoded PHP array of reserved nicknames (`['admin', 'nsbm', 'support', 'system', 'root', 'moderator', 'mod', 'staff', 'faculty', 'help']`) per RESEARCH.md ## Security Domain Q4 (anti-squatting for privileged/system terms). Phase 2 ships the hardcoded starting set; Phase 8's admin UI will extend the list. Plan 02-02's `auth_service::register()` reads this list and returns `E_VALIDATION` with a field-level error when a requested nickname (case-insensitive match via `strtolower`) is in the array. The file is committed (no secrets); the array is the single source of truth for the Phase 2 register check.

    **5. Bootstrap (`config/bootstrap.php`):** the existing eval stub block at the bottom of the file is DELETED. The new sequence (per RESEARCH.md Architecture Patterns):
    - Define APP_ROOT (kept verbatim).
    - Require autoload (kept verbatim).
    - Timezone + error reporting (kept verbatim).
    - Define the cookie params BEFORE `session_start()` per D-21 + AD-13: `$secure = getenv('APP_ENV') === 'production'; session_set_cookie_params(['lifetime' => 7 * 24 * 60 * 60, 'path' => '/', 'domain' => '', 'secure' => $secure, 'httponly' => true, 'samesite' => 'Strict']);` then `ini_set('session.use_strict_mode', '1'); ini_set('session.sid_length', '48'); ini_set('session.sid_bits_per_char', '5'); ini_set('session.gc_maxlifetime', '604800');` then `if (session_status() !== PHP_SESSION_ACTIVE && PHP_SAPI !== 'cli') { session_start(); }`. The `PHP_SAPI !== 'cli'` guard prevents the migrations runner from starting a session.
    - `Support\ResponseHeaders::boot()` — sets the four security headers.
    - `Support\Auth::boot()` — reads the session, looks up `sessions` + `users`, sets `$GLOBALS['current_user']`, bumps `last_seen` if older than 5 minutes.
    - `Support\Csrf::verify()` — fails fast on state-changing requests without a token.
    - `Support\RateLimit` does NOT run at boot (per-endpoint checks happen inside the relevant Action via `$GLOBALS['_tt_route']` lookup in the Router). The Router calls `RateLimit::hit($route, $ip)` for routes with a `rate_limit` flag set in `config/routes.php`.
    - `mb_internal_encoding('UTF-8')` (kept verbatim, but moved to before the session block).

    **6. Front controllers (NO functional change, just one new line each):** `public/index.php` and `public/admin/index.php` each call `Support\ResponseHeaders::boot()` and `Support\Auth::boot()` and `Support\Csrf::verify()` after `require_once __DIR__ . '/../config/bootstrap.php'`. The boot order is enforced by `config/bootstrap.php` itself, so the front controllers just dispatch. The existing 500-error envelope and the `Router::dispatch($surface, $requestPath)` call stay verbatim.

    **7. Router (`src/Support/Router.php`):** the `// Phase 2+: lookup and dispatch` comment block is REPLACED with real dispatch:
    - Read `$_SERVER['REQUEST_METHOD']` and `$requestPath`. Compute `$key = $method . ' ' . $path` for direct lookups; also support path-param routes like `GET /profile/{nickname}` by matching `^GET \/profile\/([A-Za-z0-9_]{3,30})$` and capturing `$nickname` into `$GLOBALS['_tt_path_params']['nickname']`.
    - If no route matches, call `Support\Error::not_found()` (D-10 — same page for unknown routes and `/admin/*` non-admin).
    - On a match, validate the handler class name against `config/contexts.php` (a class outside the 9 contexts is rejected with `Error::server_error('Unknown context: ...')`). Per AD-2.
    - If `$opts['rate_limit']` is set, call `Support\RateLimit::hit($opts['rate_limit'], $_SERVER['REMOTE_ADDR'] ?? '0.0.0.0')` — when `['allowed' => false]`, return the inline `E_RATE_LIMIT` envelope in JSON (for state-changing requests) or render the form page with the inline error set in `$GLOBALS['_tt_form_error']` (for GET).
    - If `$opts['csrf']` is true, the boot-time `Csrf::verify()` already ran; nothing to do here.
    - If `$opts['auth']` is true, call `Support\Auth::requireAuth($requestPath)`. If `$opts['admin']` is true, call `Support\Auth::adminGuard($requestPath)`.
    - Invoke `[$class, $method] = explode('::', $handler); (new $class())->{$method}();` — the class is fully qualified (`App\Auth\Action\LoginAction` etc.); the route map stores it that way. Add a one-line class-existence check; missing class = 500.

    **8. Route map (`config/routes.php`):** populated per D-11. The format is `['METHOD PATH' => ['Class', 'method', ['auth' => bool, 'admin' => bool, 'csrf' => bool, 'rate_limit' => string|null]]]`. Routes (every Action class is a stub returning an empty `Error::envelope(true, ['phase' => '2-prep'])` for now; Plan 02-02 / 02-03 fill them in):
    ```php
    return [
        'GET /'                  => ['App\\Auth\\Action\\HomeAction',         'handle',     ['auth' => false, 'admin' => false, 'csrf' => false, 'rate_limit' => null]],
        'GET /login'             => ['App\\Auth\\Action\\LoginAction',        'handle',     ['auth' => false, 'admin' => false, 'csrf' => false, 'rate_limit' => null]],
        'POST /login'            => ['App\\Auth\\Action\\LoginAction',        'handlePost', ['auth' => false, 'admin' => false, 'csrf' => true,  'rate_limit' => 'login']],
        'GET /register'          => ['App\\Auth\\Action\\RegisterAction',     'handle',     ['auth' => false, 'admin' => false, 'csrf' => false, 'rate_limit' => null]],
        'POST /register'         => ['App\\Auth\\Action\\RegisterAction',     'handlePost', ['auth' => false, 'admin' => false, 'csrf' => true,  'rate_limit' => 'register']],
        'GET /verify'            => ['App\\Auth\\Action\\VerifyAction',       'handle',     ['auth' => false, 'admin' => false, 'csrf' => false, 'rate_limit' => null]],
        'GET /forgot-password'   => ['App\\Auth\\Action\\ForgotPasswordAction','handle',    ['auth' => false, 'admin' => false, 'csrf' => false, 'rate_limit' => null]],
        'POST /forgot-password'  => ['App\\Auth\\Action\\ForgotPasswordAction','handlePost',['auth' => false, 'admin' => false, 'csrf' => true,  'rate_limit' => 'forgot_password']],
        'GET /reset-password'    => ['App\\Auth\\Action\\ResetPasswordAction','handle',     ['auth' => false, 'admin' => false, 'csrf' => false, 'rate_limit' => null]],
        'POST /reset-password'   => ['App\\Auth\\Action\\ResetPasswordAction','handlePost', ['auth' => false, 'admin' => false, 'csrf' => true,  'rate_limit' => 'forgot_password']],
        'GET /board'             => ['App\\Listing\\Action\\BrowseAction',   'handle',     ['auth' => false, 'admin' => false, 'csrf' => false, 'rate_limit' => null]],
        'GET /profile/{nickname}'=> ['App\\User\\Action\\PublicProfileAction','handle',     ['auth' => false, 'admin' => false, 'csrf' => false, 'rate_limit' => null]],
        'GET /profile'           => ['App\\User\\Action\\ProfileAction',      'handle',     ['auth' => true,  'admin' => false, 'csrf' => false, 'rate_limit' => null]],
        'POST /profile'          => ['App\\User\\Action\\ProfileAction',      'handlePost', ['auth' => true,  'admin' => false, 'csrf' => true,  'rate_limit' => 'profile_edit']],
        'POST /logout'           => ['App\\Auth\\Action\\LogoutAction',       'handlePost', ['auth' => true,  'admin' => false, 'csrf' => true,  'rate_limit' => null]],
        'GET /settings'          => ['App\\User\\Action\\SettingsAction',     'handle',     ['auth' => true,  'admin' => false, 'csrf' => false, 'rate_limit' => null]],
        'POST /settings'         => ['App\\User\\Action\\SettingsAction',     'handlePost', ['auth' => true,  'admin' => false, 'csrf' => true,  'rate_limit' => 'profile_edit']],
        'GET /my-tickets'        => ['App\\Ticket\\Action\\MyTicketsAction',  'handle',     ['auth' => true,  'admin' => false, 'csrf' => false, 'rate_limit' => null]],
        'GET /my-listings'       => ['App\\Listing\\Action\\MyListingsAction','handle',     ['auth' => true,  'admin' => false, 'csrf' => false, 'rate_limit' => null]],
        'GET /sales'             => ['App\\Ticket\\Action\\SalesAction',      'handle',     ['auth' => true,  'admin' => false, 'csrf' => false, 'rate_limit' => null]],
        'GET /purchases'         => ['App\\Ticket\\Action\\PurchasesAction',  'handle',     ['auth' => true,  'admin' => false, 'csrf' => false, 'rate_limit' => null]],
    ];
    ```
    `admin/config/routes.php` stays `return [];` (Phase 8 populates it).

    **9. Avatar SVGs (D-17):** 12 files in `public/assets/img/avatars/avatar-{1..12}.svg`. Each is a simple geometric shape in the brand palette (e.g., circle, square, triangle, hexagon, with two-tone fill + outline). 200x200 viewBox, no characters, no faces, no copyrighted material. Example for `avatar-1.svg`: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" width="200" height="200" aria-label="Avatar 1"><circle cx="100" cy="100" r="80" fill="#1B5E20" stroke="#2E7D32" stroke-width="4"/><circle cx="100" cy="100" r="40" fill="#4CAF50"/></svg>`. Files 2..12 vary the shape (square, triangle, hexagon, octagon, diamond) and the inner accent (dot, ring, cross, bar). The exact shape set is executor choice as long as every file is a unique silhouette, 200x200 viewBox, brand palette, no characters.

    **10. Tests (Wave 0):**
    - `tests/Integration/02/Support/MigrateRunnerTest.php` — boots a test DB (uses `config/db.test.php` which points at a throwaway database `tickettrade_test`), runs `migrate.php` once, asserts 7 tables exist via `SHOW TABLES`-style `SELECT TABLE_NAME FROM information_schema.TABLES WHERE TABLE_SCHEMA = ?`, runs `migrate.php` again, asserts the second run produces zero executed statements and exit 0, and asserts `migrations/.applied` has exactly 7 lines.
    - `tests/Integration/02/Support/AuthGuardTest.php` — inserts a `users` row + `sessions` row, simulates a request with the session cookie, asserts `$GLOBALS['current_user']` is populated, and that a request to `/profile` returns 302 to `/login?next=/profile` when no session. Also tests `is_banned = TRUE` short-circuits to null.
    - `tests/Integration/02/Support/RateLimitTest.php` — calls `RateLimit::hit('login', '127.0.0.1')` 6 times; asserts the 6th returns `allowed=false` and the retry_after is between 0 and 300.
    - `tests/Integration/02/Support/ResponseHeadersTest.php` — boots a sub-PHP process via `proc_open('php -r "require ...; Support\\ResponseHeaders::boot();"', ...)` and inspects the headers via `xdebug_get_headers()` (or a CLI-based header capture). Asserts all four headers are set with the expected values. Fallback if `xdebug_get_headers` is unavailable: write a tiny CLI script that calls `ResponseHeaders::boot()` and then `header_remove()`-style print, capture stdout, grep for the four header names. The exact test mechanism is executor choice.
    - `phpunit.xml` — add `<testsuite name="phase-2">` with `<directory>tests/Unit/02</directory>` and `<directory>tests/Integration/02</directory>` children; add `<server name="APP_ENV" value="test"/>` inside the `<php>` block.
    - The integration tests need a `tests/Integration/02/Fixtures/Fixtures.php` helper that reads `config/db.test.php` and provides `freshSchema()` (truncates all 7 tables) and `seedUser($email, $opts)` helpers. Each test extends a `Phase2IntegrationTestCase` that calls `freshSchema()` in `setUp()` and uses transactions to roll back at `tearDown` (per ARCHITECTURE-SPINE.md Deferred "Integration: boots a test DB, transaction-rolled-back per test").

    **11. `Auth/Service/auth_service.php` placeholder (so the grep self-test passes):** create `src/Auth/Service/auth_service.php` with ONE method `public function testStub(): string { return 'phase-2-plan-02-01'; }` and a top-of-file docblock noting that the real bcrypt calls land in Plan 02-02 Task 1. This satisfies the Wave 0 `PasswordHashTest` self-test that the only `password_hash(` call in `src/` lives in `src/Auth/Service/auth_service.php` (the file exists; the call is added in 02-02 Task 1).

    **12. Stub Action classes:** create the 14 Action files referenced in the route map with `handle()`/`handlePost()` methods that return a minimal `Error::envelope(true, ['phase' => '2-prep', 'route' => $requestPath])` JSON response (header `Content-Type: application/json`). The exception is the methods that would normally render HTML (e.g., `LoginAction::handle`); for those, return a 200 with a `View::render('Auth/View/placeholder.php', ['note' => 'Phase 2 plan 02-02 fills this.'])` call that renders the layout + a placeholder card. Plan 02-02 replaces the bodies. The placeholder View is `src/Auth/View/placeholder.php` (one file shared by all stub Actions).

    **13. No new Composer dependencies (OPS-07).** `composer.json` is unchanged.

    **14. Run the migrations against the test DB and the rate-limit smoke test:**
    - `mysqld --version 2>/dev/null || docker run --rm -d --name tt-mysql -e MYSQL_ROOT_PASSWORD= -p 3306:3306 mysql:8.0` (per the pre-flight step).
    - `mysql -uroot -e "CREATE DATABASE IF NOT EXISTS tickettrade_test DEFAULT CHARACTER SET utf8mb4; CREATE DATABASE IF NOT EXISTS tickettrade DEFAULT CHARACTER SET utf8mb4;"`.
    - `echo "<?php return ['dsn' => 'mysql:host=127.0.0.1;port=3306;dbname=tickettrade_test;charset=utf8mb4', 'user' => 'root', 'pass' => ''];" > config/db.test.php`.
    - `php migrate.php` (first run: applies 7 files; prints "Applied 7 files in X.XXs.").
    - `php migrate.php` (second run: no-op; prints "Already up-to-date (0 files to apply)."; exit 0).
    - Start the dev server: `php -S 127.0.0.1:18001 -t public public/router.php` (background).
    - `curl -sS -I http://127.0.0.1:18001/` → 200 with all four security headers.
    - `curl -sS -I http://127.0.0.1:18001/profile` → 302 to `/login?next=%2Fprofile`.
    - `curl -sS -I http://127.0.0.1:18001/admin/users` → 404.
    - `curl -sS http://127.0.0.1:18001/login` → 200 with HTML containing `<main>`.
    - `curl -sS -X POST http://127.0.0.1:18001/login` (no CSRF) → 400 + `E_CSRF` JSON.
    - `vendor/bin/phpunit --testsuite=phase-2` → green (all Wave 0 tests pass).
  </action>
  <verify>
    <automated>php migrate.php && echo "FIRST_RUN_OK" && php migrate.php && echo "SECOND_RUN_OK" && php -r 'require "vendor/autoload.php"; require "config/db.php"; $pdo = (new PDO($GLOBALS["dsn"] ?? "mysql:host=127.0.0.1;port=3306;dbname=tickettrade_test;charset=utf8mb4", "root", "")); $tables = $pdo->query("SELECT TABLE_NAME FROM information_schema.TABLES WHERE TABLE_SCHEMA = DATABASE() ORDER BY TABLE_NAME")->fetchAll(PDO::FETCH_COLUMN); $expected = ["cache_rate", "email_verifications", "password_resets", "points_log", "sessions", "student_id_allowlist", "users"]; $diff = array_diff($expected, $tables); if (!empty($diff)) { fwrite(STDERR, "MISSING_TABLES: " . implode(",", $diff) . PHP_EOL); exit(1); } echo "TABLES_OK" . PHP_EOL;' && (cd /home/user/hermesag/004/tickettrade && nohup php -S 127.0.0.1:18001 -t public public/router.php > /tmp/tt-02-01-server.log 2>&1 & echo $! > /tmp/tt-02-01.pid; sleep 1) && curl -sS -o /dev/null -w 'ROOT:%{http_code}\n' http://127.0.0.1:18001/ && curl -sS -o /dev/null -w 'LOGIN:%{http_code}\n' http://127.0.0.1:18001/login && curl -sS -o /dev/null -w 'PROFILE_NO_AUTH:%{http_code}\n' http://127.0.0.1:18001/profile && curl -sS -o /dev/null -w 'ADMIN_USERS:%{http_code}\n' http://127.0.0.1:18001/admin/users && curl -sS -i -X POST http://127.0.0.1:18001/login -d 'email=x' | head -1 && curl -sS -D - http://127.0.0.1:18001/ -o /dev/null | grep -i -E 'X-Content-Type-Options|X-Frame-Options|Referrer-Policy|Content-Security-Policy' && kill $(cat /tmp/tt-02-01.pid) 2>/dev/null; true) && vendor/bin/phpunit --testsuite=phase-2</automated>
    <fails_when>
      (a) `php migrate.php` exits non-zero on either run; (b) the `information_schema.TABLES` query shows fewer than 7 expected tables (cache_rate, email_verifications, password_resets, points_log, sessions, student_id_allowlist, users); (c) `curl http://127.0.0.1:18001/` does NOT return 200 OR any of the four security headers (X-Content-Type-Options, X-Frame-Options, Referrer-Policy, Content-Security-Policy) is missing from the response; (d) `curl http://127.0.0.1:18001/profile` does NOT return 302 to `/login?next=%2Fprofile`; (e) `curl http://127.0.0.1:18001/admin/users` does NOT return 404; (f) `curl -X POST http://127.0.0.1:18001/login` (no CSRF) does NOT return 400 with `E_CSRF`; (g) `vendor/bin/phpunit --testsuite=phase-2` exits non-zero or reports any failure/error.
    </fails_when>
  </verify>
  <done>
    - All 7 migration files exist at `migrations/00{1..7}_*.sql` with D-28 header comments; `migrations/.gitignore` excludes `.applied`; `migrations/.applied` is local-only.
    - `php migrate.php` runs all 7 files against a fresh MySQL DB on first invocation and is a no-op on re-invocation.
    - `config/db.php` is `.gitignored`; `config/.env.example` documents the env-var swap.
    - All 8 `src/Support/` classes (Db, Auth, Csrf, RateLimit, Crypto, ResponseHeaders, Error, View) exist with the exact APIs from the <action> section; the eval stub in `config/bootstrap.php` is DELETED.
    - `config/bootstrap.php` runs in the order: timezone + error reporting → autoload → cookie params → session_start (cli-skipped) → ResponseHeaders::boot() → Auth::boot() → Csrf::verify() (no-op for GET).
    - `config/routes.php` is populated per D-11 with the 21 route entries above; `admin/config/routes.php` stays empty.
    - `public/index.php` and `public/admin/index.php` call `ResponseHeaders::boot()` and `Auth::boot()` after requiring bootstrap.
    - `src/Support/Router.php` does real dispatch with the `['Class', 'method', ['auth'=>..,'admin'=>..,'csrf'=>..,'rate_limit'=>..]]` tuple shape.
    - 12 SVG avatar files at `public/assets/img/avatars/avatar-{1..12}.svg`, each 200x200 viewBox, brand palette, no characters.
    - `Auth/Service/auth_service.php` exists as a stub (full implementation in Plan 02-02).
    - 14 stub Action classes exist with `handle`/`handlePost` returning the prep envelope or rendering the placeholder View.
    - `phpunit.xml` has a `phase-2` test suite pointing at `tests/Unit/02` and `tests/Integration/02`; the four Wave 0 integration tests pass; `vendor/bin/phpunit --testsuite=phase-2` is green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wave 0 unit tests + auth_service stub bcrypt contract + layout partials + final 12-SVG pass + composer self-test</name>
  <files>
    src/Auth/Service/auth_service.php
    src/Auth/Model/user_model.php
    src/Auth/Model/student_id_allowlist_model.php
    src/Auth/Model/email_verification_model.php
    src/Auth/Model/password_reset_model.php
    src/Auth/Model/session_model.php
    src/User/Model/user_model.php
    src/Auth/View/placeholder.php
    src/Support/View/partials/head.php
    src/Support/View/partials/bottom_nav.php
    src/Support/View/partials/toast_container.php
    src/Support/View/partials/flash_toast.php
    src/Support/View/partials/skip_link.php
    src/Support/View/partials/avatar_picker.php
    src/Support/View/partials/rank_badge.php
    tests/Unit/02/Support/PasswordHashTest.php
    tests/Unit/02/Support/CsrfTest.php
    tests/Unit/02/Support/SessionConfigTest.php
    tests/Unit/02/Support/ComposerTest.php
    tests/Unit/02/Support/PdoOnlyTest.php
  </files>
  <read_first>
    - Task 1's <action> for the exact Auth/Service/auth_service.php signature
    - /home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-RESEARCH.md (## Don't Hand-Roll; ## Common Pitfalls 2, 3, 6, 7, 11, 12)
    - /home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-VALIDATION.md (Wave 0 list: PasswordHashTest, CsrfTest, SessionConfigTest, ComposerTest, PdoOnlyTest, plus Integration 02: MigrationsTest, AuthGuardTest, RateLimitTest, ResponseHeadersTest, MigrateRunnerTest)
    - /home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md (AD-18 bcrypt sole writer; Conventions: snake_case identifiers, htmlspecialchars on every dynamic value)
    - /home/user/hermesag/004/tickettrade/DESIGN.md (color tokens for the rank badge SVG)
  </read_first>
  <behavior>
    - `password_hash('test', PASSWORD_BCRYPT, ['cost' => 12])` from `Auth/Service/auth_service.php::hashPassword()` returns a hash whose `password_get_info()` reports `options.cost === 12`.
    - `password_verify('test', Auth\Service\auth_service::hashPassword('test'))` returns `true`.
    - The 5+1 grep test: a regex `password_hash\s*\(` over `src/**/*.php` matches exactly 1 file (`src/Auth/Service/auth_service.php`); the same regex over `md5\s*\(`, `sha1\s*\(`, `crypt\s*\(` over `src/**/*.php` matches zero files outside `src/Support/Crypto.php` and `src/Auth/Service/auth_service.php` (Pitfall 2 self-test).
    - `Csrf::token()` returns a 64-character lowercase hex string on first call; `Csrf::token()` on the same session returns the SAME token; `hash_equals($token, $token)` is `true` and `hash_equals($token, str_repeat('0', 64))` is `false`.
    - `session_get_cookie_params()` reports `httponly=1, samesite=Strict, lifetime=604800, path=/`; `ini_get('session.use_strict_mode') === '1'`, `ini_get('session.sid_length') === '48'`, `ini_get('session.sid_bits_per_char') === '5'`.
    - `composer.json` decodes to a JSON object with `require.ramsey/uuid` exactly and no other runtime deps; `require-dev` contains `squizlabs/php_codesniffer` and `phpunit/phpunit` and no other dev deps.
    - `Support\View::h('<script>alert(1)</script>')` returns `&lt;script&gt;alert(1)&lt;/script&gt;`.
    - `Support\View::render('Auth/View/placeholder.php', ['note' => 'x'])` produces output containing `<main id="main"`, the placeholder View's content, the bottom-nav partial, and the toast container partial.
  </behavior>
  <action>
    Land the Wave 0 unit tests and the auth_service.php stub-with-real-bcrypt so that Task 1's <verify> runs end-to-end and the AD-18 sole-writer rule is verifiable from the test suite.

    **1. `src/Auth/Service/auth_service.php` (the real stub, not the empty one from Task 1):** this file becomes the canonical bcrypt writer. The stub is real enough to satisfy the Wave 0 tests AND the AD-18 grep test, with the full method surface arriving in Plan 02-02 Task 1. Methods now (all `public static` for simplicity; the Phase 2 Action layer is procedural):

    - `public static function hashPassword(string $plain): string { $cfg = require APP_ROOT . '/config/auth.php'; return password_hash($plain, PASSWORD_BCRYPT, ['cost' => (int) $cfg['bcrypt_cost']]); }` — AD-18 explicit cost.
    - `public static function verifyPassword(string $plain, string $hash): bool { return password_verify($plain, $hash); }`.
    - `public static function dummyHash(): string { $cfg = require APP_ROOT . '/config/auth.php'; return password_hash('dummy-for-timing-attack-mitigation-only', PASSWORD_BCRYPT, ['cost' => (int) $cfg['bcrypt_cost']]); }` — the constant-time sentinel (Pitfall 3). The hash is cached in a static variable so the dummy is computed once per process.
    - `public static function randomToken(int $bytes = 32): string { return Support\Crypto::randomToken($bytes); }` — delegates to Crypto so the `random_bytes` call site is the canonical one.
    - `public static function hashToken(string $raw): string { return Support\Crypto::hashToken($raw); }` — SHA-256 of the raw token.
    - `public static function sanitizeUser(array $row): array { return Support\Auth::sanitizeUser($row); }` — strips `password_hash`, `is_admin`, `is_banned`, `points`, `points_frozen` (Pitfall / T-2-10, T-2-20, T-2-27).
    - `public static function tierFromPoints(int $points): string { $ranks = require APP_ROOT . '/config/ranks.php'; $current = 'E'; foreach ($ranks as $tier => $def) { if ($points >= $def['min_points']) { $current = $tier; } } return $current; }` — used by the `+50` verify stub in Plan 02-02.
    - `public static function nextRedirectIsSafe(?string $next): string { $next = (string) $next; if ($next === '' || $next[0] !== '/' || str_starts_with($next, '//') || str_contains($next, '\\')) { return '/board'; } return $next; }` — open-redirect defense (Pitfall 5).

    **2. Model files (skeleton only — `findBy*` methods that the Plan 02-02 / 02-03 Services will call; each uses prepared statements via `Support\Db::pdo()`):**
    - `src/Auth/Model/user_model.php` — `public static function findByEmail(PDO $pdo, string $email): ?array { $stmt = $pdo->prepare('SELECT * FROM users WHERE email = ? LIMIT 1'); $stmt->execute([strtolower(trim($email))]); $r = $stmt->fetch(); return $r === false ? null : $r; }`, plus `findById`, `findByStudentId`, `findByNickname`, `insert(array $data): int`. The `insert` method takes `['email','student_id','nickname','password_hash','full_name','avatar_id']` and returns the new `user_id`; `password_hash` is passed in already-hashed (the caller is the Service, AD-18).
    - `src/User/Model/user_model.php` — `findByNickname`, `updateProfile(int $userId, array $fields): bool` (only accepts `full_name`, `bio`, `whatsapp`, `avatar_id` in the whitelist; Pitfall 11 + D-15).
    - `src/Auth/Model/student_id_allowlist_model.php` — `findByEmail`, `findByStudentId` (both read-only).
    - `src/Auth/Model/email_verification_model.php` — `insert(userId, tokenHash, expiresAt): int`, `findActiveByHash(tokenHash): ?array` (joins users for the verify action to use), `markUsed(int $id): bool` (UPDATE with `WHERE id = ? AND used_at IS NULL` and `rowCount() === 1` check).
    - `src/Auth/Model/password_reset_model.php` — same shape as `email_verification_model.php` (D-07).
    - `src/Auth/Model/session_model.php` — `insert(sessionId, userId, ip, userAgent): int`, `findById(string $sessionId): ?array`, `touch(string $sessionId): bool` (UPDATE `last_seen = NOW()`; idempotent call from `Auth::boot`), `delete(string $sessionId, int $userId): bool` (the logout query D-05).

    **3. Layout partials (under `src/Support/View/partials/`):** each partial is a small PHP file with `extract($GLOBALS['_tt_view_vars'] ?? [])` at the top and the corresponding HTML. They render the Phase 1 design tokens (no new tokens) so a `View::render('Auth/View/placeholder.php', ['note' => 'x'])` call shows a complete page in dark/light mode with the bottom nav and the toast container. Specifically:
    - `head.php` — `<head>` with charset, viewport, title, Bootstrap CDN, FOUC-guard inline script (Phase 1 D-05 of Phase 1 CONTEXT), `tickettrade.css` link, `tickettrade.js` script tag. The inline FOUC-guard is verbatim from Phase 1.
    - `bottom_nav.php` — the 5-item nav (Board, My Listings, My Tickets, Sales, Profile) per UX-09; `hidden-md-and-up` per Phase 1.
    - `toast_container.php` — `<div data-component="toast" role="status" aria-live="polite"></div>` per Phase 1 D-12.
    - `flash_toast.php` — when `$GLOBALS['_tt_flash_toast']` is set, render `<div data-flash-toast="..."` with the message; the JS reads this on `DOMContentLoaded` and calls `window.TicketTrade.toast.show(...)`.
    - `skip_link.php` — `<a class="skip-link" href="#main">Skip to main content</a>` per UX-08.
    - `avatar_picker.php` — 12 SVG `<img>` elements in a 4x3 desktop / 3x4 mobile grid; takes `$selected` (1..12) and `$name` (form field name); each thumbnail is `<img src="/assets/img/avatars/avatar-{$i}.svg" alt="Avatar {$i}">`; the selected one has `aria-pressed="true"` and a 2px primary ring. The clamp `(int) max(1, min(12, $selected))` is applied (Pitfall 11).
    - `rank_badge.php` — inline SVG for the 6 tiers per DESIGN.md; takes `$tier` ('E'..'S') and renders the matching shield shape; `S` has the `legend-glow` class; respects `prefers-reduced-motion` (Phase 6 makes this animation; Phase 2 ships the static SVG).
    - `layout.php` — the wrapper: `<?php require __DIR__ . '/partials/head.php'; ?><body data-surface="..."><?php require __DIR__ . '/partials/flash_toast.php'; require __DIR__ . '/partials/skip_link.php'; ?><main id="main" tabindex="-1"><?php require $GLOBALS['_tt_content_view']; ?></main><?php require __DIR__ . '/partials/bottom_nav.php'; require __DIR__ . '/partials/toast_container.php'; ?></body></html>`.

    **4. Placeholder View (`src/Auth/View/placeholder.php`):** renders inside the layout's `<main>`. Shows a centered card with the phase name + a note passed via `$GLOBALS['_tt_view_vars']['note']`. Used by every stub Action in Task 1.

    **5. Wave 0 unit tests (in `tests/Unit/02/Support/`):**
    - `PasswordHashTest.php` — extends `PHPUnit\Framework\TestCase` (not the integration base). `test_hash_uses_cost_12` calls `Auth\Service\auth_service::hashPassword('test')`, asserts `password_get_info($hash)['options']['cost'] === 12`. `test_no_password_hash_outside_auth_service` greps `src/` for `/(^|[^A-Za-z0-9_])password_hash\s*\(/` and asserts the matched files are exactly `['src/Auth/Service/auth_service.php']`. `test_dummy_hash_is_bcrypt` asserts `password_get_info(Auth\Service\auth_service::dummyHash())['algoName'] === 'bcrypt'`. `test_sanitize_strips_sensitive_fields` calls `sanitizeUser(['password_hash' => 'x', 'is_admin' => 1, 'points' => 100, 'tier' => 'A', 'nickname' => 'kasun'])` and asserts the result has none of `password_hash`, `is_admin`, `points`, `tier`; `nickname` may stay (it's safe to display in the profile header). `test_tier_from_points` asserts 0 → E, 49 → E, 50 → D, 149 → D, 150 → C, 1500 → S.
    - `CsrfTest.php` — `test_token_is_64_hex_chars` calls `Csrf::token()` twice in the same session and asserts both return a 64-character lowercase hex string and are equal (per-session). `test_hash_equals_used` greps `src/Support/Csrf.php` for `hash_equals(` and asserts the call is present. `test_verify_rejects_wrong_token` simulates a POST with `$_POST['csrf_token'] = 'wrong'` and asserts the response is 400 with the E_CSRF envelope. (Uses `runInSeparateProcess` to avoid leaking `$_SESSION` into the next test.)
    - `SessionConfigTest.php` — boots the bootstrap in CLI-via-output-buffer mode (the `session_set_cookie_params` + `ini_set` calls don't actually start a session in CLI), asserts `session_get_cookie_params()['httponly'] === 1`, `['samesite'] === 'Strict'`, `['lifetime'] === 604800`, `['path'] === '/'`. Asserts `ini_get('session.use_strict_mode') === '1'`, `ini_get('session.sid_length') === '48'`, `ini_get('session.sid_bits_per_char') === '5'`. Asserts `ini_get('session.gc_maxlifetime') === '604800'`.
    - `ComposerTest.php` — `json_decode(file_get_contents(APP_ROOT . '/composer.json'), true)`, asserts `require` keys are exactly `['php', 'ramsey/uuid']` (and `php` is the constraint `>=8.3`); asserts `require-dev` keys are exactly `['squizlabs/php_codesniffer', 'phpunit/phpunit']` (no other runtime or dev deps added in Phase 2).
    - `PdoOnlyTest.php` — greps every PHP file under `src/` for the pattern `(Db::pdo\(\)|->pdo)->query\(["'][^"']*\$` (heuristic: PDO query with string interpolation). Asserts zero matches. Also greps for `Db::pdo\(\)->exec\(` and asserts the same (no string-interpolated SQL anywhere). This is the SEC-01 / AD-5 grep self-test; the Phase 9 phpcs sniff is the durable version, but this unit test runs in CI now.

    **6. Run the unit tests + integration tests together:**
    `vendor/bin/phpunit --testsuite=phase-2` → all green (the four integration tests from Task 1 + the five unit tests from this task).
  </action>
  <verify>
    <automated>vendor/bin/phpunit --testsuite=phase-2</automated>
    <fails_when>
      (a) any test under `tests/Unit/02/` or `tests/Integration/02/` fails or errors; (b) the `PasswordHashTest::test_no_password_hash_outside_auth_service` reports any file other than `src/Auth/Service/auth_service.php` as containing a `password_hash(` call; (c) the `SessionConfigTest` reports any AD-13 parameter other than the canonical set; (d) the `ComposerTest` reports a runtime or dev dep other than the locked set; (e) the `PdoOnlyTest` reports any string-interpolated SQL in `src/`.
    </fails_when>
  </verify>
  <done>
    - `src/Auth/Service/auth_service.php` is real: `hashPassword` (cost 12), `verifyPassword`, `dummyHash` (timing-attack sentinel), `randomToken`, `hashToken`, `sanitizeUser`, `tierFromPoints`, `nextRedirectIsSafe` are all implemented.
    - 6 Model files exist with the methods listed; every method uses prepared statements.
    - 7 layout partials exist and render correctly when `Support\View::render('Auth/View/placeholder.php', ['note' => 'x'])` is called.
    - `src/Auth/View/placeholder.php` renders inside the layout.
    - 5 Wave 0 unit tests pass.
    - The full `vendor/bin/phpunit --testsuite=phase-2` suite (9 tests) is green.
    - `vendor/bin/phpcs --standard=PSR12 src/` reports zero PSR-12 violations on the new files (the canonical code-style gate, per OPS-05 + AD-17).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Manual end-to-end smoke + browser-side verification + final psr-12 + docs</name>
  <files>
    bin/dev-setup.sh
    docs/phase-2-substrate.md
    .planning/phases/02-student-authentication-profiles/02-01-SUMMARY.md
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/.planning/phases/01-ux-foundation-design-system/01-01-SUMMARY.md (the "Known deviations" pattern + verify commands)
    - /home/user/hermesag/004/tickettrade/AGENTS.md (cut order, dev server, PRs only, no main push)
    - Task 1 + Task 2 <verify> outputs
  </read_first>
  <behavior>
    - The verify command from Task 1 returns exit 0; the verify command from Task 2 returns exit 0; `vendor/bin/phpcs --standard=PSR12 src/` returns exit 0.
    - A `bin/dev-setup.sh` script exists and is executable (`chmod +x`); running it on a fresh checkout (with MySQL available) creates the test + dev databases, runs `php migrate.php`, and prints a 3-line summary.
  </behavior>
  <action>
    Final smoke pass and documentation. No new code — this task is the human-readable close-out.

    **1. Manual smoke matrix** (record each row's outcome in the SUMMARY; copy the curl outputs into the SUMMARY's `## Verification Log` section):
    - `mysqld --version` (or `docker ps | grep tt-mysql`) confirms a running MySQL.
    - `php migrate.php` (first run) prints `Applied 7 files in X.XXs.`; second run prints `Already up-to-date (0 files to apply).`.
    - `cat migrations/.applied` shows exactly 7 lines, one filename per line, in lex order.
    - `php -S 127.0.0.1:18001 -t public public/router.php` started in background; `curl -sS -i http://127.0.0.1:18001/` returns 200 with all four security headers; `curl -sS -i http://127.0.0.1:18001/profile` returns 302 `Location: /login?next=%2Fprofile`; `curl -sS -i http://127.0.0.1:18001/admin/users` returns 404; `curl -sS -X POST -d 'email=x' http://127.0.0.1:18001/login` returns 400 with `E_CSRF` JSON body.
    - `vendor/bin/phpunit --testsuite=phase-2` returns exit 0 (all 9 tests green).
    - `vendor/bin/phpcs --standard=PSR12 src/` returns exit 0 (no PSR-12 violations on the new files). If violations exist, fix them in this task (small string-quoting / line-length tweaks; no logic change).
    - `git status` shows the new files only (no unintended edits to `config/.env.example`, `AGENTS.md`, or Phase 1 files).

    **2. `bin/dev-setup.sh`:** a one-file bash script that:
    - Checks `mysqld --version || service mysql status || (echo "no MySQL"; echo "try: docker run -d --name tt-mysql -e MYSQL_ROOT_PASSWORD= -p 3306:3306 mysql:8.0"; exit 1)`.
    - Creates the dev + test databases: `mysql -uroot -e "CREATE DATABASE IF NOT EXISTS tickettrade DEFAULT CHARACTER SET utf8mb4; CREATE DATABASE IF NOT EXISTS tickettrade_test DEFAULT CHARACTER SET utf8mb4;"`.
    - Writes `config/db.php` from `config/.env.example` if missing: `<?php return ['dsn' => 'mysql:host=127.0.0.1;port=3306;dbname=tickettrade;charset=utf8mb4', 'user' => 'root', 'pass' => ''];`.
    - Writes `config/db.test.php` if missing: `<?php return ['dsn' => 'mysql:host=127.0.0.1;port=3306;dbname=tickettrade_test;charset=utf8mb4', 'user' => 'root', 'pass' => ''];`.
    - Runs `composer install --no-interaction --no-progress` if `vendor/` is missing.
    - Runs `php migrate.php` and prints the result.
    - Prints a 3-line summary: "Dev DB ready, test DB ready, migrations applied. Visit http://127.0.0.1:18001/ after `php -S 127.0.0.1:18001 -t public public/router.php`."
    - Sets `chmod +x bin/dev-setup.sh` so the executor can `./bin/dev-setup.sh`.

    **3. `docs/phase-2-substrate.md`:** a 200-300 line developer-notes document. Sections:
    - "What's in this plan" — one paragraph + the file list.
    - "Bootstrap ordering" — the exact sequence in `config/bootstrap.php` with line numbers, and why each step is where it is (response headers before body, session before Auth, Auth before CSRF).
    - "The route map" — copy the route table from `config/routes.php` with a one-line description per route; mark which routes are auth-required and which are admin-only.
    - "AD-18: bcrypt only in Auth/Service/auth_service.php" — show the grep test and how to run it; explain why the Custom sniff is Phase 9.
    - "How to add a new state-changing Action" — step-by-step: (1) add a route in `config/routes.php` with `csrf: true`, (2) write the Action with `handle` + `handlePost`, (3) call the Service, (4) return the envelope or render a View.
    - "How to add a new Support class" — step-by-step: (1) create `src/Support/Foo.php` with PSR-4 autoload (no manual require), (2) use `App\Support\Foo` from anywhere, (3) if it touches the DB, call `Support\Db::pdo()` not a new PDO.
    - "Common pitfalls" — the 5 most likely mistakes a Phase 3+ implementer will make: (1) starting a session before `ResponseHeaders::boot()` (breaks CSP), (2) calling `password_hash(` outside `auth_service.php` (AD-18 violation), (3) forgetting to update `config/routes.php` when adding an Action (results in 404, not 500), (4) using `$_SESSION['session_id']` instead of `$_COOKIE[session_name()]` in `Auth::boot()` (the cookie IS the session id; the session array doesn't carry it), (5) running `php migrate.php` against the dev DB while a PHPUnit suite is running against the test DB (the runner writes `.applied` in the project root, which is shared; use `APP_ENV=test` for the test run).

    **4. SUMMARY:** write `02-01-SUMMARY.md` per the SUMMARY template. Sections: `## What Got Built`, `## Verification Log` (paste the smoke matrix outcomes), `## Deviations from PLAN`, `## Next Steps` (hand off to Plan 02-02: "Auth/Service/auth_service.php is real, the models are skeleton, the route guards work; Plan 02-02 fills the Action bodies and the View files.").

    **5. No git commit.** The orchestrator commits after verification.
  </action>
  <verify>
    <automated>bash -c 'php migrate.php > /dev/null && vendor/bin/phpunit --testsuite=phase-2 && vendor/bin/phpcs --standard=PSR12 src/ && test -x /home/user/hermesag/004/tickettrade/bin/dev-setup.sh && test -f /home/user/hermesag/004/tickettrade/docs/phase-2-substrate.md && test -f /home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-01-SUMMARY.md && echo "ALL_OK"'</automated>
    <fails_when>
      (a) `php migrate.php` exits non-zero; (b) `vendor/bin/phpunit --testsuite=phase-2` exits non-zero; (c) `vendor/bin/phpcs --standard=PSR12 src/` reports any PSR-12 violation; (d) `bin/dev-setup.sh` is not executable; (e) `docs/phase-2-substrate.md` or `02-01-SUMMARY.md` is missing.
    </fails_when>
  </verify>
  <done>
    - `bin/dev-setup.sh` is executable and idempotent; running it on a fresh checkout sets up dev + test DBs and runs `php migrate.php`.
    - `docs/phase-2-substrate.md` exists and covers bootstrap ordering, the route map, the AD-18 rule, the "how to add" recipes, and the 5 common pitfalls.
    - `02-01-SUMMARY.md` exists and includes the verification log + deviations + hand-off to Plan 02-02.
    - `vendor/bin/phpcs --standard=PSR12 src/` is clean.
    - `vendor/bin/phpunit --testsuite=phase-2` is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Front Controller | All HTTP traffic enters through `public/index.php` or `public/admin/index.php` after the dev `public/router.php` decides which one. The front controller's only job is to dispatch via `Support\Router::dispatch($surface, $requestPath)`. No `php://input` is read directly by an Action — the Router hands off a normalized `$_SERVER['REQUEST_METHOD']` + `$path` pair. |
| Session cookie → Support\Auth | The `PHPSESSID` cookie is the only credential the Auth guard trusts. `cookie_httponly=1` blocks JS access; `cookie_samesite=Strict` blocks cross-site requests; `cookie_secure=1` (prod) blocks plaintext HTTP. `use_strict_mode=1` rejects client-supplied session IDs. |
| $_POST → Support\Csrf | The `csrf_token` hidden input is verified with `hash_equals()` against `$_SESSION['csrf_token']`. The token is per-session, not per-request (D-13). |
| IP → Support\RateLimit | The IP from `$_SERVER['REMOTE_ADDR']` is the bucket key. Behind a proxy the `X-Forwarded-For` header would be the real IP, but Phase 2 is local-only so `REMOTE_ADDR` is sufficient. The 5/5min/login counter lives in the `cache_rate` table; the `INSERT ... ON DUPLICATE KEY UPDATE` is atomic. |
| PDO → MySQL | Every query uses `Support\Db::pdo()` (request-scoped singleton). `ERRMODE_EXCEPTION` + `EMULATE_PREPARES=false` + `DEFAULT_FETCH_MODE=FETCH_ASSOC` + `utf8mb4` (AD-5 verbatim). No string interpolation in SQL (verified by `PdoOnlyTest`). |
| CLI → migrate.php | The runner reads `config/db.php` (or `config/db.test.php` when `APP_ENV=test`). The `.applied` file is the only local state; it is in `.gitignore` (per-migration-dir) so each dev's set is independent. |
| File system → 12 SVG avatars | Static webroot assets, no auth, no per-user check (D-17). The closed 1..12 set is enforced by `(int)` cast + `min/max` clamp (Pitfall 11). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Spoofing | `Support\Auth::boot()` — session ID acceptance | high | mitigate | `use_strict_mode=1` rejects unknown IDs; `session_regenerate_id(true)` on login (lands Plan 02-02) prevents fixation (Pitfall 1). |
| T-02-02 | Spoofing | `Support\Csrf::verify()` — token comparison | high | mitigate | `hash_equals()` constant-time compare; token is 64 hex chars from `random_bytes(32)`; per-session, not per-request. |
| T-02-03 | Tampering | `migrate.php` — schema changes | high | mitigate | Each file in a single transaction; `.applied` updated in the same transaction; re-running is a no-op (Pitfall 9, 14). `APP_ENV=test` switches DSN. |
| T-02-04 | Tampering | `Support\RateLimit::hit()` — counter manipulation | medium | mitigate | `INSERT ... ON DUPLICATE KEY UPDATE count = count + 1` is atomic; PK on `rate_key` prevents row spoofing; `expires_at` TTL bounds storage. |
| T-02-05 | Repudiation | Login / logout / register | medium | partial | `sessions` row carries `user_id`, `ip`, `user_agent`, `created_at`, `last_seen`; Phase 9's `audit_log` is the durable version. |
| T-02-06 | Information Disclosure | Security response headers | high | mitigate | `Support\ResponseHeaders::boot()` is called at front-controller boot before any body output; CSP string lives in `config/security_headers.php` for one-place tuning (D-20/D-21). |
| T-02-07 | Information Disclosure | `Support\Error::not_found()` / `server_error()` | medium | mitigate | Generic 404/500 copy; no stack traces in HTML; internal message logged to `error_log` only when `APP_ENV !== 'production'` (AD-16). |
| T-02-08 | Information Disclosure | `Support\View::h()` — XSS in templates | high | mitigate | `htmlspecialchars($s, ENT_QUOTES, 'UTF-8')`; every View uses `<?= View::h($s) ?>`; the partials in this plan all use the helper. |
| T-02-09 | Denial of Service | Login flood | high | mitigate | `Support\RateLimit` 5/5min/IP per NFR-SEC-007; the rate-limit error copy is "Too many attempts. Try again in 5 minutes." (D-12). |
| T-02-10 | Denial of Service | Migration runner runs during test bootstrap | medium | mitigate | `APP_ENV=test` switches DSN; PHPUnit sets `APP_ENV=test` via `<server>` in `phpunit.xml` (Pitfall 14). |
| T-02-11 | Denial of Service | `cache_rate` table growth | low | deferred | TTL cleanup via Phase 9 cron; Phase 2 ships the `expires_at` column and the `idx_cache_rate_expires` index. |
| T-02-12 | Elevation of Privilege | Non-admin hits `/admin/*` | high | mitigate | `Support\Auth::adminGuard()` returns `Support\Error::not_found()` — same page as unknown routes (D-10, AD-14 "don't reveal the resource exists"). |
| T-02-13 | Elevation of Privilege | `is_admin` flip via profile edit | high | mitigate | `auth_service::sanitizeUser()` strips `is_admin`; profile edit Service (Plan 02-02) only accepts `full_name`, `bio`, `whatsapp`, `avatar_id` in the whitelist. |
| T-02-14 | Elevation of Privilege | `is_banned` not checked on every request | high | mitigate | `Support\Auth::boot()` checks `users.is_banned = FALSE` on every authenticated request (D-06); banning a user = immediate logout across all devices with no DELETE storm. |
| T-02-15 | Tampering | Migration runner token format mismatch | medium | mitigate | `CHAR(64)` for `token_hash`; `CHAR(48)` for `session_id`; `SessionConfigTest` asserts the canonical PHP session config matches the DB column width (Pitfall 12, 13). |
| T-02-16 | Tampering | Avatar picker XSS | medium | mitigate | Closed list of 12 SVG files; `(int)` cast + clamp 1..12 in `Support\View\partials\avatar_picker.php`; no URL input (Pitfall 11). |
| T-02-17 | Tampering | `Support\RateLimit` `rate_key` overflow | low | mitigate | `VARCHAR(190)` allows 188 chars after the `route:ip:` prefix; the `Y-m-d-H-MM` bucket keeps it well under 100 chars in practice. |
| T-02-18 | Information Disclosure | CSP allows inline scripts while planning nonce hardening | medium | accept | D-21 explicitly keeps `'unsafe-inline'` for `script-src` to allow Phase 1 FOUC-guard; nonce hardening deferred to Phase 9+; documented in both `ResponseHeaders.php` and `head.php`. |
| T-02-SC | Tampering | Composer installs | high | mitigate | No new runtime deps (OPS-07); `ramsey/uuid` is the only runtime dep; phpcs/phpunit are dev-only. The `ComposerTest` greps `composer.json` to enforce. |

## ASVS L1 Compliance Notes

- **V1 Architecture:** AD-1..AD-20 read at planning time; AD-13 substrate is Phase 2's primary architectural commitment; AD-18 sole-writer rule enforced by `PasswordHashTest` grep.
- **V2 Authentication:** The `Auth\Service\auth_service.php` is the sole bcrypt writer (AD-18); the cost-12 constant lives in `config/auth.php`; no `md5(`/`sha1(`/`crypt(` outside `auth_service.php` and `Support\Crypto` (verified by `PasswordHashTest`).
- **V3 Session Management:** `use_strict_mode=1`, `sid_length=48`, `sid_bits_per_char=5`, `cookie_httponly=1`, `cookie_samesite=Strict`, `cookie_secure=1` (prod only, D-21), `gc_maxlifetime=604800`; `session_set_cookie_params` called BEFORE `session_start`; `PHP_SAPI !== 'cli'` guard prevents the migrations runner from starting a session.
- **V4 Access Control:** `Support\Auth::requireAuth()` (D-08 `?next=` bounce); `Support\Auth::adminGuard()` 404-on-non-admin for `/admin/*` (D-10); `users.is_admin` boolean.
- **V5 Input Validation:** All Actions in later plans will validate input server-side; `Support\View::h()` is the canonical escape wrapper; `Support\Csrf` is the canonical CSRF check.
- **V6 Cryptography:** `password_hash($pw, PASSWORD_BCRYPT, ['cost' => 12])`, `password_verify`, `random_bytes(32)`, `hash('sha256', $raw)`, `hash_equals()`; `Support\Crypto` is the only place raw hash primitives live.
- **V7 Error Handling:** `Support\Error::not_found()` (404), `Support\Error::method_not_allowed()` (405), `Support\Error::server_error($msg)` (500 + log); AD-16 failure envelope `{ok, data, error:{code, message, fields?}}` from `Support\Error::envelope()`.
- **V8 Data Protection:** `users.password_hash` is never passed to a View (sanitized in `auth_service::sanitizeUser`); tokens stored as `hash('sha256', $raw)` (D-03, D-07).
- **V9 Communications:** All traffic over HTTPS in prod; CSP restricts `script-src` to `'self' cdn.jsdelivr.net 'unsafe-inline'` (D-21).
- **V10 Malicious Code:** `composer.json` declares only `ramsey/uuid` at runtime; `composer.lock` is committed; `ComposerTest` enforces this in CI.
- **V11 Business Logic:** `Support\Auth::boot()` short-circuits on `is_banned`; `last_seen` is updated in a 5-min idempotency window; `Support\RateLimit` enforces the locked 5/5min/IP cap.
- **V12 Files and Resources:** Static avatar SVGs in `public/assets/img/avatars/` (D-17, webroot-served directly). `Support\ImageProxy` is deferred to Phase 3.
- **V13 API and Web Service:** No API endpoints in Phase 2; all endpoints are server-rendered PHP.
- **V14 Configuration:** `config/bootstrap.php` reads `APP_ENV`, sets `Asia/Colombo` timezone, configures session per AD-13; `config/db.php` is `.gitignored`; `config/.env.example` documents the swap.
</threat_model>

<verification>
- [ ] All 7 migration files exist at `migrations/00{1..7}_*.sql` with D-28 header comments; `migrations/.applied` is local-only (in `.gitignore` per migration-dir).
- [ ] `php migrate.php` runs all 7 files against a fresh MySQL test DB on first invocation; re-running is a no-op; `migrations/.applied` has 7 lines.
- [ ] All 8 `Support` classes (Db, Auth, Csrf, RateLimit, Crypto, ResponseHeaders, Error, View) exist with the exact APIs from the <action> section; the eval stub in `config/bootstrap.php` is DELETED.
- [ ] `config/bootstrap.php` runs in the canonical order: timezone → autoload → cookie params → session_start (cli-skipped) → ResponseHeaders::boot() → Auth::boot() → Csrf::verify().
- [ ] `config/routes.php` is populated per D-11 with 21 route entries; `admin/config/routes.php` stays empty.
- [ ] 12 SVG avatar files at `public/assets/img/avatars/avatar-{1..12}.svg`, each 200x200 viewBox, brand palette, no characters.
- [ ] `vendor/bin/phpunit --testsuite=phase-2` is green (4 integration + 5 unit = 9 tests).
- [ ] `vendor/bin/phpcs --standard=PSR12 src/` is clean.
- [ ] `bin/dev-setup.sh` is executable and idempotent; `docs/phase-2-substrate.md` exists; `02-01-SUMMARY.md` exists.
- [ ] Curl smoke matrix (root 200, /login 200, /profile 302, /admin/users 404, POST /login without CSRF 400) matches the expected codes.
- [ ] `<threat_model>` block enumerates trust boundaries and the STRIDE register with 18+ threats and ASVS L1 compliance notes.
</verification>

<success_criteria>
- [ ] All 9 success criteria in ROADMAP.md Phase 2 entry that depend on the substrate (criteria 2, 5, 8) are now verifiable end-to-end.
- [ ] The eval stub `Support\ResponseHeaders` in `config/bootstrap.php` is DELETED; the real class is autoloaded via PSR-4.
- [ ] `Support\Auth::requireAuth()` redirects unauthenticated users from `/profile`, `/settings`, `/my-tickets`, `/my-listings`, `/sales`, `/purchases` to `/login?next=...`; non-admin `/admin/*` returns 404.
- [ ] Login attempts from the same IP are rate-limited at 5/5min; the 6th attempt returns the inline `E_RATE_LIMIT` error.
- [ ] Every response (200, 302, 404, 500) carries the four AD-13 security headers.
- [ ] The `+50` points stub in Plan 02-02 can call `auth_service::tierFromPoints(50)` and receive `'D'` (Rookie); the stub is the only place outside Phase 6 that writes to `points_log` and `users.tier` (AD-10).
- [ ] `bin/dev-setup.sh` bootstraps a fresh checkout in under 60 seconds.
- [ ] The Wave 0 test suite (`tests/Unit/02` + `tests/Integration/02`) is green; `vendor/bin/phpcs --standard=PSR12 src/` is clean.
</success_criteria>

<output>
Create `/home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

Phase 2 Plan 02-01 (this plan) ships the **Phase 2 substrate layer** that every later phase (02-02, 02-03, 3..9) assumes. Concretely, the following artifacts land on disk and are exercised by the verify command:

### Database schema (7 forward-only migrations)

1. `migrations/001_initial.sql` — placeholder demonstrating the runner works
2. `migrations/002_users_auth.sql` — `users` + `student_id_allowlist` (D-01, D-02, D-05, D-06, D-15, D-18)
3. `migrations/003_sessions.sql` — `sessions` table (D-05, AD-13, Pitfall 13)
4. `migrations/004_email_verifications.sql` — `email_verifications` table (D-03, Pitfall 12)
5. `migrations/005_password_resets.sql` — `password_resets` table (D-07)
6. `migrations/006_student_id_allowlist.sql` — documentation no-op (table created in 002)
7. `migrations/007_cache_rate.sql` — `cache_rate` table (D-12, AD-13)
8. `migrations/.gitignore` — excludes `migrations/.applied` from git (D-25)

### Migrations runner

9. `migrate.php` — ~80 LOC CLI runner (D-22..D-28, AD-6); `APP_ENV=test` switches DSN (Pitfall 14); `.applied` written atomically (tempnam + rename); single transaction per file; `IF NOT EXISTS` discipline.

### Support classes (the cross-cutting layer)

10. `src/Support/Db.php` — PDO singleton per AD-5; `connectFromConfig()` helper for the migrations runner; `MYSQL_ATTR_INIT_COMMAND` sets `SET NAMES utf8mb4` on connect.
11. `src/Support/Auth.php` — session guard per D-04..D-06, AD-13; `boot()`, `requireAuth()`, `adminGuard()`, `currentUser()`, `sanitizeUser()`.
12. `src/Support/Csrf.php` — per-session token (`bin2hex(random_bytes(32))`); `token()`, `verify()`; `hash_equals()` compare.
13. `src/Support/RateLimit.php` — fixed 5-min window, `INSERT ... ON DUPLICATE KEY UPDATE` atomic check-and-increment; `hit()`.
14. `src/Support/Crypto.php` — sole home of `random_bytes` wrapper, `hash('sha256', ...)`, `hash_equals`, `hash_hmac` (AD-18, Phase 9 phpcs sniff allow-list).
15. `src/Support/ResponseHeaders.php` — REPLACES the eval stub in `config/bootstrap.php` (D-20); four AD-13 headers; CSP from `config/security_headers.php`.
16. `src/Support/Error.php` — `not_found()`, `method_not_allowed()`, `server_error()`, `envelope()` (AD-16).
17. `src/Support/View.php` — `h()` (htmlspecialchars), `render($view, $vars)`, `partial($name, $vars)`.

### Layout partials (the page chrome every Action uses)

18. `src/Support/View/layout.php` — `head` + `flash_toast` + `skip_link` + `<main>` + content View + `bottom_nav` + `toast_container`.
19. `src/Support/View/partials/head.php` — `<head>` with FOUC-guard inline script (Phase 1 D-05), Bootstrap CDN, `tickettrade.css/.js`.
20. `src/Support/View/partials/bottom_nav.php` — 5-item nav (UX-09).
21. `src/Support/View/partials/toast_container.php` — `<div data-component="toast" role="status" aria-live="polite">`.
22. `src/Support/View/partials/flash_toast.php` — server-set flash message.
23. `src/Support/View/partials/skip_link.php` — accessibility skip link (UX-08).
24. `src/Support/View/partials/avatar_picker.php` — 12-SVG grid with `(int)` cast + clamp 1..12 (D-17, Pitfall 11).
25. `src/Support/View/partials/rank_badge.php` — 6-tier inline SVG per DESIGN.md.

### Config

26. `config/db.php` — `.gitignored` PDO DSN (AD-17).
27. `config/.env.example` — env-var documentation.
28. `config/auth.php` — `bcrypt_cost=12`, `session_lifetime_seconds=604800`, `last_seen_idempotency_seconds=300` (D-04, AD-18).
29. `config/security_headers.php` — CSP string (D-20, D-21).
30. `config/rate_limits.php` — `login 5/5min, register 5/hr, forgot_password 3/hr, profile_edit 30/hr` (D-12, D-13, AD-13).
31. `config/error_codes.php` — stable error code registry (AD-16).
32. `config/ranks.php` — 6-tier ladder definition (D-23); Phase 6 expands this.
33. `config/bootstrap.php` — bootstrap ordering (D-13 sequence); eval stub DELETED.
34. `config/routes.php` — 21 student route entries (D-11).

### Static assets

35-46. `public/assets/img/avatars/avatar-{1..12}.svg` — 12 SVG files, 200x200 viewBox, brand palette, no characters (D-17).

### Front controllers + router

47. `public/index.php` — `ResponseHeaders::boot()` + `Auth::boot()` + `Csrf::verify()` (no functional change beyond the implicit boot).
48. `public/admin/index.php` — same.
49. `src/Support/Router.php` — real dispatch; `[Class, method, opts]` tuples; `auth` / `admin` / `csrf` / `rate_limit` flags; 404 via `Support\Error::not_found()`.

### Auth context (skeleton for Plan 02-02)

50. `src/Auth/Service/auth_service.php` — `hashPassword` (cost 12), `verifyPassword`, `dummyHash` (timing-attack sentinel), `randomToken`, `hashToken`, `sanitizeUser`, `tierFromPoints`, `nextRedirectIsSafe` — real bcrypt writer per AD-18; full method surface arrives in Plan 02-02.
51. `src/Auth/Model/user_model.php` — `findByEmail`, `findById`, `findByStudentId`, `insert`.
52. `src/Auth/Model/student_id_allowlist_model.php` — `findByEmail`, `findByStudentId`.
53. `src/Auth/Model/email_verification_model.php` — `insert`, `findActiveByHash`, `markUsed`.
54. `src/Auth/Model/password_reset_model.php` — same shape.
55. `src/Auth/Model/session_model.php` — `insert`, `findById`, `touch`, `delete`.
56. `src/User/Model/user_model.php` — `findByNickname`, `updateProfile` (whitelist: `full_name`, `bio`, `whatsapp`, `avatar_id`).
57. `src/Auth/View/placeholder.php` — placeholder card for stub Actions.

### 14 stub Action classes (Plan 02-02 / 02-03 fill them)

58-71. `src/Auth/Action/{Home,Login,Logout,Register,Verify,ForgotPassword,ResetPassword}Action.php`, `src/User/Action/{Profile,Settings,PublicProfile}Action.php`, `src/Listing/Action/{Browse,MyListings}Action.php`, `src/Ticket/Action/{MyTickets,Sales,Purchases}Action.php` — each has `handle`/`handlePost` returning the prep envelope or rendering the placeholder View.

### Wave 0 tests

72. `tests/Unit/02/Support/PasswordHashTest.php` — cost 12, sole-writer rule, sanitize, tierFromPoints.
73. `tests/Unit/02/Support/CsrfTest.php` — token format, `hash_equals`, verify rejects wrong.
74. `tests/Unit/02/Support/SessionConfigTest.php` — AD-13 canonical set.
75. `tests/Unit/02/Support/ComposerTest.php` — locked dep set.
76. `tests/Unit/02/Support/PdoOnlyTest.php` — no string-interpolated SQL.
77. `tests/Integration/02/Support/MigrateRunnerTest.php` — runs 7 + idempotent + .applied format.
78. `tests/Integration/02/Support/AuthGuardTest.php` — boot sets current_user; requireAuth bounces; is_banned short-circuits.
79. `tests/Integration/02/Support/RateLimitTest.php` — 5/5min/IP enforced.
80. `tests/Integration/02/Support/ResponseHeadersTest.php` — 4 headers set on every response.
81. `phpunit.xml` — `<testsuite name="phase-2">` with `<directory>tests/Unit/02</directory>` + `<directory>tests/Integration/02</directory>`; `<server name="APP_ENV" value="test"/>`.

### Documentation + tooling

82. `bin/dev-setup.sh` — executable; one-shot dev env bootstrap.
83. `docs/phase-2-substrate.md` — developer notes; bootstrap ordering; route map; AD-18 rule; "how to add" recipes; common pitfalls.
84. `02-01-SUMMARY.md` — close-out summary; verification log; deviations; hand-off to Plan 02-02.
