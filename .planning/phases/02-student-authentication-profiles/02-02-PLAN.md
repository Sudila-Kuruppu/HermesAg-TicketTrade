---
phase: 02-student-authentication-profiles
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/Auth/Action/register_action.php
  - src/Auth/Action/login_action.php
  - src/Auth/Action/logout_action.php
  - src/Auth/Action/verify_action.php
  - src/Auth/Action/forgot_password_action.php
  - src/Auth/Action/reset_password_action.php
  - src/Auth/Service/auth_service.php
  - src/Auth/Model/user_model.php
  - src/Auth/Model/student_id_allowlist_model.php
  - src/Auth/Model/email_verification_model.php
  - src/Auth/Model/password_reset_model.php
  - src/Auth/Model/session_model.php
  - src/Auth/View/register.php
  - src/Auth/View/login.php
  - src/Auth/View/forgot_password.php
  - src/Auth/View/reset_password.php
  - src/Auth/View/verify_success.php
  - src/User/Action/profile_edit_action.php
  - src/User/Action/settings_action.php
  - src/User/Service/user_service.php
  - src/User/Model/user_model.php
  - src/User/View/profile_edit.php
  - src/User/View/settings.php
  - src/Points/Service/points_service.php
  - src/Points/Model/points_log_model.php
  - src/Listing/Action/browse_action.php
  - src/Listing/View/board.php
  - src/Ticket/Action/my_tickets_action.php
  - src/Ticket/View/my_tickets.php
  - src/Ticket/Action/sales_action.php
  - src/Ticket/View/sales.php
  - src/Ticket/Action/purchases_action.php
  - src/Ticket/View/purchases.php
  - src/Listing/Action/my_listings_action.php
  - src/Listing/View/my_listings.php
  - src/Auth/View/placeholder.php
  - public/index.php
  - config/routes.php
  - config/bootstrap.php
  - tests/Integration/02/Auth/RegisterFlowTest.php
  - tests/Integration/02/Auth/LoginFlowTest.php
  - tests/Integration/02/Auth/LogoutTest.php
  - tests/Integration/02/Auth/VerifyTokenTest.php
  - tests/Integration/02/Auth/PasswordResetTest.php
  - tests/Integration/02/Auth/RegisterCsrfTest.php
  - tests/Integration/02/Support/RouteGuardTest.php
  - tests/Integration/02/Auth/SessionRefreshTest.php
  - tests/Integration/02/User/ProfileEditTest.php
  - tests/Integration/02/User/SettingsTest.php
  - tests/Unit/02/Support/LoginTimingTest.php
  - tests/Unit/02/User/ProfileEditValidationTest.php
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - PROF-01
  - PROF-04
  - SEC-01
  - SEC-02
  - SEC-06
  - SEC-08
user_setup: []

estimate:
  tokens: 85000
  raw_tokens: 42500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Submitting the `/register` form with a valid `@students.nsbm.ac.lk` email, a student ID present in `student_id_allowlist`, a unique nickname, and a password ≥ 8 chars creates a `users` row (with `is_verified=FALSE`, `points=0`, `tier='E'`, `is_admin=FALSE`, `is_banned=FALSE`, `avatar_id` randomized 1..12, `created_at`/`updated_at` set to NOW()), writes an `email_verifications` row with `token_hash`, `expires_at=NOW()+24h`, `used_at=NULL`, auto-starts a session (writes a `sessions` row, sets `PHPSESSID` cookie, calls `session_regenerate_id(true)`), and redirects to `/board` with a flash toast containing the actual `GET /verify?token=…` URL as a clickable link."
    - "Clicking the verify link in the flash toast consumes the token (`UPDATE email_verifications SET used_at = NOW() WHERE id = ? AND used_at IS NULL`, `rowCount() === 1`), sets `users.is_verified = TRUE`, writes a `points_log` row with `delta = 50`, `event_uuid` UUID v7, `reference_type = 'email_verification'`, `reference_id = users.user_id`, `balance_after = 50`, recomputes `users.tier` from `config/ranks.php` (50 → 'D'), and renders a Bootstrap modal 'Email verified! +50 points' with a 'Continue to board' CTA that closes the modal and navigates to `/board`."
    - "Submitting `/login` with a correct email + password starts a session (writes a `sessions` row with `last_seen = NOW()`, `ip = REMOTE_ADDR`, `user_agent = …`, calls `session_regenerate_id(true)`), and redirects to `$next` if `nextRedirectIsSafe($next)` is true, else `/board`. Submitting with a wrong password renders the login View with an inline `alert alert-danger` containing the exact copy 'Email or password is incorrect.' (D-12); the inline error is rendered inside the form, NOT in the flash-toast container."
    - "Submitting `/login` 6 times in 5 minutes from the same IP returns the inline 'Too many attempts. Try again in 5 minutes.' error on the 6th attempt (D-12, NFR-SEC-007); the 7th attempt within the window returns the same error. The constant-time `password_verify` sentinel hash from `auth_service::dummyHash()` makes the missing-user case take the same wall-clock time as the wrong-password case (Pitfall 3; the unit test asserts the difference is < 5ms over 100 iterations)."
    - "POSTing `/logout` deletes the `sessions` row for the matching `(session_id, user_id)` (D-05) and redirects to `/`; the next request to a private route returns 302 to `/login` because the session row no longer exists. Calling `session_regenerate_id(true)` is not required for logout (D-05 is DELETE-based), but `session_destroy()` and `setcookie(session_name(), '', time() - 3600, '/')` clear the cookie."
    - "Submitting `/forgot-password` with a registered email always shows the same toast 'If that email is registered, a reset link is in your inbox.' (D-07 anti-enumeration); the actual reset link is generated and stored in `password_resets` with `token_hash`, `expires_at=NOW()+24h`, `used_at=NULL`, but the link is NEVER surfaced in the UI. In dev (D-02-style simulation), an `error_log('[dev-reset-link] ...')` line writes the raw token to the dev log for manual testing; the production path does not. Submitting with a non-registered email shows the same toast and writes no `password_resets` row."
    - "Submitting `/reset-password?token=…` with a valid unexpired token shows a password form (password + confirm, both ≥ 8 chars and matching). Submitting the form updates `users.password_hash` through `auth_service::hashPassword()` (cost 12), marks the `password_resets` row `used_at = NOW()`, starts a session for that user, and redirects to `/board`. Re-using the same token returns 400 + `E_TOKEN_INVALID` (the row is already used). Submitting an expired token (>24h) returns 400 + `E_TOKEN_INVALID`."
    - "Submitting `/profile` POST with `full_name`, `bio`, `whatsapp` (`^(\\+94|0)7[0-9]{8}$`), and `avatar_id` (1..12, clamped) updates the user's row; submitting an invalid WhatsApp returns the form with the inline field error. The `nickname`, `is_admin`, `is_banned`, `points`, `tier` fields are NOT editable — `user_service::updateProfile()` accepts only the 4 whitelisted fields (D-15, T-02-20, T-02-27). The profile edit form does NOT show a `nickname` field (D-15)."
    - "GETting `/settings` shows the theme toggle (Light / Dark / System radios, current value from `localStorage.tickettrade.theme` or `data-surface` default) and a 'Log out' button (destructive-styled `btn-outline-danger`). POSTing `/settings` is a no-op for theme (theme lives in localStorage per Phase 1 D-07); POSTing the logout button posts to `POST /logout`."
    - "GETting a private route (`/my-tickets`, `/my-listings`, `/sales`, `/purchases`) without a session returns 302 to `/login?next=/my-tickets`; GETting with a valid session returns 200 with a placeholder page (the actual tables land in Phases 4/5; this plan ships the route guards + a 'coming soon' card so the nav links don't 404)."
  artifacts:
    - src/Auth/Action/register_action.php
    - src/Auth/Action/login_action.php
    - src/Auth/Action/logout_action.php
    - src/Auth/Action/verify_action.php
    - src/Auth/Action/forgot_password_action.php
    - src/Auth/Action/reset_password_action.php
    - src/Auth/Service/auth_service.php
    - src/Auth/Model/user_model.php
    - src/Auth/Model/student_id_allowlist_model.php
    - src/Auth/Model/email_verification_model.php
    - src/Auth/Model/password_reset_model.php
    - src/Auth/Model/session_model.php
    - src/Auth/View/register.php
    - src/Auth/View/login.php
    - src/Auth/View/forgot_password.php
    - src/Auth/View/reset_password.php
    - src/Auth/View/verify_success.php
    - src/User/Action/profile_edit_action.php
    - src/User/Action/settings_action.php
    - src/User/Service/user_service.php
    - src/User/Model/user_model.php
    - src/User/View/profile_edit.php
    - src/User/View/settings.php
    - src/Points/Service/points_service.php
    - src/Points/Model/points_log_model.php
    - src/Listing/Action/browse_action.php
    - src/Listing/View/board.php
    - src/Ticket/Action/my_tickets_action.php
    - src/Ticket/View/my_tickets.php
    - src/Ticket/Action/sales_action.php
    - src/Ticket/View/sales.php
    - src/Ticket/Action/purchases_action.php
    - src/Ticket/View/purchases.php
    - src/Listing/Action/my_listings_action.php
    - src/Listing/View/my_listings.php
    - tests/Integration/02/Auth/RegisterFlowTest.php
    - tests/Integration/02/Auth/LoginFlowTest.php
    - tests/Integration/02/Auth/LogoutTest.php
    - tests/Integration/02/Auth/VerifyTokenTest.php
    - tests/Integration/02/Auth/PasswordResetTest.php
    - tests/Integration/02/Auth/RegisterCsrfTest.php
    - tests/Integration/02/Support/RouteGuardTest.php
    - tests/Integration/02/Auth/SessionRefreshTest.php
    - tests/Integration/02/User/ProfileEditTest.php
    - tests/Integration/02/User/SettingsTest.php
    - tests/Unit/02/Support/LoginTimingTest.php
    - tests/Unit/02/User/ProfileEditValidationTest.php
  key_links:
    - "`Auth\\Service\\auth_service.php` is the SOLE caller of `password_hash(` and `password_verify(` (AD-18). All other Services / Models go through `auth_service::hashPassword(plain)` to obtain a hash and `auth_service::verifyPassword(plain, hash)` to check one. The `PasswordHashTest::test_no_password_hash_outside_auth_service` grep test (from Plan 02-01) still passes after this plan lands."
    - "`Auth\\Service\\auth_service::register()` writes a `users` row + an `email_verifications` row + auto-starts a session, all in one transaction. The session row's `session_id` is the value `session_id()` (PHP function) returns AFTER `session_regenerate_id(true)`. The transaction ensures that a partial registration (users row without session, or session without users) is impossible."
    - "`Points\\Service\\points_service::awardVerificationBonus($userId)` opens a transaction, inserts a `points_log` row with `event_uuid = Uuid::uuid7()->toString()` (the canonical UUID v7 primitive from `ramsey/uuid`), updates `users.points = points + 50` and `users.tier = auth_service::tierFromPoints(points + 50)`, and commits. If either statement fails, the whole thing rolls back (Pitfall 16, AD-10). The stub signature is the Phase 6 contract; Phase 6's real `awardVerificationBonus` has the same shape."
    - "`Support\\Auth::requireAuth()` runs from `Router::dispatch` BEFORE the Action is invoked (Plan 02-01's bootstrap). For private routes (`auth: true` in `config/routes.php`), an unauthenticated user is redirected to `/login?next=...` and the Action never runs. For admin routes, `adminGuard()` returns 404 BEFORE consulting `?next=` (D-10 + Pitfall 10)."
    - "`User\\Service\\user_service::updateProfile($userId, $fields)` accepts ONLY `['full_name', 'bio', 'whatsapp', 'avatar_id']`; any other key in `$fields` is silently dropped (whitelist at the Service layer, not the View). The WhatsApp regex `^(\\+94|0)7[0-9]{8}$` is enforced inside the Service via `validateWhatsApp()` (SEC-08). The avatar_id is `(int)` cast and clamped 1..12 before the UPDATE (Pitfall 11)."
    - "`Auth\\Service\\auth_service::login($email, $password)` calls `Support\\Auth::findUserForLogin($email)` to get the user row OR null, then ALWAYS runs `password_verify($password, $user['password_hash'] ?? auth_service::dummyHash())` regardless of whether the user exists. The dummy hash is computed once (static cache) and reused — the timing attack vector is closed (Pitfall 3, T-02-01)."
    - "`Auth\\Service\\auth_service::issueEmailVerification($userId)` and `auth_service::issuePasswordReset($userId)` both generate `bin2hex(random_bytes(32))` (64 hex chars via `Support\\Crypto::randomToken()`), store `hash('sha256', $raw)` (64 hex chars via `Support\\Crypto::hashToken()`), and return the raw token to the caller. The caller (Action) puts the raw token in the flash toast for dev simulation. `auth_service::consumeEmailVerification($raw)` and `consumePasswordReset($raw)` SELECT + UPDATE in the same transaction with the `WHERE used_at IS NULL` guard (D-03, D-07, T-02-25)."
---

<objective>
Land the visible Phase 2 user flows: register (with simulated email verification), login, logout, forgot-password, reset-password, verify, profile edit, and settings. The 14 stub Actions from Plan 02-01 are replaced with real bodies. The `Auth/Service/auth_service.php` becomes the full method surface (sole bcrypt writer, sole session lifecycle, sole token lifecycle). The `Points/Service/points_service.php` stub lands so the verify action can call it. The `User/Service/user_service.php` lands for non-password profile fields. The placeholder Views for `/board`, `/my-tickets`, `/my-listings`, `/sales`, `/purchases` are filled with stub pages that exercise the auth-required route guards (Phase 3/4/5 fill the real listings/tickets data).

Purpose: a student can register, verify their email (via the dev flash-toast link), log in, edit their profile, change the theme on /settings, and log out. The `+50` points are awarded on verify. The rate-limit on login fires after 5 attempts in 5 minutes. The forgot-password flow is the same anti-enumeration shape. Every state-changing endpoint is behind CSRF. Every authenticated endpoint redirects to `/login?next=…` when the session is gone. This is the user-facing capability that closes the Phase 2 success criteria 1..6 from the ROADMAP.

Output: green `vendor/bin/phpunit --testsuite=phase-2`; an end-to-end `php -S 127.0.0.1:18001 -t public public/router.php` session that can register, verify, log in, edit profile, change settings, and log out, all from a real browser or curl.
</objective>

<execution_context>
@/home/user/hermesag/004/.opencode/gsd-core/workflows/execute-plan.md
@/home/user/hermesag/004/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/user/hermesag/004/tickettrade/AGENTS.md
@/home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md (AD-2 bounded contexts, AD-5 PDO, AD-10 points log, AD-13 auth/csrf/rate-limit, AD-16 failure envelope, AD-18 bcrypt sole writer)
@/home/user/hermesag/004/tickettrade/DESIGN.md (form input tokens, alert-danger for inline errors, success modal pattern)
@/home/user/hermesag/004/tickettrade/EXPERIENCE.md (Login state pattern: cold load / wrong credentials / rate-limited; Register state pattern; voice and tone for error copy)
@/home/user/hermesag/004/tickettrade/composer.json
@/home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-01-PLAN.md (substrate; this plan extends the stub Actions, fills the stub Service, adds the new files)
@/home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-01-SUMMARY.md (deviations + how the substrate actually shipped)
@/home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-CONTEXT.md (D-01..D-28 verbatim; D-12 inline error, D-13 anti-enumeration, D-15 nickname locked, D-19 random avatar 1..12, D-23 stub +50, agent's Discretion login form layout, register form anti-enumeration, rate-limit shape, settings scope)
@/home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-RESEARCH.md (## Architecture Patterns — register/login/logout flow shapes; ## Common Pitfalls 1, 2, 3, 4, 5, 6, 7, 11, 15, 16; ## Security Domain T-2-01..T-2-28; ## Validation Architecture — Phase Requirements → Test Map; ## Assumptions Log A2, A3, A4, A8, A9, A10, A11, A12, A13, A14, A15, A16, A17; ## Open Questions Q1, Q2, Q3, Q4, Q5, Q6, Q7, Q8)
@/home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-VALIDATION.md (per-task verify map: 02-02-01..02-02-07 → RegisterFlowTest, RegisterCsrfTest, LoginFlowTest, LogoutTest, RouteGuardTest, ProfileEditTest, VerifyTokenTest, PasswordResetTest; sampling continuity)
@/home/user/hermesag/004/tickettrade/.planning/REQUIREMENTS.md (AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, PROF-01, PROF-04, SEC-01, SEC-02, SEC-06, SEC-08)
@/home/user/hermesag/004/tickettrade/.planning/ROADMAP.md (Phase 2 success criteria 1..6)
@/home/user/hermesag/004/tickettrade/.gitignore
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Register + verify end-to-end tracer — from form to /board with the +50 stub</name>
  <files>
    src/Auth/Service/auth_service.php
    src/Auth/Action/register_action.php
    src/Auth/Action/verify_action.php
    src/Auth/Action/home_action.php
    src/Auth/View/register.php
    src/Auth/View/verify_success.php
    src/Points/Service/points_service.php
    src/Points/Model/points_log_model.php
    src/Auth/Model/email_verification_model.php
    src/Auth/Model/student_id_allowlist_model.php
    src/Auth/Model/user_model.php
    src/Auth/Model/session_model.php
    src/Listing/Action/browse_action.php
    src/Listing/View/board.php
    public/index.php
    config/bootstrap.php
    config/routes.php
    tests/Integration/02/Auth/RegisterFlowTest.php
    tests/Integration/02/Auth/VerifyTokenTest.php
    tests/Integration/02/Auth/RegisterCsrfTest.php
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/AGENTS.md
    - /home/user/hermesag/004/tickettrade/ARCHITECTURE-SPINE.md (AD-2 contexts; AD-5 PDO; AD-10 points sole writer; AD-13 auth/session shape; AD-16 failure envelope; AD-18 bcrypt sole writer; Conventions table)
    - /home/user/hermesag/004/tickettrade/EXPERIENCE.md (Login state pattern: cold / wrong credentials / rate-limited; Register state pattern: cold / email not @students.nsbm / student ID not in allowlist / nickname taken; voice and tone: no apology before the error, no technical detail in user-facing copy)
    - /home/user/hermesag/004/tickettrade/composer.json
    - /home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-CONTEXT.md (D-01 student_id_allowlist empty; D-02 register auto-logs-in + flash toast with verify URL; D-03 email_verifications table with token_hash/expires_at/used_at; D-04 7-day session; D-05 DB-backed sessions; D-06 is_banned short-circuit; D-13 anti-enumeration error copy; D-15 nickname locked; D-19 random avatar 1..12; D-23 +50 stub signature; agent's Discretion: login form layout, rate-limit shape, +50 stub signature; OQ-A2, A3, A6, A8, A15, A16, A17)
    - /home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-RESEARCH.md (## Architecture Patterns — register/login/logout flow, error_codes registry, route table; ## Common Pitfalls 1, 2, 3, 6, 7, 11, 12, 15, 16; ## Security Domain T-2-01..T-2-28; ## Validation Architecture — Phase Requirements → Test Map; ## Open Questions Q2, Q3, Q4, Q6, Q7)
    - /home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-VALIDATION.md (per-task verify map: 02-02-01 AUTH-01 RegisterFlowTest, 02-02-02 AUTH-01+SEC-02 RegisterCsrfTest, 02-02-06 AUTH-01+SEC-02 VerifyTokenTest)
    - /home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-01-PLAN.md (the substrate: Support\Auth boot, Support\Csrf verify, Support\RateLimit hit, the 7 migration tables, the route map, the View::render / View::h helpers, the layout.php + 7 partials, the auth_service stub from Task 2)
    - /home/user/hermesag/004/tickettrade/config/error_codes.php
    - /home/user/hermesag/004/tickettrade/config/ranks.php
    - /home/user/hermesag/004/tickettrade/config/routes.php
    - /home/user/hermesag/004/tickettrade/config/bootstrap.php
  </read_first>
  <behavior>
    - Submitting `/register` with email `alice@students.nsbm.ac.lk` (after the test seeds a `student_id_allowlist` row for that email + student ID `NSBM/2024/001`), student ID `NSBM/2024/001`, nickname `alice`, password `correcthorse` creates a `users` row with all the fields above, writes an `email_verifications` row with `token_hash = hash('sha256', $rawToken)`, `expires_at = NOW() + 24h`, `used_at = NULL`, auto-starts a session (writes a `sessions` row, sets `PHPSESSID` cookie, calls `session_regenerate_id(true)`), sets `$GLOBALS['current_user']`, sets `$GLOBALS['_tt_flash_toast']` containing the raw `/verify?token=…` URL, and redirects to `/board` with the flash toast.
    - The `/board` View renders a placeholder "Welcome to the board, @alice" + the flash toast (visible to the user as a clickable link).
    - Clicking the verify link in the toast (or `curl GET /verify?token=…` with the raw token) consumes the token: `UPDATE email_verifications SET used_at = NOW() WHERE id = ? AND used_at IS NULL` returns `rowCount() === 1`; `UPDATE users SET is_verified = TRUE WHERE user_id = ?` succeeds; the `Points/Service/points_service::awardVerificationBonus($userId)` stub runs: BEGIN; INSERT INTO points_log (event_uuid via `Uuid::uuid7()`, reference_type='email_verification', reference_id=$userId, delta=50, balance_after=50, event_at=NOW()); UPDATE users SET points=50, tier='D' WHERE user_id=$userId; COMMIT. The response is HTML containing the Bootstrap modal markup with the title "Email verified! +50 points", the user's nickname, and a "Continue to board" CTA button.
    - Re-visiting `/verify?token=…` with the same token returns 400 + `E_TOKEN_INVALID` envelope (the row is `used_at != NULL`).
    - Visiting `/verify?token=invalidtoken` returns 400 + `E_TOKEN_INVALID` envelope.
    - Submitting `/register` with email `evil@example.com` (not `@students.nsbm.ac.lk`) returns the form with the field-level error "Use your `@students.nsbm.ac.lk` email" on the email field (D-13 format-only branch).
    - Submitting `/register` with a valid-format email `bob@students.nsbm.ac.lk` but no matching row in `student_id_allowlist` returns the form with the combined error "Email or student ID not recognized. Check both and try again." (D-13 anti-enumeration).
    - Submitting `/register` with the same email as an existing `users` row returns the same combined error (D-13 — don't leak which field is wrong).
    - Submitting `/register` with a nickname that already exists in `users.nickname` returns the form with the field-level error "Nickname taken. Pick another." (D-13 — nickname uniqueness is public).
    - Submitting `/register` with a password < 8 chars returns the form with the field-level error "Password must be at least 8 characters." (D-13 field-level + AUTH-04 + OQ-6).
    - Submitting `/register` without a `csrf_token` field returns 400 + `E_CSRF` (the boot-time `Support\Csrf::verify()` catches it; the Action never runs).
  </behavior>
  <action>
    Land the register + verify tracer slice end-to-end. This is the thinnest user-visible capability that exercises every layer: Service (auth_service + points_service stub), Model (user_model, student_id_allowlist_model, email_verification_model, session_model, points_log_model), Action (register, verify, home, browse), View (register form, verify success modal, board placeholder), and the route map. The `+50` points stub is exercised; the auto-login + flash toast with the verify URL is exercised; the one-shot token consumption is exercised.

    **1. `src/Auth/Service/auth_service.php` (full method surface):** the stub from Plan 02-01 Task 2 is extended. The new methods (all `public static`):

    - `register(string $email, string $password, string $nickname, string $studentId, string $fullName, ?int $avatarId = null): array` — returns `['ok' => true, 'user_id' => $id, 'verify_token' => $raw]` on success, `['ok' => false, 'error' => ['code' => 'E_AUTH_ALLOWLIST', 'message' => '...']]` on allowlist miss / duplicate email, `['ok' => false, 'error' => ['code' => 'E_NICKNAME_TAKEN', ...]]` on duplicate nickname, `['ok' => false, 'error' => ['code' => 'E_PASSWORD_WEAK', ...]]` on weak password. Inside: validates `strlen($password) >= 8` (AUTH-04), validates `filter_var($email, FILTER_VALIDATE_EMAIL) && str_ends_with(strtolower($email), '@students.nsbm.ac.lk')` (D-13 format), validates the email + studentId pair against `student_id_allowlist` via `Support\Auth::findUserForLogin` helper (or a direct `student_id_allowlist_model::findByEmail`), checks `users` for duplicate email, validates the requested nickname is NOT in `RESERVED_NICKNAMES` (loaded via `require APP_ROOT . '/config/reserved_nicknames.php'`; case-insensitive match via `strtolower($nickname)`); on hit returns `['ok' => false, 'error' => ['code' => 'E_VALIDATION', 'message' => 'Nickname reserved. Pick another.', 'field' => 'nickname']]` (per RESEARCH.md ## Security Domain Q4, anti-squatting for admin/nsbm/support/system/root/moderator/mod/staff/faculty/help), checks `users` for duplicate nickname, picks `avatar_id = $avatarId ?? random_int(1, 12)` (D-19), opens a PDO transaction: INSERT users row with `password_hash = self::hashPassword($password)` + INSERT email_verifications row with `token_hash = self::hashToken($rawToken)` + `expires_at = NOW() + INTERVAL 24 HOUR` + COMMIT. Returns `['ok' => true, 'user_id' => $id, 'verify_token' => $raw]`. The raw token is returned to the caller (the Action puts it in the flash toast per D-02).
    - `verifyEmail(string $rawToken): array` — looks up `email_verifications` by `token_hash = self::hashToken($rawToken) AND used_at IS NULL AND expires_at > NOW()`; if not found, returns `['ok' => false, 'error' => ['code' => 'E_TOKEN_INVALID', ...]]`; if found, opens a transaction: UPDATE email_verifications SET used_at = NOW() WHERE id = ? AND used_at IS NULL (asserts `rowCount() === 1`); UPDATE users SET is_verified = TRUE WHERE user_id = ?; calls `Points\Service\points_service::awardVerificationBonus($userId)` (the stub); COMMIT. Returns `['ok' => true, 'user_id' => $userId, 'nickname' => $nickname]`.
    - `startSession(int $userId): void` — generates a 48-char session ID via `session_id()` AFTER `session_regenerate_id(true)`, inserts a `sessions` row with `session_id, user_id, last_seen = NOW(), ip = inet_pton($_SERVER['REMOTE_ADDR'] ?? '127.0.0.1'), user_agent = substr($_SERVER['HTTP_USER_AGENT'] ?? '', 0, 255)`, and sets `$GLOBALS['current_user']` from the `users` row. Called by `register()` (auto-login) and `login()` (on success) and `resetPassword()` (on success).
    - `updateLastSeen(string $sessionId): void` — UPDATE sessions SET last_seen = NOW() WHERE session_id = ?. Called from `Support\Auth::boot()` for the 5-min idempotency window (already wired in Plan 02-01).
    - `endSession(string $sessionId, int $userId): void` — `DELETE FROM sessions WHERE session_id = ? AND user_id = ?` (D-05 exact query); called from `logoutAction`. Also calls `session_destroy()` and `setcookie(session_name(), '', time() - 3600, '/')` to clear the cookie.

    The `auth_service` also gets `findUserForLogin(string $email): ?array` (used by `login()`) which normalizes email to lowercase + trim and calls `user_model::findByEmail`. The `login()` method:

    - `login(string $email, string $password): array` — returns `['ok' => true, 'user_id' => $id]` on success or `['ok' => false, 'error' => ['code' => 'E_AUTH_INVALID', 'message' => 'Email or password is incorrect.']]` on any failure. The implementation: `$user = self::findUserForLogin($email); $hash = $user['password_hash'] ?? self::dummyHash(); $verified = self::verifyPassword($password, $hash); if ($verified && $user !== null) { self::startSession((int) $user['user_id']); return ['ok' => true, ...]; } return ['ok' => false, 'error' => ['code' => 'E_AUTH_INVALID', 'message' => 'Email or password is incorrect.']];` — the constant-time sentinel ensures the missing-user case runs `password_verify` against a real bcrypt hash (Pitfall 3).

    The `auth_service` also gets `requestPasswordReset(string $email): array` (always returns `['ok' => true, 'token' => $rawTokenOrNull]`) — for anti-enumeration, the function does NOT return whether the email exists. If the email exists, it generates a token and inserts a `password_resets` row; the raw token is returned only when `getenv('APP_ENV') !== 'production'` (dev simulation per D-07). In prod, the function returns `['ok' => true, 'token' => null]` for both branches. The Action never surfaces the token to the user; it sets a flash toast with the same copy "If that email is registered, a reset link is in your inbox." regardless. In dev, the Action additionally writes `error_log('[dev-reset-link] /reset-password?token=... for ' . $email)` for manual testing (the answer to OQ-7).

    The `auth_service` also gets `consumePasswordReset(string $rawToken, string $newPassword): array` — same shape as `verifyEmail`: SELECT WHERE token_hash = ? AND used_at IS NULL AND expires_at > NOW(); if not found, return `E_TOKEN_INVALID`; otherwise UPDATE used_at, UPDATE users.password_hash, startSession, return success.

    **2. `src/Auth/Model/email_verification_model.php` (real implementation of Task 1's skeleton):**
    - `public static function insert(PDO $pdo, int $userId, string $tokenHash, string $expiresAt): int` — INSERT, return `lastInsertId()`.
    - `public static function findActiveByHash(PDO $pdo, string $tokenHash): ?array` — SELECT WHERE token_hash = ? AND used_at IS NULL AND expires_at > NOW(); return row or null.
    - `public static function markUsed(PDO $pdo, int $id): bool` — UPDATE WHERE id = ? AND used_at IS NULL; return `rowCount() === 1`.
    - `public static function findByUserId(PDO $pdo, int $userId, bool $onlyActive = true): ?array` — for the test's "did we issue a token" check.

    **3. `src/Auth/Model/student_id_allowlist_model.php` (real):**
    - `findByEmail(PDO $pdo, string $email): ?array` — lookup by normalized email.
    - `findByStudentId(PDO $pdo, string $studentId): ?array` — lookup by normalized student ID.

    **4. `src/Auth/Model/user_model.php` (real):**
    - `findByEmail(PDO $pdo, string $email): ?array` — normalized lookup.
    - `findById(PDO $pdo, int $userId): ?array`.
    - `findByStudentId(PDO $pdo, string $studentId): ?array`.
    - `findByNickname(PDO $pdo, string $nickname): ?array` — case-insensitive match (`LOWER(nickname) = LOWER(?)`); preserves the original case in the returned row.
    - `insert(PDO $pdo, array $data): int` — accepts `['email', 'student_id', 'nickname', 'password_hash', 'full_name', 'avatar_id']`; sets `created_at`/`updated_at` to NOW(); returns the new `user_id`.

    **5. `src/Auth/Model/session_model.php` (real):**
    - `insert(PDO $pdo, string $sessionId, int $userId, ?string $ip, ?string $userAgent): int`.
    - `findById(PDO $pdo, string $sessionId): ?array`.
    - `touch(PDO $pdo, string $sessionId): bool` — UPDATE last_seen = NOW().
    - `delete(PDO $pdo, string $sessionId, int $userId): bool` — DELETE WHERE session_id = ? AND user_id = ?; returns `rowCount() === 1`.

    **6. `src/Points/Service/points_service.php` (STUB, signature matches Phase 6):**
    - The class has one method: `public static function awardVerificationBonus(int $userId): array`. It opens a transaction, generates `event_uuid = \Ramsey\Uuid\Uuid::uuid7()->toString()`, INSERTs a `points_log` row (`user_id, delta=50, reference_type='email_verification', reference_id=$userId, balance_after = 50, event_uuid, event_at=NOW()`), UPDATEs `users SET points = 50, tier = 'D' WHERE user_id = ?` (in Phase 2, the verify bonus is always 50; Phase 6 generalizes), COMMITs. Returns `['ok' => true, 'event_uuid' => $uuid]`. The `tier = 'D'` literal is a Phase 2 simplification (the +50 always moves a new user from E to D); Phase 6 will call `auth_service::tierFromPoints($newPoints)` instead. The stub is the only writer of `points_log` and the only updater of `users.points` and `users.tier` outside Phase 6 (AD-10 sole-writer rule).

    **7. `src/Points/Model/points_log_model.php` (STUB, just the insert helper):**
    - `insert(PDO $pdo, int $userId, int $delta, string $referenceType, ?int $referenceId, int $balanceAfter, string $eventUuid, ?string $metadataJson = null): int` — INSERT, return `lastInsertId()`.

    **8. `src/Auth/Action/register_action.php`:**
    - Class `App\Auth\Action\RegisterAction` with `handle()` (GET renders the form) and `handlePost()` (validates + calls `auth_service::register` + auto-login + flash toast + redirect).
    - `handle()`: if `Support\Auth::currentUser() !== null` (already logged in), redirect to `/board`. Otherwise `Support\View::render('Auth/View/register.php', ['csrf_token' => Support\Csrf::token(), 'values' => $_POST ?? [], 'error' => $GLOBALS['_tt_form_error'] ?? null])`.
    - `handlePost()`: validate `$_POST` has `email, student_id, nickname, password, full_name`; the field-level validation runs FIRST (D-13 anti-enumeration: format errors are public; allowlist/duplicate errors are combined):
      - Email format: `filter_var($email, FILTER_VALIDATE_EMAIL) && str_ends_with(strtolower($email), '@students.nsbm.ac.lk')`; on miss, set `$GLOBALS['_tt_form_error'] = ['code' => 'E_VALIDATION', 'message' => 'Use your `@students.nsbm.ac.lk` email.', 'fields' => ['email' => '...']]` and re-render.
      - Password length: `strlen($password) >= 8`; on miss, set field-level error and re-render.
      - Other fields (student_id, nickname, full_name) — empty / too short (nickname `[A-Za-z0-9_]{3,30}` per OQ-3): set field-level error and re-render.
      - The combined check (D-13): call `auth_service::register(...)`. On `E_AUTH_ALLOWLIST` error, set `$GLOBALS['_tt_form_error'] = ['code' => 'E_AUTH_ALLOWLIST', 'message' => 'Email or student ID not recognized. Check both and try again.', 'fields' => null]`. On `E_NICKNAME_TAKEN`, set `fields => ['nickname' => 'Nickname taken. Pick another.']` (nickname uniqueness is public). On success, the Service has already inserted the `users` + `email_verifications` rows; the Action now calls `auth_service::startSession($userId)` to auto-login (D-02), sets `$GLOBALS['_tt_flash_toast'] = ['type' => 'success', 'message' => 'Account created. Verification link: <a href="/verify?token=' . $verifyToken . '">Click to verify</a>']` (the URL is the actual `GET /verify?token=…` per D-02 dev simulation), and `header('Location: /board'); exit;`.

    **9. `src/Auth/Action/verify_action.php`:**
    - Class with `handle()` (GET reads `$_GET['token']`, calls `auth_service::verifyEmail`, renders the success modal or the error page).
    - On `E_TOKEN_INVALID`: render the layout + a card with "Verification link is invalid or expired. Try registering again or contact support." + a link to `/register`. HTTP 400.
    - On success: render the layout + the `Auth/View/verify_success.php` partial, which is a centered Bootstrap modal (`max-width: 600px`) with the checkmark icon, "Email verified! +50 points" heading, the user's nickname as a subtitle, the new tier badge (rendered via `Support\View::partial('rank_badge.php', ['tier' => 'D'])`), and a "Continue to board" link that closes the modal and navigates to `/board`. The modal JS (`window.TicketTrade.toast.show(...)` from Phase 1 + a Bootstrap modal auto-open hook) handles the close.

    **10. `src/Auth/View/register.php`:** the form template. Centered card, max-width 480px, fields: email (with `@students.nsbm.ac.lk` hint below), student ID (with `NSBM/2024/001` format hint), full name, nickname, password (with "8+ characters" hint). "Register" primary button at the bottom. The field-level error renders under each input via `<div class="invalid-feedback d-block">…</div>`; the combined anti-enumeration error renders as an `alert alert-danger` at the top of the form (not a toast). The CSRF token is a hidden input `<input type="hidden" name="csrf_token" value="<?= View::h($csrf_token) ?>">`. The form posts to `/register`.

    **11. `src/Auth/View/verify_success.php`:** the centered Bootstrap modal markup. Uses `data-component="modal"` so the existing Phase 1 JS auto-opens. Includes the rank badge partial.

    **12. `src/Listing/Action/browse_action.php` + `src/Listing/View/board.php`:** the `/board` route (public-browse per D-09). For Phase 2, the View is a placeholder: a centered card with "Welcome to the board, @<?= View::h($currentUserNickname) ?>" if logged in, or "Welcome, guest" otherwise, plus the flash-toast container (which renders the verify-link toast on the post-register redirect). The action also renders a list of `Sign in to buy` placeholder cards (D-09 — no real listings data yet, but the UI shape is in place). A simple "Coming soon — Phase 3 fills this" subhead explains the placeholder.

    **13. `src/Auth/Action/home_action.php`:** the `/` route. Renders a stub landing page (the Phase 1 `Support\View\landing.php` stays as a fallback when this Action is missing). The new Action renders a "TicketTrade — verified NSBM student marketplace" hero, "Get Started" CTA → `/register`, "Sign In" CTA → `/login`. Phase 3 replaces this with the real landing.

    **14. Route map (`config/routes.php`):** the Plan 02-01 map is extended. The new routes are: `GET /verify` (already there, now points to the real `VerifyAction`), and `POST /verify` if you choose to support form-based verify (no — the verify is GET-only, the link in the email is the form). No new routes; the existing `register, verify, board, home` entries now point to real classes.

    **15. Public front controller (`public/index.php`):** the Phase 1 controller stays; it already calls `ResponseHeaders::boot()`, `Auth::boot()`, and `Csrf::verify()`. The new `home_action` and `browse_action` render via `Support\View::render(...)`. The 500-error envelope stays.

    **16. Bootstrap (`config/bootstrap.php`):** one addition — after `Auth::boot()` runs, the flash-toast payload is read from `$_SESSION['_tt_flash_toast'] ?? null` and copied into `$GLOBALS['_tt_flash_toast']`, then `unset($_SESSION['_tt_flash_toast'])`. The session is the carrier so the flash survives a 302 redirect (D-02 + D-07). The `Support\View\partials\flash_toast.php` partial reads `$GLOBALS['_tt_flash_toast']` and emits the `<div data-flash-toast="…">` markup; the Phase 1 JS reads the attribute on `DOMContentLoaded` and calls `window.TicketTrade.toast.show(...)`.

    **17. Wave 0 tests:**
    - `tests/Integration/02/Auth/RegisterFlowTest.php` — extends `Phase2IntegrationTestCase` (defined in Plan 02-01 Task 1's fixtures). In `setUp()`: `freshSchema()`; `student_id_allowlist_model::insert` for one demo user. In each test: simulates a POST to `/register` with the seeded data; asserts the DB row counts (1 user, 1 email_verifications with `used_at=NULL`); asserts the session cookie is set; asserts the response is a 302 to `/board`. Five test methods: `test_register_creates_user_with_allowlist_match`, `test_register_combined_error_on_allowlist_miss`, `test_register_combined_error_on_student_id_miss`, `test_register_combined_error_on_duplicate_email`, `test_register_nickname_taken_error`, `test_register_password_min_length_error`, `test_register_email_format_error`. The HTTP layer is exercised via the dev server OR a programmatic `App\Support\Router::dispatch` invocation with a `$_SERVER` superglobal set.
    - `tests/Integration/02/Auth/RegisterCsrfTest.php` — asserts a POST without `csrf_token` returns 400 + `E_CSRF` envelope (the `Csrf::verify()` boot-time check catches it; no DB writes).
    - `tests/Integration/02/Auth/VerifyTokenTest.php` — seeds a `users` row + an `email_verifications` row, calls `auth_service::verifyEmail($raw)`, asserts the `email_verifications.used_at` is set, the `users.is_verified` is TRUE, and a `points_log` row exists with `delta=50, reference_type='email_verification', event_uuid` is a valid UUID v7 string. The UUID v7 check uses `Ramsey\Uuid\Uuid::fromString($eventUuid)->getType()` and asserts it returns `Uuid::TYPE_UNIX_TIME` (= 7). Also: `test_consume_sets_used_at`, `test_verify_awards_50_points`, `test_verify_token_used_twice_returns_error`, `test_verify_expired_token_returns_error`.

    **18. End-to-end smoke:** start the dev server, walk the flow with curl, capture the output for the SUMMARY.
    - `php -S 127.0.0.1:18001 -t public public/router.php` in background.
    - Insert a demo allowlist row: `mysql -uroot tickettrade_test -e "INSERT INTO student_id_allowlist (student_id, email, created_at) VALUES ('NSBM/2024/001', 'alice@students.nsbm.ac.lk', NOW());"`.
    - `curl -c /tmp/tt.cookies -sS http://127.0.0.1:18001/register` to get the CSRF token (extract from the HTML).
    - `curl -b /tmp/tt.cookies -c /tmp/tt.cookies -sS -X POST -d 'csrf_token=…&email=alice@students.nsbm.ac.lk&student_id=NSBM/2024/001&nickname=alice&full_name=Alice&password=correcthorse' -i http://127.0.0.1:18001/register` → expect 302 to `/board` + `Set-Cookie: PHPSESSID=…`.
    - `curl -b /tmp/tt.cookies -sS http://127.0.0.1:18001/board` → expect 200 with the flash-toast HTML containing `/verify?token=…`.
    - Extract the verify token from the HTML; `curl -b /tmp/tt.cookies -sS "http://127.0.0.1:18001/verify?token=…"` → expect 200 with the verify_success modal markup.
    - `mysql -uroot tickettrade_test -e "SELECT * FROM users WHERE nickname='alice';"` → expect 1 row with `is_verified=TRUE, points=50, tier='D'`.
    - `mysql -uroot tickettrade_test -e "SELECT * FROM points_log;"` → expect 1 row with `delta=50, event_uuid` a UUID v7.
  </action>
  <verify>
    <automated>bash -c 'cd /home/user/hermesag/004/tickettrade && mysql -uroot tickettrade_test -e "INSERT INTO student_id_allowlist (student_id, email, created_at) VALUES (\"NSBM/2024/001\", \"alice@students.nsbm.ac.lk\", NOW()) ON DUPLICATE KEY UPDATE created_at=NOW();" && mysql -uroot tickettrade_test -e "TRUNCATE users; TRUNCATE email_verifications; TRUNCATE password_resets; TRUNCATE sessions; TRUNCATE points_log;" && (nohup php -S 127.0.0.1:18002 -t public public/router.php > /tmp/tt-02-02-server.log 2>&1 & echo $! > /tmp/tt-02-02.pid; sleep 1) && CSRF=$(curl -sS -c /tmp/tt.cookies http://127.0.0.1:18002/register | grep -oP "name=\"csrf_token\" value=\"[a-f0-9]+\"" | head -1 | grep -oP "[a-f0-9]+$") && RESP=$(curl -sS -b /tmp/tt.cookies -c /tmp/tt.cookies -X POST -d "csrf_token=$CSRF&email=alice@students.nsbm.ac.lk&student_id=NSBM/2024/001&nickname=alice&full_name=Alice&password=correcthorse" -i http://127.0.0.1:18002/register) && echo "$RESP" | head -1 && echo "$RESP" | grep -i "set-cookie" | head -1 && TOKEN=$(curl -sS -b /tmp/tt.cookies http://127.0.0.1:18002/board | grep -oP "/verify\\?token=[a-f0-9]+" | head -1 | sed "s|/verify?token=||") && echo "TOKEN=$TOKEN" && curl -sS -b /tmp/tt.cookies "http://127.0.0.1:18002/verify?token=$TOKEN" | grep -E "Email verified|verify_success" | head -1 && mysql -uroot tickettrade_test -e "SELECT nickname, is_verified, points, tier FROM users WHERE nickname=\"alice\";" -B && mysql -uroot tickettrade_test -e "SELECT user_id, delta, reference_type, balance_after FROM points_log;" -B && kill $(cat /tmp/tt-02-02.pid) 2>/dev/null; true' && vendor/bin/phpunit --testsuite=phase-2 --filter='RegisterFlowTest|VerifyTokenTest|RegisterCsrfTest'</automated>
    <fails_when>
      (a) the POST to `/register` does NOT return 302 with a `Set-Cookie: PHPSESSID=…` header; (b) after POST, the `users` table does not contain exactly one row with `nickname='alice', is_verified=FALSE, points=0, tier='E'`; (c) the `email_verifications` table does not contain exactly one row with `used_at=NULL`; (d) the `GET /board` response does NOT contain a `/verify?token=…` link in the flash-toast; (e) the `GET /verify?token=…` response does NOT contain the "Email verified" / verify_success markup; (f) after verify, `users.is_verified` is not TRUE OR `users.points != 50` OR `users.tier != 'D'`; (g) the `points_log` table does not contain exactly one row with `delta=50, reference_type='email_verification'` and an `event_uuid` parseable as UUID v7; (h) `vendor/bin/phpunit --testsuite=phase-2 --filter='RegisterFlowTest|VerifyTokenTest|RegisterCsrfTest'` exits non-zero.
    </fails_when>
  </verify>
  <done>
    - `src/Auth/Service/auth_service.php` has the full method surface: `hashPassword, verifyPassword, dummyHash, randomToken, hashToken, sanitizeUser, tierFromPoints, nextRedirectIsSafe, findUserForLogin, register, verifyEmail, login, startSession, updateLastSeen, endSession, requestPasswordReset, consumePasswordReset`. The file is the SOLE caller of `password_hash(` and `password_verify(` (AD-18).
    - `src/Auth/Model/{user,student_id_allowlist,email_verification,session}_model.php` and `src/Points/Model/points_log_model.php` have the methods listed in the <action> section; every method uses prepared statements.
    - `src/Auth/Action/{register,verify,home}_action.php` and `src/Listing/Action/browse_action.php` have the real `handle`/`handlePost` bodies.
    - `src/Auth/View/{register,verify_success}.php` and `src/Listing/View/board.php` render correctly inside the layout.
    - `src/Points/Service/points_service.php` stub has the `awardVerificationBonus($userId)` method with the Phase 6 signature.
    - The dev-server smoke flow (curl walk-through) succeeds end-to-end: allowlist insert → POST /register → 302 to /board → flash toast with /verify?token=… → GET /verify → success modal → DB shows `is_verified=TRUE, points=50, tier='D'` and one `points_log` row with `delta=50, event_uuid=UUIDv7`.
    - The `RegisterFlowTest`, `RegisterCsrfTest`, and `VerifyTokenTest` Wave 0 tests are green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Login + logout + forgot-password + reset-password + profile edit + settings flows</name>
  <files>
    src/Auth/Action/login_action.php
    src/Auth/Action/logout_action.php
    src/Auth/Action/forgot_password_action.php
    src/Auth/Action/reset_password_action.php
    src/User/Action/profile_edit_action.php
    src/User/Action/settings_action.php
    src/User/Service/user_service.php
    src/User/Model/user_model.php
    src/Auth/View/login.php
    src/Auth/View/forgot_password.php
    src/Auth/View/reset_password.php
    src/User/View/profile_edit.php
    src/User/View/settings.php
    src/Ticket/Action/my_tickets_action.php
    src/Ticket/View/my_tickets.php
    src/Ticket/Action/sales_action.php
    src/Ticket/View/sales.php
    src/Ticket/Action/purchases_action.php
    src/Ticket/View/purchases.php
    src/Listing/Action/my_listings_action.php
    src/Listing/View/my_listings.php
    config/routes.php
    tests/Integration/02/Auth/LoginFlowTest.php
    tests/Integration/02/Auth/LogoutTest.php
    tests/Integration/02/Auth/PasswordResetTest.php
    tests/Integration/02/Auth/SessionRefreshTest.php
    tests/Integration/02/User/ProfileEditTest.php
    tests/Integration/02/User/SettingsTest.php
    tests/Integration/02/Support/RouteGuardTest.php
    tests/Unit/02/User/ProfileEditValidationTest.php
    tests/Unit/02/Support/LoginTimingTest.php
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-CONTEXT.md (D-04 session refresh; D-05 logout; D-07 forgot-password/reset; D-08 ?next= bounce; D-12 inline error copy; D-13 anti-enumeration; D-15 nickname locked; D-16 WhatsApp private; PROF-01 WhatsApp regex; agent's Discretion: rate-limit response shape, /settings scope = theme + logout)
    - /home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-RESEARCH.md (## Architecture Patterns — login redirect logic with open-redirect defense; ## Common Pitfalls 1, 4, 5, 10, 11, 15; ## Security Domain T-2-09, T-2-10, T-2-13, T-2-14, T-2-15, T-2-20, T-2-21, T-2-22, T-2-27; ## Open Questions Q3, Q4, Q6, Q7)
    - /home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-VALIDATION.md (02-02-03 LoginFlowTest, 02-02-04 LogoutTest, 02-02-05 ProfileEditTest, 02-02-07 PasswordResetTest)
    - Task 1 <action> for the auth_service shape and the register flow pattern
  </read_first>
  <behavior>
    - `GET /login` returns 200 with a centered card (max-width 400px) containing email + password fields, a "Sign in" primary button, a "Register" link left, a "Forgot password?" link right, and a hidden CSRF input. If `$GLOBALS['_tt_form_error']` is set, an `alert alert-danger` renders above the form with the error message (NOT in the flash-toast).
    - `POST /login` with a correct email + password returns 302 to `$next` (where `$next` is the value of `$_GET['next']` after `nextRedirectIsSafe` validation) OR `/board` when `next` is missing or unsafe.
    - `POST /login` with a wrong password returns 200 with the login form re-rendered and an `alert alert-danger` containing the exact text "Email or password is incorrect." (D-12); the alert is inside the form card, NOT in the flash-toast.
    - `POST /login` 6 times in 5 minutes from the same IP: the 6th attempt returns the login form with "Too many attempts. Try again in 5 minutes." in the inline alert. The 7th attempt within the window returns the same error. The `Support\RateLimit` block fires BEFORE the password verify, so the constant-time guarantee is preserved (Pitfall 3 + D-12).
    - `POST /login` without a `csrf_token` returns 400 + `E_CSRF` envelope.
    - `POST /logout` deletes the `sessions` row (via `auth_service::endSession`), clears the `PHPSESSID` cookie, destroys the PHP session, and returns 302 to `/`. The next request to a private route returns 302 to `/login` because the session row is gone.
    - `GET /forgot-password` returns 200 with a single email field and a "Send reset link" button. The hint copy reads "If that email is registered, a reset link is in your inbox." (D-07).
    - `POST /forgot-password` with ANY email returns 302 to `/login` with a flash toast "If that email is registered, a reset link is in your inbox." — the same copy for both branches (D-07 anti-enumeration). For a registered email, a `password_resets` row is created with `token_hash` + `expires_at=NOW()+24h` + `used_at=NULL`; the raw token is written to `error_log('[dev-reset-link] /reset-password?token=... for ' . $email)` (OQ-7 answer). For an unregistered email, no row is created.
    - `GET /reset-password?token=…` with a valid unexpired token returns 200 with a password form (password + confirm, both ≥ 8 chars, must match). With an invalid/expired/used token, returns 400 + the "Verification link is invalid or expired" card.
    - `POST /reset-password` with valid token + matching passwords returns 302 to `/board` with the session set; the `password_resets.used_at` is set; the `users.password_hash` is updated via `auth_service::hashPassword()`. Re-using the token returns 400 + `E_TOKEN_INVALID`.
    - `GET /profile` (authenticated) returns 200 with a profile edit form: full_name, bio (textarea), WhatsApp (input with `^(\+94|0)7[0-9]{8}$` regex hint), avatar (12-illustration grid with the user's current `avatar_id` selected). The form does NOT include a `nickname` field (D-15). The form's action is `POST /profile`.
    - `POST /profile` with valid fields updates the `users` row via `user_service::updateProfile($userId, $fields)`. The `nickname` and other non-whitelisted fields are NOT accepted (T-02-20, T-02-27). On success, returns 302 to `/profile` with a flash toast "Profile updated."
    - `POST /profile` with an invalid WhatsApp (`0771234567` — only 10 digits, no `+94` prefix) returns the form with the field-level error "Use a Sri Lankan mobile (e.g., +94771234567 or 0771234567)." On the boundary cases: `+94771234567` (correct), `0771234567` (correct), `94771234567` (incorrect), `+947712345678` (incorrect).
    - `POST /profile` with `avatar_id=99` (out of range) clamps to 12 and saves; the same for `avatar_id=0` clamps to 1.
    - `GET /settings` (authenticated) returns 200 with the theme toggle (Light / Dark / System radios, current value read from `localStorage.tickettrade.theme` via a tiny inline script that populates a hidden field) and a "Log out" button (`btn-outline-danger`, opens a Bootstrap confirm modal on click, modal submit posts to `POST /logout`).
    - `POST /settings` is a no-op (theme lives in localStorage per Phase 1 D-07); the action renders the same `/settings` page. The only state-changing POST from `/settings` is the logout button.
    - `GET /my-tickets` (no session) returns 302 to `/login?next=/my-tickets`. (Authenticated) returns 200 with a placeholder "My tickets — coming in Phase 4" card. Same for `/my-listings`, `/sales`, `/purchases`.
    - `GET /admin/users` (non-admin, even authenticated) returns 404 (D-10).
  </behavior>
  <action>
    Land the login/logout/forgot/reset/profile/settings flows and the auth-required route guards for the Phase 2 stub pages (`/board`, `/my-tickets`, `/my-listings`, `/sales`, `/purchases`). This task fills in the remaining 11 stub Actions from Plan 02-01.

    **1. `src/Auth/Action/login_action.php`:** class with `handle()` (GET renders the form) and `handlePost()` (validates + checks rate limit + calls `auth_service::login` + redirect).
    - `handle()`: if already logged in, redirect to `$next = auth_service::nextRedirectIsSafe($_GET['next'] ?? null)` or `/board`. Otherwise render the login form with `csrf_token`, `next` (preserved across the GET → POST round-trip), and any prior `error`.
    - `handlePost()`: 
      - Rate-limit check FIRST: `$rl = Support\RateLimit::hit('login', $_SERVER['REMOTE_ADDR'] ?? '0.0.0.0'); if (!$rl['allowed']) { $GLOBALS['_tt_form_error'] = ['code' => 'E_RATE_LIMIT', 'message' => 'Too many attempts. Try again in 5 minutes.']; Support\View::render('Auth/View/login.php', ['csrf_token' => Support\Csrf::token(), 'next' => $_GET['next'] ?? null]); return; }`
      - Validate `email` + `password` non-empty.
      - Call `auth_service::login($email, $password)`.
      - On `ok=true`: `session_regenerate_id(true)` is called inside `auth_service::startSession`; the `Support\Csrf::token()` is re-fetched (the new session has no token yet, so a fresh one is generated); `header('Location: ' . auth_service::nextRedirectIsSafe($_GET['next'] ?? null)); exit;`
      - On `ok=false` (any error, including `E_AUTH_INVALID` for wrong password, missing user, banned user): re-render the form with the inline `alert alert-danger` containing the error message from `$error['message']`. The error code is logged (`error_log('[login] ' . $error['code'] . ' ip=' . $_SERVER['REMOTE_ADDR'])`) for ops visibility, but the user-facing copy is generic per D-12.

    **2. `src/Auth/Action/logout_action.php`:** class with `handlePost()` (the route is `POST /logout` only).
    - `$user = Support\Auth::currentUser(); if ($user !== null) { auth_service::endSession(session_id(), (int) $user['user_id']); } session_destroy(); setcookie(session_name(), '', time() - 3600, '/'); header('Location: /'); exit;`. Even when `$user` is null (somehow), the cookie is cleared and the session is destroyed.

    **3. `src/Auth/Action/forgot_password_action.php`:** class with `handle()` (GET form) and `handlePost()` (validates + calls `auth_service::requestPasswordReset` + redirect with flash toast).
    - `handle()`: if logged in, redirect to `/board`. Otherwise render the forgot-password form.
    - `handlePost()`: validate `email` non-empty + `filter_var($email, FILTER_VALIDATE_EMAIL)`. Call `auth_service::requestPasswordReset($email)` (always returns ok). The dev-only `error_log('[dev-reset-link] /reset-password?token=' . $token . ' for ' . $email)` runs in dev (per OQ-7). Set `$GLOBALS['_tt_flash_toast'] = ['type' => 'info', 'message' => 'If that email is registered, a reset link is in your inbox.']`; redirect to `/login`. The D-07 anti-enumeration is in the copy, NOT in the function return value.

    **4. `src/Auth/Action/reset_password_action.php`:** class with `handle()` (GET reads `$_GET['token']`, renders the form or the error) and `handlePost()` (validates passwords, calls `auth_service::consumePasswordReset`, redirect).
    - `handle()`: if `$_GET['token']` is missing, redirect to `/login` with a flash "Verification link is missing the token." Otherwise call `auth_service::peekPasswordReset($_GET['token'])` (a read-only check that returns the row or null without consuming). If null, render the error card. Otherwise render the password form with `csrf_token` + the token as a hidden input.
    - `handlePost()`: validate `password` + `password_confirm` non-empty, `strlen($password) >= 8`, `$password === $password_confirm`. On any validation miss, re-render the form with the field-level error. On all valid, call `auth_service::consumePasswordReset($raw, $password)`. On `E_TOKEN_INVALID`, render the error card. On success, `auth_service::startSession($userId)` runs inside `consumePasswordReset`, redirect to `/board` with a flash toast "Password reset. You're now signed in."

    **5. `src/User/Service/user_service.php`:** class with the full method surface.
    - `validateWhatsApp(?string $s): ?string` — returns the canonicalized value (`+94` + 9 digits OR `0` + 9 digits) if valid, null if empty, throws `\InvalidArgumentException` with a human message if invalid format. The regex is `^(\+94|0)7[0-9]{8}$` (SEC-08, AGENTS.md Constraints).
    - `validateAvatarId(mixed $v): int` — `(int) $v` cast + `max(1, min(12, $v))` clamp (Pitfall 11).
    - `getById(int $userId): ?array` — wraps `user_model::findById` + `auth_service::sanitizeUser`.
    - `getByNickname(string $nickname): ?array` — wraps `user_model::findByNickname` + sanitize; used by Plan 02-03's `public_profile_action`.
    - `getPublicProfile(string $nickname): ?array` — same as `getByNickname`; alias for the public read view.
    - `randomAvatarId(): int` — `random_int(1, 12)` (D-19; called by `auth_service::register`).
    - `updateProfile(int $userId, array $fields): array` — accepts ONLY the 4 whitelisted fields (`full_name`, `bio`, `whatsapp`, `avatar_id`); strips any other key; calls `validateWhatsApp` and `validateAvatarId`; UPDATEs via `user_model::updateProfile`; returns `['ok' => true, 'updated' => $cleanFields]` or `['ok' => false, 'error' => ['code' => 'E_VALIDATION', 'message' => '...', 'fields' => ...]]`.

    **6. `src/User/Model/user_model.php` (real):** methods from Plan 02-01 Task 2 plus `updateProfile(int $userId, array $fields): bool` — accepts a pre-whitelisted `$fields` array; UPDATEs only the columns present; uses prepared statement with named placeholders (`:full_name, :bio, :whatsapp, :avatar_id`); updates `updated_at = NOW()`; returns true on success.

    **7. `src/User/Action/profile_edit_action.php`:** class with `handle()` (GET renders the form) and `handlePost()` (validates + calls `user_service::updateProfile` + redirect).
    - `handle()`: if not logged in, `Support\Auth::requireAuth` is called by the Router already; render the form pre-populated with the current values.
    - `handlePost()`: validate `full_name` non-empty, `bio` ≤ 500 chars, `whatsapp` via `user_service::validateWhatsApp` (empty is OK; format invalid → field-level error), `avatar_id` via `validateAvatarId`. On any error, re-render. On all valid, call `user_service::updateProfile($userId, $cleanFields)`. On success, redirect to `/profile` with flash toast "Profile updated." On `E_VALIDATION`, re-render with the field map.

    **8. `src/User/Action/settings_action.php`:** class with `handle()` (GET renders the settings page) and `handlePost()` (handles the destructive logout; the theme radios are pure localStorage per Phase 1 D-07 and do NOT post).
    - `handle()`: render the settings page with the current user row, the theme toggle (current value from the user-agent or `data-surface` default — Phase 1's `window.TicketTrade.getTheme()` reads it client-side; the server doesn't need to know), and the logout button.
    - `handlePost()`: the only POST from this page is the logout button (form action=`/logout`, method=POST). The `handlePost` is a no-op redirect to `/settings` for safety (any direct POST that isn't logout is ignored).

    **9. Views:**
    - `src/Auth/View/login.php` — centered card, max-width 400px, email + password fields, "Sign in" button, "Register" link left, "Forgot password?" link right. The hidden `csrf_token` + `next` (preserved as `<input type="hidden" name="next" value="<?= View::h($next ?? '') ?>">`). The inline `alert alert-danger` is rendered ABOVE the form fields when `$GLOBALS['_tt_form_error']` is set. The error message text uses `View::h()` to prevent XSS (T-02-13).
    - `src/Auth/View/forgot_password.php` — centered card, single email field, "Send reset link" button.
    - `src/Auth/View/reset_password.php` — password form (new + confirm); the token is a hidden input.
    - `src/User/View/profile_edit.php` — full_name, bio (textarea, 500 char max with a live counter), WhatsApp (with `+94771234567 or 0771234567` hint), avatar picker partial. The `<form action="/profile" method="post">` includes the CSRF token.
    - `src/User/View/settings.php` — three radio buttons (Light / Dark / System) with `data-theme-controller` (Phase 1 hook); the logout form is a separate `<form action="/logout" method="post">` with the CSRF token, inside a Bootstrap modal that's opened by a `btn-outline-danger` button. The destructive styling follows UX-DR-37 (the form requires explicit confirm before submitting).
    - The 5 placeholder Views (`board.php` from Task 1 already exists; the new ones are `my_tickets.php`, `my_listings.php`, `sales.php`, `purchases.php`) — each is a centered card with "Coming in Phase N" copy and a "Back to board" link. The auth-required route guard exercise is the deliverable here, not the real data.

    **10. Ticket / Listing / User Actions and Views (placeholder bodies):**
    - `src/Ticket/Action/{my_tickets,sales,purchases}_action.php` — each has `handle()` that renders the corresponding View. The data layer is empty in Phase 2; the View shows a "Coming in Phase 4" card.
    - `src/Ticket/View/{my_tickets,sales,purchases}.php` — placeholder cards.
    - `src/Listing/Action/my_listings_action.php` + `src/Listing/View/my_listings.php` — same pattern; "Coming in Phase 3."

    **11. Route map (`config/routes.php`):** the existing entries are unchanged; the Action classes behind them are now real. The `?next=` open-redirect defense is in `auth_service::nextRedirectIsSafe` (used by both `LoginAction::handle` and `LoginAction::handlePost`).

    **12. Wave 0 tests:**
    - `tests/Integration/02/Auth/LoginFlowTest.php` — seeds a user, simulates POST /login with correct creds, asserts 302 to `/board` and `Set-Cookie: PHPSESSID=…` + the `users.sessions` row exists. Wrong creds: 200 with the form re-rendered + the inline `alert alert-danger` text. Missing user: same wrong-creds response + the constant-time check (`LoginTimingTest` covers timing).
    - `tests/Integration/02/Auth/LogoutTest.php` — seeds a user + session, POST /logout, asserts 302 to `/` + the session row is deleted + the cookie is cleared.
    - `tests/Integration/02/Auth/PasswordResetTest.php` — seeds a user, POST /forgot-password, asserts a `password_resets` row exists; POST /forgot-password for a non-registered email asserts NO row; GET /reset-password with the raw token renders the form; POST /reset-password with valid passwords succeeds; re-POST with the same token returns 400.
    - `tests/Integration/02/Auth/SessionRefreshTest.php` — seeds a user + session, calls `Auth::boot()` (which `updateLastSeen`s), asserts `sessions.last_seen` is fresh. Then advance the clock (the test uses a 5-min-ago timestamp on the row), call `Auth::boot()` again, assert `last_seen` updated.
    - `tests/Integration/02/User/ProfileEditTest.php` — seeds a user, POST /profile with valid fields, asserts the row updated; POST /profile with invalid WhatsApp, asserts 200 + form re-rendered + field error.
    - `tests/Integration/02/User/SettingsTest.php` — seeds a user, GET /settings, asserts 200 + the theme radios + the logout button.
    - `tests/Integration/02/Support/RouteGuardTest.php` — unauth GET /my-tickets → 302 to /login?next=/my-tickets; authed GET /my-tickets → 200; non-admin GET /admin/users → 404.
    - `tests/Unit/02/Support/LoginTimingTest.php` — runs `password_verify('dummy', $user_hash)` and `password_verify('dummy', $dummy_hash)` 100 times each, asserts the average wall-clock difference is < 5ms. The test may need a `pcov`/`xdebug` exclusion; if the test is too flaky in CI, it's marked `@group timing` and skipped in `phpunit.xml`'s default group.
    - `tests/Unit/02/User/ProfileEditValidationTest.php` — direct calls to `user_service::validateWhatsApp` with valid + invalid inputs (boundary cases). Direct calls to `user_service::validateAvatarId` with `0`, `1`, `12`, `13`, `'99'`, `null`.

    **13. End-to-end smoke (full Phase 2 user journey):**
    - Start the dev server.
    - Seed an allowlist row + a test user.
    - GET /login → 200 + form.
    - POST /login with correct creds → 302 to /board.
    - GET /my-tickets (authed) → 200 + placeholder card.
    - POST /logout → 302 to /.
    - GET /my-tickets (no auth) → 302 to /login?next=/my-tickets.
    - GET /admin/users (authed but not admin) → 404.
    - GET /profile (authed) → 200 + form; POST /profile with valid fields → 302 to /profile + flash toast; assert `users.full_name`, `users.bio`, `users.whatsapp`, `users.avatar_id` updated; `users.nickname` unchanged.
    - GET /settings (authed) → 200 + theme radios + logout button.
  </action>
  <verify>
    <automated>bash -c 'cd /home/user/hermesag/004/tickettrade && mysql -uroot tickettrade_test -e "INSERT INTO student_id_allowlist (student_id, email, created_at) VALUES (\"NSBM/2024/002\", \"bob@students.nsbm.ac.lk\", NOW()) ON DUPLICATE KEY UPDATE created_at=NOW();" && mysql -uroot tickettrade_test -e "TRUNCATE users; TRUNCATE sessions; TRUNCATE points_log; TRUNCATE password_resets; TRUNCATE email_verifications;" && HASH=$(php -r "echo password_hash(\"correcthorse\", PASSWORD_BCRYPT, [\"cost\" => 12]);") && mysql -uroot tickettrade_test -e "INSERT INTO users (email, student_id, nickname, password_hash, full_name, avatar_id, created_at, updated_at) VALUES (\"bob@students.nsbm.ac.lk\", \"NSBM/2024/002\", \"bob\", \"$HASH\", \"Bob Smith\", 1, NOW(), NOW());" && (nohup php -S 127.0.0.1:18003 -t public public/router.php > /tmp/tt-02-02b.log 2>&1 & echo $! > /tmp/tt-02-02b.pid; sleep 1) && CSRF=$(curl -sS -c /tmp/ttb.cookies http://127.0.0.1:18003/login | grep -oP "name=\"csrf_token\" value=\"[a-f0-9]+\"" | head -1 | grep -oP "[a-f0-9]+$") && curl -sS -b /tmp/ttb.cookies -c /tmp/ttb.cookies -X POST -d "csrf_token=$CSRF&email=bob@students.nsbm.ac.lk&password=correcthorse" -i http://127.0.0.1:18003/login | head -3 && curl -sS -b /tmp/ttb.cookies -o /dev/null -w "MY_TICKETS:%{http_code}\n" http://127.0.0.1:18003/my-tickets && curl -sS -b /tmp/ttb.cookies -o /dev/null -w "PROFILE:%{http_code}\n" http://127.0.0.1:18003/profile && curl -sS -b /tmp/ttb.cookies -o /dev/null -w "SETTINGS:%{http_code}\n" http://127.0.0.1:18003/settings && curl -sS -b /tmp/ttb.cookies -o /dev/null -w "ADMIN_USERS:%{http_code}\n" http://127.0.0.1:18003/admin/users && CSRF2=$(curl -sS -b /tmp/ttb.cookies -c /tmp/ttb.cookies http://127.0.0.1:18003/profile | grep -oP "name=\"csrf_token\" value=\"[a-f0-9]+\"" | head -1 | grep -oP "[a-f0-9]+$") && curl -sS -b /tmp/ttb.cookies -c /tmp/ttb.cookies -X POST -d "csrf_token=$CSRF2&full_name=Bob+Jr&bio=Hello&whatsapp=%2B94771234567&avatar_id=5" -i http://127.0.0.1:18003/profile | head -3 && mysql -uroot tickettrade_test -e "SELECT nickname, full_name, bio, whatsapp, avatar_id FROM users WHERE nickname=\"bob\";" -B && CSRF3=$(curl -sS -b /tmp/ttb.cookies -c /tmp/ttb.cookies http://127.0.0.1:18003/settings | grep -oP "name=\"csrf_token\" value=\"[a-f0-9]+\"" | head -1 | grep -oP "[a-f0-9]+$") && curl -sS -b /tmp/ttb.cookies -c /tmp/ttb.cookies -X POST -d "csrf_token=$CSRF3" -i http://127.0.0.1:18003/logout | head -3 && curl -sS -b /tmp/ttb.cookies -o /dev/null -w "POST_LOGOUT_MY_TICKETS:%{http_code}\n" http://127.0.0.1:18003/my-tickets && kill $(cat /tmp/tt-02-02b.pid) 2>/dev/null; true' && vendor/bin/phpunit --testsuite=phase-2</automated>
    <fails_when>
      (a) POST /login with correct creds does NOT return 302 with `Set-Cookie: PHPSESSID=…`; (b) authed GET /my-tickets does NOT return 200; (c) authed GET /admin/users does NOT return 404; (d) POST /profile with valid fields does NOT update the row (asserting `full_name != 'Bob Jr'` or `avatar_id != 5` after the POST); (e) POST /logout does NOT return 302 to `/` AND a subsequent GET /my-tickets does NOT return 302 to /login; (f) `vendor/bin/phpunit --testsuite=phase-2` exits non-zero (any of the Wave 0 tests for LoginFlowTest, LogoutTest, PasswordResetTest, SessionRefreshTest, ProfileEditTest, SettingsTest, RouteGuardTest, LoginTimingTest, ProfileEditValidationTest).
    </fails_when>
  </verify>
  <done>
    - `src/Auth/Action/{login,logout,forgot_password,reset_password}_action.php` and `src/User/Action/{profile_edit,settings}_action.php` and the 5 placeholder Actions have the real bodies.
    - `src/Auth/View/{login,forgot_password,reset_password}.php` and `src/User/View/{profile_edit,settings}.php` and the 5 placeholder Views render correctly inside the layout.
    - `src/User/Service/user_service.php` has the full method surface (`validateWhatsApp`, `validateAvatarId`, `getById`, `getByNickname`, `getPublicProfile`, `randomAvatarId`, `updateProfile`).
    - `src/User/Model/user_model.php` has the `updateProfile` method.
    - The dev-server smoke flow (login → my-tickets → profile → settings → logout) succeeds end-to-end. The DB shows the profile fields updated; the `users.nickname` and `users.is_admin` are NOT modified.
    - The 9 Wave 0 test files are green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Final test pass + PSR-12 + docs + SUMMARY</name>
  <files>
    docs/phase-2-flows.md
    .planning/phases/02-student-authentication-profiles/02-02-SUMMARY.md
  </files>
  <read_first>
    - /home/user/hermesag/004/tickettrade/.planning/phases/01-ux-foundation-design-system/01-01-SUMMARY.md (format reference)
    - /home/user/hermesag/004/tickettrade/AGENTS.md (dev conventions)
    - Task 1 + Task 2 <verify> outputs
  </read_first>
  <behavior>
    - The verify command from Task 1 returns exit 0; the verify command from Task 2 returns exit 0; `vendor/bin/phpcs --standard=PSR12 src/` returns exit 0.
    - `docs/phase-2-flows.md` and `02-02-SUMMARY.md` exist and are non-empty.
  </behavior>
  <action>
    Final smoke pass, PSR-12 sweep, and documentation. No new code.

    **1. Run all Wave 0 tests + integration tests + the full `phase-2` suite.** Record the output in the SUMMARY.
    - `vendor/bin/phpunit --testsuite=phase-2` — all green.
    - `vendor/bin/phpcs --standard=PSR12 src/` — zero violations on the new files. If violations exist, fix them in this task (small string-quoting / line-length tweaks; no logic change).
    - `git status` shows only the expected new/modified files.

    **2. Manual end-to-end smoke matrix** (record each row's outcome in the SUMMARY; copy the curl outputs into the SUMMARY's `## Verification Log` section):
    - `php -S 127.0.0.1:18001 -t public public/router.php` started in background; `bin/dev-setup.sh` re-runs without error.
    - `curl -sS -i http://127.0.0.1:18001/login` returns 200 with all four security headers; the body contains a `<form action="/login" method="post">` + the CSRF hidden input.
    - POST /login with correct creds (seeded user) returns 302 to /board with Set-Cookie.
    - POST /login with wrong password returns 200 with `Email or password is incorrect.` in an `alert alert-danger`.
    - POST /login 6 times in a row (no real session; just hit the rate limit) returns 200 with `Too many attempts. Try again in 5 minutes.` on the 6th and 7th.
    - GET /my-tickets without session returns 302 to /login?next=%2Fmy-tickets; with session returns 200.
    - GET /admin/users (authed, not admin) returns 404.
    - GET /profile (authed) returns 200 with the form (no nickname field); POST /profile with `+94771234567` + `avatar_id=5` returns 302 to /profile + the DB shows the row updated.
    - POST /profile with `whatsapp=invalid` returns 200 with the field-level error.
    - GET /settings (authed) returns 200 with the theme radios + logout button.
    - POST /logout returns 302 to /; subsequent GET /my-tickets returns 302 to /login?next=%2Fmy-tickets.
    - Forgot-password flow: POST /forgot-password with a registered email → 302 to /login + flash toast; a `password_resets` row exists; `error_log` shows the dev-reset-link line.
    - Reset-password flow: GET /reset-password?token=… with the token from the dev log returns 200 with the password form; POST /reset-password with valid new password returns 302 to /board + the `password_resets.used_at` is set.

    **3. `docs/phase-2-flows.md`:** a 200-300 line developer notes document covering:
    - "The auth_service surface" — the full method list, in execution order, with the corresponding requirement ID and D-NN tag.
    - "How to add a new state-changing form" — step-by-step: (1) add the route in `config/routes.php` with `csrf: true` and optional `rate_limit`, (2) write the Action's `handlePost`, (3) call the Service, (4) return the envelope or render the View, (5) write the Wave 0 test.
    - "The flash-toast pattern" — how `$_SESSION['_tt_flash_toast']` carries the message across 302 redirects, how `bootstrap.php` copies it to `$GLOBALS['_tt_flash_toast']` on the next request, and how `Support\View\partials\flash_toast.php` renders the `<div data-flash-toast="…">` that the Phase 1 JS picks up.
    - "The +50 points stub" — why `Points/Service/points_service.php::awardVerificationBonus($userId)` is a stub, the signature that Phase 6 will use, the transaction shape (BEGIN; INSERT points_log; UPDATE users; COMMIT), and the AD-10 sole-writer rule.
    - "The anti-enumeration error copy" — D-13 verbatim, with the rationale (don't leak which field is wrong) and the exact inline `alert alert-danger` markup. The two field-level cases (format error, nickname taken) are explicitly allowed because the format is public and the nickname is public.
    - "Common pitfalls" — the 5 most likely mistakes a Phase 3+ implementer will make: (1) echoing the raw error message without `View::h()` (T-02-13), (2) returning a `flash-toast` for the login error instead of an inline `alert` (Pitfall 15), (3) calling `password_hash(` outside `auth_service.php` (AD-18), (4) accepting `next=…` from the URL without `nextRedirectIsSafe` (Pitfall 5), (5) returning the `email_not_in_allowlist` vs `student_id_not_in_allowlist` vs `email_already_registered` as three distinct errors (D-13 + Pitfall 6).

    **4. SUMMARY:** write `02-02-SUMMARY.md` per the SUMMARY template. Sections: `## What Got Built`, `## Verification Log` (paste the smoke matrix outcomes), `## Deviations from PLAN`, `## Next Steps` (hand off to Plan 02-03: "All 8 flows work end-to-end; Plan 02-03 fills `/profile/{nickname}` as the public read view.").
  </action>
  <verify>
    <automated>bash -c 'cd /home/user/hermesag/004/tickettrade && vendor/bin/phpunit --testsuite=phase-2 && vendor/bin/phpcs --standard=PSR12 src/ && test -f /home/user/hermesag/004/tickettrade/docs/phase-2-flows.md && test -f /home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-02-SUMMARY.md && echo "ALL_OK"'</automated>
    <fails_when>
      (a) any test in `tests/Unit/02/` or `tests/Integration/02/` fails; (b) `vendor/bin/phpcs --standard=PSR12 src/` reports any PSR-12 violation; (c) `docs/phase-2-flows.md` or `02-02-SUMMARY.md` is missing.
    </fails_when>
  </verify>
  <done>
    - `vendor/bin/phpunit --testsuite=phase-2` is green (16+ tests across register, login, logout, verify, forgot/reset, profile edit, settings, route guard, session refresh, timing, validation).
    - `vendor/bin/phpcs --standard=PSR12 src/` is clean.
    - `docs/phase-2-flows.md` exists and covers the auth_service surface, the "how to add" recipe, the flash-toast pattern, the +50 stub, the anti-enumeration copy, and the 5 common pitfalls.
    - `02-02-SUMMARY.md` exists and includes the verification log + deviations + hand-off to Plan 02-03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /register, /login, /forgot-password, /reset-password, /verify | Untrusted input (email, student ID, nickname, password, full name, token); D-13 anti-enumeration + D-03/D-07 one-shot tokens + D-12 inline error copy are the canonical defenses. |
| Browser → /profile, /settings (state-changing POST) | Authenticated user; CSRF check at boot; `user_service::updateProfile` whitelist at the Service layer; `whatsapp` regex at the Service layer; `avatar_id` clamp at the Service layer. |
| $_GET['next'] → /login redirect | Open-redirect defense via `auth_service::nextRedirectIsSafe` (Pitfall 5); rejects `//` (protocol-relative), `\` (Windows path), and any URL not starting with `/`. |
| $_POST['password'] → bcrypt writer | `Auth\Service\auth_service` is the SOLE writer (AD-18); explicit `cost=12`; `password_get_info` asserted by `PasswordHashTest`. |
| $_POST['password'] (login) → bcrypt verify | Constant-time compare via `password_verify`; missing-user case runs against `dummyHash()` sentinel (Pitfall 3); `LoginTimingTest` enforces < 5ms difference. |
| Session cookie → `Support\Auth::boot` | `use_strict_mode=1` rejects unknown IDs; `session_regenerate_id(true)` on register + login + reset (Pitfall 1); `users.is_banned` short-circuit. |
| $_SERVER['REMOTE_ADDR'] → `Support\RateLimit` | IP-based bucket; `INSERT ... ON DUPLICATE KEY UPDATE count = count + 1`; fixed 5-min window; `cache_rate.expires_at` TTL; 6th attempt within the window returns the inline `E_RATE_LIMIT` error. |
| $_POST['csrf_token'] → `Support\Csrf::verify` | `hash_equals()` constant-time; per-session token; 64 hex chars from `random_bytes(32)`. |
| `users.password_hash` → View | `auth_service::sanitizeUser` strips `password_hash` (and `is_admin`, `is_banned`, `points`, `points_frozen`); T-02-10 + T-02-20 + T-02-27. |
| `points_log` write → `Points\Service\points_service::awardVerificationBonus` | AD-10 sole-writer rule; BEGIN; INSERT points_log with UUID v7; UPDATE users.points + users.tier; COMMIT (Pitfall 16). The stub signature matches Phase 6. |
| `?next=/admin/*` for non-admin | `Support\Auth::adminGuard` returns 404 BEFORE consulting `?next=` (Pitfall 10); D-10. |
| `error_log('[dev-reset-link] …')` | Dev-only log line carrying the raw password-reset token; explicit `getenv('APP_ENV') !== 'production'` guard so prod does not leak tokens to logs (OQ-7). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Spoofing | Login — email enumeration via timing | high | mitigate | `password_verify` always runs against SOMETHING (user hash or `dummyHash()` sentinel); `LoginTimingTest` enforces < 5ms difference over 100 iterations. |
| T-02-02 | Spoofing | Register — email enumeration via error copy | high | mitigate | D-13 combined message for the 3 failure cases (email not in allowlist / student ID not in allowlist / email already registered); format error and nickname-taken error are the only field-level cases. |
| T-02-03 | Spoofing | Allowlist bypass via case/whitespace | medium | mitigate | `strtolower(trim($email))` in `auth_service::findUserForLogin` and `student_id_allowlist_model::findByEmail`; `users.email` and `student_id_allowlist.email` are stored lowercase by convention. |
| T-02-04 | Tampering | CSRF on state-changing endpoints | high | mitigate | `Support\Csrf::verify()` at bootstrap for POST/PUT/PATCH/DELETE; `hash_equals()`; per-session token from `random_bytes(32)`. |
| T-02-05 | Tampering | Brute-force login | high | mitigate | `Support\RateLimit` 5/5min/IP per NFR-SEC-007; the rate-limit error fires BEFORE the password verify so the constant-time guarantee is preserved. |
| T-02-06 | Tampering | Brute-force verify/reset token | high | mitigate | Token is `bin2hex(random_bytes(32))` (64 hex chars); stored as `hash('sha256', $raw)` (64 hex chars in `token_hash CHAR(64)`); one-shot via `used_at IS NULL` guard; expires in 24h. |
| T-02-07 | Repudiation | User claims they did not log in | medium | partial | `sessions` row carries `user_id`, `ip`, `user_agent`, `created_at`, `last_seen`; the full `audit_log` is Phase 9. |
| T-02-08 | Repudiation | User denies registering | low | mitigate | `users.created_at` + the `email_verifications` row + the first `sessions` row. |
| T-02-09 | Information Disclosure | Login error reveals which field is wrong | high | mitigate | Single combined message per D-12: "Email or password is incorrect." with no field-level highlight. |
| T-02-10 | Information Disclosure | Profile edit form leaks `password_hash` | high | mitigate | `auth_service::sanitizeUser` strips `password_hash` (and `is_admin`, `is_banned`, `points`, `points_frozen`); T-02-10 + T-02-20 + T-02-27. |
| T-02-11 | Information Disclosure | Stack traces in browser | medium | mitigate | `Support\Error::server_error($internalMessage)` writes to `error_log` only when `APP_ENV !== 'production'`; HTML output is the generic 500 copy (AD-16). |
| T-02-12 | Information Disclosure | Forgot-password reveals whether email is registered | high | mitigate | D-07 always shows the same toast "If that email is registered, a reset link is in your inbox." regardless of whether the row is created. |
| T-02-13 | Information Disclosure | XSS via error message copy | high | mitigate | `Support\View::h($s, ENT_QUOTES, 'UTF-8')` on every dynamic value in the login / register / reset / verify / profile Views. |
| T-02-14 | Information Disclosure | XSS via avatar picker | medium | mitigate | Closed 1..12 set; `(int)` cast + `max(1, min(12, $v))` clamp in `user_service::validateAvatarId`; the View never echoes a user-supplied URL. |
| T-02-15 | Denial of Service | Login flood | high | mitigate | `Support\RateLimit` 5/5min/IP; the 6th attempt within the window is blocked at the Router layer (before the Action runs). |
| T-02-16 | Denial of Service | Mass registration | high | mitigate | `Support\RateLimit` 5/60min/IP for the `register` route (config/rate_limits.php). |
| T-02-17 | Denial of Service | Mass forgot-password | high | mitigate | `Support\RateLimit` 3/60min/IP for the `forgot_password` route. |
| T-02-18 | Denial of Service | Mass profile edit | low | mitigate | `Support\RateLimit` 30/60min/user for the `profile_edit` route. |
| T-02-19 | Denial of Service | Session table growth | low | deferred | TTL cleanup via Phase 9 cron; Phase 2 ships the `last_seen` column and the `idx_sessions_last_seen` index. |
| T-02-20 | Elevation of Privilege | `is_admin` flip via profile edit | high | mitigate | `user_service::updateProfile` accepts ONLY the 4 whitelisted fields; `auth_service::sanitizeUser` strips `is_admin` before passing the row to the View. |
| T-02-21 | Elevation of Privilege | Open-redirect via `?next=` | high | mitigate | `auth_service::nextRedirectIsSafe` rejects anything not starting with `/`, anything starting with `//`, and anything containing `\`. |
| T-02-22 | Elevation of Privilege | Session fixation | high | mitigate | `session_regenerate_id(true)` on `auth_service::startSession` (called by register, login, reset); `use_strict_mode=1` rejects unknown IDs (Pitfall 1). |
| T-02-23 | Elevation of Privilege | Bcrypt cost downgrade | high | mitigate | Single call site in `auth_service::hashPassword` with explicit `['cost' => 12]`; `PasswordHashTest` greps `src/` and asserts no other `password_hash(` exists; the `config/auth.php` constant is read fresh on every call. |
| T-02-24 | Tampering | Token reuse via `used_at` not set | high | mitigate | `UPDATE email_verifications SET used_at = NOW() WHERE id = ? AND used_at IS NULL` returns `rowCount() === 1`; the SELECT before also has `WHERE used_at IS NULL`; re-using a token returns `E_TOKEN_INVALID`. |
| T-02-25 | Tampering | `points_log` write outside the user-update transaction | high | mitigate | `Points\Service\points_service::awardVerificationBonus` wraps the INSERT and the UPDATE in `BEGIN; ... COMMIT`; if either fails, the whole thing rolls back (Pitfall 16, AD-10). |
| T-02-26 | Information Disclosure | Dev reset-link token in `error_log` | medium | mitigate | `error_log('[dev-reset-link] …')` is gated on `getenv('APP_ENV') !== 'production'`; the function returns the raw token only in dev; the production path returns `null` and writes no log (OQ-7). |
| T-02-27 | Tampering | Nickname change after registration | high | mitigate | D-15: profile edit form does not include a `nickname` field; `user_service::updateProfile` whitelist does not include `nickname`; the column is locked at register. |
| T-02-28 | Tampering | WhatsApp regex bypass | medium | mitigate | `user_service::validateWhatsApp` is the canonical `^(\+94|0)7[0-9]{8}$` check; the View hints the format but the Service is the gate (SEC-08). |

## ASVS L1 Compliance Notes

- **V1 Architecture:** AD-1..AD-20 read at planning time; AD-13 substrate is Phase 2's primary architectural commitment; AD-18 sole-writer rule enforced by `PasswordHashTest` (from Plan 02-01) and now exercised by the register + verify + login + reset + profile-edit flows.
- **V2 Authentication:** `Auth\Service\auth_service.php` is the sole bcrypt writer + verify + token issuer + session manager; cost 12; no `md5(`/`sha1(`/`crypt(`/`password_hash(` outside this file and `Support\Crypto` (verified by `PasswordHashTest::test_no_password_hash_outside_auth_service`).
- **V3 Session Management:** AD-13 verbatim (`use_strict_mode=1`, `sid_length=48`, `sid_bits_per_char=5`, `cookie_httponly=1`, `cookie_samesite=Strict`, `cookie_secure=1` in prod, `gc_maxlifetime=604800`); `session_regenerate_id(true)` on register + login + reset; `users.is_banned` short-circuit.
- **V4 Access Control:** `Support\Auth::requireAuth()` route guard (D-08 `?next=` bounce); `Support\Auth::adminGuard()` 404-on-non-admin for `/admin/*` (D-10); profile edit whitelist (T-02-20, T-02-27).
- **V5 Input Validation:** `filter_var(FILTER_VALIDATE_EMAIL)`, `@students.nsbm.ac.lk` suffix check, password `strlen >= 8`, WhatsApp `^(\+94|0)7[0-9]{8}$`, avatar_id clamp 1..12, nickname `[A-Za-z0-9_]{3,30}` (per OQ-3).
- **V6 Cryptography:** `password_hash($pw, PASSWORD_BCRYPT, ['cost' => 12])`, `password_verify`, `random_bytes(32)` for verify/reset tokens, `hash('sha256', $raw)` for token storage, `hash_equals()` for CSRF compare. `Support\Crypto` is the only place raw hash primitives live outside `auth_service.php`.
- **V7 Error Handling:** AD-16 failure envelope `{ok, data, error:{code, message, fields?}}` from `Support\Error::envelope()`; inline `alert alert-danger` for form errors (not flash toasts, per D-12 + Pitfall 15).
- **V8 Data Protection:** `users.password_hash` is never passed to a View (sanitized in `auth_service::sanitizeUser`); tokens stored hashed (D-03, D-07); the dev reset-link log line is prod-gated.
- **V9 Communications:** All traffic over HTTPS in prod; CSP restricts `script-src` to `'self' cdn.jsdelivr.net 'unsafe-inline'` (D-21).
- **V10 Malicious Code:** `composer.json` declares only `ramsey/uuid` at runtime (the UUID v7 primitive for the `+50` stub); `composer.lock` is committed; `ComposerTest` enforces this in CI.
- **V11 Business Logic:** D-01 allowlist check; D-04 7-day refresh-on-activity; D-06 `is_banned` short-circuit; D-13 anti-enumeration; D-15 nickname lock; D-19 random avatar 1..12; D-23 `+50` stub signature; one-shot tokens via `used_at`.
- **V12 Files and Resources:** Static avatar SVGs in `public/assets/img/avatars/` (D-17, webroot-served directly). `Support\ImageProxy` is deferred to Phase 3.
- **V13 API and Web Service:** No API endpoints in Phase 2; all endpoints are server-rendered PHP.
- **V14 Configuration:** `config/bootstrap.php` reads `APP_ENV`, sets `Asia/Colombo` timezone, configures session per AD-13; `config/db.php` is `.gitignored`; `config/.env.example` documents the swap.
</threat_model>

<verification>
- [ ] All 14 Action files (5 from Plan 02-01, 9 from this plan) have real bodies; the 5 placeholder Views (`board.php`, `my_tickets.php`, `my_listings.php`, `sales.php`, `purchases.php`) render correctly inside the layout.
- [ ] `Auth\Service\auth_service.php` is the SOLE caller of `password_hash(` and `password_verify(` (verified by `PasswordHashTest`).
- [ ] `User\Service\user_service.php` has the full method surface; `updateProfile` accepts only the 4 whitelisted fields.
- [ ] `Points\Service\points_service.php` stub has `awardVerificationBonus($userId)` with the Phase 6 signature.
- [ ] The dev-server end-to-end smoke flow (register → verify → login → my-tickets → profile → settings → logout) succeeds.
- [ ] The 9 Wave 0 test files (RegisterFlowTest, RegisterCsrfTest, VerifyTokenTest, LoginFlowTest, LogoutTest, PasswordResetTest, SessionRefreshTest, ProfileEditTest, SettingsTest, RouteGuardTest, LoginTimingTest, ProfileEditValidationTest) are green.
- [ ] `vendor/bin/phpcs --standard=PSR12 src/` is clean.
- [ ] The "Coming in Phase N" placeholder cards on `/my-tickets`, `/my-listings`, `/sales`, `/purchases` confirm the route guard works (302 to login when unauth; 200 + card when authed).
</verification>

<success_criteria>
- [ ] All 6 success criteria in ROADMAP.md Phase 2 entry that depend on user flows (criteria 1, 2, 3, 4, 6) are now verifiable end-to-end.
- [ ] A student can register with `@students.nsbm.ac.lk` + student ID, see the verify flash toast, click the verify link, see the success modal, log in, edit their profile (full name + bio + WhatsApp + avatar), and log out.
- [ ] Wrong credentials show a single inline "Email or password is incorrect." (D-12, T-02-09); the 6th login attempt in 5 minutes shows "Too many attempts. Try again in 5 minutes." (D-12, NFR-SEC-007).
- [ ] The `+50` points stub fires on verify; the user row is updated; the `points_log` row carries a UUID v7 `event_uuid`.
- [ ] Route guards redirect unauthenticated users from `/profile`, `/settings`, `/my-tickets`, `/my-listings`, `/sales`, `/purchases` to `/login?next=…`; non-admin `/admin/*` returns 404.
- [ ] The `User\Service\user_service` whitelist is the canonical gate for `updateProfile` — `nickname`, `is_admin`, `is_banned`, `points`, `tier` are NOT editable from the form.
- [ ] `bin/dev-setup.sh` + the dev server can demo the full Phase 2 user journey in under 2 minutes.
- [ ] `vendor/bin/phpunit --testsuite=phase-2` and `vendor/bin/phpcs --standard=PSR12 src/` are both green.
</success_criteria>

<output>
Create `/home/user/hermesag/004/tickettrade/.planning/phases/02-student-authentication-profiles/02-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

Phase 2 Plan 02-02 (this plan) ships the **user-facing flows** that close success criteria 1, 2, 3, 4, 5, 6 in the ROADMAP Phase 2 entry. Concretely:

### Auth context (the visible flows)

1. `src/Auth/Service/auth_service.php` — full method surface: `register, verifyEmail, login, startSession, endSession, updateLastSeen, requestPasswordReset, consumePasswordReset, findUserForLogin` (plus the Plan 02-01 Task 2 stub methods `hashPassword, verifyPassword, dummyHash, randomToken, hashToken, sanitizeUser, tierFromPoints, nextRedirectIsSafe`). The sole bcrypt writer + sole session/token lifecycle owner.
2. `src/Auth/Action/register_action.php` — GET form + POST validate + auto-login + flash toast.
3. `src/Auth/Action/login_action.php` — GET form + POST rate-limit + verify + redirect with `?next=` defense.
4. `src/Auth/Action/logout_action.php` — DELETE the session row + clear the cookie.
5. `src/Auth/Action/verify_action.php` — GET consume token + render success modal.
6. `src/Auth/Action/forgot_password_action.php` — GET form + POST anti-enumeration redirect.
7. `src/Auth/Action/reset_password_action.php` — GET form + POST update password + auto-login.
8. `src/Auth/Action/home_action.php` — GET landing page (Phase 1 stub; Phase 3 fills).
9. `src/Auth/View/{register,login,forgot_password,reset_password,verify_success}.php` — the 5 Auth Views.
10. `src/Auth/Model/{user,student_id_allowlist,email_verification,password_reset,session}_model.php` — 5 Models with prepared statements.

### User context (profile + settings)

11. `src/User/Service/user_service.php` — `validateWhatsApp, validateAvatarId, getById, getByNickname, getPublicProfile, randomAvatarId, updateProfile` (whitelist gate).
12. `src/User/Model/user_model.php` — `findByNickname, updateProfile`.
13. `src/User/Action/profile_edit_action.php` — GET form + POST validate + update.
14. `src/User/Action/settings_action.php` — GET theme + logout button.
15. `src/User/View/{profile_edit,settings}.php` — the 2 User Views.

### Points context (the +50 stub)

16. `src/Points/Service/points_service.php` — `awardVerificationBonus($userId)` stub; transaction shape matches Phase 6; sole writer of `points_log` and the only updater of `users.points`/`users.tier` outside Phase 6.
17. `src/Points/Model/points_log_model.php` — `insert` helper.

### Listing / Ticket contexts (placeholder bodies for the route guards)

18. `src/Listing/Action/browse_action.php` — `/board` (public-browse per D-09; "Coming in Phase 3" placeholder).
19. `src/Listing/View/board.php` — placeholder.
20. `src/Listing/Action/my_listings_action.php` — `/my-listings` (auth-required; "Coming in Phase 3" placeholder).
21. `src/Listing/View/my_listings.php` — placeholder.
22. `src/Ticket/Action/my_tickets_action.php` — `/my-tickets` (auth-required; "Coming in Phase 4" placeholder).
23. `src/Ticket/View/my_tickets.php` — placeholder.
24. `src/Ticket/Action/sales_action.php` — `/sales` (auth-required; "Coming in Phase 4" placeholder).
25. `src/Ticket/View/sales.php` — placeholder.
26. `src/Ticket/Action/purchases_action.php` — `/purchases` (auth-required; "Coming in Phase 4" placeholder).
27. `src/Ticket/View/purchases.php` — placeholder.

### Front controller + bootstrap

28. `public/index.php` — no functional change beyond the implicit boot.
29. `config/bootstrap.php` — one addition: copy `$_SESSION['_tt_flash_toast']` to `$GLOBALS['_tt_flash_toast']` after `Auth::boot()` runs, then unset the session key (the session is the carrier for the cross-redirect message).
30. `config/routes.php` — route map entries for `/verify` etc. are unchanged; the Action classes behind them are now real.

### Wave 0 tests (12 files)

31. `tests/Integration/02/Auth/RegisterFlowTest.php` — 7 methods: happy path + 4 anti-enumeration error cases + format error + password length.
32. `tests/Integration/02/Auth/RegisterCsrfTest.php` — POST without CSRF → 400 + `E_CSRF`.
33. `tests/Integration/02/Auth/VerifyTokenTest.php` — token consumed + `is_verified=TRUE` + `points_log` row with UUID v7.
34. `tests/Integration/02/Auth/LoginFlowTest.php` — happy path + wrong password + missing user + rate limit.
35. `tests/Integration/02/Auth/LogoutTest.php` — DELETE session + clear cookie + redirect.
36. `tests/Integration/02/Auth/PasswordResetTest.php` — issue token + consume + re-use fails + expired fails + dev-log line.
37. `tests/Integration/02/Auth/SessionRefreshTest.php` — `last_seen` update on authenticated request.
38. `tests/Integration/02/Support/RouteGuardTest.php` — private route bounce + admin 404 + authed access.
39. `tests/Integration/02/User/ProfileEditTest.php` — 4 fields updated + WhatsApp regex + avatar clamp + nickname immutable.
40. `tests/Integration/02/User/SettingsTest.php` — theme radios + logout button.
41. `tests/Unit/02/Support/LoginTimingTest.php` — constant-time `password_verify` (missing user vs wrong password < 5ms).
42. `tests/Unit/02/User/ProfileEditValidationTest.php` — `validateWhatsApp` boundary cases + `validateAvatarId` clamp.

### Documentation

43. `docs/phase-2-flows.md` — developer notes: auth_service surface; "how to add a new form" recipe; flash-toast pattern; the `+50` stub; anti-enumeration copy; 5 common pitfalls.
44. `02-02-SUMMARY.md` — close-out summary; verification log; deviations; hand-off to Plan 02-03.
